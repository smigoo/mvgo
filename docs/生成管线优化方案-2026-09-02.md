# mvgo 组件生成管线优化方案（2026-09-02）

> 基于 2026-09-02 全天对 `mc-max-1788345051113-d22deaea` / `mc-max-1788349399134-9c3aff7f` / cc416ea4 / 24e63b7c 真实日志与快照的逐行取证，覆盖四个问题：① 效果反复+最终崩溃、② 中途满意无法提前落盘、③ workspace 残留清理刷屏、④ 9c3aff7f 布局识别错误 + 图标尺寸错误。所有行号基于当前 `src` 源码。
>
> **实施状态（2026-09-02 晚，按「帮我执行」全部落地）**：1.1 / 1.2 / 1.3 / 2.1 / 3.1 / 4.1(方案C) / 4.2(方案D) / 5.1–5.4 已实施并验证（见文末「实施记录与验证」）；问题四方案 C/D 已落地（C-1/C-2 + D-1/D-2，提交 c332997）。问题五小节在复盘后扩展：5.1 onFilesReady / 5.2 extractComponentTagNames / 5.3 code-validator import 别名 / 5.4 injectEchartsFallback 吞换行（echarts import 被注释吞噬，RUNTIME-009）。附带：generate 运行时门禁确定性缺失升级硬 BLOCK（commit c332997，含 hasDeterministicRuntimeMissing）。

---

## 问题一：效果反复（好→坏→好→坏→崩溃）

### 1.0 根因复盘（已闭环）

预览效果沿时间轴出现的两个「变坏」拐点，机制完全不同：

| 拐点 | 时间（d22deaea） | 现象 | 性质 | 根因 |
|---|---|---|---|---|
| A | 18:50 | SectionChart 542B 空壳 | 假坏（中间态） | 回退时**全量重生成**，旧好文件被清掉、新文件未轮到 |
| B | 19:09 | 丢 vue/echarts import | 真坏（终态） | `fixedFiles` 覆写挤掉运行库 import |

- **拐点 A**：语义校验只拦到 1 个坏块（SectionHeader 截断），但回退路径**不写 `_reviseFiles`**，engineer 把 8 个 chunk 全部重做 → 预览跟着候选快照换帧反复摆动。
- **拐点 B**：全量重生成完成后内存态本是好的（含 `import {ref...} from 'vue'` + `import * as echarts from 'echarts'`），但 `CodeFixPipeline`/`validateAndFixGeneratedFiles` 产出的 `fixedFiles` 在最终落盘时覆写掉这些 import（`microcode-engineer.js:5546` 注释已承认同类事故「好的改坏」，但今日补的「资源 import 终验兜底」只补图片 import、不补运行库 import）。

---

### 1.1 修复 A：回退局部化（治「反复」，4 处落点）　✅ 已实施（2026-09-02）

> 实际落点与设计差异：`revision-files.js` 末尾新增 `resolveTargetFilesFromIssues(issues, generatedFiles)` 与 `extractFilesFromErrorMsg(errMsg, generatedFiles)`；graph 三处回退路径写 `state._reviseFiles`（**一律转数组**，消费端 `:995` 用 `.length > 0` + `.includes(f)`）；P0-2 结构顺序门禁置 `['package/index.vue']`。已 acorn 解析 + 动态 import 冒烟（精确命中 / 占位 `(全部)`→hasAmbiguous / basename 去前缀 / 无命中→`[]` 四态全绿）。

**核心**：让三处回退路径在回退前按 BLOCK issue 计算 `_reviseFiles`，engineer 只重做受影响 chunk、其余从磁盘恢复。

**三处回退路径（都只写 `_l0CodeRetryGuidance`、不写 `_reviseFiles`）**：

| # | 路径 | 位置 | 现状 |
|---|---|---|---|
| 1 | semantic-incomplete（engineer 内语义校验失败） | `mc-component-graph-phase2.js:1218-1236` | `decideRetry` 返回无 `_reviseFiles` |
| 2 | code-structure-validator（L0-B）失败 | `mc-component-graph-phase2.js:3703-3719` | return 无 `_reviseFiles` |
| 3 | P0-2 结构顺序门禁 | `mc-component-graph-phase2.js:3859-3868` | 回 engineer 无 `_reviseFiles` |

**落点 1（新增工具函数）**：在 `src/ai-engine/utils/revision-files.js` 增加 `resolveTargetFilesFromIssues(issues, generatedFiles)`，从 L0-B 的 `issues[].file` 提取受影响文件集（与现有 `resolveTargetFiles` 的 `critiques[].location` 结构不同，需单独适配）：

```js
// issues[] 形如 { id, severity, file }，file 可能是精确路径或占位
export function resolveTargetFilesFromIssues(issues, generatedFiles = []) {
  const genSet = new Set(generatedFiles || [])
  const targetSet = new Set()
  let hasAmbiguous = false
  for (const it of issues || []) {
    const f = it && it.file
    if (!f) continue
    if (genSet.has(f)) { targetSet.add(f); continue }
    // 占位（'(全部)' / '(跨文件)'）→ 无法定位到具体文件
    if (/^\(.+\)$/.test(f)) { hasAmbiguous = true; continue }
    // 相对路径去前缀匹配（如 'components/X.vue' → 'package/components/X.vue'）
    const base = f.split('/').pop()
    const hit = [...genSet].find((g) => g.split('/').pop() === base)
    if (hit) targetSet.add(hit)
  }
  return { targetFiles: targetSet, hasAmbiguous, hasTargets: targetSet.size > 0 }
}
```

**落点 2（路径 1，semantic-incomplete）**：`mc-component-graph-phase2.js:1219-1236`，`decideRetry` 命中后，从 `errMsg` 解析坏文件（`errMsg` 里形如 `package/components/SectionHeader.vue: <script> 仅有 import...`），写入 `_reviseFiles`：

```js
if (decision.ok) {
  state._pendingRetry = true
  // ... 既有字段 ...
  // 新增：从错误消息提取坏文件 → 局部重做
  state._reviseFiles = extractFilesFromErrorMsg(errMsg, state.generatedFiles || [])
  // ...
}
```

**落点 3（路径 2，L0-B）**：`mc-component-graph-phase2.js:3703` 的 return 增加：

```js
_reviseFiles: !result.pass && !result._bypassed
  ? resolveTargetFilesFromIssues(
      (result.issues || []).filter((i) => i.severity === 'BLOCK'),
      state.generatedFiles || [],
    )
  : null,
```

**落点 4（路径 3，P0-2 结构顺序）**：`mc-component-graph-phase2.js:3861` 回 engineer 前置 `state._reviseFiles = ['package/index.vue']`（区块顺序问题必在 index.vue 模板）。

**消费端（已具备，仅需对齐路径格式）**：`microcode-engineer.js:992-1011` 已支持 `retryTargetFiles = _reviseFiles || targetFiles || []`，非空时只重做包含目标文件的 chunk。需确认 `_reviseFiles` 与 `chunk.files`（形如 `package/components/SectionChart.vue`）路径格式一致。

**边界**：`hasAmbiguous` 为真（存在 `(全部)`/`(跨文件)` 占位 issue）时，`_reviseFiles` 为空 → 回退到现状全量重生成，但**至少对可定位的 issue 局部化**，不再一刀切。

**验证**：构造「SectionHeader 截断」复现，确认重做只包含 SectionHeader 对应 chunk（`chunk-meta.json` completedBlocks 其余跳过）；预览不再出现拐点 A 的 542B 空壳。

---

### 1.2 修复 B0：资源 import 注入幂等性（**新发现·最高优先级，先于 B**）　✅ 已实施（2026-09-02）

> 实际落点：`resource-import-guard.js` 三条正则（defaultRe/namedRe/placeholderRe）`\s`→`[ \t]`、去尾部 `.*`，附「🛡️ P0 修复」注释；`resource-import-guard.spec.ts` 新增 5 条幂等性用例，**36/36 通过**；完整 jest 对比基线零新增失败（19 failed suites / 672 passed tests，失败全为 `backend-root.js:26 import.meta.url` 环境问题）。

> 样本：`mc-max-1788349718313-24e63b7c`（2026-09-02 19:51–19:57，已 completed，但预览报 `echarts is not defined`）

#### 1.2.0 根因（已 100% 复现）

`backend-node/src/ai-engine/utils/resource-import-guard.js:383` 的去重正则**跨行吞噬**：

```js
const defaultRe = new RegExp(`^\\s*import\\s+${v}\\s+from\\s*['"][^'"]*['"]\\s*;?\\s*.*$`, 'gm')
cleanedCode = cleanedCode.replace(defaultRe, '')   // ← 吃掉的不止一行
```

- 尾段 `\s*;?\s*` 中的 **`\s` 匹配换行符** → 越过空行；
- 紧随的 **`.*$`** 把下一行整条 import 吃干净。

实测匹配范围（`v='icon3'`）：

```
import icon3 from '../../resources/images/icon-3561.png'\n
\n
import { ref, watch, onMounted, onUnmounted} from 'vue'   ← 被一起删掉
```

**幂等性破坏链**（真实跑数，`9-package_components_FlowPrediction.vue` + 真实 mapping）：

| 次数 | 触发点 | vue | echarts | icon3 | size |
|---|---|---|---|---|---|
| 原始 LLM 输出 | — | 1 | 1 | 0 | 12743 |
| 第 1 次注入 | `generateCode` 生成内阶段（约 :1725） | 1 | 1 | 1 | 12800 ✅ |
| **第 2 次注入** | **终验兜底（`:5546-5570`）** | **0** | 1 | 1 | 12745 ❌ |
| 第 3 次注入 | 任意再次触发 | 0 | **0** | 1 | 12709 ❌ |

**每跑一次注入，就吃掉紧随资源 import 之后的下一行 import**——先丢 `vue`，再丢 `echarts`。这与日志完全吻合：engineer 内注入后预览正常（用户看到 echarts 渲染），终验兜底注入后 `vue` 消失，最终态 `echarts` 也消失 → 运行时 `echarts is not defined`。

时间线（24e63b7c）：

| 时刻 | 事件 |
|---|---|
| 19:56:54.372 | 终验兜底注入 FlowPrediction.vue 后落盘（12408） |
| 19:56:54.395 | 候选快照 `r-2159c4d8`（12800，import 仍齐全） |
| 19:56:57.933 | 运行时门禁 `RUNTIME-004 真实预览状态为 render-error` → **generate 模式降级为完成，不阻断** |
| 19:56:59.575 | 终态快照 `r-bfd8b9d3`（12826，**vue=0 ech=0**）→ 发布 → 预览报错 |
| 19:57:07 | `DemoService` 「AI 修复渲染错误」自动介入 |
| 19:58:29 | AI 补回 import（代价：`<style lang="less" scoped>` + `@import` 被平铺为 `<style scoped>`，微码主题体系被破坏；tab key 被改回 `c-monitor-*`） |

> ⚠️ 这段「资源 import 终验兜底注入」是**今天（2026-09-02）为修 `53e4d387` 事故新增的代码**（`microcode-engineer.js:5546-5570`，注释写明「injectResourceImports 只补缺失 import、绝不删已有内容，零回归风险」）。
> **它引入了第二次调用 → 幂等性被破坏 → 造成今天下午/晚上的「好的改坏」回归。** 这解释了「之前这个组件只要几分钟、效果也好，现在不行了」。

#### 1.2.1 修复（1 处代码，2 条正则）

`resource-import-guard.js:383-389`，把 `\s` 收窄为 `[ \t]`、去掉尾部 `.*`：

```js
// 现状（跨行吞噬）
const defaultRe = new RegExp(`^\\s*import\\s+${v}\\s+from\\s*['\"][^'\"]*['\"]\\s*;?\\s*.*$`, 'gm')
const namedRe   = new RegExp(`^\\s*import\\s*\\{\\s*([^}]*)\\s*\\}\\s*from\\s*['\"][^'\"]*['\"]\\s*;?\\s*.*$`, 'gm')

// 修复版（只吃本行，绝不跨行）
const defaultRe = new RegExp(`^[ \\t]*import[ \\t]+${v}[ \\t]+from[ \\t]*['\"][^'\"]*['\"][ \\t]*;?[ \\t]*$`, 'gm')
const namedRe   = new RegExp(`^[ \\t]*import[ \\t]*\\{[ \\t]*([^}]*)[ \\t]*\\}[ \\t]*from[ \\t]*['\"][^'\"]*['\"][ \\t]*;?[ \\t]*$`, 'gm')
```

`namedRe` 的 `([^}]*)` 保留可跨行能力（多行具名 import 仍可匹配），仅收紧首尾锚点。

**实测验证**：对同一内容连续清理 3 轮，`vue=1 ech=1` 恒定、size 恒定 12744（现状版 3 轮后 `vue=0 ech=0`）。

#### 1.2.2 附带修复：generate 模式运行时门禁降级

`mc-component-graph-phase2.js`（`generate-runtime-verify`，日志 `:11:56:57.933`）当前把 `RUNTIME-004 render-error` 降级为 warning 照常发布。
建议：仅对 `echarts is not defined` / `is not defined` / `Cannot read properties of undefined` 这类**确定性运行时缺失**升级为 BLOCK（或至少写进 `task.result` 并在前端标红），避免坏产物被当成功任务交付。

---

### 1.3 修复 B：覆写后重校验 + 运行库 import 兜底（治「崩溃」，2 处落点）　✅ 已实施（2026-09-02）

> 实际落点：`resource-import-guard.js` 导出 `ensureRuntimeLibraryImports(content)`（幂等、只补缺失、随 default 导出）；`microcode-engineer.js` 终验兜底之后、落盘之前对 `finalTruthFiles` 每个 `.vue` 调用；覆写后重跑 `validateVueScriptSemantics`，命中 issue 且 `allFiles[fp]` 存在则回退覆写前版本并计数。5 场景冒烟全绿（补 echarts / 补 vue / 幂等引用相等 / 无缺失原样 / 已有不重复补）。

> 依赖 1.2 先行：**不修根因的话，B 补回来的 import 会被下一次注入再删掉。**

**落点 1（覆写后重校验）**：`microcode-engineer.js:5545` 统一终态落盘前，对 `finalTruthFiles` 重跑 `validateVueScriptSemantics`；命中 issue 则不落盘坏文件、回退到覆写前版本并告警（当前语义校验跑在覆写前，覆写后的坏版本零拦截）。

**落点 2（运行库 import 兜底）**：在 `resource-import-guard.js` 同层新增 `ensureRuntimeLibraryImports(content)`，并在 `microcode-engineer.js:5552`（资源 import 终验兜底）之后、`:5571` 落盘之前调用：

```js
// 纯函数，幂等：只补缺失，绝不删已有
export function ensureRuntimeLibraryImports(content) {
  if (typeof content !== 'string' || !content.includes('<script')) return content
  const body = content
  const hasEcharts = /\becharts\s*\.\s*(init|setOption|dispose)/.test(body)
    || /\becharts\.init\(/.test(body)
  const usedVue = new Set()
  for (const n of ['ref', 'reactive', 'computed', 'watch', 'onMounted', 'onUnmounted', 'nextTick', 'onBeforeUnmount']) {
    if (new RegExp(`\\b${n}\\s*\\(`).test(body)) usedVue.add(n)
  }
  const missingEcharts = hasEcharts && !/import\s+\*\s+as\s+echarts\s+from\s*['"]echarts['"]/.test(body)
  const existingVue = new Set()
  for (const m of body.matchAll(/import\s*\{([^}]*)\}\s*from\s*['"]vue['"]/g)) {
    for (const s of m[1].split(',')) existingVue.add(s.trim())
  }
  const missingVue = [...usedVue].filter((n) => !existingVue.has(n))
  if (!missingEcharts && missingVue.length === 0) return content
  const lines = []
  if (missingEcharts) lines.push(`import * as echarts from 'echarts'`)
  if (missingVue.length > 0) lines.push(`import { ${missingVue.join(', ')} } from 'vue'`)
  const inject = '\n' + lines.join('\n') + '\n'
  return body.replace(/<script\s+setup[^>]*>/, (m) => m + inject)
}
```

**验证**：复现 d22deaea 场景，确认最终发布版 SectionChart/SectionHeader 含 vue/echarts import、无 `echarts is not defined`。

---

## 问题二：中途满意提前落盘（锁定终态）

### 2.0 现状与缺口（已核实）

- 快照底座：每 chunk 完成即落 revision，磁盘已有任意时刻好快照 ✅
- `publish-to-workspace`（`tasks.service.ts:1191`）：已支持从快照发布，**但闸门限 failed/cancelled**（`:1196`）
- `speed-pass`（`:2245`）：running 跳过 L0-B 之后 + 标记 completed，**但跳不过 engineer 内覆写**

### 2.1 方案（4 处改动）　✅ 已实施（2026-09-02）

> 实际落点与设计差异：
> - 改动 3（engineer 锁检查）**修正为「条件写盘 + 空对象解构」而非提前 return**——`generateCode` 返回值在方法末尾（`{ files, ... }`），提前 return 会让调用方 `codeResult` 变 undefined 整链报错；改为 `isSessionLocked ? { written: [], skipped: [] } : this.writeFiles(...)`，收尾（component-meta 留痕等）不中断。
> - `tasks.service.ts` 补 `LockSatisfiedRecord` 接口（首版漏定义 → `nest build` TS2322 失败，二次构建通过）；`approval.type='lock-satisfied'`。
> - 新增 `POST /tasks/:sessionId/lock` 端点 + `lockTask(sessionId, revision?)`：取快照 → 临时副本 → `copyToWorkspace` → `markSessionLocked` → `userApproved=true` → abort + `sendComplete`。
> - 前端：`generator.ts` 新增 `lockTask`；`ResultPanel.vue` 加「满意，锁定此版本」按钮（`canLock` computed + `confirmLock` + `.btn-lock` 样式）。
> - dist 符号已核对：`session-lock-registry.js` 存在、lockTask 6/3 处、isSessionLocked 3 处。

**改动 1（共享锁注册表）**：新增 `src/ai-engine/utils/session-lock-registry.js`：

```js
const locked = new Set()
export function markSessionLocked(sessionId) { locked.add(sessionId) }
export function isSessionLocked(sessionId) { return locked.has(sessionId) }
export function clearSessionLock(sessionId) { locked.delete(sessionId) }
```

**改动 2（lock 端点）**：`tasks.controller.ts` 新增 `POST :sessionId/lock`，`tasks.service.ts` 新增 `lockTask()`：
1. 校验 `running`（或 `paused`）；
2. 从 latest revision 走 `resolvePublishSourceDir → copyToWorkspace` 发布到 workspace（复用 `publishToWorkspace` 的从快照发布链路，去掉 `:1196` 的 failed/cancelled 限制，或抽出共享私有方法）；
3. `markSessionLocked(sessionId)` + `task.userApproved = true` + `abort()` + `sendComplete`（复用 `speed-pass` 的 abort/sendComplete 逻辑）。

**改动 3（engineer 内锁检查）**：`microcode-engineer.js` 两处：
- `:5524` `writeFiles(fixedFiles)` 之前；
- `:5571` 统一终态落盘之前；
各加 `if (isSessionLocked(sessionId)) return/skip`，保留已发布的好快照。sessionId 从 `input.ctx?.sessionId` 取（engineer 入参已含 `GenerationContext`）。

**改动 4（前端按钮）**：预览面板加「满意，锁定此版本」按钮，传当前 revision id，调 `POST :sessionId/lock`，成功后 toast 提示并结束任务。

**验证**：跑一个 max 任务，分块阶段点锁定，确认 workspace 产物=锁定时刻快照、任务 completed、后续不再覆写。

---

## 问题三：workspace 残留清理刷屏（附带）

### 3.0 根因（环境护栏，非代码 bug）

`workspace-preview-publisher.js` 清理 `.quality-backup-*.cleanup-*` 残留时：
1. `unlinkSync` 被 WorkBuddy safe-delete 护栏拦截（server.log 中 `SAFE_DELETE` 拦 283 次）；
2. 代码的 `find -depth -delete` 兜底也被 WorkBuddy 程序代理拦：真实错误 `Command failed: find ... -delete\nBrokered program policy check unavailable`；
3. 文件删不掉 → `rmdir` 报 `ENOTEMPTY` → 重试 5 次放弃 → 刷屏 + 残留目录。

### 3.1 方案　✅ 已实施（2026-09-02，代码侧降噪）

> 实际落点：`workspace-preview-publisher.js` `MAX_DELETE_ATTEMPTS` 5→1（附注释：环境护栏拦删除时重试只会确定性复现同一拦截 + 刷屏）；`retryOrAbandonCleanup` 末段 `logger.error` → `logger.warn`。已确认 dist `MAX_DELETE_ATTEMPTS = 1`。治本（WorkBuddy safe-delete hooks 白名单放行 `**/.cleanup-*`）需在环境侧配置，未做。

- **治本（推荐）**：WorkBuddy safe-delete hooks 白名单放行 `**/.quality-backup-*.cleanup-*`（或 `**/.cleanup-*`），或提高阈值；
- **降噪（代码侧，治标）**：`workspace-preview-publisher.js` 清理重试 5→1、ERROR→WARN。

---

## 问题四：9c3aff7f 布局识别错误 + 图标大小错误（新增取证章节，方案待执行）

> 样本：`mc-max-1788349399134-9c3aff7f`（2026-09-02 19:45 completed，420×186「环境监测」面板：4-tab 切换 + CO 面积折线）。用户反馈「布局识别错误、icon 大小错误」，并追问是**布局分析错误**还是**代码实现错误**。以下取证基于 workspace 产物 + `_figma-size.json` + `.mc-gen/resource-dom-mapping.json` + `.mc-gen/cache/figma-node-data.json` + `tasks.json` 分析台账，逐项给出归因。**方案 C（4.1）+ 方案 D（4.2）已于 2026-09-02 深夜落地（提交 c332997）。**

### 4.0 归因总表

| 现象（截图反馈） | 归因 | 代码/数据证据 |
|---|---|---|
| 标题栏右侧出现「柱状图切换图标（蓝色，当前激活）」**中文文字占位** | 🟡 **布局分析错误**（内容区元素被误判进 `header-right`）+ 兜底把 icon 当文字渲染 | `headerSlots` 两条 `slotType:"header-right"`、`figmaNodeId:"tabs-icon"/"num"`（`tasks.json` 台账）；`resource-mounter.js:1385` T09 兜底用 `slot.content` 注入 `<span>` 文字 |
| tab 栏里出现两个 **48px 大图标**（Figma 真值 **24×24**） | 🔴 **代码实现错误**（自动挂载尺寸硬编码）+ 图标脱离容器被裸挂 | `resource-dom-mapping.json`：icon-7941/7945 `figmaBox`/`visualMeta` 均 **24×24**；产物内联 `max-width:48px;max-height:48px`（`resource-mounter.js:676/685` `AUTO_MOUNT_ICON_MAX_PX=48`） |
| 图标出现在**错误位置**（tab 行最左、顺序颠倒） | 🟡 布局分析错误链（`tabs-icon` 容器 `downloadStatus:missing` 被跳过 → 图标失去 52×24 容器，落入兜底裸挂） | mapping 89:43 `tabs-icon` 52×24 @ x=1816 但 `downloadStatus:"missing"`、被 `filterAvailableResources` 过滤；产物 `<img :src="icon2">`(x=1844) 排在 `icon1`(x=1816) 前 |
| 橙色角标数字「6」丢失 | 🟡 布局分析错误链（`num` 被判 header-right 后 css 替代 14×14 未落地） | mapping 2:7949 `num` 14×14 `#f53f3f` `downloadStatus:"css"`；产物无角标 |
| tab 栏高 32px vs 真值 27px | 🔴 代码实现（微差，非主因） | 产物 `.c-env-monitor-tabs-container { height:32px }`；mapping bg 2:7890 `height:27` |

**结论：布局异常两处来源都有，但主导是「布局分析错误 → 校验只 warn 不 reject → 误判流入产物」的链式问题；icon 大小 48px 则是纯代码实现硬编码（分析层给的真值 24×24 是准的）。**

### 4.0.1 Figma 真值 vs 分析判定（识别错误的确证）

```
Figma 节点树（figma-node-data.json，真值）：
2:7879 cp-环境监测 FRAME 420×186 @ (1475,865)
├─ 2:7880 bg
├─ 2:7881 header GROUP 377×28 @ y=875      ← 仅「环境监测」标题 + 装饰线
├─ 89:41 slot-con FRAME 380×145 @ y=898    ← 内容区
│  └─ 89:42 sub-t FRAME 380×32  @ y=898    ← tab 栏容器
│     ├─ 2:7889 tabs-list GROUP 295×27 @ x=1495   ← 左侧 4 个 tab 文字
│     ├─ 89:43 tabs-icon FRAME 52×24 @ x=1816     ← 右侧 2 个图标（24×24 各一）
│     └─ 2:7949 num GROUP 14×14 @ x=1861          ← 橙色角标「6」
```

- `tabs-icon`(y=905) / `num`(y=898) 是 `sub-t`(y=898) 的子节点，**属内容区**，与 header(2:7881, y=875) 无重叠；
- 分析层却输出 `headerSlots: [{slotType:"header-right", elementType:"icon", content:"柱状图切换图标（蓝色，当前激活）", figmaNodeId:"tabs-icon"}, {...figmaNodeId:"num"}]`；
- **校验层已发出警告但未拦截**：`task.result.headerSlotValidation = { validatedSlots:[], rejectedNodes:[], warnings:["预览图检测到undefined，但Figma中未找到同行节点" ×2], ... }` —— `header-slot-validator.js:547-552` 对「预览图检测到但 Figma 无同行节点」只 `warnings.push`，`rejectedNodes` 数组存在但**恒空**；
- 误判 headerSlots 流入 prompt → 模型没画 → T09 兜底（`resource-mounter.js:1324-1390`）用 `slot.content` 注入**纯文字 `<span>`**（「柱状图切换图标（蓝色，当前激活）」），同时其 icon 资源又被 autoMount 以裸 `<img>` 挂进 tabs-container → 同一图标双份渲染（标题栏文字 + tab 栏 48px 图）。

### 4.0.2 icon 48px 硬编码（代码实现错误的确证）

- `resource-dom-mapping.json` 对 icon-7941 / icon-7945 的 `figmaBox` 与 `visualMeta` **都是准确的 24×24**（分析层没犯错）；
- `resource-mounter.js:676` `export const AUTO_MOUNT_ICON_MAX_PX = 48`（2026-09-02 上午为修 a57e4409「裸挂失控放大」新增的兜底上限），`:685` 只输出 `max-width:48px;max-height:48px;object-fit:contain`，**完全没读 mapping 里的 figmaBox**；
- 只写 `max-*` 不写 `width/height` → 实际渲染尺寸 = PNG 原始像素（Figma 常导出 2x/3x），48 仅拦上界；把 24×24 图标放到 27px 高的 tab 行里，视觉即「图标过大 + 撑行」。
- 调用点 `resource-mounter.js:776` 手里**已有完整 `m`（含 figmaBox）**，补尺寸是低风险改动。

### 4.1 方案 C：headerSlots 越界几何校验（治「识别误判流入产物」）

| 落点 | 文件 | 改动 |
|---|---|---|
| C-1 校验升级 | `validators/header-slot-validator.js:547-552` | `_matchSlotCandidates` 未匹配分支从 `warnings.push` 改为同时写 `result.rejectedNodes.push({ slotCandidate, reason:'figma-no-same-row' })`（rejectedNodes 字段已存在）；让 graph 消费端按 rejectedNodes 从 `state.headerSlots` **剔除**对应候选，杜绝误判流入 prompt |
| C-2 消费前几何包含校验（双保险） | `resource-mounter.js` T09 兜底（:1324）与 engineer headerSlots 注入前 | 用 `figmaNodeData` 查候选 `figmaNodeId` 对应节点 bbox，与 header 容器 bbox（如 2:7881）做相交判定；**不交 → 视为越界**：不注入 header_right 插槽，改按 `figmaPath` 回落到所在 section body（tab 行右侧），保留几何位置 |
| C-3（提示，非本期） | visual-parser prompt | 让分析层区分「header 行内控件」与「body 顶部 tab 行控件」，仅前者进 headerSlots |

**验证**：重跑 9c3aff7f，确认①标题栏不再出现中文文字占位；②两图标出现在 tab 行右侧（原 89:43 位置）、顺序为 7941 在左 7945 在右；③橙色角标「6」出现在原 num 位置。

### 4.2 方案 D：自动挂载 icon 尺寸取 figmaBox（治「图标大小」）

| 落点 | 文件 | 改动 |
|---|---|---|
| D-1 尺寸真值化 | `resource-mounter.js:685` `injectImgChild` + `:776` 调用点 | `injectImgChild(content, openTag, varName, alt='', size)` 新增 size 入参；调用点把 `m.figmaBox?.width/height`（或 `m.visualMeta?.width/height`）整数化并 **clamp 到 [12, 128]** 传入，输出 `width:Wpx;height:Hpx;object-fit:contain`；mapping 无尺寸时才回落 `AUTO_MOUNT_ICON_MAX_PX` 的 `max-*` 兜底。尺寸来源优先取**子资源自身 figmaBox**（24×24），不取 missing 容器（52×24） |
| D-2 顺序保真（可选） | 同调用点 | 同容器内多图标按 `figmaBox.x` 升序输出，避免 icon2(x=1844) 排在 icon1(x=1816) 前 |

**验证**：产物内联样式出现 `width:24px;height:24px`（而非 `max-width:48px`）；图标不再撑行。

---

## 问题五：Vue3 降级三段生成 ReferenceError（P0，**已修复**）

> 样本：`mv-max-1788352276525-bdfe875b`（2026-09-02 20:40–20:44）。Vue3 Figma 模式整文件生成截断（claude-opus-4-8 首轮 113s 截断）→ 降级 template/script/style 三段，脚本段生成成功后节点报 `❌ 失败: onFilesReady is not defined`，Phase 2 中断。

### 5.0 根因（git 取证，已闭环）

| 项 | 结论 |
|---|---|
| 报错性质 | **ReferenceError**（读取未声明裸标识符），非 not-a-function |
| 代码位置 | `vue3-engineer.js` runChunk 内 `pushChunkSnapshot({ ..., onFilesReady, ... })`（:456） |
| 作用域缺口 | `Vue3Engineer.generateCode(input)` 覆写层解构（:231-239）**从未包含 `onFilesReady`**；基类 `microcode-engineer.js:4521` 有 `onFilesReady = null`，Vue3 覆写时漏带 |
| 为何今天才炸 | commit d68b165（09-02 17:02）把旧的 `if (typeof onFilesReady === 'function')` 守卫改成**裸传 `onFilesReady`**。`typeof 未声明标识符` 安全（返回 "undefined"），裸读取必抛 ReferenceError → 守卫掩盖了既有缺口，P1-Slice2 重构揭开 |
| 触发条件 | 任一分块产出文件（res.files 非空）走到快照推送 —— 整文件成功路径或降级路径都会触发；本样本是降级后首个模板段 |

### 5.1 修复（1 行解构，已实施）

`vue3-engineer.js` generateCode 解构清单追加：

```js
onFilesReady = null, // 完整 files map 候选快照回调（graph 已传 state.onFilesReady，见 mc-component-graph-vue3.js:744）
```

与基类对齐；`pushChunkSnapshot` 内部对非函数值安全跳过（chunk-snapshot-pusher.js:95）。`execute()`（:989）本就解构 onFilesReady、:1649 用 typeof 守卫，无同类问题。

**验证**：acorn 解析通过；符号级 def（:245 `onFilesReady = null`）+ call（:456）各 1；已含入最终 `nest build`。待服务重启后真实 Vue3 任务冒烟。

### 5.2 extractComponentTagNames 漏 import（P0，**已修复**）

> 样本：`mv-max-1788356556816-9986a036`（2026-09-02 21:42–21:44）。Vue3 整文件生成成功（49.8s LLM 调用完成），但节点报 `❌ 失败: extractComponentTagNames is not defined`，Phase 2 中断。

#### 5.2.0 根因（git 取证，已闭环）

| 项 | 结论 |
|---|---|
| 报错性质 | **ReferenceError**（读取未声明裸标识符） |
| 代码位置 | `vue3-engineer.js:661` `for (const tag of extractComponentTagNames(tplMatch[1]))` |
| 历史 import | 8-18（61da00f1）`:24` 有 `import { extractComponentTagNames } from '../utils/sfc-semantics.js'` |
| 丢失时机 | 8-30 拆分重构（15ea74f）把 import 移除，但保留 `:661` 调用 → 潜伏至今 |
| 潜伏原因 | 此前 Vue3 任务先在前置环节失败（onFilesReady，5.1），修完 onFilesReady 后本轮走到此处才暴露 |
| 导出方 | `utils/sfc-semantics.js:168` 导出 `extractComponentTagNames(templateContent)` |

#### 5.2.1 修复（1 行 import，已实施）

`vue3-engineer.js` 顶部 import 段追加：

```js
import { extractComponentTagNames } from '../utils/sfc-semantics.js';
```

补回与 61da00f1 同源的 import。

**验证**：acorn 解析通过；符号级 def（:68）+ call（:667）各 1；`nest build` 通过（4m47s）；dist 同步（import 在第 68 行）；服务重启就绪（PID 72681，13030 监听正常）。

**提交**：`04a593e` #code# #P0# vue3-engineer: 补回 extractComponentTagNames import

### 5.3 code-validator import 别名不匹配（P0，**已修复**）

> 样本：`mv-max-1788357827195-f8f3163a`（2026-09-02 22:03–22:26）。Vue3 任务走到容器尺寸校验时报 `❌ 失败: _validateContainerSizePure is not defined`。

#### 5.3.0 根因（已闭环）

| 项 | 结论 |
|---|---|
| 报错性质 | **ReferenceError**（读取未声明裸标识符） |
| 代码位置 | `code-validator.js:553` `} = _validateContainerSizePure(code, rootWidth, rootHeight);` |
| import 别名 | `code-validator.js:10` `validateContainerSize as validateContainerSizePure`（**无下划线**） |
| 不匹配 | import 别名无 `_` 前缀，调用处带 `_` 前缀 → `_validateContainerSizePure` 未声明 → ReferenceError |
| 触发面 | 微码 `microcode-engineer.js:54` import 别名**带** `_`（正确），`code-validator.js` 是拆分重构时单独写出的模块、别名漏了 `_` |

#### 5.3.1 修复（2 行别名对齐，已实施）

`code-validator.js` import 别名改为与调用处一致：

```js
import {
  validateContainerSize as _validateContainerSizePure,
  validateResourceUsage as _validateResourceUsagePure,
} from '../../utils/post-process.js';
```

**验证**：动态 import 冒烟全过（validateContainerSize / validateResourceUsage 均不再抛 ReferenceError）；`nest build` 通过；dist 同步；服务重启就绪（PID 86345）。

**提交**：`185bbbd` #code# #P0# code-validator: 修复 _validateContainerSizePure import 别名不匹配

### 5.4 injectEchartsFallback 吞换行（P0，**已修复**）—— echarts import 被注释吞噬

> 样本：`mv-max-1788359428498-ee0cbe69`（2026-09-02 22:30–22:36）。vue3 模式生成，产物发布后 runtimeGate `RUNTIME-009 ReferenceError: echarts is not defined`（`eval (:3:1430)`）。**generate 模式把 RUNTIME BLOCK 降级为 warning 照常发布**（已知待办再次应验）。

#### 5.4.0 根因（产物铁证 + 注入器复现，已闭环）

| 项 | 结论 |
|---|---|
| 产物铁证 | `package/index.vue:49` = `// #endregionimport * as echarts from 'echarts'` —— echarts import 拼进 `#endregion` 注释行尾，整行变注释 |
| 报错性质 | **注释吞噬 import**（语法安全、语义致命）—— `validateVueSfc` 只查语法不查语义，注释行语法合法 → SFC 自检不拦 |
| 注入器 | `code-healer.js injectEchartsFallback`（N2 图表兜底，vue3/微码共用）：`microcode-engineer.js:5546-5590` 的运行库兜底走 `ensureRuntimeLibraryImports`（注入点在 `<script setup>` 后，安全）；vue3 N2 兜底走 code-healer 这条注入器 |
| 精确 bug | `code-healer.js:1522` 旧写法 `replace(/(\n<\/script>)/, scriptInject + '\n' + '$1')`：当 script 块末行是 `// #endregion`（无尾随空行直接 `</script>`）时，正则匹配吞掉了 `#endregion` 与 `</script>` 间唯一换行 → import 粘在注释行尾 |
| 触发条件 | 被注入文件 script 末尾恰是 `// #endregion` 且无尾随空行 —— vue3 LLM 产物大量用 `#region/#endregion` 分块，命中率高 |
| 为何部分文件正常 | `AreaChart.vue` 的 echarts import 是 LLM 自写（正常换行）✅；只有走 N2 兜底注入的 index.vue 中招 |

#### 5.4.1 修复（1 行 replace，已实施）

`code-healer.js:1522` 改锚 `</script>` 本身、替换串前后显式带换行：

```js
patched = patched.replace(/(<\/script>)/, `\n${scriptInject}\n$1`);
```

任何情况下 import 都独占新行。

**防御性审计**：同函数模板 canvas 注入为 HTML 上下文拼接（无注释风险）；`ensureRuntimeLibraryImports`（resource-import-guard.js:1033）注入点在 `<script setup>` 开标签后、前缀 `\n`（安全）；全库 grep 无其他同款 `/(\n<...>)/` 吞换行模式。

**验证**：单测 +1（code-healer.spec.ts「#endregion 紧贴 `</script>` → import 不粘连」P0 回归）**41/41 过**；acorn 54892B 通过；dist 同步（:1522 注释 + 修复）；服务重启就绪（PID 13632，13030 HTTP 200）。

**提交**：`d633a72` #code# #P0# code-healer: 修复 injectEchartsFallback script 注入吞换行

> **附带待办**：generate 模式运行时门禁把 RUNTIME BLOCK 降级 warning 照常发布 → 坏产物当成功任务交付（memory 待办「对 `is not defined` 类确定性缺失升级 BLOCK」仍未做）。

---

## 实施顺序（建议）

1. **1.2 资源 import 注入幂等性**（1 处代码、2 条正则，已 100% 复现 + 已验证修复版；今天新引入的回归，收益/风险比最高，**建议首个做**）　→ **✅ 已实施**
2. **1.1 局部重做**（治反复，收益最大、与今日复盘直接对应）　→ **✅ 已实施**
3. **1.3 覆写治理**（治崩溃，依赖 1.2 先行）　→ **✅ 已实施**
4. **2.1 锁定终态**（用户侧兜底，需前后端 + 引擎三处联动）　→ **✅ 已实施**
5. **3.1 残留清理**（环境配置 + 降噪，可随时做）　→ **✅ 已实施（代码侧降噪）**
6. **4.1 方案 C + 4.2 方案 D**（9c3aff7f 布局/图标，本晚新增取证）　→ **✅ 已实施**（C-1/C-2 + D-1/D-2，c332997，待重跑 9c3aff7f 冒烟）
7. **5.1 Vue3 onFilesReady 解构修复**（bdfe875b P0，本晚新增）　→ **✅ 已实施**（待重启后冒烟）
8. **5.2 extractComponentTagNames import 补回**（9986a036 P0，5.1 修完暴露的下一个）　→ **✅ 已实施**
9. **5.3 code-validator import 别名对齐**（f8f3163a P0）　→ **✅ 已实施**
10. **5.4 injectEchartsFallback 吞换行**（ee0cbe69 P0，echarts import 被注释吞噬）　→ **✅ 已实施**（待重启后冒烟）

### 1.2 的验证清单（改完必跑）

```bash
# 1) 单元复现：同一内容连续 3 轮 injectResourceImports，vue/echarts 必须恒定保留
# 2) 回归：对 components/ 全部 .vue 跑一遍，确认 size 与 import 数不变
# 3) 冒烟：重跑一次 figma → max 微码组件，确认终态 .vue 的 vue/echarts import 齐全、无 echarts is not defined
grep -c "from 'vue'"  temp-components/<gid>/<cid>/package/components/*.vue
grep -c "import \* as echarts" temp-components/<gid>/<cid>/package/components/*.vue
```

## 回滚与风险

- 全部改动为「确定性后处理 + 局部化重试」，不触碰生成 prompt 与模型路由，回归面小；
- `_reviseFiles` 引入后，若 `resolveTargetFilesFromIssues` 定位错误，最坏退回全量重生成（等于现状），不会更坏；
- 运行库 import 兜底为纯函数幂等注入，只补缺失、绝不删已有内容，零回归风险；
- 回滚开关：`_reviseFiles` 与锁检查可分别用 env 开关（如 `LOCAL_RETRY=false` / `LOCK_ON_SATISFY=false`）快速关闭。

---

## 实施记录与验证（2026-09-02 21:15，按「帮我执行」）

| 节 | 改动文件 | 验证结果 |
|---|---|---|
| 1.1 | `utils/revision-files.js`（+2 函数）、`graphs/mc-component-graph-phase2.js`（import + 3 落点 `_reviseFiles`） | acorn 解析通过；动态 import 冒烟 4 态全绿（精确/占位 hasAmbiguous/basename/退化空） |
| 1.2 | `utils/resource-import-guard.js`（3 正则收窄） | `resource-import-guard.spec.ts` 36/36；完整 jest 对比基线零新增 |
| 1.3 | `utils/resource-import-guard.js`（+`ensureRuntimeLibraryImports`）、`roles/microcode-engineer.js`（终验兜底后注入 + 覆写后重校验回退） | 5 场景冒烟全绿；acorn 解析 214373B 通过 |
| 2.1 | 新增 `utils/session-lock-registry.js`；`tasks/tasks.service.ts`（+`LockSatisfiedRecord` + `lockTask`）、`tasks.controller.ts`（+`POST :sessionId/lock`）；`roles/microcode-engineer.js`（2 处条件写盘锁检查）；前端 `api/generator/generator.ts` + `components/generate/ResultPanel.vue` | registry 冒烟 5 项全绿（含 `LOCK_ON_SATISFY=false` 开关）；`nest build` 通过（首版 TS2322 已修）；dist 符号核对齐（lockTask/isSessionLocked/registry 文件均存在） |
| 3.1 | `utils/workspace-preview-publisher.js`（MAX_DELETE_ATTEMPTS 5→1、ERROR→WARN） | dist `MAX_DELETE_ATTEMPTS = 1` 确认 |
| 构建 | `nest build`（两次，共约 14min，第二次含全部改动） | ✅ references copied to dist；后端全部改动已生效待重启 |
| 前端类型 | `npx vue-tsc --noEmit` | 报错仅来自 `src/workspace/custom-components/` 生成产物（既有，非本次改动）；`src/api/generator.ts` / `ResultPanel.vue` 待过滤结果确认 |
| jest 基线 | 改动前后两次完整运行 | FAIL 集合与 `6 failed / 672 passed` 一致 → 零回归 |
| 5.2 | `roles/vue3-engineer.js`（+1 import） | acorn 过；def(:68)/call(:667) 各 1；nest build 4m47s 过；dist 同步；服务重启（PID 72681） | 
| 5.3 | `roles/microcode/code-validator.js`（import 别名 `_` 对齐） | 动态 import 冒烟全过（不再 ReferenceError）；nest build 过；服务重启（PID 86345） |
| 5.4 | `roles/microcode/code-healer.js`（:1522 replace 换行）、`code-healer.spec.ts`（+1 P0 回归） | 单测 **41/41**；acorn 54892B 过；dist 同步；服务重启（PID 13632，13030 HTTP 200） |
| 门禁 | `roles/screenshot-renderer.js`（+`hasDeterministicRuntimeMissing`）、phase2/vue3 graph 降级条件收紧 | 6 态冒烟全绿（is not defined→true / 404→false / ResizeObserver→false）；acorn 通过 |
| 4.1 C | `validators/header-slot-validator.js`（未匹配写 rejectedNodes）、phase2/vue3 graph（剔除）、`resource-mounter.js ensureHeaderSlots`（bbox 越界过滤） | acorn 通过；C-2 过滤逻辑基于 9c3aff7f 真值验证（tabs-icon y=905 与 header 不相交 → 剔除） |
| 4.2 D | `resource-mounter.js injectImgChild`（+size 入参，figmaBox 24×24 精确宽高）+ `autoMountUnusedIcons`（x 升序 + 追加式注入） | D-1/D-2 冒烟 3 项全绿（size→24px / 无 size→max-48 / icon1 在 icon2 前）；acorn 通过 |

### 4.3 新增问题：后处理污染 + 快照冻结不同步（2026-09-08）

**现象**：`mc-max-1788871378450-011998bb` 详情页 LESS 编译失败，报错指向 `code-snapshots/.../package/index.vue:396:1`，但当前 workspace 的 `index.vue` 只有 50 行；说明冻结快照仍是坏版，而 workspace 已经历过终态自愈。

**根因**：
- `style-class-consolidator` 只校验括号平衡，未校验收敛结果是否混入分隔文本 / 非 Less 片段；
- `microcode-engineer.writeFiles` 的终态落盘与 `finalTruthFiles` 回写不同步，导致快照冻结的是旧内容；
- 运行时门禁分级已经改善渲染阶段误阻断，但无法修复已污染的产物源。

**治理动作**：
1. 终态落盘后必须从磁盘回读并回写 `finalTruthFiles`，确保快照与磁盘终态一致；
2. `common.less` 收敛前增加内容净化校验，分隔线 / 孤立注释 / 非 Less 片段直接丢弃或回退；
3. `style-class-consolidator` 遇到异常只回退，不叠加后续自愈；
4. 快照冻结前补 `index.vue/common.less` 语法与行数一致性校验；
5. 预览页与 workspace 产物增加 hash / 行数差异告警。

**验证**：再次生成同类组件时，快照行数应与 workspace 对齐，`common.less` 不应再出现 `==============` 这类碎片，预览与参考图的差异只允许来自视觉分析误差，不应再来自快照/终态不一致。

> **未做**：真实任务冒烟（重跑 figma → max 组件验证终态 import 齐全 + 锁定按钮端到端 + 5.4 修复后 vue3 图表可渲染 + 4.1/4.2 重跑 9c3aff7f 验证布局/图标）需用户在前端操作；C-3（visual-parser prompt 分层提示，非本期）。
