# mvgo 部署指南（统一）

> 版本：v1.1 ｜ 日期：2026-08-12（更新：补充 ECS 实测真实拓扑与切 Java 方案）
> 适用范围：Node→Java 迁移完成后的双运行时架构（Spring Boot + NestJS 共存）
> 配套文档：`../architecture/Node-Java-职责边界.md`（架构边界）。旧 zip 分包手册已从当前 docs 仓库移除，不再作为部署依据。

---

## 0. 部署前必读：架构速览

mvgo 后端现在是**两个运行时共存**：

```
浏览器 / 门户 iframe
      │
      ▼  :80 (nginx)  /  :2610 (vite dev)
┌──────────────────────────────────────────────┐
│  Java  :8080  —— 统一 API 网关 + 业务 CRUD 层  │
│   • 鉴权 SessionGuardInterceptor（校验 Token） │
│   • 统一响应信封 Result<T>                      │
│   • 原生 Controller：auth/admin/group/projects/ │
│     sessions/skills/documents/models/component- │
│     split/operation-log/health                 │
│   • NodeProxyController：反向代理到 Node        │
│            │  /api/tasks /config /phase2 /vue3 │
│            │  /demo /preview /lite /microcode … │
│            ▼                                    │
│  Node  :13030 —— AI 生成引擎 + 内存态任务执行   │
└──────────────────────────────────────────────┘
```

**关键结论（与旧架构最大的不同）**：

| 维度 | 旧架构 | 当前架构（必须按此部署） |
|------|--------|--------------------------|
| `/api` 入口 | 直连 Node 13030 | **统一进 Java 8080** |
| AI 管线（phase2/vue3/demo/preview） | Node 直连 | **经 Java 代理**到 Node |
| 鉴权 | Node 侧 | **Java 侧 SessionGuard** |
| 唯一例外 | — | `/api/progress`（SSE）前端直连 Node 13030 |

### ⚠️ 生产真实部署形态（2026-08-12 ECS 实测）

> 此前文档假设"ECS = zip 直起"或"docker-compose 容器化"，**均与生产实测不符**。生产实际是：

- **Nginx 是独立 docker 容器**（公司统一网关镜像 `hub.microvideo.cn/midware/nginx:1.30.4-alpine-logrotate`，容器名 `nginx`，映射 `80 / 8282 / 8888`）
- **后端是内网裸进程/服务**（非本仓库 docker-compose 编排）：
  - `192.168.112.1`（本机主内网 IP）：Node `13030`、Nacos `8848`、portlet `39680`、mvbtapi `15503`
  - `192.168.112.2`：microcode-framework `18080`
  - **Java `8080` 计划部署在本机 `/home/mvbt/mvgo/backend-java/`，截至本版尚未启动**
- **用了 Nacos（8848）** 作配置/注册中心（本地 dev 无）
- 仓库根 `docker-compose.yml` + `docker/nginx/nginx.conf` 仅用于**新环境容器化部署参考**，生产当前未用该编排

> 本机 `hostname -I` 实测：`192.168.112.1 10.200.200.1 10.200.201.x`（后者为 docker 网桥，忽略）。**所有 `proxy_pass` 必须用 `192.168.112.1`，不能用 `127.0.0.1`**（nginx 容器内 127.0.0.1 是容器自身，非宿主）。

---

## 1. 通用前置

### 1.1 端口与角色

| 服务 | 端口 | 角色 | 持久化 |
|------|------|------|--------|
| 前端（Nginx 静态 + 反代） | 80 | 静态资源 + `/api` 反代 | 无状态 |
| Java（Spring Boot） | 8080 | 统一入口 / 鉴权 / CRUD / 代理 | MongoDB |
| Node（NestJS） | 13030 | AI 引擎 / 任务执行 / SSE | MongoDB + 内存 `Map` |
| Nginx 监控 | 8282 | `stub_status` 状态面板（运维用，非业务） | — |
| 历史遗留 | 8888 | 与 80 同 server 多开端口（见 §2.2，部署时摘掉） | — |

### 1.2 MongoDB（两端共用，最重要）

Java 与 Node **必须连同一个 MongoDB 实例与库**，否则用户/组/项目/会话/文档各写各的库、数据互不可见。

- **生产**：阿里云 RDS `langgraph-server`（URI 见 `backend-node/.env.production` 的 `MONGODB_URI`）
- **两端通过同一个 `MONGODB_URI` 取值**：Node 从 `.env.production` 读；Java 从启动环境变量 `MONGODB_URI` 读（必须一致）

### 1.3 密钥文件清单（均 gitignored，严禁入库）

| 文件 | 用途 | 部署位置 |
|------|------|----------|
| `backend-node/.env` | Node 基础（Figma 限流等） | 生产（内网裸服务） |
| `backend-node/.env.production` | Node 生产密钥（SESSION_SECRET / FIELD_ENCRYPTION_KEY / MONGODB_URI / MC_PREVIEW_BASE_URL / DEFAULT_ADMIN_UIDS） | 生产 |
| `backend-node/.env.development` | Node 开发环境（含 `DEV_AUTO_LOGIN=true`） | 仅本地 dev，生产禁用 |
| `backend-java/` | Java jar + 启动（prod 环境变量在启动命令行注入） | 生产 `/home/mvbt/mvgo/backend-java/` |

### 1.4 本地开发启动

> 本机完整启动三件套（前端 / Node / Java），访问 `http://localhost:2610`。前置依赖：**MongoDB**（`brew services start mongodb-community` 或 `mongod`），监听 `127.0.0.1:27017`。

#### 1.4.1 启动顺序与端口

| 顺序 | 服务 | 端口 | 启动方式 |
|------|------|------|----------|
| 0 | MongoDB | 27017 | 自启 / `brew services start mongodb-community` |
| 1 | Node 后端 | 13030 | 根目录 `node start-node.js`（已含 `DEV_AUTO_LOGIN=true`） |
| 2 | Java 后端 | 8080 | 根目录 `node start-java.js`（dev profile + `DEV_AUTO_LOGIN=true`） |
| 3 | 前端 Vite | 2610 | 根目录 `node start-frontend.js` |

#### 1.4.2 Node 后端

```bash
cd backend-node

# 构建（增量构建；按需清理 tsbuildinfo，不要删除 dist）
rm -f tsconfig.build.tsbuildinfo
npm run build

# 返回项目根目录，以脱离当前会话的方式启动 Node（含 DEV_AUTO_LOGIN=true）
cd ..
node start-node.js
```

环境变量来源：`.env` + `.env.development`（`DEV_AUTO_LOGIN=true`、`MONGODB_URI=mongodb://localhost:27017/langgraph-server`）。
校验：`curl http://localhost:13030/api/user/info -H "Token: dev"` 应返回用户信息而非 401。

**已知坑**：
- 日常构建不要删除 `dist/`；按需执行 `rm -f tsconfig.build.tsbuildinfo` 后再 `npm run build`。确需干净构建时，先停止服务并按项目清理流程处理，避免误删其他产物。
- 端口 13030 残留旧进程会让新进程 `EADDRINUSE` 静默退出；启动前先执行 `lsof -nP -iTCP:13030 -sTCP:LISTEN` 核对 PID，再按确认后的 PID 停止进程。

#### 1.4.3 Java 后端（⚠️ 必须注入 `DEV_AUTO_LOGIN=true`，否则 Token: dev 被拒 401）

以下命令从项目根目录执行：

```bash
# 启动（dev profile，连本地 MongoDB，开启开发免登录）
node start-java.js
```

| 变量 | 开发值 | 生产值 | 说明 |
|------|--------|--------|------|
| `DEV_AUTO_LOGIN` | **`true`** | 不设置 | 未设置时 Java `SessionGuardInterceptor` 要求真实门户 Token，`Token: dev` 会 401 |
| `MONGODB_URI` | 默认 `mongodb://localhost:27017/langgraph-server` | 必填（RDS URI） | 两端必须指向同一库 |
| `SPRING_PROFILES_ACTIVE` | `dev` | `prod` | prod profile 下 `DEV_AUTO_LOGIN` 强制失效 |
| `NODE_BACKEND_URL` | 默认 `http://localhost:13030/api` | `http://192.168.112.1:13030/api` | Java 反向代理 Node 地址 |

**沙箱环境陷阱**：某些 CI/沙箱会注入 `SERVER__PORT=0`，Spring 宽松绑定会把 `server.port` 设为 0（随机端口），启动后 `curl :8080` 连不上。临时修复：`unset SERVER__PORT` 或显式 `SERVER__PORT=8080`。

校验：`curl http://localhost:8080/api/auth/current -H "Token: dev"` 返回 `dev-local` 用户即正常。

#### 1.4.4 前端

```bash
# 首次启动依赖安装
cd frontend
npm install
cd ..

# 从项目根目录启动 Vite
node start-frontend.js
```

代理规则（`frontend/vite.config.js`，以源码中的具体前缀配置为准）：
- Java 8080：`/api/auth`、`/api/group`、`/api/component-split`、`/api/projects`、`/api/admin`、`/api/page-generator`、`/api/sessions`、`/api/chat`、`/api/skills`、`/api/documents`、`/api/token-usage`、`/api/microcode/doc`、`/api/models`
- Node 13030：`/api/component`、`/api/tasks`、`/api/screen-layout`、`/api/user/git-credential`、`/api/workflows`、`/api/apifox`、`/api/progress`、`/api/phase2`、`/api/vue3`、`/api/demo`、`/api/preview`、`/api/config`、`/api/lite` 及默认 `/api`
- 静态资源 `/mvgo/` → 同 Vite 服务

访问：`http://localhost:2610/mvgo/`，dev 模式前端请求自动带 `Token: dev`。

---

## 2. 部署方式 A：生产 ECS（公司 nginx 容器 + 内网裸服务）【当前生产实际形态】

> 本节基于 2026-08-12 在 ECS（`iZbp1hik17it6sucvjuotaZ`，内网 `192.168.112.1`）实测，是**当前生产真实形态**。

### 2.1 服务器目录结构（实测）

```text
/home/mvbt/nginx/                  ← Nginx 容器运行目录（挂载进容器）
  ├─ docker-compose.yml            ← 仅启动公司统一 nginx 容器
  ├─ nginx.conf                    ← 容器内 /etc/nginx/nginx.conf（http 主配置，include vhost/*.conf）
  ├─ gzxj.conf                     ← 可能是主配置或 vhost 源（见下方定位）
  ├─ vhost/gzxj.conf               ← 业务路由（容器内 /etc/nginx/vhost/gzxj.conf 的源）
  ├─ www/mvgo/                     ← 前端静态资源（dist.zip / distb.zip）
  ├─ logs/  html/

/home/mvbt/mvgo/                  ← 应用源码/数据目录
  ├─ backend-node/                 ← Node 后端（内网 192.168.112.1:13030）
  ├─ backend-java/                 ← Java 后端（计划部署，本机 192.168.112.1:8080，尚未启动）
  ├─ frontend/  workspace/  temp-components/
  ├─ nacos/  nacos-standalone/
  ├─ dist.tar.gz  backend-lite.zip  backend-node-fix-preview.zip
```

> **定位 gzxj.conf 宿主源（改之前先确认）**：
> ```bash
> grep -rl "listen 8888" /home/mvbt/nginx/     # 命中即生效源（通常 /home/mvbt/nginx/vhost/gzxj.conf 或 /home/mvbt/nginx/gzxj.conf）
> ```
> 容器内路径固定为 `/etc/nginx/vhost/gzxj.conf`（`nginx -T` 可见），宿主编排挂载其中之一。

### 2.2 端口说明（8888 / 8282 真相）

实测 `nginx -T` 结论：

- **8888**：`server { listen 80; listen 8888; }` —— 与 80 **同一 server、同一套 location 规则**（前端静态 + `/api` 代理），非独立业务。历史遗留多开端口（外部调用方可能曾走 8888，后统一到 80 未摘）。**切 Java 时一并摘掉 `listen 8888;`**。
- **8282**：主配置 `server { listen 8282; location /basic_status { stub_status on; } }` —— nginx 状态监控面板，运维用，非业务，保留。
- **80**：业务主入口。

### 2.3 ⚠️ 切 Java 入口方案（迁移后必做，当前生产 `/api` 仍过期指向 Node）

当前 `gzxj.conf`：
```nginx
location /api { proxy_pass http://192.168.112.1:13030/api; }   # ← 直连 Node，绕过 Java 8080（过期）
```
必须改为 `/api → Java 8080`，仅 `/api/progress → Node 13030`。

**完整 gzxj.conf 修正版（基于实测，仅改路由相关段，其余内部服务保留）**：

```nginx
server {
    listen 80;
    # listen 8888;   ← 历史遗留，已摘（与 80 同站多端口，无独立用途）

    server_name _;
    access_log logs/proxy-access.log main;
    error_log logs/proxy.error.log;

    proxy_set_header X-Forwarded-Port 443;
    if ($request_method !~ ^(GET|POST)$) {
        return 405 '{"rid":"","code":405,"message":"Method Not Allowed","detail":"Only GET and POST are allowed","data":null}';
    }
    proxy_set_header Host $host:$server_port;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    ignore_invalid_headers off;
    client_max_body_size 0;
    proxy_buffering off;

    root /www;

    location / {
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }
    location /preview/ { try_files $uri $uri/ /mvgo/index.html; }
    location /mvgo/    { try_files $uri $uri/ /mvgo/index.html; }

    # ===== 迁移后新增：AI 引擎 SSE 直连 Node（按 sessionId 鉴权，无 Token 依赖） =====
    location /api/progress {
        proxy_pass http://192.168.112.1:13030/api/progress;
        proxy_http_version 1.1;
        proxy_set_header Host $host:$server_port;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header SourceHost $remote_addr;
        proxy_buffering off;
        proxy_cache off;
        chunked_transfer_encoding off;
        tcp_nodelay on;
    }

    # ===== 迁移后修改：统一业务入口改走 Java 8080（原直连 Node 13030） =====
    location /api {
        proxy_pass http://192.168.112.1:8080/api;
        proxy_set_header Host $host:$server_port;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header SourceHost $remote_addr;
    }

    # ===== 以下内部服务原样保留（非 mvgo 业务，勿动） =====
    location /nacos {
        proxy_pass http://192.168.112.1:8848/nacos;
        proxy_set_header Host $host;
        proxy_set_header SourceHost $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_pass_header Set-Cookie;
        proxy_cookie_path /nacos /nacos;
        proxy_redirect http://192.168.112.1:8848/nacos/ /nacos/;
    }
    location /portlet {
        proxy_pass http://192.168.112.1:39680/portlet;
        proxy_set_header SourceHost $remote_addr;
    }
    location /mvbtapi {
        proxy_pass http://192.168.112.1:15503/mvbtapi;
        proxy_set_header SourceHost $remote_addr;
    }
    location /microcode-framework {
        proxy_pass http://192.168.112.2:18080/microcode-framework;
        proxy_set_header SourceHost $remote_addr;
    }
}
```

> 注：`/api/progress` 必须放在 `/api` **之前**（精确前缀优先）。`/nacos` `/portlet` `/mvbtapi` `/microcode-framework` 是内部服务，原样保留。

改完重载（容器网络内，无需重启容器）：
```bash
# 1) 在宿主编辑源文件（见 §2.1 定位命令）
vim <gzxj.conf 宿主源>
# 2) 校验配置再重载
docker exec nginx nginx -t
docker exec nginx nginx -s reload
```

### 2.4 前端发布（解压到 www 目录）

```bash
cd /home/mvbt/nginx/www/mvgo
# 按实际发布方式覆盖 dist/ 或解压 dist.zip
# 发布后建议 nginx -s reload（见 §2.3）
```

### 2.5 ⚠️ Node 后端启动（production 模式，禁止使用旧开发启动方式）

**问题**：本地开发启动器 `start-node.js` 会设置 `NODE_ENV=development` 和 `DEV_AUTO_LOGIN=true`，仅限本地联调。
生产必须加载生产环境变量并以 `NODE_ENV=production` 启动，禁止复用本地开发启动器。

**正确做法**：
```bash
cd /home/mvbt/mvgo/backend-node
pids=$(lsof -ti tcp:13030 2>/dev/null || true)
[ -z "$pids" ] || kill -9 $pids
sleep 1

set -a; . ./.env; set +a
set -a; . ./.env.production; set +a
NODE_ENV=production \
  nohup node dist/main.js >> server.log 2>&1 &
```

### 2.6 ⚠️ Java 后端启动（生产，尚未部署）

Java 计划部署在本机 `/home/mvbt/mvgo/backend-java/`，端口 `192.168.112.1:8080`。**启动前必须先 `mvn package` 把 jar 传到该目录**。

```bash
cd /home/mvbt/mvgo/backend-java
pids=$(lsof -ti tcp:8080 2>/dev/null || true)
[ -z "$pids" ] || kill -9 $pids
sleep 1

MONGODB_URI='mongodb://root:<密码>@<RDS_HOST>:3717/langgraph-server?replicaSet=mgset-xxxxxx&authSource=admin' \
SPRING_PROFILES_ACTIVE=prod \
NODE_BACKEND_URL=http://192.168.112.1:13030/api \
nohup java -jar mvgo-app/target/mvgo-app-1.0.0-SNAPSHOT.jar > server.log 2>&1 &
```

| 变量 | 值 | 说明 |
|------|----|------|
| `MONGODB_URI` | 与 Node `.env.production` 完全一致 | 两端共用同一 RDS 库 |
| `SPRING_PROFILES_ACTIVE` | `prod` | 启用 prod profile |
| `NODE_BACKEND_URL` | `http://192.168.112.1:13030/api` | Java 代理 Node 地址（**用内网 IP，非 localhost/127.0.0.1**） |
| `DEV_AUTO_LOGIN` | **不设置** | 生产 fail-closed，仅靠 Token 鉴权 |

检查：`curl --noproxy '*' -s http://localhost:8080/api/health`（Java `HealthController` 须返回正常响应；Node `13030` 不提供该路由）

### 2.7 发布顺序（铁律）

1. 构建 + 传 jar → `/home/mvbt/mvgo/backend-java/`
2. **先起 Java（§2.6），确认 `:8080` 通**
3. 再改 `gzxj.conf` 路由（§2.3）+ `nginx -s reload`
4. 最后验证（§2.8）

> ⚠️ 第 2 步 Java 未起前**不要**改第 3 步路由，否则 `/api` 全 502。

### 2.8 验证

```bash
curl -s http://localhost/api/auth/current              # 经 nginx → Java 8080
curl -s http://localhost/api/v2/pipeline/health         # Java 代理 → Node ok
curl -s http://localhost/api/progress/<sessionId>       # 直连 Node SSE（如可用）
```

---

## 3. 部署方式 B：Docker Compose（仓库，推荐新环境）

> 配置文件：`docker-compose.yml`（已清除 mysql 死配置、Node 双 env_file 加载生产密钥）、`docker/nginx/nginx.conf`（路由已修正确）、根 `.env` / `.env.example`。
> **注意**：此编排用于新环境容器化部署参考；当前生产形态见 §2（公司 nginx 容器 + 内网裸服务 + Nacos），未使用该 compose。

### 3.1 准备密钥（gitignored，部署机本地准备）

```bash
cp .env.example .env
#   编辑 .env，填入真实 MONGODB_URI（= backend-node/.env.production 的值）
# backend-node/.env.production 从安全渠道获取（gitignored，不随仓库分发）
```

### 3.2 启动

```bash
docker compose up -d
docker compose logs -f backend-java
```

compose 保证：Java 注入 `MONGODB_URI` + `SPRING_PROFILES_ACTIVE=prod` + `NODE_BACKEND_URL=http://backend-node:13030/api`；
Node 加载双 env_file（`NODE_ENV=production`）；Nginx `/api → Java`、`/api/progress → Node`（见 `docker/nginx/nginx.conf`）。

### 3.3 验证

```bash
curl -s http://localhost:8080/api/health
curl -s http://localhost:8080/api/v2/pipeline/health
```

---

## 4. 发布前核对清单

- [ ] **MongoDB 一致**：Java 与 Node 的 `MONGODB_URI` 指向同一 RDS 实例与库
- [ ] **Nginx 路由**：`/api` → `192.168.112.1:8080`（Java），仅 `/api/progress` → `192.168.112.1:13030`（Node）；`listen 8888` 已摘
- [ ] **Nginx 配置源**：已定位并编辑 `/home/mvbt/nginx/.../gzxj.conf` 宿主源，`nginx -t` 通过
- [ ] **Node 是 production 模式**：`NODE_ENV=production`，未加载 `.env.development`、未设 `DEV_AUTO_LOGIN`
- [ ] **Java 带 prod 环境变量**：`SPRING_PROFILES_ACTIVE=prod` + `MONGODB_URI` + `NODE_BACKEND_URL=http://192.168.112.1:13030/api`
- [ ] **顺序正确**：Java 先起并 `:8080` 通，再改 nginx 路由 + reload
- [ ] **前端 index.html 无强缓存**：发布后硬刷新验证（见 §5）

---

## 5. 已知坑（发布必看）

1. **前端 `index.html` 缓存**：静态资源带 hash 可长缓存，但 `index.html` 不带。`gzxj.conf` 建议给 `location = /mvgo/index.html` 加 `Cache-Control: no-cache`；发布后硬刷新（Cmd/Ctrl+Shift+R）验证。
2. **`proxy_pass` 必须用内网 IP**：nginx 是独立容器，`127.0.0.1` 是容器自身非宿主。所有后端 `proxy_pass` 用 `192.168.112.1`（已确认本机主内网 IP）。
3. **Node 沙箱回收（仅本地开发）**：本地后台进程约 6 分钟被回收；ECS/Docker 常驻不受影响。
4. **tsc 增量缓存（仅 Node 本地构建）**：`dist/` 被清后须 `rm -f tsconfig.build.tsbuildinfo` 再 `nest build`。
5. **curl 走代理**：`HTTP_PROXY` 会导致 curl 返回 502/000，排查用 `curl --noproxy '*'`。
6. **⚠️ 生产网关仅允许 GET/POST（2026-09-03 实测）**：公司 nginx 容器 `gzxj.conf` 有方法过滤器：
   ```nginx
   if ($request_method !~ ^(GET|POST)$) {
       return 405 '{"rid":"","code":405,"message":"Method Not Allowed","detail":"Only GET and POST are allowed","data":null}';
   }
   ```
   **影响**：前端 `http.delete()` / `http.put()` / `http.patch()` 调用会直接返回 405，请求不到后端。
   **修复**：后端 `@DeleteMapping`/`@Delete`/`@Put` 改为 `@PostMapping`，前端 `http.delete()`/`http.put()` 改为 `http.post()`。已修复全量 DELETE/PUT 路由：
   - **Java `AdminController.java`**：`@DeleteMapping("operation-log")` → `@PostMapping("operation-log/delete")`；`@DeleteMapping("users/{id}")` → `@PostMapping("users/{id}/delete")`；`@DeleteMapping("components/{id}")` → `@PostMapping("components/{id}/delete")`
   - **Node `page-skeleton.controller.ts`**：`@Put(':groupId/:id')` → `@Post(':groupId/:id/update')`；`@Delete(':groupId/:id')` → `@Post(':groupId/:id/delete')`；`@Delete(':groupId/:id/file')` → `@Post(':groupId/:id/file/delete')`
   - **Node `component.controller.ts`**：`@Delete(':componentId/file')` → `@Post(':componentId/file/delete')`
   - **Node `recipes.controller.ts`**：`@Delete(':name')` → `@Post(':name/delete')`
   - **Node `agent-builder.controller.ts`**：`@Delete(':name')` → `@Post(':name/delete')`
   - **Node `admin.controller.ts`**：`@Delete('users/:id')` → `@Post('users/:id/delete')`（合并重复方法）
   - **前端 `ManagementView.vue`**：`http.delete('/api/admin/operation-log')` → `http.post('/api/admin/operation-log/delete')`
   - **前端 `page-skeleton.ts`**：`http.delete(...)` → `http.post(.../delete)`
   - **前端 `demo/index.vue`**：`http.delete(.../file?path=...)` → `http.post(.../file/delete?path=...)`
   - **前端 `workflow.ts`**：`axios.delete('/api/recipes/${name}')` → `axios.post('/api/recipes/${name}/delete')`
   **验证**：前端 `http.delete(` / `http.put(` / `http.patch(` 全局搜索已 0 匹配，`npm run build` 通过。
7. **⚠️ SSE / 预览 iframe 401（2026-09-03 实测）**：
   - **根因**：预览 iframe 内 `getAuthToken()` 无法从父窗口取到门户 Token（跨域/非 portlet 环境），且 `SessionGuard` 只认 Header 不认 query token。
   - **修复链**：
     1. **Node `SessionGuard`** 兼容 query token：`request.query?.token`（`session.guard.ts`）
     2. **前端 `api-token.js`** 兜底取 URL token：`window.location.search` + `window.location.hash` 双路径
     3. **`preview-resolver.ts`** 构建预览 URL 时拼 `token` 到 query
     4. **`buildSnapshotFileUrl()`** 拼接 token 参数，保证运行时 loader 读快照文件不 401
   - **部署注意**：Node `session.guard.ts` 改动需 `npm run build` 生成 `dist/auth/session.guard.js` 才生效；前端改动需 `npm run build` 后发布 dist。

---

## 6. 回滚 / 备份

### 6.1 发布前备份

| 组件 | 备份内容 | 路径 |
|------|----------|------|
| 前端 | 旧版本 dist 目录或 zip | `/home/mvbt/nginx/www/mvgo/` |
| Node | dist、`.env.production`、`data/`、`workspace/` | `/home/mvbt/mvgo/backend-node/` |
| Java | jar 包、启动脚本 | `/home/mvbt/mvgo/backend-java/` |
| Nginx | `gzxj.conf` | `/home/mvbt/nginx/vhost/gzxj.conf` |

### 6.2 回滚方式

- **前端回滚**：还原旧 dist 目录 / 解压旧 `dist.zip`，`nginx -s reload`。
- **Node 回滚**：还原旧 `dist/`、`npm ci`（如依赖变更）、还原旧 `.env.production`，重新 `nohup node dist/main.js`。
- **Java 回滚**：还原旧 jar 包，按 §2.6 重启。
- **Nginx 路由回滚**：还原旧 `gzxj.conf`（把 `/api` 改回 `proxy_pass http://192.168.112.1:13030/api`），`docker exec nginx nginx -s reload`。

---

## 附录 A. Node 侧资源与依赖清单

> 来源：整合自 `node-cloud-deploy-resources.md`（原 Node-only 旧文档，仅保留仍相关项）。

### A.1 计算资源

| 项目 | 规格 | 说明 |
|------|------|------|
| 运行时 | Node.js 22 | `Dockerfile` 基础镜像 `node:22-alpine` |
| 端口 | 13030（内部） | 网关/Nginx 映射到 80/443 |
| CPU | ≥2 核 | LangGraph AI 管线 + Puppeteer 截图并发 |
| 内存 | ≥4 GB | Puppeteer Chromium 单实例 + AI 请求处理 |
| 磁盘 | ≥20 GB | node_modules 422M + dist 4.4M + workspace/data 增长 |

### A.2 MongoDB（必需）

- 云端需提供 MongoDB 实例（Atlas / 云厂商托管 / 自建），生产端点见 §2.6 环境变量表。
- 用途：组件、分组、会话、文档、技能、Token 用量等。

### A.3 持久化存储 Volume

| 目录 | 用途 | 备注 |
|------|------|------|
| `workspace/` | AI 生成的 Vue3 组件产物 | 需与前端共享或改对象存储 + CDN 预览 |
| `data/` | `tasks.json`、api-bindings.json、apifox-tasks.json、metrics-history.jsonl 等 | JSON 文件存储，容器重启需保留 |

### A.4 Chromium / Puppeteer

| 项目 | 说明 |
|------|------|
| 用途 | `screenshot-renderer.js` 对生成的 Vue 组件截图 |
| 系统依赖 | `chromium nss freetype harfbtz ca-certificates ttf-freefont font-noto-cjk` |
| 环境变量 | `PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true` + `PUPPETEER_EXECUTABLE_PATH` |
| 修复版 Dockerfile | `backend-node/Dockerfile.cloud`（原 `Dockerfile` 缺 Chromium 与构建工具链） |

### A.5 原生编译依赖

`node:22-alpine` 默认无构建工具链，`npm ci` 会失败。修复版 Dockerfile 已加 `apk add python3 make g++`，用于编译：

| 包 | 说明 |
|----|------|
| bcrypt | C++ 原生，密码哈希 |
| sharp | C++ 原生，图像处理/压缩 |
| sqlite-vec | 原生向量（源码未见实际调用） |
| tunnel | SSH 隧道，可能未被使用 |

### A.6 外部 API 服务（服务端调用，用户无感）

> **架构澄清**：所有外部 AI API 均由 **Node 后端（服务端）** 发起请求，用户浏览器只与 ECS 通信。
> 因此：
> - 用户在前端配置中心填写哪家的 Key，**都不需要额外操作**；
> - ECS 安全组 **出方向** 通常默认全放行即可，无需针对具体域名做白名单；
> - 以下清单仅用于 **网络排障**（如某次调用超时/500，检查对应域名是否通）。

| 服务 | 用途 | 端点 | 凭据来源 |
|------|------|------|----------|
| DashScope (Qwen) | 视觉任务 — 图像分析/VisualParser | `dashscope.aliyuncs.com/compatible-mode/v1` | 用户在前端配置中心填写 |
| Anthropic (Claude) | 文本任务 — 代码生成/审查/布局 | `code.newcli.com/claude/aws` | 用户在前端配置中心填写 |
| Figma API | 设计稿拉取、组件分解 | `api.figma.com` | 用户在前端配置中心填写 |

> AI Key 由用户在前端配置中心页面填写，存入后端 `data/ai-config.json`，运行时动态传入，**服务端 `.env.production` 无需填写任何 AI Key**。

### A.7 Node 环境变量（生产）

| 变量 | 生产要求 | 说明 |
|------|----------|------|
| `MONGODB_URI` | 必填 | 与 Java 共用同一 RDS |
| `SESSION_SECRET` | 必填，随机串 | 覆盖默认值，禁止硬编码 |
| `NODE_ENV` | `production` | 生产模式 |
| `PORT` | `13030` | 内部端口 |
| `FIELD_ENCRYPTION_KEY` | 必填 | AES 字段加密 |
| `MC_PREVIEW_BASE_URL` | 视业务而定 | 微码预览基地址 |
| `DEFAULT_ADMIN_UIDS` | 必填 | 默认管理员门户 uid 列表 |
| `PORTAL_BASE_URL` | 必填 | 门户回调地址 |
| AI Keys | 可留空 | 运行时前端传入 |
