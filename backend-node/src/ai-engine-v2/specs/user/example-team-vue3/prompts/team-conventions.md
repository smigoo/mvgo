# 团队内部约定（示例）

本文件用于演示「用户规范包自带 prompt 素材」的注入机制，内容仅作示意。

## 命名

- 组件目录使用 kebab-case，统一 `t-` 前缀
- 组合式函数以 `use` 开头

## 样式

- 颜色一律走 CSS 变量，禁止硬编码色值
- 间距使用 4 的倍数

## 禁止项

- 禁止在模板中写复杂表达式，抽到 computed
- 禁止使用 `any`
