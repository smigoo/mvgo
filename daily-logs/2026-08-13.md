# 2026-08-13 工作日志

## 入口页原型落地为真实 Vue 组件
- 创建 `frontend/src/views/generate/EntryPage.vue`：把 `prototype-component-generate-entry.html` 生产化。
  - `<script setup lang="ts">` + scoped style（引用项目 `tokens.css` 语义变量，蓝色品牌 + `--brand-cta` 绿色 CTA，无硬编码色值）
  - 复用 `@/api/lite` 的 `generateLite`（入参 `figmaUrl / imageBase64 / htmlContent / requirementDoc + componentType('vue3'|'microcode') + generationTier('lite'|'max')`），成功后跳 `/tasks/:sessionId`
  - 复用 `TierToggle` / `CodeTypeToggle` 组件；保留智能剪贴板识别、动态校验、生成状态过渡、偏好 localStorage
  - 最近生成列表为 MVP 静态占位，待接 `GET /api/tasks?limit=5`
- 路由 `generator-routes.js`：`/generator` 由重定向工作台改为指向 `EntryPage`；`/generator/components` 工作台路径不变
- 验证：SFC 经 `@vue/compiler-sfc` 解析通过（template / scriptSetup / scoped style 正确）；**未做 dev 运行时验证**
- 交付物：`dev-handoff-entry-page.md`（对接说明）、原型 `prototype-component-generate-entry.html` 仍作高保真参考

## 入口页重写为三栏工作室布局（基于截图参考）
- 用户提供「组件生成舞台」截图作为视觉参考，要求优化原两栏表单式 EntryPage.vue
- 完全重写 `EntryPage.vue` 为**三栏布局**（`280px 1fr 260px`）：
  - **左栏（配置面板）**：面包屑 + 标题、输入源 Tab（截图/Figma）、紧凑上传区、V3/MC Pill 切换、规格卡片(Lite)、更多选项折叠、绿色 CTA 按钮、「跳过」链接
  - **中栏（生成舞台）**：顶栏切换器(组件/框架 Vue3|微码 + Lite|Max)、大号上传区（空态图标+标题+描述+Tab，有内容时预览）、工作流 4 步骤（选择素材→确认规格→开始生成→下载交付）
  - **右栏（最近任务）**：统计概览（4 色数字徽章：进行中/排队/失败/完成）+ 任务列表（状态点+名称+时间+徽章）
- 保留全部原有逻辑：剪贴板智能识别、校验、generateLite 调用、偏好持久化、a11y
- 零硬编码色值，全部引用 `tokens.css` 语义变量（`--component-vue3-*` / `--brand-*` / `--tier-*` 等）
- 响应式：≤1024px 折叠为单栏堆叠；≤640px 紧凑间距

## 合并入口页到工作台 + 画布支持拖拽素材
- 用户要求：删除独立入口页 `/generator`（原 EntryPage.vue），直接在 `/generator/components`（WorkflowEditor 工作流编辑器）的中央画布支持拖拽图片 / URL。
- `generator-routes.js`：`/generator` 由指向 EntryPage 改为 `redirect: '/generator/components'`。
- 物理删除 `frontend/src/views/generate/EntryPage.vue`（仅 generator-routes.js 引用，已无残留 import）。
- `WorkflowEditor.vue` 改造：
  - `onDrop` 增加分支：当 `dragHandler` 为空（非左侧栏节点拖拽）时走 `handleCanvasExternalDrop`，识别拖入的 **图片文件 / URL 文本**（classifyClipboardText：figma/url/image/html）。
  - Figma 链接自动填入运行对话框 `runForm.figmaUrl` 并解析 fileKey/nodeId；图片/网页/HTML 作为「素材来源」浮条展示（`sourceMaterial`）。
  - 新增 `@paste` 画布粘贴识别（图片 / 链接）。
  - 空态提示「拖拽截图、或粘贴 Figma / 图片链接到此处」；拖拽高亮遮罩；左上「素材来源」浮条（按类型着色 + 清除按钮）。
  - 运行对话框原 `to="/generator"` 链接（已失效）改为 `to="/tasks"`（任务列表查看进度）。
  - 复用已有 `showToast(msg, type)`。
- 验证：`@vue/compiler-sfc` 解析 + compileScript + compileTemplate 通过（scriptSetup: true，template errors: 0）。
- 注意：WorkflowEditor 是 Vue Flow 节点管线编辑器，「画布拖素材」语义上=为运行对话框提供来源输入，非直接触发生成；如需拖入即跑需后续接 runWorkflow。

## 方案 A 落地：中间舞台即主入口（2026-08-13）
- 用户选 A：把 `/generator/components`（= `components.vue`）中间空闲态改为真实投放区，与左侧并列入口、中间为主舞台，消除"去左侧配"空话。
- `IdlePanel.vue` 重写：`.stage-card` 现为拖拽/粘贴投放区（tabindex=0 接 paste；drop 收图片文件或 URL 文本；dragover 高亮遮罩）。未就绪=英雄区（提示改为"拖拽截图/粘贴 Figma 链接"）；已就绪=素材预览缩略图+「生成组件」主按钮（emit generate）。emit 新增 `material`/`generate`。
- `components.vue` 接线：`idleMaterial` computed（screenshot→images[0].preview；figma→figmaUrl+figmaPreview.imageUrl）；`onIdleMaterial`（image→readFile，figma url→selectSource('figma')+figmaUrl，其他→showToast）；`onIdleGenerate`→go()；轻量 `showToast`/`toastMsg`（本项目无共享 toast）。
- 验证：compiler-sfc 两文件均 OK（scriptSetup+0 模板错误）。
- 已知小缺口：Figma 来源需在左侧确认预览图后中间"生成"才可点（canGo 依赖 previewConfirmed）；中间暂不能确认预览，仍是左侧 UI。

## 用户偏好（确认）
- 任务详情页已按用户多轮反馈收敛：两栏布局（左配置/信息栏 260px + 中间主舞台撑满）、头部操作合并为 1 主按钮+溢出菜单、元数据下移到左侧「任务信息」、进度条与生成阶段合并为头部横向 5 步阶段条、删「相关任务」/「版本」卡片。

## 后端重启 + dist 构建根因修复（TaskDetail + Phase2 截图节点）
- TaskDetail.vue 多轮修复收敛：SSE 自动重连导致已完成任务重复请求 /api/progress → 加终态跳过+onerror 强制关闭；日志连续相同条聚合为 ×N；Tab「解锁」→「流程」；移除子步骤展开/子步骤区（只留主阶段）；SSE 新日志实时滚动到底（watch(filteredLogs)+watch(activeTab) 触发 scrollLogToBottom）。
- 失败任务 mc-max-1786615660033-4075f9db 根因：Phase2 图 screenshot-renderer 节点 `mc-component-graph-phase2.js:1125` 调 `join(...)`/`existsSync(...)`，但该 .ts 顶层从未 import（仅 1884+/2282+ 等局部 await import 可见），抛 `join is not defined` 拖挂整图（组件其实已生成完，仅截图节点崩）。
  - 修复：`mc-component-graph-phase2.js` 顶部补 `import { join } from 'path'` / `import { existsSync } from 'fs'`。
- 开发环境却用生产 URL 截图：`screenshot-renderer.js:getFrontendCandidates()` 原把 `https://go.microvideo.cn` 排在本地 2610 前，本机有公网时永远打生产。
  - 修复：本地 `127.0.0.1:2610`/`localhost:2610` 移到 `DEFAULT_FRONTEND_PUBLIC_URL` 之前；探测为 fail-over，本地不可达自动回退生产，env 安全（dev/prod 皆正确）。`.env.development` 未设 MC_PREVIEW_BASE_URL 等，故 dev 走本地。
- **构建链路重大纠正（重要）**：`build-backend.sh` 不是可靠构建入口！它用 `tsc -p tsconfig.build.json`(module:nodenext) 仅同步 .js/.md，且其「dist/main.js 就绪」是硬编码误导（自检只数 .js 同步数）。真实入口产物 `dist/main.js` 由 `npm run build`(nest build，含 assets 拷贝 .js) 产出。
  - 正确重启步骤：`rm -f tsconfig.build.tsbuildinfo && rm -rf dist && npm run build` → `bash .start-server.sh`。
  - 坑：`.tsbuildinfo` 残留 + nest-cli `deleteOutDir:false` → tsc 认为 main.ts 已最新跳过 emit → 13030 启动 `MODULE_NOT_FOUND dist/main.js`。必须清 .tsbuildinfo。
  - 验证：dist/main.js 生成；13030 探活 HTTP 401(存活)；2610/8080 监听。

## 环境变量单一真相源 + 防「本地好线上崩」（#95-#100 全部完成）
- 用户诉求：完善不同场景环境变量，所有功能走环境拿配置，防止本地好了发布线上出问题。
- 新增 `backend-node/src/config/runtime-env.ts`（单一真相源，沿用 `backend-root.ts` 模块模式，ai-engine 的 ESM `.js` 经 `'../../config/runtime-env.js'` import）：
  - `getPreviewBaseUrl()`（必填，prod 缺失启动失败；dev 回退 127.0.0.1:2610）
  - `getFrontendCandidates()`（主地址=MC_PREVIEW_BASE_URL；dev 追加本地 2610/2611；**删除硬编码 `https://go.microvideo.cn` 与 5 个僵尸别名** FRONTEND_URL/APP_BASE_URL/PUBLIC_BASE_URL/SITE_URL/PUBLIC_SITE_URL）
  - `getJavaBackendUrl()`（dev 回退 localhost:8080/api；prod 由校验强制）
  - 质量参数 getter：`getMaxIterations/getQualityScoreThreshold/getMinIterations/getVisualSimilarityThreshold/getMaxVisualIterations`，默认已对齐本地严格值 **3/90/1/95/1**（不再静默走 1 轮/75 分）
  - `validateRuntimeEnv()`：prod 缺失 MONGODB_URI/SESSION_SECRET/FIELD_ENCRYPTION_KEY/MC_PREVIEW_BASE_URL/JAVA_BACKEND_URL 任一即启动失败；dev 打印配置来源
- 收口：`screenshot-renderer.js` + `workspace-preview-publisher.js` 删除重复 `getFrontendCandidates`/硬编码域名/僵尸别名，统一调 runtime-env；`operation-log.service.ts` 改走 `getJavaBackendUrl`；phase2/vue3 图 + `visual-comparator.js` + `api-binding.service.ts` 质量参数改走 getter。
- `main.ts` 在 `bootstrap()` 最前调用 `validateRuntimeEnv()`（fail-fast 在 NestFactory 之前）。
- env 文件：`.env.development` 补 `MC_PREVIEW_BASE_URL=http://127.0.0.1:2610`；`.env.production` 补质量参数；三个 `.example` 模板补全为权威文档。
- **`deploy-cloud.sh` 关键修复（真实线上真相源）**：
  - SESSION_SECRET 改「复用服务器已有值」（读现有 .env.production），不再每次部署重新随机 → 避免全员登出
  - FIELD_ENCRYPTION_KEY 只复用 / 强制 export，**绝不自动重新生成**（变更令历史加密数据失效）；两者皆无则 `exit 1` 拒绝部署
  - heredoc 补齐 FIELD_ENCRYPTION_KEY / JAVA_BACKEND_URL / 4 个质量参数（此前漏写 → 线上启动失败 / 审计日志静默失效 / 质量降级）
- 验证：`npm run build` 通过（exit 0）；grep 确认 dist 业务代码已无直接 `process.env.MAX_ITERATIONS` 等读取与僵尸别名；`bash -n deploy-cloud.sh` 通过。
