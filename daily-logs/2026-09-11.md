# 2026-09-11 工作日志

## Loop 2 结构表接管 — 审计而非盲改
- 背景：接续 09-10 落地的 Loop 0/0.5/1 + 1.C/0.A，计划文档 `docs/pipeline-loop-234-execution-2026-09-10.md` 列了 Loop 2 四个落点。
- 审计结果（代码实证）：4 个落点中 **3 个已落地**，仅落点④真实缺位：
  - ① 水平行判据：`inline-row-rebuilder.js:26` `rebuildSectionsPreservingInlineRows` 已用 `inferFlexDirection.sideBySide`（y重叠≥50% + x不相交）做连通分量聚类，输出 `layout:'horizontal'` —— 已存在，阈值=计划值。
  - ② nav 强制：`visual-parser.js:1648` 已专写 `@antd/tab` 分支（cons/ 业务内容、tabs 竖导航），`figma-connector.js:769` 已下钻 cons/ 取真实图标/导航，planner 已含 nav 关键词。
  - ③ chrome 不进 headerSlots：`chrome-section-filter.js` 已剔除 title-itself/panel-title/sub-header；`inline-header-slot-inferrer.js:mergeHeaderSlots` 已带 `rejectedKeys`/`headerBBox` 几何拒回（C-1/C-2，0.A 闭环）。
  - ④ 节点只落一次：`resource-mounter.js` 只有 bg 别名去重（`dedupeBgMultiRefs/:404`、`dedupeSameImageAliases/:508`），**无**「同一 figmaNodeId 只落一次」的确定性 DOM 去重。❌ 真实缺口。
- 关键纠正：计划文档把落点①写成"y重叠→horizontal 重建 section"，与现有实现不符；`inlineCompositeRows` 实际已被消费（headerSlots 兜底推断，非布局重建，0908 P1-3 锁定）。**不新建布局重建路径**（重复造轮 + 自伤风险）。
## 落点④ 二次深查（避免臆造补丁）
- 计划落点④写"同一 figmaNodeId 只落一次（slots 或 content 二选一），重叠删 DOM 不只删合约"。但**实证不成立**：
  - `fixtures/0907/{device,env,traffic}/resource-dom-mapping.json` 三个样本：`dupNodeId=0`、`dupAssignedVarName=0`——每个 figmaNodeId 天然唯一映射一个 assignedVarName，映射层无重复。
  - `resource-mounter.js:ensureHeaderSlots`（T09）已含两道去重：P1-3 契约层（:2052 与子组件文本重叠则跳过注入）+ P5 DOM 级（:2086 已有 <template> 与子组件重叠则删整模板）；C-2 越界几何过滤（:2157）再拦 header 误判候选。
  - 三样本 `.vue` 产物里 `icon*/bg*` 多引用是**子组件各自引用**（如 HeaderStats.vue 用 bg4 作局部 var），非同一节点被挂两次——不构成"重复挂载"。
- 结论：落点④是**臆造缺口**，无真实复现。强行写 `dedupeSameFigmaNode` 纯函数 = 无复现补丁（违反治理"确定性、先复现"铁律）。**Loop 2 实际无实体代码可写**，四个落点全部已闭环。
- 推荐收尾：不盲改；把 0908 已覆盖的 ①/②/③/④ 行为补一层 spec 固化（水平行聚类 / tab 强制 / chrome 不进 headerSlots / T09 双保险），以防后续回归。或确认 Loop 2 已实质完成，转 Loop 3。
## Loop 3 类名与尺寸由 Manifest 写出 — 审计（同 Loop 2，脚手架已就位）
- 计划落点：① classNames 唯一生成器（genClassKey 单一 token）② flex 由装配写 ③ containsChart 装配写 min-height + VERT-004 并入装配。
- 实证：
  - `class-name-generator.js:19` genClassKey 已是单一事实源，被 `prompt-loader.js:441` + `microcode-engineer.js:641/1689/1770` 消费；`class-name-generator.spec.ts` 已锁 12 例。
  - `resource-mounter.js:2360 fixSectionHeightsForResource` 已由 ctx.sectionHeights（engineer `_buildSectionHeightsMap` 构建）驱动写 `flex: <系数> 1 0`，根类豁免 `-root\b`（:2390），量纲=比例（与 FLEX-005 兼容）——flex 已非 LLM 自由发挥。
  - `post-process.js:30 injectChartMinHeight`（主图160/紧凑100 分级，#577）已在 `postProcessIndexVue:1815` 接线；`vertical-layout-guard.js:200 VERT-004` 补 min-height:0；`flex-sibling-guard.js` FLEX-005 已是单一事实源（`detectFlexSiblingIssues` + `normalizeFlexSiblingScale`），`flex-sibling-guard.spec.ts` 锁量纲归一。
- 验收门槛可量化：CODE-003-HIT-RATE≥95% / FLEX-005 零 BLOCK / chart clientHeight>0。但三样本 `fixtures/0907/*.vue` 是**陈旧手工快照**（c-device-monitor-malvtjd5- 长类名、早于 genClassKey+consolidateSubComponentClasses），**不能**作为 Loop 3 度量证据。
- 结论：Loop 3 机制层已闭环。要做"真验收"必须走**真实图端到端重生成**（env/traffic/device 三样本），得到 CODE-003-HIT-RATE 实值——这是唯一能判定"是否还需实体代码"的动作。
## Loop 3 真验收的环境阻塞（结论：本轮无法安全完成）
- 凭证：data/ai-config.json 实测有有效 visionApiKey/textApiKey（health 接口 credentialsOk:false 是接口分辨率问题，文件凭证齐）。
- e2e runner (__e2e-run.mjs) 只接受 --image（screenshot），三样本 fixtures/0907 无截图（仅 figma 树+mapping+vision.json）→ 无法直接跑。
- HTTP POST /api/v2/pipeline/generate 支持 figmaUrl+figmaToken（fixtures 有真实 fileKey/nodeId+token 配置），但带 @UseGuards(SessionGuard) 需有效会话，且 figmaUrl 真实形态未知 → 盲调风险高（撞守卫/打线上/计费）。
- 视觉评分还需前端 :2610（实测 2610 通）。
- 结论：Loop 3「真验收」需真实端到端重生成，本轮无法安全完成。机制层（genClassKey/fixSectionHeightsForResource/injectChartMinHeight/VERT-004/FLEX-005）均已就位且有单测锁定。
## Loop 4 双裁判门禁 — 已交付（本轮唯一实体代码）
- 用户拍板：判定 Loop 2/3 已闭环（机制层全就位），直接进 Loop 4。
- Loop 4 两个文件原「完全不存在」→ 已新建：
  - `utils/figma-golden-extractor.js`：从输入侧（Figma 树 + 资源下载结果）独立生成与 Working Manifest 同构 Golden JSON（版本 wm-1 + golden 元信息）。树优先推导 figmaPath/bbox，树缺失回退 mapping 字段并记 provenance。绝不 import engineer/mounter/parser。
  - `utils/manifest-auditor.js`：diff(golden,working)+verifyProduct，structural.shifted(bbox>8px或>5%)/resource.misbound/contract.missingPass 任一非空→BLOCK（fail-closed）。同独立约束。
  - `docs/golden-manifests/{env,traffic,device}.json` 三样本锁定 hash：device=be8654d8dfd8aac8 / env=0d573094606d4908 / traffic=fb1277ed4cb927a5。
    - 实证：device/env 树派生 100% 命中（12/4 节点 path+bbox 与 mapping 完全一致）；traffic 9 节点 figmaNodeId 全不在 Figma 树中（mapping 与树跨 revision 不一致），已由 provenance.nodeMissing 记档、不误 BLOCK。
  - `utils/manifest-auditor.spec.ts`：9 项 TDD，锁「Golden 与自身 diff BLOCK=0」「注入错绑(改挂section/臆造变量/bbox偏移)必 BLOCK」「契约未启用不误 BLOCK」。
- 验证：acorn OK；61 项核心回归全绿（0907/0908/l5/l6/l7 + resource-attribution + header-slot-contract + manifest-auditor）；nest build 成功；env -i 重启 13030 → PID 46896 监听、health 200、无 ESM 加载错误。
- 提交：backend-node@4cb797e / docs@66730a0 / 根仓 88d2de72 + 7e7394bc（子模块指针）。含可复现锁样工具 `utils/__gen-golden.mjs`。
- 范围边界：Loop 4 交付=「独立模块+锁样+spec」；**未**接入运行图（计划 §落点4「门禁只留三类」+ §落点5「拆补丁堆」属下一离散步骤，按治理纪律单独走）。
- 待办：若要把 auditor 真正变成 gate，需在 phase2/vue3 graph 的落盘/校验节点接 audit(golden, working, files) 并据 blocked 拒绝；补丁堆（flex fixer/双通道 headerSlots/forceAll 残留/style-class-consolidator 取舍）需后续单独治理。

## Loop 4 门禁接入落盘（本轮，按用户「现在做但不重启」）
- 已接 microcode-engineer.js 终态落盘前（finalTruthFiles 写盘前，约 :6306→:6302 上移）。双档设计：
  - 无 Golden（活流量）→ `auditSelfConsistency(working, files)`：仅验契约声明即写入（verifyProduct，CODE-018 确定性探测），确定性不误杀。
  - 有 Golden（`this.goldenManifest`，外部重建/CI 注入）→ 完整 `audit(golden, working, files)`。
  - 关键纪律：禁用 `audit(null,...) `（会把每个资源判臆造全量 BLOCK）。默认 WARN 不阻断；`MANIFEST_AUDIT_ENFORCE=1` 升级 BLOCK（fail-closed 拒绝落盘）。门禁自身异常仅告警不拖垮流程。
  - auditor 新增 `auditSelfConsistency` 导出；extractor 补 spec（同构产出+hash 稳定+纯树 100% 命中）。
- 验证：acorn OK；66 项核心回归全绿（含新建 14 项 TDD）；nest build 成功；dist 新符号落地；静态模拟漏 import→BLOCK、契约完整→0 项、audit(null) 路径已禁用。**已按用户要求不重启**（运行 PID 46896 未动）。
- 提交：backend-node@53c2b0f / 根仓 361185a6（子模块指针）。
- 范围边界：vue3 graph 走同一 engineer.execute 落盘路径（vue3-engineer.js:1634 调父类 writeFiles），故本轮一次接两图；Golden 注入链路（重建任务/CI 预生成 this.goldenManifest）尚未接，属后续离散步骤。补丁堆拆除（forceAll 残留/双通道 headerSlots/flex fixer）仍待单独 Loop。

## 真实生成失败 CODE-019 死锁 — 治本（本轮，不重启）
- 失败任务：mc-max-1789002096933-a65c69f7 / c-env-monitor-wit1topz-a65c69f7 @ 09:04，CODE-019 3×BLOCK 不收敛禁止发布（子组件 TabSwitcher.vue 接收并使用 bg1，父 index.vue 漏传 :bg1）。
- 根因死锁链（铁证 workspace/custom-components/c-env-monitor-wit1topz-a65c69f7/package/index.vue）：
  父只 import bg2/icon1/icon2（恰是模板里写出的）→ 漏 :bg1 → 父不 import bg1 → autoWire 因「父未声明 且 fileSectionMap 契约漏命中 bg1 所属 section」而 inContract=false → 放弃接线 → CODE-019。
  本质：Loop 1 收口 forceAll 时落的「主组件走契约/默认已用变量注入」把「父模板漏写的资源」排除出主组件 script，父无该变量则 autoWire 补不上。
- 治本（props-wiring-guard.js autoWireSubComponentProps）：子组件实际使用的资源 prop 只要是该任务 success 资源变量（effectiveMapping 全集）即强制保证父级持有（与「资源只归主组件持有、子组件 defineProps+父透传」契约一致，不臆造数据源）。ensureParentImport 改用 contractMapping 精确注入该 var（默认分支只注入代码已用变量，bg1 恰是漏写，须先指定 allowedNames）。
- 验证：acorn OK；98 项核心回归全绿（新增 1 死锁回归）；nest build 成功；**端到端用真实失败产物复验**：修复后自动补 import bg1 + :bg1="bg1"，detectMissingPropsWiring 残留 0。未重启（用户明确测试中）。
- 提交：backend-node@4f50fe0 / 根仓 df5ac566（子模块指针）。
- ⚠️ 注：本次修复需 env -i 重启 13030 才对实时生成生效；当前 PGID 46896 未重启（按用户要求）。重生成同一组件前应重启。

## 「AI 修复假成功」根因 + 修复（本轮，已重启 13030 验证）
- 现象：点 [M1-3] AI 修复 → 提示「修复完成，正在重新检查」→ 错误依旧。
- 三重叠真因（缺一不可）：
  1. **配置错配**（根因）：Playground / mc-spec 修复走 `resolveTextConfig({})` 不带 config，而 `TEXT_DEFAULTS` 只从 `data/ai-config.json` 兜底了 apiKey（deepseek 的 sk-60b），baseURL 仍为空、model 用硬编码 `claude-opus-4-8` → 拿 deepseek key 打 Anthropic 端点。
  2. **temperature 400**：硬编码 `temperature: 0.2`（normalizeRequestTemperature 对 claude-opus-4-8 原样返回）→ 端点返回 `temperature is not supported for claude-opus-4-8 when set to non-default values`，0.15s 秒失败。
  3. **提示词缺陷**：M1-3 实际是 `declare.json` **缺 version 字段**（AI 读到无此字段就反问用户 A/B/C，不落盘）；且提示词没说 declare.json 在根目录（AI 先找 package/declare.json）。
- 修复：
  - `ai-engine/utils/ai-defaults.js`：新增 `resolveSavedSlot(role)` —— 完整解析 ai-config.json 的 text/vision 槽（模型库 binding 降维优先 → legacy 扁平字段回退，含 baseURL/model/temperature/thinkingType/providers/pickStrategy）；缓存按 mtime 失效（改配置无需重启）。`resolveTextConfig/resolveVisionConfig` 无入参时用该槽补全，并把已保存 providers 注入池。
  - `ai-engine/graphs/playground-agent-graph.js`：temperature 不再硬编码——优先用配置温度，无配置则**不传该字段**；新增 `TEMPERATURE_LOCKED_MODELS` + `invokeModel()` 运行时自愈（捕获 `temperature is not supported` → 重建无温度模型重试并记住该模型）。
  - `frontend/src/api/mc-spec.ts`：`buildMcSpecFixPrompt` 加 `FIX_TARGET_HINT`（按 M1~M5 定位主要修改对象，明确 declare.json 在根目录）+「字段缺失/值为空则按规范直接补齐默认值，**不要反问用户**」+「必须 write_file 落盘」。
- 验证（本地，PID 重启后）：
  - `resolveTextConfig({})` → deepseek-v4-flash @ api.deepseek.com，temperature=0，thinkingType=disabled（不再出现 claude-opus-4-8）。
  - 真实 `/api/demo/ai-chat` 调用：8.9s 成功（此前 0.15s 秒失败），AI 真读文件真推理。
  - M1-3 修复落盘 `modifiedFiles:["declare.json"]` → 重查 **M1-3 ✅ 变绿**（15/12 → 16/11）。
  - 预览：`/api/preview/<gid>/{任务号|真实目录名}/declare.json` 均 200 且含 `version: v1.0.0`（任务号与尾缀双形态都能解析）。
  - 下载：`/api/component/download/<任务号>` 200/673KB，zip 内 declare.json 含 version v1.0.0 ✅。
- 组件副本澄清：`c-environment-monitor-c298235f` 在 workspace 只存在 `frontend/workspace/custom-components/` 一处（根仓 `workspace/` 与 `backend-node/workspace/` 都没有），本轮不存在多副本不同步问题。
- ⚠️ 生产未部署：https://go.microvideo.cn/api/mc-spec/skills → `{"source":"java","message":"No static resource api/mc-spec/skills"}` = nginx 已转发到 Java，但 **Java NodeProxyController 未重新打包**（新前缀没上线）；Node dist / frontend dist / skills 同样未发。
- ⚠️ 下载 zip 仍含 `.snapshots/`（含 125KB 截图）、`.backups/`、`.cache/`、`.checkpoint/` —— 排除规则待用户确认后再改。

## A/B/D 三项收尾（本轮）
### A. 交付包瘦身（已完成）
- 新增 `src/common/utils/package-filter.ts`：EXCLUDED_PACKAGE_DIRS = .snapshots/.backups/.cache/.checkpoint/.mc-gen/node_modules/.git，
  提供 `isExcludedPackageEntry` 与 `packageEntryFilter()`（archiver `directory(dir,dest,data)` 的 data 回调）。
- 三处打包统一：phase2.packageComponent、tasks 失败代码下载、demo legacy `addComponentDirToArchive`（其 SKIP_DIRS 原漏 .cache/.checkpoint，现改为共享常量）。
- 实测：environment-monitor 下载包 **673,631 B → 255,243 B（-62%）**，只剩 18 个真实交付文件（含 resources/images/mc-preview.png 等正常资源，不应排除）。
- 单测 `package-filter.spec.ts` 9/9 全绿（正斜杠/反斜杠、深路径、空值安全）。

### B. git 提交（已完成，5 个仓库）
- backend-node `f6b5885`（mc-spec 服务 + 目录解析单一事实源 + AI 修复治本 + 包瘦身）
- frontend `4ba4438`（检查按钮 P1 + 逐项 AI 修复 + 预览 snapshot=0）
- backend-java `2441f8d`（NodeProxyController 加 /api/mc-spec）
- docs `945e9fc`（方案第 9 节）
- 根仓 `a04e65c9`（nginx location + 四子模块指针）
- ⚠️ 检测到**并行改动**（config.controller/service、user-ai-config.service/schema 在 22:49–22:51 被他人修改）→ 按纪律只 add 本次显式路径，未用 `git add -A`。

### D. P2 自定义 skill 上传（已完成）
- 后端 `299cb90` / 前端 `eaf39e2` / 根仓 `c62308a7`。
- 安全：逐条 entry 校验（绝对路径/盘符/../空字节拒绝）、拒符号链接、条目≤500、单文件≤8MB、总量≤20MB、
  skillId 白名单 + 目录越界双保险、`.tmp-<rand>` 解压校验入口后原子 rename、覆盖先挪 `.trash-<rand>`。
- 18 项安全单测全绿；端到端实测：上传（顶层目录剥离、frontmatter 解析）→ 列表 → 用自定义 skill 检查 → 卸载 → 内置不可删(400)。
- 依赖：新增 `adm-zip@^0.5.18` + `@types/adm-zip`（纯 JS）。
- 前端：工具行「管理 skill」（v-if isAdmin）+ 弹窗上传/卸载；后端 assertAdmin 二次把关。

### 🔴 新发现：同一尾缀三份产物并存（需治理）
- `c298235f` 尾缀同时命中 3 个目录：
  1. `workspace/custom-components/mc-lite-1789035969084-c298235f`（22:52 新建，mtime 最新）
  2. `frontend/workspace/custom-components/c-environment-monitor-c298235f`（22:44）
  3. `frontend/workspace/custom-components/mc-lite-1789035969084-c298235f`（22:52）
- 内容差异大：任务号目录 declare.json **更完整**（有 attribute.aspectRatio、layoutConfig.list+default、
  themeConfig light/dark），但 `componentId` 写成**任务号**（M1-1 失败）；规范命名目录缺这些字段（11 项失败）。
  规范检查结果：任务号目录 24/3/1 vs 规范命名目录 16/11/1。
- 解析现状：`resolveComponentDirStrict` 按 roots 顺序取首个 → 现选中 root1 的 `mc-lite-…`（六条链路一致 ✅）。
- 已加 WARN 日志「组件目录尾缀多命中」打印全部候选，便于定位。
- 待用户决策：以哪份为准（建议：任务号目录内容更全，但 componentId 需改回 `c-environment-monitor-c298235f`；
  或把目录改名）。**未擅自删除/改名任何目录**。

### 🔧 模型配置「刷新后看不到了」根因定位 + 修复（ConfigPanel.vue）
- 现象：用户保存模型配置后刷新，打开设置面板「数据看不到了」。
- 实测定论（浏览器 + curl 双验证）：后端 `GET /api/config/ai` 正常回吐 `models`+`binding`（dev-local 用户含 m_smoke1）；前端加载链路 `watch(visible)` → `applyConfigToForm(localStorage)` → `loadConfigFromBackend()` 正确渲染，**模型卡片本身一直可见**。
- 真正的「看不到」形态：模型卡片可见，但**主模型/槽位下拉框刷新后显示「请选择主模型」为空**。`binding.text.primaryId` 已绑定（=m_smoke1），但 `slotCandidates(slot)` 要求 `capability` 非空 → m_smoke1 的 `capability=''`（已检测但无能力）被排除 → `<select>` 无匹配项回退占位符，造成「已保存主模型消失」错觉。
- 修复（`frontend/src/components/generator/ConfigPanel.vue`）：
  - 新增 `slotOptions(slot)`：在合格候选基础上，若已绑定的 `primaryId` 模型不在候选中也保留为选项 → 刷新后下拉框显示已绑定主模型（仅用于展示，不新增候选）。
  - `poolCandidates(slot)` 同步保留已绑定的池成员（cap='' 也不消失）。
  - 三处主模型 `<select>`（unified/text/vision）v-for 改 `slotOptions`。
- 验证：浏览器刷新后 text 主模型下拉框由「请选择主模型」变为「纯文本模型（已选中）」；`npm run build:safe` 编译通过(24.8s)。Vite 静默退出老问题再现，已 env -i 重启(:2610)。

---

## 第七轮（00:12–00:20）命名统一收尾：工具口径修正 + S2 收紧 + 重启（含一次误杀）

### A. 审计脚本口径过期（本轮新发现）
- `naming-audit.mjs` 的 `ROOTS` / `SEARCH_ROOT_ORDER` **仍停在 S5 之前**：把退役根 `projectRoot/workspace` 标 `inSearchRoot: true`，把真实写入根 `backend-node/workspace` 标 `false`。
- 后果：输出「→ 解析会最先命中 `mc-lite-…-c298235f`（来源 root-ws/custom）」——**与 S5 后事实相反**，属误导性结论。
- 修正：`SEARCH_ROOT_ORDER = ['be/ws/custom','be/ws/vue3','fe/custom','fe/vue3']`；`root-ws/*` 加 `retired: true` 且 `inSearchRoot: false`；`be/ws/*` 标 `[写入根]`；JSON 输出新增 `retired`。重跑后结论变为「最先命中 `c-environment-monitor-c298235f`（来源 be/ws/custom）」✅。

### B. S2 排序漏洞：伪规范名（本轮新发现 + 修复）
- 存量存在 `c-mc-lite-1788491849328-99e8660b`（`c-` 前缀裹任务号）。`isEncodedComponentName` 复用 `isEncodedSessionId`，只认 `mc|mv|cp|page-` 开头 → **拦不住它**；于是被 `CANONICAL_NAME_RE = /^c-[a-z]/` 判为 **rank 0（最优）**，同尾缀多命中时会盖过真规范名。
- 修复 `component-resolver.js`：新增 `EMBEDDED_TASK_ID_RE = /\d{13}/`（规范名 `c-<语义>-<8hex>` 绝不含 13 位时间戳），`namingRank` 中**最先**判定，命中即 rank 2；`namingRank`/`rankDirsByNaming` 改为 export 便于断言。
- 断言 **7/7 PASS**（`c-environment-monitor-c298235f`→0、`c-mc-lite-…`→2、`mc-evaltest-fresh-0001`→1、`v2-e2e-p2verify-…`→2）；排序验证真规范名排第一、伪规范名垫底。
- 构建 ✅，dist 已含新符号（`grep -c EMBEDDED_TASK_ID_RE` = 2，`namingRank` 在 :53）。

### C. 重启（PID 31402 → 34022）
- `kill 31402` → 第 2s 端口释放、无残留；启动成功 00:14:16~17，监听 13030，`/api` 200、`/api/apifox/catalogs` 200。
- **教训 1**：`start-node.js` 在**项目根**且内部硬编码 `BE_DIR`，应写绝对路径 `node /Users/smigoo/工作/mvgo/start-node.js`；本次写成 `cd backend-node && node start-node.js` 仍跑起来是靠 cwd 兜住，不可复制。判据：server.log 出现 `===== [start-node] ===== <ISO> =====` 分隔线。
- 🔴 **教训 2（真实事故）**：重启前我用 `find workspace -maxdepth 2 -newermt '-60 minutes'` 判断「无在跑任务」→ **误判**（探不到 `temp-components/<session>/<task>/.mc-gen/` 深层写入）→ 杀掉的正是一个**跑到 `visual-parser` 阶段的任务 `mc-max-1789056777461-5e9f0512`**（日志 00:13:46 仍在发起 Vision 模型请求）。
  - 损失评估：该任务**尚未写 workspace**（三个根 find 该尾缀均为空），无半成品/脏目录；仅剩 `temp-components/6a51f7caed28c5d5b13c61a5/mc-max-1789056777461-5e9f0512/{resources,.checkpoint}` 中间产物。**需用户重跑该任务**。
  - 正确判据（已写入 MEMORY.md）：看 `server.log` 尾部近 1–2 分钟是否有 `visual-parser`/`发起模型请求`/`Checkpoint 已保存`。

### D. 清理后状态（审计复核）
- 活跃根任务号形态目录 **全部清零**：`fe/custom` 125（任务号 0 / 规范 111）、`fe/vue3` 2、`be/ws/custom` 123（0 / 118）、`be/ws/vue3` 3。
- 退役根 `projectRoot/workspace/custom-components` 仍 **9**（100% 任务号名，未动）。
- 同尾缀多名字：**1 组**（`c298235f`，3 份副本 / 2 种名字）；其余遗留 `4a-can` 2 条（`c-mc-lite-…-99e8660b`，由 S3 归一）、`4a-other` 2 条（`mc-evaltest-fresh-0001`，测试数据）、4c 12 条（`v2-e2e-*`/`page-page-*` 中文 ID）。

---

## 打包前端 + Java 后端（生产构建产物，00:17–00:20）

**前端（✅ 完成）**
- `cd frontend && env -i PATH="<managed node>:/usr/local/bin:/usr/bin:/bin" HOME=/Users/smigoo npm run build:safe`（`vite build --mode production --emptyOutDir false`），**23.18s** 通过。
- 旧 `dist` 先 `mv dist dist.old-<ts>`（避免 stale，rename 不触发 safe-delete 钩子），打完确认 `dist/index.html` 引用 `/mvgo/...`（base 正确），清理旧 dist。
- 产物：`frontend/dist/`（34MB，含 `index.html` + `static/` + `config/` + `log/` + `topology.html` 等）。部署目标 `/home/mvbt/nginx/www/mvgo/`，`rsync -av --delete`。

**Java 后端（✅ 完成）**
- ⚠️ 本机**无 `mvn`/`brew` 在 PATH**，但 Homebrew 装在 `/opt/homebrew` 且公式已装：**JDK17 = `/opt/homebrew/opt/openjdk@17`（17.0.20）**、**Maven = `/opt/homebrew/opt/maven`（3.9.9）**。直接用绝对路径二进制即可，无需重装。
- 命令：`env -i PATH="/opt/homebrew/opt/openjdk@17/bin:/opt/homebrew/opt/maven/bin:/usr/local/bin:/usr/bin:/bin" HOME=/Users/smigoo JAVA_HOME=/opt/homebrew/opt/openjdk@17 mvn clean package -pl mvgo-app -am -DskipTests -B`（env -i 清掉继承的 HTTP_PROXY，否则 Maven 下载会走 127.0.0.1 代理失败）。
- 结果：**BUILD SUCCESS**，7 模块全过，`mvn-app/target` 已含 `mvgo-app-1.0.0-SNAPSHOT.jar`（fat jar，91,199,204 B）。
- 按部署文档坑#10 验证：抽嵌套 `BOOT-INF/lib/mvgo-api-1.0.0-SNAPSHOT.jar` → `NodeProxyController.class`/`SessionAuthService.class` 均在（09-11 新鲜编译）；`strings` 命中 `page-skeleton`（×2，白名单修复）、`upsertPortalUser`（×1，门户用户自动建号修复）→ 源码改动确已进 jar。
- 本地 sha256：`b131835ec5a96e0f7450830bbd7404aae34c54da381e17b4d3c6b543a3a3bf92`（ECS 上传后比对一致才算完好，防坑#8 截断）。

**部署（未执行，待用户确认）**：需 ECS_IP + `MONGODB_URI`/`FIELD_ENCRYPTION_KEY`/`SESSION_SECRET` 注入。前端 `rsync`，Java `scp jar` + `bash deploy-java.sh`（或裸 `java -jar`，带 `SPRING_PROFILES_ACTIVE=prod` + `NODE_BACKEND_URL=http://192.168.112.1:13030/api`）；二者均须先起 Java 再改 nginx `gzxj.conf`（`/api → 8080`，仅 `/api/progress → 13030` 在前）。

---

## 重启 Node（00:19，用户指令「重启 Node」，未构建）

- 重启前：`dist/main.js` PID **34022** 占 13030；server.log 尾部无 visual-parser / 发起模型请求 / Checkpoint；temp-components 近 10 分钟无深层写入 → 无在跑生成任务。
- `kill 34022` 后 LISTEN 空、无残留进程。
- `cd /Users/smigoo/工作/mvgo && env -i PATH="<managed node 22.22.2-2>:/usr/bin:/bin" HOME=/Users/smigoo node start-node.js` → spawned **37774**。
- 探活：`GET /api` **200** `Hello World!`（1s）；LISTEN PID **37774**；server.log `Mapped {/api/v2/pipeline/...}` + `Nest application successfully started`。
- 本次只重启、未 `npm run build`。

## 组件生成两案（2026-09-11 00:28-00:41）

### 案① device-monitor-7aqvvejd「只有上面一部分」（已定位根因，未完全收尾）
- **症状**：`c-device-monitor-7aqvvejd-eb8e5d6d`（node 2:8417，12 设备卡）预览只显示顶部 Switch 段。
- **根因**：微码根容器 `<div class="...-slot-con">` 的 `height:100%` 被 resource-mounter 规则②改写成 `flex:1 1 0`；其父级 `.pannel-content` 是 block 定高（default-panel/index.vue:89-93，`height:calc(100%-38px)` 无 flex）→ `flex:1` 失效 → slot-con 塌成第一个子元素 Switch `flex:0 0 65px` 的高度，Tabs/Main 的 flex-grow 分不到空间被 `overflow:hidden` 裁掉。
- **取证**：puppeteer 实测 slot-con `clientH=65 vs scrollH=369`；修复后 `387==387` 无溢出。
- **修复**（已改 2 实时源 backend-node/workspace + frontend/workspace，未提交）：① slot-con `flex:1 1 0`→`height:100%`；② MainSection.vue scoped `.device-grid` `overflow:hidden`→`overflow-y:auto`。
- **遗留（未收尾）**：修复后暴露二级问题——Tabs flexGrow 2.309:1 分到 225px（内容需 304 溢出）、Main 只 97px，summary-row 吃掉 ~100px → **grid 被压到 0 高**（12 卡仍不可见）。根子是 flexGrow 系数与实际内容量不匹配，属比例预算问题，需重算 section 高度分配或让 Main/Tabs 按内容 min-content 排布。slot-con 修对了，但整体比例还没收敛。

### 案② env-monitor「覆盖率 10%」失败（分析已结束）
- **任务**：`mc-max-1789057276785-064a5fc9`（fileKey 1t7Dmmpsl5i0PC8BJt2QLf / node 2:7879，4 tab + 面积折线图，之前成功过）。
- **覆盖率计算逻辑**（visual-parser.js:1713-1873）：`coverageRate = matched/(matched+missing)×100`。matched=Figma 元素(name/characters)能在 Vision **结构化字段**(sections 的 name/label/title/text/value/content、headerSlots、visualElements keys)命中；missing=内容元素(TEXT有文字)/容器(FRAME/GROUP有子节点)未命中；uncertain 装饰短文本不入分母。**刻意不读 summary 自由文本**（fail-closed）。
- **实锤**：totalFigma=14 matched=1 missing=9 rate=10%。missing = 标题「环境监测」+ 4 tab 标签(一氧化碳/洞内照明/洞外光强/能见度)+ tabs-list/tabs-icon/num 容器。
- **根因**：Vision 这次结构化输出退化——layout.sections 只剩 2 个（`sub-t` inline-row，children 名是 figma node id 2:7949/2:7889/89:43 无文字；`面积折线图区域`）。4 个 tab 标签和标题**只写在 summary 自由文本/componentTitle 元数据**，没进 sections[].children[].text → 覆盖率读不到 → 10%。
- **结论**：覆盖率算得对（真低，非虚低）；门禁 fail-closed 正确拦截了退化分析（非误杀）；"之前成功现在 10%" = Vision 模型输出方差（重试逻辑已有但同模型同 prompt 退化会复现）。
- **建议（未动）**：不读 summary 兜底（自证虚高，削弱门禁）；治本方向 = 强化 vision prompt 要求 tab/列表标签必须落到 sections[].children[].text，或多模型重试取最高覆盖率。

### 案① 收尾（最终根因：planner 臆造 section-main + resource-mounter 误改 slot-con）
- **Figma 真值**（2:8417 cp-设备监测 420×425）：slot-con 只有 2 孩子 = switch(89:38,65px,汇总卡隧道设备56302/5+南北接线1280/3) + @antd/tab(89:37,317px = tabs左46px竖排导航 + cons右367px 12设备卡)。
- **两个根因**：
  1. planner `effectiveSections` 臆造了第 4 个 `section-main`(id合成/flexGrow=0/height=0)——把 @antd/tab 内部的 cons 错拆成独立"主内容区"→ codegen 生成冗余 MainSection（又一遍汇总卡+设备网格），与 Switch/Tabs 上下堆叠抢高度。
  2. resource-mounter 规则② 把 slot-con 的 `height:100%` 误改 `flex:1 1 0`（`-root` 豁免漏了 `-slot-con` 根容器）→ 塌成 65px。
- **修复**（已改 2 实时源 backend-node/workspace + frontend/workspace，未提交）：① slot-con `flex:1 1 0`→`height:100%`；② index.vue 删 `<MainSection/>` + import。TabsSection 内部本就是正确左右结构（tabs-nav+cons-grid），删冗余后填满 322px≈Figma 317px。
- **验证**：puppeteer 全绿——slotCon 387==387、tab/tabsNav/cons/consGrid 均 322==322 零溢出、12 设备卡全渲染、mainSection 已删、无 console error。
- **系统性治本（未做，待立项）**：① resource-mounter `ROOT_SELECTOR_RE` 需同时豁免组件根容器（不只 `-root` 后缀）；② planner 不得把容器内部 content 子区域错拆成独立 section（@antd/tab 应保持单 section）。

## 预览页 [object Object] 报错修复（01:08）
- **接口**：`/api/tasks/{componentId}/code-snapshots/latest`（`tasks.controller#getLatestCodeSnapshot`），独立打开 401。
- **用途**：取 candidate/lastGood/partial 快照 revision，让 URL 无 `?sessionId&revision` 的入口在 workspace 副本缺失时仍能预览；对「已发布到 custom-components 的组件」不是必需，静态 `/api/preview` 即可。
- **根因**：① `core/http.js#resolveErrorMessage` 把后端 `detail: { path }` 这个对象直接 `String()` → `[object Object]`；② 401 触发 `notifyUnauthorized` 在预览页弹窗。
- **修复**：
  - `core/http.js`：改取字符串 `message/error`（detail 为对象时跳过）；新增 `silent401` 选项。
  - `preview/index.vue`：`ensureLatestSnapshot` 两处 `/api/tasks/.../code-snapshots/latest` 传 `{ silent401: true }`，本页 401 静默忽略。
- **验证**：`npm run build:safe` 通过（23.34s）。

## Java + Node 打包（01:13–01:14）
- **Node**：`backend-node` `rm -f tsconfig.build.tsbuildinfo && npm run build`（env -i 管理 node 22.22.2-2）成功，`dist/main.js` 01:14，`grep -c tempComponentsDir dist/preview/preview.controller.js`=1（预览回退根已进产物）。
- **Java**：`backend-java` `env -i JAVA_HOME=/opt/homebrew/opt/openjdk@17 mvn clean package -pl mvgo-app -am -DskipTests -B` 成功（3.25s），`mvgo-app-1.0.0-SNAPSHOT.jar` 91MB @ 01:14。`BOOT-INF/lib/mvgo-business-1.0.0-SNAPSHOT.jar` 内 `AdminService.class` 含 `portalUid` 符号 → 周彤统计双键修复确已进包。
- **部署待办（用户手动）**：Node 推 `dist/` + 重启 13030（杀真 PID 后 `lsof` 空再起）；Java `scp jar` + `bash deploy-java.sh`（或 `java -jar` 带 prod profile + `NODE_BACKEND_URL`）；先起 Java 再调 nginx `/api → 8080`。前端 dist 本会话未重新打包（上次 00:20 已 build:safe）。ECS CSS 404 仍需跑止血命令（补 `resources/styles/index.css`）。

## 根治三线落地（2026-09-11 01:00）—— 层① 完成，层②③ 待做

### 背景
用户反馈两个组件生成失败（traffic-monitor / env-monitor），全被 L0-B 门禁拦死：
- traffic: CODE-019(资源prop未传)×9、CODE-020(34 class)、语义门禁
- env: COMP-001(4 section只组装1)、CODE-021(死import SubTSection)、FLEX-003、CODE-020
根因同源：codegen 分 chunk 生成多文件，LLM 跨文件不一致（同一份 section 契约被多次解释）。用户拍板「软失败+评分」兜底，但优先做根治三线。

### 层① 确定性 index.vue 模板装配（已提交 f0e6e7b）
- `code-generator.js#buildDeterministicIndexTemplate`：有计划子组件（collectLeafSections(resolvePlanSections) 非空）时，模板骨架由系统从 effectiveSections 树 + assignSectionComponentNames 确定性生成（base-panel + header#header-right插槽 + slot-con div + 子组件标签顺序），跳过 LLM template 段（buildTemplateChunkMiddle）。
- `resource-mounter.js#pruneDeadSubComponentImports`：删死 import（`import X from './components/X.vue'` 但 X 模板 0 处引用），与 ensureSubComponentImport（只补缺失）对称。
- 关键链：子组件由 `_detectSubComponents` 从 index.vue 的 import 反推生成 → 模板+import 确定性 ⇒ 整条子组件链确定性。
- 测试：`code-generator.deterministic-template.spec.ts` 7/7 + 回归 54/54 绿（需 mock backend-root/sfc-semantics/prompt-loader/resource-import-guard/provider-pool/llm-timeout 绕过 import.meta）。

### 🔴 关键耦合（决定层②方向）
层① 确定性模板生成 `<TabsSection />` **不带 props**，而当前资源契约是 **P0/D（2026-09-09，resource-handling-guide.md 方案C禁止子组件本地import）**：父 forceAll import 一次 + 子 defineProps 接收 + autoWire 父透传。两者冲突 → 层① 单独上会**新触发 CODE-019**。
→ 层②「去 prop 化」= **反转 P0/D 契约**：子组件本地 import 资源（去掉 skipResourceVars + 去掉「本地import→defineProps」改写 + 去掉 autoWire + 父不再 forceAll）。之所以现在安全：层① 确定性模板让父组件变成纯装配（不用任何资源），「父子双份持有」的 P0/D 根因被消除，子组件成为资源唯一持有者。
触点（已定位）：microcode-engineer.js:1604-1635（P0/D改写+skipResourceVars）、:1698（contracts）、:2104-2110（父forceAll/contract）、:6172（autoWireSubComponentProps）、resource-import-guard.js:344-411（skipResourceVars/forceAll/contractMapping 分支）。

### 层③（未做）class/flex 归一
CODE-020（模板class未定义34个）、FLEX-003（同class两套flex）→ 后处理：class 统一补全组件前缀 + flex 跨文件去重归一。

### 软失败兜底（未做，已确认形态）
评分 = 单一 0-100 分 + 维度拆解（结构/资源/类名/语义）+ issue 清单；重试耗尽不 throw，任务标记「完成（降级）」。

---

## 生产部署执行（01:23，用户登 ECS 手动操作）
- **ECS 真实 SSH 主机 = `root@iZbp1hik17it6sucvjuotaZ`**（用户终端提示符 `root@iZbp1hik17it6sucvjuotaZ:/home/mvbt/mvgo/...` 证实；内网 IP `192.168.112.1`）。后续部署命令直接用此 host，勿再用 `<ECS>` 占位。
- **两处误操作**：
  1. 上传命令（`shasum /Users/smigoo/...` + `scp ... <ECS>:/...`）被用户在 **ECS 上** 执行——`/Users/smigoo/工作/mvgo/...` 是 Mac 本地路径、ECS 不存在；`<ECS>` 占位未替换 → **新 dist（预览回退根治）与新 jar（周彤双键修复）都没传到 ECS**，ECS 仍是上一版产物。
  2. 上传必须在 **Mac 终端** 跑，ECS 只跑重启/校验。
- **Node 当前状态**：用户 `lsof -ti tcp:13030 | xargs kill -9` 释放端口后，`NODE_ENV=production nohup node dist/main.js >> server.log 2>&1 &` **立即 Exit 1**。真实报错被 `grep -E "Mapped|Nest..."` 过滤掉（grep 无匹配≠启动成功）。已请用户 `tail -n 50 server.log` 看全量报错定位。
- **✅ Exit 1 根因已定位（01:24）**：`Error: Cannot find module 'adm-zip'`（require stack → `dist/mc-spec/mc-spec.service.js:52`，模块顶层 require）。`node_modules` 目录在但**缺 `adm-zip`**——本会话「第七轮 D」P2 自定义 skill 上传新增依赖 `adm-zip@^0.5.18`（纯 JS，backend-node `npm install` 装过），但 ECS 没同步装。`.env.production` 正常（dotenvx 注入 6 项无报错），排除密钥问题。
- **📌 部署教训（固化）**：新增 Node 运行时依赖后，ECS 部署**不能只 scp `dist`**——`node_modules` 不会自动更新，必须同步 `package.json` + `package-lock.json` 再 `npm ci --omit=dev`（lock 不一致则 `npm install --omit=dev`）；缺包可即时 `npm install adm-zip@0.5.18 --omit=dev` 兜底拉起。
- **Java `Unable to access jarfile` 误报**：用户把 Java 启动命令在 `backend-node` 目录下跑了（未 `cd /home/mvbt/mvgo/backend-java`），且 jar 未上传（scp 在 ECS 上跑失败）→ 非 Java 问题。修正：Java 命令必须 `cd backend-java` 后跑。
- **待办**（继续）：① Mac 端 `scp -r dist` + `package.json` + `package-lock.json` + `scp jar`；② ECS `npm ci --omit=dev`（装 adm-zip）→ restart Node；③ Java `cd backend-java` 后 `java -jar`（带 prod + `NODE_BACKEND_URL` + 同 `MONGODB_URI`）；④ 两端 jar sha256 比对；⑤ 验证 `Nest application successfully started` + `/api/health`=ok。本地 jar 01:14 重建，sha256 以 `shasum -a 256` 实时值为准。
- ⚠️ ECS Node 现宕机（kill 后新进程退 1），重启成功前 `/api` 相关 AI 能力不可用。

## 重新打包 + 自助部署交接（01:37–01:38）
- 用户指令「重新打 node+java 包，给命令，自己上传重启」。已重打：
  - **Node dist** 01:38（`rm -f tsconfig.build.tsbuildinfo && env -i 管理node22.22.2-2 npm run build`），`grep -c tempComponentsDir dist/preview/preview.controller.js`=1（预览回退根治进产物）。
  - **Java jar** 01:38（`env -i JDK17+maven mvn clean package -pl mvgo-app -am -DskipTests -B`）BUILD SUCCESS，91MB；本地 sha256=`22d00209d7b60394690fd5c32882212c80d3b565aac565e51056b5c06bf161d7`；嵌套 `mvgo-business` 的 `AdminService.class` 含 `portalUid`（grep -c=1，周彤双键修复确认进包）。
- 已交付完整自助命令（Mac scp + ECS `npm ci --omit=dev` 装 adm-zip + 双端重启），三条红线：① ECS 必须 `npm ci`（否则缺 adm-zip Exit1）② Java 必须 `cd /home/mvbt/mvgo/backend-java` ③ scp 内网主机名需 FlClash 隧道/或换公网 IP。
- 上传卡点（已定位，待用户侧解决）：ECS 内网主机名 `iZbp1hik17it6sucvjuotaZ` 仅经 FlClash 增强模式可达（DNS→192.168.112.1 + VPC 隧道），退出 FlClash 后本机无法解析；历史/known_hosts/仓库均无公网 IP 记录 → 用户需重拉 FlClash(选可用节点) 或取弹性公网 IP 直连。

## 软失败兜底（已提交 b87084e）
- 需求：评分不合格的组件不写「失败」，改降级完成 + 展示评分明细（单一 0-100 + 维度拆解，用户已确认形态）。
- `gate-score.js#computeGateScore(issues)`：L0-B issue → 0-100 分（BLOCK 扣 10 / WARN 扣 2，下限 0）+ 四维度拆解（结构/资源/类名/语义，前缀映射 CODE-019→资源、CODE-020→类名、COMP-001/CODE-021/FLEX→结构、SFC-/TEXT-→语义）+ 单维度明细最多 5 条。
- 接线 `mc-component-graph-phase2.js`：① code-structure-validator 节点 return 前 `result.gateScore = computeGateScore(result.issues)`；② 条件边「BLOCK 且重试耗尽」原 `return 'l0b-fail'`(throw) → 改 `state._l0SoftFail=true + state._gateScore=gs + return 'complete'`（跳过 do-not-invent-check/精修，符合降级语义）；③ complete 节点 onProgress 带 gateScore/l0SoftFail，返回体透出 `l0SoftFail`/`gateScore`。
- 硬底线保留：`complete` 节点的 `assertArtifactIntegrity`（缺文件/空产物）仍 fail-closed，与软失败并行不冲突。
- 测试：gate-score.spec.ts 5/5；build 通过；dist 里 `return 'l0b-fail'` 已归零。
- 待办：前端 TaskCenter/TaskDetail 读 `gateScore`/`l0SoftFail` 展示「降级完成 + 评分明细」（后端已透出，前端展示未做）。Vue3 图（mc-component-graph-vue3.js）的 L0-B 同类逻辑未同步软化（用户当前失败均为微码 mc-*）。

## low-coverage 10%/30% 治本（mc-max-1789061627741-4b9c97b7 + 1789060415682）
- 根因两处：① inline-row-merger 把 member 当 name 写节点号（已改 texts.join('')）；② 更关键——
  coverage 检查覆盖率时，几何 section 成员容器（tabs-list/tabs-icon/num 等无语义名节点）进 missing → 虚低。
- 治本新增 `confirmedNodeIds`：凡 layout 中 inline-row/container-rebuild 几何块的成员 figmaNode id
  直接视为已确认（bbox 真值并入），不参与字符匹配。复算真实 Figma 树 30%→100%，过 35% 闸门。
- error-humanizer 改 low-coverage 分支：先于「臆造」匹配，带真实 coverageRate，且说明「重试重新识别（不复用缓存）」，
  不再误导为「视觉识别未完成 / 复用缓存」。
- ⚠️ 实战教训：本轮前期声称「构建后重启」但 PID 37774 仍是 00:19 旧实例（dist 未重载）→ 改动不生效。
  **改 dist 后必须真杀旧进程 + 真重启**才生效。本次已 env -i build（exit0）+ kill 37774 + start-node.js → 新 PID 6138，探活 200。
- 验证：本地精确复算确认新 coverage 100% 过闸；未跑端到端（避免再烧一次 Vision 调用）。

---

## 第八轮（01:53–02:15）"生成的组件没有高度、看不到图表"根因定位与修复

用户报：`mc-max-1789062564333-f1ff01eb`（`c-env-monitor-d0ela8hg-f1ff01eb`）的 `c-env-monitor-d0ela8hg-c-env-monitor-slot-con` 没有高度，看不到图表。

### A. 浏览器实测（agent-browser，决定性证据）
| 元素 | 修复前 | 修复后 |
|---|---|---|
| `.mc-preview-component-root` | 420×186（高度链正常） | 同 |
| `.pannel-content`（父） | 420×156（**有**空间） | 同 |
| `.…-slot-con`（内容根） | **405×40** | **405×150** |
| `.…-chart` | **405×0** | **405×110** |
| `.…-chart-body` | 405×80（min-height 生效） | 405×92 |
| canvas | 1 个（405×80）但**被裁掉不可见** | 405×92 可见 |

根因链：宿主面板 `.pannel-content` 是 **block**（6/6 面板都无 `display:flex`）→ 内容根只写 `flex: 1 1 0` + `min-height: 0`（**无 `height`**）→ flex 撑高完全失效 → 高度退化为内容高度 40px（= Tab 行 32 + gap 8）→ 叠加根容器 `overflow: hidden` → 图表(405×80)被裁掉。

### B. 管线缺陷（本次主修）
`generation-context.js#createFixPipeline` 组装内置规则时**漏传 `figmaNodeData`** → `code-fix-rules.js:932` 的 `if (figmaNodeData)` 恒假 → **`anchor-root-container`（根容器尺寸中性锚定：缺 width/height 则补 100% + 注入 aspect-ratio 真值比例）从未注册**。
- 2026-08-31「迁出为共享规则」重构漏的一处接线；Vue3 侧因 `vue3-engineer.js:862` 另有一份独立注册而幸免，微码侧只剩注释声称「由 fixPipeline 接管」。
- 修：`figmaNodeData: ctx.figmaNodeData`（`GenerationContext` 本就有该字段）。
- 回归守卫：`code-fix-rules-root-anchor.spec.ts` 新增 describe「createFixPipeline 接线回归」3 例 → **17/17 PASS**；`code-fix-rules` 全量 20/20 PASS。
- 构建 ✅，dist 校验 `grep -c "figmaNodeData: ctx.figmaNodeData"` = 1。
- **旁证**：01:56 / 02:02 另两个任务（`-8c815194` / `-46859f40`）的 `index.less` 同样**无 `aspect-ratio`** → 系统性缺陷，非个例。

### C. 就地修复产物（立即可见）
用管线自身规则（非手改）：`anchorRootContainerInFiles` → `common.less` 补 `height: 100%; aspect-ratio: 420 / 186;` → `recompileIndexCss` 重编 `index.css` → backend + frontend 两份副本均处理。
- ⚠️ **踩坑**：该函数的 files map **必须含 `package/index.vue`**，否则 `detectRootContainerClass` 拿不到根类名 → 整体 no-op（第一次执行报「无需改动」即此因）。
- 浏览器复验：图表坐标轴（40/30/20/10/0、0–24 时）、图例、预警线全部出现；无报错。

### D. 服务
`kill 6138`（用户此前自行重启的进程）→ 第 2s 端口释放 → 启动（**绝对路径** `node /Users/smigoo/工作/mvgo/start-node.js`）→ 新 **PID 37535**（02:06:52）→ 监听 13030。重启前已按正确判据确认无在跑任务（server.log 尾部 + 深层 find）。

## 02:41 收尾覆盖率修复 + 层②反转 + 层①配套 + 层③（commit 821add9/f47eca9/1799dad，已重启生效）
- **覆盖率虚低修复**：inline-row-merger.js 子节点 name 由 String(m)（节点号）改为 collectSubtreeEvidence 子树文案拼接，env-monitor 10% 虚低根因；+3 spec 16/16。（821add9）
- **层② 反转 P0/D**：子组件资源改回本地 import、父不再透传。新增 props-wiring-guard.stripResourcePropsFromDefineProps（对象/数组/泛型三形态去资源 prop，**必须先于 injectResourceImports**——collectDeclaredBindings 把 defineProps 键记为已声明，不先删 prop 撞名复核会拒绝注入）；去 skipResourceVars:true / import→defineProps 改写 / contractMapping / autoWire 整块；splitTopLevelLoose 支持 TS 泛型 `;` 分隔。+7 spec 39/39。（f47eca9）
- **新 bug（c-device-monitor-1fduq67s-46859f40）**：确定性模板生成 4 子组件标签（Switch/Tabs/Tabs2/Header），仅 HeaderSection.vue 有文件、script 仅 1 import → 悬空标签渲染断裂。取证：缓存 chunk3 原有 5 import，终版只剩 1（后处理/AI 修复重写 script 丢 import；语义门禁对 subCompTags 白名单放行拦不住）。
  - 修①：_unionTemplateTagSubComponents 把模板 PascalCase 标签并入子组件生成清单（3 个 complexity 分支，对齐 vue3-engineer 同款修法）；
  - 修②：落盘终验兜底 ensureSubComponentImport 幂等补 import。
- **层③**：新增 class-prefix-fixer.js（模板类名统一补前缀，CODE-020 确定性自愈，与 findUndefinedComponentClasses 同口径：后缀命中才改写、多候选取最短；.vue 自有 scoped 样式计入已定义）；validator 同步修误报面（旧只认 *.less）。+8 spec；回归 8/39/23/85 全过。（1799dad）
- **坑**：WorkBuddy Bash 管道 `npm run build 2>&1 | tail` 会吞输出（改为落盘 /tmp/*.log 再读）；BSD grep `\|` 又双叒踩（一律 -E）。
- 遗留：前端 TaskCenter/TaskDetail 展示 gateScore/l0SoftFail 未做；vue3 graph L0-B 软失败未同步；planner 拆容器内部内容为 section 的系统性问题待立项。

---

## 第九轮（03:05–03:15）图表不可见真因：Loop 2.1.E「真值洗白」

用户第二轮回击「依旧看不到图表？」→ 上轮「高度」只解决一半，真正让 series 消失的是 **`series[].type` 非法**。

### 根因四环（全部有代码/日志证据）
1. **核心**：`chart-type-guard.js#normalizeChartOptionType` 的 `fallback` 直接取**未校验的 truth**；而调用方 `microcode-engineer.js:5508` 传的 `chartsArr[0].type` 就是 **vision 原始输出**。→ 真值本身是 `area-line`/`面积折线图`/`分组柱状图` 时，非法值被「收敛」成同一个非法值，还打印「已收敛 N 处」制造假通过。
   - 铁证：`[2026-09-10T17:51:48.529Z] [WARN] [microcode-engineer] 🛡️ [mc] 2.1.E 非法 series.type 已收敛 2 处（真值=area-line）`
2. **T3 同病**：`microcode-engineer.js:5479` / `vue3-engineer.js:1243` 的 `targetType` 同样取未校验真值 → `bar` 被强制改成非法别名。
3. **收敛正则两处错**：`/series\s*:\s*\[[\s\S]*?\]/`（lazy）只到第一个 `]`（常是 `colorStops` 闭合）→ 同数组后半段逃过；且 `type\s*:\s*['"]...` **无差别替换数组内所有 type 键** → `lineStyle.type`（solid/dashed）与 `color.type`（linear）被污染。
   - 实锤 `c-traffic-monitor-ppheeeem-9c86b889/ChartsSection.vue:153/158/162/173/182`：158 行 `lineStyle:{type:'分组柱状图'}`、173 行第二段 series 却仍是合法的 `'line'`（lazy 够不到）。
4. **覆盖缺口**：`vue3-engineer.js` 此前**完全没引用** `chart-type-guard`（grep 空），只有 T3。

**运行时后果**：ECharts 抛 `Unknown series area-line` → **整条 series 被丢弃**，坐标轴/图例仍在，曲线全无（与「只有空坐标系」的观感一致）。

### 修复
- `utils/chart-type-guard.js` 重写：新增 `resolveEchartsType`（~90 条别名表 `area-line`/`面积折线图`/`分组柱状图`/`双系列分组柱状图`… + 关键词兜底，不可识别返 null）；`isValidEchartsType` 保持严格（别名必须判 false）；`fallback = resolveEchartsType(truth) || 'line'`；新增 `findBalancedSpan`/`findSeriesArraySpans`（括号配平、跳过字符串与注释）与治本入口 `normalizeSeriesInSource`（**只改 series 元素第一层 type**，另修 `color:{type}`→linear/radial/pattern、`lineStyle:{type}`→solid/dashed/dotted）。
- 接线：mc T3/2.1.E/N2 三处 + vue3 T3/**新增 2.1.E**/N2。
- 存量：新增 `scripts/fix-chart-series-types.mjs`（复用同内核，dry-run/`--apply`）→ **86 文件 / 146 处**。分布：`分组柱状图→bar` 72、`分组柱状图→solid` 20、`area→line` 20（**新发现类别**：无中文无连字符，首轮正则扫描漏掉）、`area-line→line` 8、渐变类 6+6+2、`面积折线图→line` 4、`双系列分组柱状图→bar` 4、`line→linear` 2。复核仅剩 `c-mc-demo` 的 MIME `application/octet-stream`（非 series）。
- 单测 `chart-type-guard.spec.ts` **35/35 PASS**（含「真值=area-line 时结果不得等于 area-line」、「嵌套键不被污染」、「lazy 漏段落」、「幂等」、「注释/字符串不误伤」）。
- 构建 ✅（dist 三文件均含新符号）；重启 ✅ PID 45128 → **58831**（03:11:48）。

### 验证（浏览器实测，决定性）
| 指标 | 修复前 | 修复后 |
|---|---|---|
| console | `[error] [ECharts] Unknown series area-line` | **0 条 [error]**（`console --clear` 后重载复验，之前 2 条是历史缓冲） |
| `.slot-con` | 405×40 | 405×150 |
| `.chart-body` / canvas | 80 被裁 / 无 | **92 / 405×92** |
| canvas 绿色像素 | — | **1585**（折线+面积） |
| canvas 红色像素 | — | **811**（markLine 预警线） |

截图 `.workbuddy/artifacts/chart-fixed-f1ff01eb.png`；报告 `chart-series-type-laundering-f1ff01eb.md`。

### 工具/踩坑
- `agent-browser` 在 `~/.workbuddy/binaries/node/versions/22.22.2-2/bin/`；`screenshot` 用法是 **位置参数** `[selector] [path]`，没有 `--path` 选项（写了会被当 selector → "Element not found"），正解 `--full --screenshot-dir <dir>`；`console --clear` 可清缓冲，**验证报错必须先清**，否则旧页面的 error 会被误读成本次仍失败。
- 递归遍历整个 `workspace` 会被 SIGTERM(137)：改为**限定深度**（组件目录 → `package/` 与 `package/components/` 两层）。
- 像素级取证（`ctx.getImageData` 统计绿/红像素）比截图目视更客观，可区分「有坐标轴」与「真有曲线」。
- 重启前判据再次生效：先 `tail server.log` 确认无 `visual-parser`/`发起模型请求`（该轮 03:07:15 任务已完成，停 3 分钟无活动才动手）；`kill` SIGTERM 5s 未退 → 升级 `kill -9`。


## 03:30 布局复发治本 + 修复管线死代码审计（commit 274524e，已重启 PID 67647）
- **复发实锤（c-env-monitor-xh8jdcpy-2aa25837）**：slot-con 又是 flex:1 1 0 塌陷 + tabs/icons 均分宽。
- **根因①（治本）**：resource-mounter 规则② ROOT_SELECTOR_RE 只豁免 -root，层①根容器是 c-{semantic}-slot-con → height:100% 仍被改写。已补 `-slot-con`。
- **根因②（治本）**：LLM 直写 flex:N 1 0 规则②管不住 → 新增 utils/root-container-normalizer.js 落盘终验归一（flex 填充→height:100%，含 .vue style；detectContentRootClass=base-panel 后第一个 -slot-con/-root 词尾容器）。+8 spec。
- **🔴 重大审计发现**：`src/ai-engine/generation-context.js`（createFixPipeline/runGenerationPipeline/registerBuiltinFixRules 全链）**零消费者=死代码**——09-11 补的 figmaNodeData 接线修在死路径上，anchor-root-container 从未真正跑过（aspect-ratio=0 判据验证）。且 detectRootContainerClass 会把 base-panel 实例类当内容根。待办：把修复管线接到真链路或删除死代码。
- **就地修复产物**：slot-con flex→height:100%；icons 容器 `.tab:last-of-type`（永不命中，.sub-t 最后 div 是 num 徽标）→ 专属 `--icons` 类（flex:0 0 auto w52px）；line-min-height 笔误→line-height；两副本 + recompileIndexCss 已同步。Figma 真值：tabs-icon fills=[]（无容器背景是设计原样，非丢资源）。
- **剩余未治本清单**：CODE-011/012（echarts 时序/resize 仅检测无修复器）、CODE-022（import 未挂载无修复器）、SEMANTIC-BINDING/RESOURCE-002/003/CODE-004/005（裸 BLOCK+重试）、vue3 graph L0-B 软失败未同步、前端 gateScore 展示未做、CODE-004/005 tailwind 无剥离器。

## 03:45 双 tabs 重复治本（commit 41cdf7a，已重启 PID 73703）
- **实锤（c-device-monitor-hsvmkuvd-cfb53488）**：effectiveSections 双叶子 `89:37`（@antd/tab 壳，elementCount=2）+ `section-main-content`（臆造语义壳，elementCount=16），type=tabs 且 responsibility 同为「标签页切换区」→ 层①装出 TabsSection+TabsSection2 → LLM 各生成「左垂直tab栏+右设备网格」完整副本（两个组件 244 行几乎同构）→ 两个大 tabs 重复。
- **治本**：section-tree.dedupeDuplicateSections（同 type=tabs 同 responsibility 分组，保留 elementCount 更大一份，递归，responsibility 不同不动）；resolvePlanSections（R1 出口）统一调用 → 模板装配/命名/prompt/COMP-001 全链受益。+6 spec 15/15。
- **就地修复**：index.vue 删 `<TabsSection2 />` + defineAsyncComponent 引用（两副本）；slot-con flex:1 1 0→height:100%；recompileIndexCss 同步。「看不见」即塌陷旧病，274524e 治本已覆盖新生成。
- recompile 脚本注意：/tmp/recompile-artifact.mjs 是硬编码路径，换组件要改路径。

## 07:35 根治方案成文（docs/0911-管线复发问题根治方案.md，按用户要求只分析规划不动手）
- 治愈验收标准：同一 Figma 3 连发 + 3 不同 Figma 不变量全绿、零人工就地修复、坏产物被门禁 BLOCK、机制静态可达。
- P0 门禁盲区闭合：CODE-023 悬空标签 BLOCK（标签↔import↔文件三要素齐备才白名单）、CODE-022 修复器（pruneUnmountedResourceImports）。
- P1 结构治本：planner sourceNodeIds 子树单一归属校验（产生层拒绝，出口去重降为兜底）；层① buildDeterministicIndexTemplate 返回 rootLayoutFacts（rootContainerClass/sectionRoots），规则②与归一器改读 facts，命名枚举豁免退役。
- P2 防复发基建：mechanism-reachability-audit.mjs 入口可达性审计 + 删 generation-context.js 死链；artifact-invariants.mjs 六条产物不变量（I1-I6）接入任务 meta。
- P3 流程：契约变更 ADR + 影响面清单；start-node.js 打印 dist/src 版本偏差、任务 meta 记代码版本。
- 执行顺序：P0 → P2-1（先清死代码）→ P1 → R2-2 → P3。

## 07:50 P0 执行完毕 + 重大认知修正（commit f4cb6b4，未重启——dist 已含但服务仍在 03:25 版本）
- **P0 完成**：CODE-023 悬空标签门禁（缺import/缺文件=BLOCK，三全缺=WARN 防误杀全局组件，components 目录空不启用）+ pruneUnmountedResourceImports 修复器接入终验链。+12 spec（91/91 validator + 6/6 prune）。
- **🔴 认知修正（重要）**：修复管线**不是死代码**——microcode-engineer.js 6012/6061 直接消费 CodeFixPipeline 且传 figmaNodeData，anchor-root-container 有注册。死的只是 generation-context.js 包装层（零消费者）。
- **anchor 未生效新主嫌疑**：修复时序——anchor 在 fix pipeline 打补丁 → 之后 validateAndFixGeneratedFiles 的 style-class-consolidator 重建 common.less → 补丁被覆盖（bbox 420×186 正常、rootCls 识别正确，排除前置条件问题）。与"chunk 快照中途正常、终版坏"历史现象同构。**待验证**。终验链的 root-container-normalizer 位置正确所以 height 能保住；aspect-ratio 仍丢（normalizer 未注入）。
- 教训：/spec 全绿+静态 grep 消费者检查都可能误判（6012 是动态 import await import(...)，静态 grep "createFixPipeline" 查不到）。
- 0911 方案文档 R2-1 已按修正改写（时序治理为核心）。

## 根治方案 P2-1 落地（commit 81a885b，后端重启 PID 96992）
- 时序取证实锤：`consolidateSubComponentClasses` 挂在 `microcode-engineer.js#writeFiles`（:3950）统一写盘出口，覆盖主产物/修复产物/终态三路 files，`mergeIntoCommonLess` 删同名块再追加 → fix pipeline 中 anchor 对 common.less 的补丁会被产物重建覆盖。**凡布局关键修复必须在写盘前最后一刻重跑**。
- I4 终验兜底：`root-container-normalizer.js` 扩展 `figmaNodeData`（bbox W/H>50 有效且根容器缺 width 或 height → 注入 aspect-ratio，语义对齐 anchorRootContainerInFiles）；终验链调用点（microcode-engineer ~:6459）传入 figmaNodeData。
- 死接线审计：新增 `scripts/mechanism-reachability-audit.mjs`（BFS，种子=ai-engine 外全部 src + engineer + graphs；解析静态 import + 动态 import() + require；必须包含 .ts 消费方——首轮只扫 .js 误报 16 个，修后 10 个真死文件：chart-type-mapper/chunk-tier/chunk-timeout/context-manifest/figma-golden-extractor/figma-meta-cache/progress-manager/tech-stack-style-extractor/runtime-static-rules/__gen-golden，待清理立项）。
- 删除 `generation-context.js` 死包装（零真实 import 消费方；code-fix-rules-root-anchor.spec 末段 createFixPipeline 回归测的是死包装本身，随删并留迁移说明；dist 残留产物手动 rm）。
- 回归：root-container-normalizer 12/12 + code-fix-rules-root-anchor 14/14 PASS；build 成功（grep 实锤新符号进 dist）。
- P2-1 剩余：R2-2 artifact-invariants.mjs（6 条产物不变量）；P1：rootLayoutFacts 数据流 + planner sourceNodeIds 单一归属。

## 「是否构建重启」状态核对（08:38–08:47，用户提问触发）
锚点：**dist 构建 = 08:17；进程重启 = 08:26（PID 96992）** → 当前进程加载 08:17 的 dist。08:44 后无新写入。

**✅ 已构建+已重启+生效（08:17 前落地）**：图表 series.type 修复（03:09）／R3-1 CODE-023（07:41）／R3-2 pruneUnmountedResourceImports（07:39）／R2-1 删 generation-context.js + scripts/mechanism-reachability-audit.mjs（08:13）／root-container-normalizer 08:07 版。

**🔴 未构建、当前不生效（08:17 之后改）**：`utils/artifact-invariants.js`（08:44 新建，**dist 缺失**）、`utils/root-container-normalizer.js`（08:44 改，dist 里是 08:17 旧版）。
⚠️ `artifact-invariants.js` **零 live 消费者**（grep 仅命中自身注释）→ 即使构建也不生效，须接进落盘终验链或 scripts。

**方案中尚未落地**：R1-1 planner sourceNodeIds（命中 0）、R1-2 rootLayoutFacts（命中 0）、R3-A docs/adr（未建）、R3-B start-node.js 版本护栏（未实现）。

**复用判据**：
```bash
stat -f "%Sm %N" -t "%m-%d %H:%M" backend-node/dist/main.js   # 构建时间
grep -n "start-node" backend-node/server.log | tail -1         # 重启时刻（UTC，+8=CST）
find backend-node/src -type f -newermt "<dist时间>"             # = 未进 dist 的改动
[ -f dist/<path> ] || echo "dist 缺失 → 未编译 / 该文件是构建后新建"
```
- `dist` 缺某文件 ≠ 编译失败，可能该文件本就是构建时间之后新建的。
- BSD `find -newermt "-10 minutes"` **静默不匹配**，必须用绝对时间。
- 本会话与并行会话可能同时改 src/日志 → 落笔前先读最新尾部，避免 Edit 锚点失效。

## 方案 A 执行：补齐覆盖根 → 收敛 → 构建(08:48) → 重启(PID 9808)

### 收敛脚本升级为「通用发现」（`scripts/fix-chart-series-types.mjs`）
- `COMPONENT_ROOTS`（枚举两个副本根）→ **`SEARCH_ROOTS` = `[backend-node/workspace, frontend/workspace, temp-components]` + `discoverComponentDirs()`**（递归找含 `package/` 的目录，跳过 `_` 前缀与 node_modules，深度 ≤ 3）。
- **一次性根治两处布局泄漏**：① `temp-components/<groupId>/<componentId>/package/`（两级）；② `custom-components/<groupId>/<taskId>/package/`（有组件多嵌一层组目录，旧脚本只扫 `<root>/<id>/package` 同样漏）。
- **复核段改内核口径**：原用「取值黑名单（中文/连字符）」→ 会漏 `'area'`（无中文无连字符的隐藏类别）且误报 MIME `application/octet-stream`。现改为对每个 .vue 跑 `normalizeSeriesInSource(src,{})`，**`changed > 0` 即仍非法** —— 与管线完全同口径。

### 结果
- 第二轮收敛：**43 文件 / 72 处**（`temp-components` 41 + workspace 2）。累计 **129 文件 / 218 处**。
- 变换分布（第二轮）：`分组柱状图→bar` 36、`area→line` 11、`分组柱状图→solid` 10、`area-line→line` 4、渐变类 3+3+1、`面积折线图→line` 2、`面积折线图→solid` 1。
- **全量复核：1745 个 .vue，仍非法 = 0** ✅；脚本幂等（再跑命中 0 处）。
- 真实脏数据三种改写全部正确（temp `mc-max-1789019701470-9c86b889/ChartsSection.vue`）：`series.type` → `bar`/`line`；被污染的 `lineStyle.type` → `solid`（同文件另一条合法的 `dashed` 未被误动）；渐变 `color.type` → `linear`。

### 构建 + 重启
- 构建 **08:48**（`rm tsconfig.build.tsbuildinfo && npm run build`）→ 补上 08:44 未编译的 `artifact-invariants.js`（dist 现已存在，与 src 一致）与 `root-container-normalizer.js` 新版；图表修复符号仍在（guard 2 / mc 3 / vue3 2）。
- 重启 **08:49:24**（老 PID 96992 SIGTERM 6s 未退 → `kill -9`）→ 新 PID **9808**，端口 13030 监听，`Nest application successfully started`。
- 重启后回归：主组件预览 **0 个 `[error]`**、canvas 405×92、绿 1585 / 红 811（与重启前一致）。

### 仍待办
- `artifact-invariants.js` 编译已进 dist，但**仍无 live 消费者**（未接落盘终验链）→ 不生效。
- 方案未落地：R1-1 `sourceNodeIds`、R1-2 `rootLayoutFacts`、R3-A `docs/adr/`、R3-B 版本护栏。
- 本轮与历轮改动**均未提交 git**。



## R2-2 产物不变量落地 + 死文件清理（commit 9d034a2 / e07bdc9，后端重启 PID 19304）
- 新增 `utils/artifact-invariants.js`（六条不变量纯函数）+ `scripts/artifact-invariants.mjs` CLI（单目录 / --recent N / --strict）；engineer 终态接入 `result.artifactInvariants` 字段随任务落库（报告性不阻断）。
- 首战实锤：`c-traffic-monitor-lfa6up1n-8c815194` 有 7 个真实悬空标签（ContentSection~7 无文件无绑定，46859f40 事故同形态）——不变量校验抓到真问题。
- 两个正则陷阱修复：① SFC 模板区截取不能用 lazy `</template>`（具名插槽提前闭合致后续标签全丢，cfb53488 TabsSection 误报实锤），改用 `<script>/<style>` 边界——`detectContentRootClass` 同源修复；② 标签提取**不剥离**具名插槽块（插槽内是真实引用，cfb53488 HeaderSection 误报实锤）。
- 死文件清理（三重确认：可达性审计+全源 grep+docs 立项记录）：删 chart-type-mapper / figma-meta-cache / progress-manager；**有 spec / docs 立项记录的一律保留并写明豁免理由**（runtime-static-rules=G3、figma-golden-extractor/__gen-golden=Loop 4 双裁判门禁、tech-stack-style-extractor=有实施报告、chunk-* / context-manifest=基建预案）。教训：删除前必查 docs/ 立项记录，有专属 spec = 可能是 TDD 待接线。
- 意外收获：工作区 resource-mounter.js 有一条不完整未提交编辑（`isRootSelector` 未定义 ReferenceError，规则②执行即崩）且已进当日 dist——已 git checkout 恢复 HEAD 正确版（ROOT_SELECTOR_RE.test）。
- 全量 jest 基线：1201 passed / 15 failed（10 suite），失败均为存量（golden 漂移=governance-v2 已知降级草稿、sfc-semantics import.meta 陷阱、subcomponent-planner config/backend-root 缺失等），非本轮引入。

## 悬空标签根治：lazy </template> 截断家族（commit 1e3b009，后端重启 PID 26590）
- 用户报 0ca84358 仍出悬空标签（Switch/Tabs/Main）。取证（server.log 行 4972-5701）：① `_unionTemplateTagSubComponents` lazy 截断 → 只 union HeaderSection → 3 个子组件文件漏生成（「子组件并行生成完成: 1 个」）；② `pruneDeadSubComponentImports` 同样截断 → 误删 3 个 import（日志 `{"dead":[...]}`）；③ L0-B BLOCK×2 后断点续跑恢复同一坏中间态 → 确定性 bug 重试必现 → 软失败降级发布 score=86。**用户假设「先删 import 后忘组件」不成立——两步是同根因独立故障，生成清单先于删 import 已错**。
- 治本：新增 `utils/sfc-template-extractor.js`（边界法单一事实源：首个 <template> → <script|<style>，jest 安全），替换 11 文件 14 处 lazy 家族；CODE-023 三全缺 WARN→BLOCK 堵死降级通道。
- 🔴 固化教训：**SFC 模板区提取禁用 `/<template>([\s\S]*?)<\/template>/` lazy 正则**——具名插槽的 </template> 提前闭合，后续标签全丢。已实锤三例（pruneDead/union/detectContentRootClass）。screenshot-renderer 的 lookahead 变体 `(?=<script|<style|$)` 是正确写法。全库 sweep 用 `grep -rnE "<template>\(\[\\\\s\\\\S\]\*\?\)<\\\\/template>"`（当前 lite.service.ts 两处低风险残留）。
- 存量产物 0ca84358 未就地修（3 个组件文件无法凭空造真实内容，拒绝 fake）→ 走固定管线重新生成验证。

## 页脚新增「建议反馈」外部入口（frontend/AppFooter.vue）
- 诉求：网站底部加建议反馈入口 → `https://aio.microvideo.cn/tzy/biz/formCustSql/formCustSqlView/njgd_yjfk`。
- 落点：`frontend/src/components/generator/AppFooter.vue` 的 `nav.af-links` 末尾，新增 `<a class="af-link" target="_blank" rel="noopener noreferrer">建议反馈</a>`；复用既有 `.af-link` 样式（含 hover/active），无需新样式。
- 该 footer 由 `App.vue` 两处 `AppFooter v-if="!hideFooter"` 渲染（登录页/全屏页自动隐藏）。
- 验证：`npm run build:safe`（env -i）exit 0，built in 22.19s。

## 260ff122 tab 文字异色根因：consolidator 丢 --active 修饰符（分析完毕待修）
- 像素取证：生成态非激活 tab 文字 ≈#bacff3（白字+蓝晕）vs Figma #2c9bea 深蓝实色；Figma 真值 #2c9bea/14px/w500 与产物 CSS 基础规则**完全一致**——文字样式没写错，是被覆盖。
- 根因：`style-class-consolidator.js#normalizeShortClass()` 设计为「去 -active/--active 尾缀取基名」（:306），`rewriteShort()`（:477）用基名查 DOM 映射后返回**丢修饰符的 DOM 类** → `.c-env-monitor-tab-item--active` 选择器被重写成 `.c-env-monitor-tab-item`（产物 common.less:257 实锤）→ 激活态白字规则污染全部 tab（同 specificity 后者胜，盖掉 240 行正确的 #2c9bea）。
- 修法方向：rewriteShort 先分离修饰符后缀、base 查映射后重组；DOM 有完整 `--active` 类时直接用。附带：consolidator:475 也是 lazy </template> 家族成员（本例无插槽未触发）。
- 同任务另一实锤（上一条已记）：bg-7890.png tabs-list 整条默认带零挂载（vision 占位 selector `.bg-container` 断裂）。

## consolidator 修饰符缺陷补证：镜像形态与顺序依赖（评估结论）
- `buildClassRewriteMap`（:391-421）确实扫 `:class` 对象 key（:409-415），但 `registerClassToken`（:429-439）用 `normalizeShortClass` 剥后缀入**基名键 + first-wins** → 同一基名下 `X` 与 `X--active` 互相抢占：先见静态 `class="..."` → map[base]=X（→ 激活选择器丢后缀，本事故）；先见 `:class` → map[base]=X--active（→ **基类选择器被改写成激活类**，缺陷镜像形态）。**攻击面随属性书写顺序漂移** = 只修 rewriteShort 不够的铁证。
- 根治要求：映射键保留修饰符（禁止跨修饰符合并）+ 重写侧精确命中优先/否则 base+suffix 重组 + 重写后同名碰撞守卫（fail-open 保留原选择器）+ normalizeShortClass 剥离仅用于 looksLikeShortClass 判定。属 P1「类名单一事实源」子项。

## consolidator 修饰符修复风险量化（评估）
- 影响面实测：workspace 254 个组件，**97 个含修饰符选择器**（`--x`/`-active`），**28 个呈「重复选择器+修饰符」疑似缺陷形态**（含 260ff122/wzk1763d/2aa25837 等）。
- 零副作用前提三条：① 多形态候选（长短并存）用**逗号双写**而非择一（否则引入新的顺序依赖）；② 碰撞守卫 fail-open 保留原选择器（惰性 > 污染）+ WARN 计数；③ `normalizeShortClass` 保持导出语义不变，只新增内部 splitModifier（避免波及 looksLikeShortClass 与既有 spec）。
- 关键结论：consolidator 只在生成期 writeFiles 出口运行 → 存量产物零影响，只有重新生成才体现变化；无接口/数据变更，重启即可。

## 「做1-2」执行记录（第1项收尾 + 第2项 R1-1/R1-2/R3-A/R3-B）
### 第 1 项收尾（artifact-invariants 误报面 + 既有 bug）
- `artifact-invariants.js`：新增 `isArtifactPath` 产物边界过滤（排除 .mc-gen/缓存/chunk 分片、node_modules/dist、隐藏目录）——chunk 片段跑 I2 三向对齐必误报（实测 5 error/31 warn 全是 `.mc-gen/cache/code-chunks/*`）。CLI `collectFiles` 同步跳过隐藏目录省 IO。
- 顺手修 I2 反向判定 pathRef 大小写缺陷：`new RegExp(...'i')` 补 i 标志，否则 PascalCase 文件名永远匹配不上小写 kebab（「已 import 但模板未用」误报死代码）。
- **既有 bug 实锤修复（code-healer.js:819 `quoteBareObjectKeysInVue`）**：改用 `extractSfcTemplate` 后残留 `tplMatch.index` 引用 → `ReferenceError: tplMatch is not defined` 直接炸穿生成（mc-max-1789091377500-624c3a24 实锤，堆栈 dist code-healer.js:819）。修复：`start = vueContent.indexOf(body)`（body 是真实子串）。
- 存量体检（--recent 6）检出真缺陷：c-device-monitor-nodvhsu2-0ca84358 3 处悬空标签、c-traffic-monitor-lfa6up1n-8c815194 8 error（未修，记为存量）。

### 第 2 项四条（方案 0911 根治方案）
- **R1-1 sourceNodeIds 单一归属**：`subcomponent-planner.js` 新增 `collectSourceNodeIds(sec)`（figmaNode/nodeId/`数字:数字`id/tabStructure.panels/子元素 nodeId），`buildEffectiveSection` 叶子分支产出 `sourceNodeIds`；`section-tree.js dedupeDuplicateSections` 增强——sourceNodeIds 交集优先去重（不依赖措辞，保留 elementCount 大者），tabs 措辞兜底保留；`generation-context-assembler.js normalizeSectionNode` 透传 sourceNodeIds（否则 generationInput.componentPlan 路径丢字段）。单测 18/18（新增 3 条 R1-1 用例）。
- **R1-2 rootLayoutFacts 数据流**：`buildDeterministicIndexTemplate` 返回值 string→`{template, rootContainerClass, sectionRoots}`；code-generator 写 `input._indexTemplateFacts`（LLM 回退路径 detect 兜底）；generateCode 返回 `indexTemplateFacts`→execute 转 `rootLayoutFacts`→pipeline.context（code-fix-rules）→`fixSectionHeightsForResource` 规则②与 `normalizeRootContainerLayout` 改读 facts、`ROOT_SELECTOR_RE` 词尾猜测退役为兜底。单测 31/31。
- **R3-A docs/adr**：`docs/adr/{README,0001-resource-attribution,0002-props-wiring,0003-component-naming}.md`（含 ADR 模板 + 反转类变更「影响面清单」模板）。
- **R3-B 版本护栏**：`start-node.js` 启动时打印 dist 构建时间 vs src 最新 mtime（STALE 打 WARN）+ git HEAD 短 hash；新增 `utils/build-version.js#getBuildVersion()`（内存缓存 git hash+dist mtime）；`microcode-engineer.js` 无条件写 `component-meta.json.codeVersion`（{gitHash, distBuildAt, writtenAt}）。

### 构建/重启
- 构建 02:08（全量，exit=0）；重启 PID 41711（10:09:29），R3-B 启动打印 `dist=02:08:57 git=1e3b009` 无 STALE。
- 全部改动未提交 git（用户未要求提交）。

### 遗留（未做）
- `subcomponent-planner-p2.spec.ts` load 失败：`jest.mock('../../config/backend-root')` 路径错误（应 `../../../config/backend-root`）且 mock 导出（getWorkspaceRoot/isDev）与现行 backend-root.js 不符——既有基线问题，与本次改动无关。
- c-device-monitor-nodvhsu2 / c-traffic-monitor-lfa6up1n 的 I2 悬空标签/死代码真缺陷待存量清理。

## P1/R1-3 设计定稿（docs commit 3913801）
- 新增 `docs/P1-类名结构单一事实源-方案-2026-09-11.md`：① 现状 5 条类名变换链取证 A实例前缀(component-naming:160/175) / B模板前缀化(class-prefix-fixer:33, engineer:6474) / C scoped↔DOM 重写(consolidator:391/429/477) / D 结构类生成(code-generator:146) / E 修复器方言源(code-healer:1339 `is-active`)；② classFacts 数据流（复用 R1-2 事实注入链：buildDeterministicIndexTemplate → input._classFacts → pipeline.context，全链 fail-open 回旧行为）；③ P1.1 采集器零行为变更 → P1.2 consolidator 消费 facts+修饰符修复 → P1.3 前缀化/方言统一 → P1.4 I7 接入；④ I7 定义：模板→样式 error / 样式→模板 warn，别名表归一（is-active→--active）。
- 方案要点固化：多形态候选一律**逗号双写**不择一；碰撞 fail-open 保留原选择器+WARN；normalizeShortClass 导出语义不动。

## 附加：subcomponent-planner-p2.spec.ts load 失败根因 + node 目录升级
- **load 失败根因（两层）**：`jest.mock('../../config/backend-root')` ① 路径少一层（spec 在 `roles/__tests__/`，应 `../../../config/backend-root.js`）② mock 导出是旧 API（getWorkspaceRoot/getLogsDir/isDev），现行 backend-root.js 是具名常量（backendRoot/workspaceRoot/logsDir...）。已修：改路径 + 导出现行键 + `{virtual:true}`。
- 依赖链：subcomponent-planner → createLogger → logger.js → `import { logsDir } from config/backend-root.js`（import.meta.url，jest CJS 会炸）→ mock 必须保留。spec 是 commit 68f3443 新增，mock 从别处复制没改层级、没跟上 backend-root 重构 → 从诞生就没跑通过。
- **修完暴露连带失败**：S3 用例「含 3 图表拆 3 chunk」断言 `TrendChart.vue`（英文），但实现 `pascalCase('趋势图')`=中文 `趋势图.vue`（pascalCase 保留 `\u4e00-\u9fff`）——spec 自身数据(name='趋势图')与断言自相矛盾的坏 spec，未修（涉及 S3 命名规范决策，待拍板）。
- 🔴 **managed node 目录升级**：22.22.2-2 → 22.22.2-3（旧目录已删）。旧后端 41711 用 22.22.2-2/bin/node 启动，其 execPath 已失效 → fork/spawn 子进程会 ENOENT。已用新 node 重启（PID 46589，10:16:11 就绪）。start-node.js 的 `process.execPath` 设计已防此问题（重启即自动换路径）。
- R3-B 护栏**首次实战**：重启时抓到 STALE（dist 02:08:57 < src style-class-consolidator.js 10:16:25）——该文件是并行会话刚改未 build，非本会话产物，不擅自 build（防编译半成品）。

## P1.1-P1.4 全部实施落地（backend-node 0973eba，后端重启 PID 53999；docs 2c5ccb6）
- P1.1 新增 `utils/class-facts.js`：collectClassFacts/collectFileClassFact/resolveDomClass/splitModifier/MODIFIER_ALIASES/normalizeModifierSuffix；修饰符独立成键；实例前缀长类内层语义别名补位（真实 token 优先）。
- P1.2 `style-class-consolidator`：删自扫模板映射（buildClassRewriteMap/registerClassToken 退役）→ 消费 classFacts（engineer 生成期注入，缺省 self-collect）；`rewriteBlockTextVariants`（精确命中优先 → base+suffix 重组 → 多候选变体，≤4 组合上限）；碰撞守卫 fail-open 保原选择器 + WARN；按重写后选择器去重；模板提取换 sfc-template-extractor。
- P1.3 prefix-fixer 换共享边界法；code-healer tab 骨架 `is-active` → `{itemClass}--active`。
- P1.4 `artifact-invariants` 新增 I7 checkModifierClassConsistency（模板→样式 error / 样式→模板 warn / 布尔别名按通配判定）；接入 engineer 终态 + CLI。
- 实测：dry-run 19/19 组件按修正方向变化（--active 恢复、`:not(--active)` 保留）；I7 精准报 260ff122 两处不一致；src/ai-engine 全量 jest **1255 passed**，9 个失败 suite 全为存量基线（safe-logger/orchestrator/subcomponent-planner-p2/code-parser/roles-request-context/ai-request-gateway/json-parser/manifest-auditor/sfc-semantics）。
- 坑：do-not-invent-validator 新增 extractSfcTemplate 调用漏加 import → 33 例全挂，已修（教训：批量改模板提取后必须跑一遍该文件 spec）。

## S3 图表文件名确定性英文化（方案 A 落地，2026-09-11）
- 根因：`extractCharts` 用 Figma 节点名（中文"趋势图"）作 chart.name → `splitMultiChartSectionIntoChunks` 用 `pascalCase(chart.name)`，而 pascalCase 正则 `[^a-zA-Z0-9\u4e00-\u9fff]+` 保留中文 → 产出中文 .vue 文件名（违反英文 PascalCase 规范）。
- 关键背景：S3 拆分结果（s3Chunks）在 graph 层是**死代码**——`mc-component-graph-phase2.js:2992` 算完后只用于日志/进度，`:3015 return { subComponentPlan: finalPlan }` 丢弃，未进 state/engineer。故中文文件名实际生产影响为零。
- 修复：删 `pascalCase`，新增 `CHART_NAME_HINTS` 词表（趋势→TrendChart/柱状→BarChart/饼→PieChart/面积→AreaChart/散点→ScatterChart/雷达→RadarChart/仪表→GaugeChart/热力→HeatmapChart）+ `assignChartFileNames(charts)`（①词表命中→语义名 ②未命中→`Chart{N}` 序号兜底 ③全局 Set 去重同名加序号）+ `assignMiscFileNames`（`Misc{N}`）。替换 splitMultiChartSectionIntoChunks 内 4 处 pascalCase 调用。
- 单测：p2 spec 从 load 失败→10 用例全绿（原 8 + 新增「序号兜底」「同名去重+misc 兜底」），s3-mutual 4 用例绿，合计 14/14。
- ⚠️ 未 build：并行会话活跃改 src（demo/screen-layout/database/tasks 等 10 文件近 3 分钟被改），且 S3 为死代码不紧急，暂不 build 防卷入半成品；留待并行会话停笔统一 build+重启。

## 生成失败修复：build-version.js 相对路径少一级（commit 5b2f3d5，重启 PID 59776）
- 症状：生成失败 `Cannot find module dist/ai-engine/config/backend-root.js' imported from dist/ai-engine/utils/build-version.js`。
- 根因：`src/ai-engine/utils/build-version.js`（R3-B 版本护栏，**并行会话 10:05 新增且未 git 跟踪**）内 `import { backendRoot } from '../config/backend-root.js'` —— 自 utils/ 解析到不存在的 src/ai-engine/config/；正确为 `../../config/backend-root.js`（logger/style-refiner 同口径）。其 import 已随 microcode-engineer.js:22 进入 HEAD（我的 0973eba 提交扫入），故启动不崩、**生成时懒加载即崩**。
- 修复：路径纠正 + 文件纳入 git 跟踪（若只改路径不 add，新检出/部署仍缺文件）。验证：重建后 `getBuildVersion()` 返回 {gitHash, distBuildAt}；`import dist/ai-engine/roles/microcode-engineer.js` 通过；Nest 启动成功。
- 🔴 教训：**并行会话产出的文件可能被自己的 git add 扫入，而文件本身未跟踪** → 提交前 `git status --short src | grep '??'` 检查未跟踪文件是否被已提交代码引用（本次即此坑）。另：根仓 start-node.js（R3-B 启动护栏，未提交）已被该会话改好，勿动。

## 构建 + 重启（10:38，PID 64020）
- 构建时 src 有 17 个未提交改动（并行会话 R1-1/R2 进展：subcomponent-planner / code-generator / visual-parser / vue3-engineer / chart-type-guard / component-resolver / section-tree / error-humanizer / config.controller / preview.controller 等）。
- 首轮构建后 R3-B 护栏报警「dist 早于 src 22s」（mc-component-graph-phase2.js 在构建后又被改）→ 二次构建后护栏干净（dist=02:38:17Z, git=5b2f3d5，无 STALE）。**R3-B 护栏首次实战生效**。
- 9 个关键 dist 模块 import 冒烟全 OK（含 class-facts / style-class-consolidator / build-version / engineer / planner）。
- 重启流程固化为：先 `awk` 扫 2-3 分钟内有无「发起模型请求/开始执行代码生成/Checkpoint」→ kill → lsof 验端口 → start-node.js (env -i) → 校验 PID + 端口 + server.log 启动行 + 护栏输出。

## 删 S3 死代码（splitMultiChartSectionIntoChunks，2026-09-11）
- 结论：S3 拆分是**从诞生（d5dc2be）就没接线的死代码**——graph 层 `mc-component-graph-phase2.js:2992` 算 s3Chunks 后只 log/进度，`:3015 return { subComponentPlan: finalPlan }` 丢弃，未进 state/engineer。其职责（多图表 section 拆多个图表子组件）已被 planner `internalSubcomponents`（splitSectionInternally R1 图表隔离）覆盖，engineer 读的是 internalSubcomponents 不是 s3Chunks。两套机制重叠违背「治本 D 互斥」原则。
- 删除范围：① subcomponent-planner.js 的 splitMultiChartSectionIntoChunks 函数 + CHART_NAME_HINTS/assignChartFileNames/assignMiscFileNames（上一轮刚加的 S3 命名英文化，随函数一并删，白做但已验证）；② graph 层 2989-3002 调用块 + onProgress 里 s3Applied 文案；③ spec：subcomponent-planner-s3-mutual.spec.ts 整个删、subcomponent-planner-p2.spec.ts 删 S3 describe 块（保留 P2' 图例测试）。
- 单测 27/27 全绿（p2 保留 8 用例 + nested-container + collapse + section-tree），全仓 grep 0 残留引用。
- 备注：发现 637 个 src 文件 mtime 被批量 touch 但内容没变（git status 仅 24 个真实变更），非"并行会话边写边改"，build 安全。

## 1afdb842 三问题根因分析（只查未改产物）+ 顺修基础设施正则（commit 486ec46）
- **#1 图例独立 DOM**：vision 数据齐全（charts[0].legend=['zk3+785CO浓度'] / legendPosition=top-right / legendType=horizontal），但生成的 echarts option **无 legend 字段**，图例被写成独立 DOM（frame-2136637694>group-1321318245>legend-dot+legend-label），且 `dispatchAction|@click` 计数=0（**完全无联动**）。链路：design-facts-compiler:39 只把 legendPosition 编进提示词 facts，**无任何代码把 charts[].legend 提升为 option.legend**；prompt-builder:539（有自定义样式→DOM 图例+联动）与 :776（必须 echarts 内置 legend，不要拆独立 DOM）**自相矛盾** → 模型选 DOM 分支且不做联动。属能力缺口+提示词冲突。
- **#2 tabs 点击无效**：ContentSection 是受控组件（activeTab 为 prop，handleTabChange 只 emit('tab-change')），而 index.vue 的 `<ContentSection />` **既无 :active-tab 也无 @tab-change** → 事件无人接、prop 恒默认 → 无切换。次级：`:class` 用无前缀 `c-env-monitor-tab-item--active`，样式里是有前缀 `c-env-monitor-x5xfv6y5-c-env-monitor-tab-item--active`（I7 已报 error）。属父壳未装配交互契约 + 修饰符类前缀不一致。
- **#3 tab 背景图**：两张图都已下载（bg-7890 整条带 295×27 / bg-tab-active-7891 激活段），但 bg-7890 被 import 为 bg1 挂到**列表图标 img**（错挂），tab 容器用 `linear-gradient(var(--colorTabBgStart/End))`（common.less:37 + ContentSection scoped:86）。mapping 的 targetDomSelector 又是 vision 占位名（.bg-container/.tab-active-container）→ 无锚点。**与 260ff122 同根（R1-3 资源挂载语义对齐未做）**。
- 🔴 顺修基础设施真 bug：`/<template\s*>/i` 在 `<template class="...">`（带属性）时匹配失败 → 模板区为空 → I2 双向失明（误报死代码 + 漏报悬空标签）。改 `/<template\b[^>]*>/i`，5 文件 6 处（含 code-fix-rules 两条 base-panel fallback）。该组件 I2 误报清零。**教训：模板起始标签正则必须允许属性**。

## R1-3 执行到位性审计（对照 docs/P1 方案，逐条取证）
结论：P1.1/P1.2/P1.4 主体落地，**P1.3 基本未落地 + I7 有判据盲区 + 两处修复未进 dist**。
- ❌ **P1.3 prefix-fixer 未接 classFacts**：`class-prefix-fixer.js` 中 classFacts 命中 0；实战对 1afdb842 跑 `fixMissingClassPrefixes` = **0 fixes**。机制：样式用 LESS 嵌套 `&--active` 表达 → `defined` 集合里从未出现完整 `--active` 类名 → suffix-hit `endsWith('-c-x-tab-item--active')` 无候选 → 模板无前缀修饰符类不被修正（DOM 事实源自始至终没参与）。
- ❌ **P1.3 code-healer 未接 classFacts**：命中 0（方案要求「生成模板前查 facts，禁止自造方言」）。
- 🟡 **S5 方言未统一到产物**：healer 骨架已改 `--active`，但 LLM 产物仍写 `is-active`（0b95f5cf TabsSection 实锤），缺「写盘前确定性归一」。
- ❌ **I7 判据盲区（false negative）**：0b95f5cf 真失配（模板 `is-active`，样式 `.c-device-monitor-tab-item.is-active` 基类**无前缀**，DOM 基类带前缀）→ I7 判「0 error/0 warn 全通过」。根因：I7 只查「修饰符是否存在于样式源」，不校验修饰符规则的选择器**基类形态**是否与 DOM 一致（复合选择器 `.base.is-active` 完全没拆）。
- ❌ **修复未进 dist**：commit 486ec46（`<template class=...>` 正则）未 build → 运行期仍误报「5 个子组件死代码」（server.log 10829/11063）。
- ❌ **I6 生成期系统性误报**：34 条「import 的资源不存在」而资源实际全部存在（bg-8788/icon-8798/icon-8817/bg-8807 磁盘可查）。根因：不变量跑在**内存快照**上而快照不含 `resources/images/*`。需加「快照无任何 images 条目 → 跳过 I6」fail-open。
- ❌ 两条验收未执行：P1.1「facts vs 现状各链 diff=0」、P1.3「重生成后 grep -c is-active=0」。
- 文档口径未同步（实现等价但机制不同）：① 多候选实现为**块级多变体**而非文档写的「逗号双写」；② facts 注入实现为 writeFiles 内 self-collect（`this._classFacts`）而非文档写的 `input._classFacts → pipeline.context`。

## 「这 6 项是根治吗」评估结论（L1/L2/L3 框架，可复用）
- 6 项定性：① prefix-fixer 接 facts = **半治本**（消灭一条链的独立判据，多源结构未变）；② 写盘前方言归一 = **后处理**，仅当「归一在 classFacts 采集之前」且「同时上 fail-closed 门禁禁方言」才算治本，否则是新增第 6 条变换链；③ I7 扩维 = 检测力（防复发基建，非防生成）；④ I6 口径 = 修我们自己的检查器假阳性；⑤⑥ = 交付/验收卫生。
- **根治三层（本轮均未动）**：L1 类名仍是 5 条链各自产出/改写（A 实例前缀/B 模板前缀化/C scoped↔DOM/D 结构类/E 修复器与 LLM 方言）→ 需**单一写入者**（骨架阶段写 facts，其余只读或写回 facts）；L2 规范与门禁无类名契约（核对 docs/mc-spec-compliance-plan 的 M-series：M1-1 componentId/M5-6 主题变量/M5-10 硬编码色…**无修饰符契约**；prompt 539 与 776 图例口径还互相矛盾）→ 契约入规范 + fail-closed；L3 方案验收无执行载体（P1.1 diff=0 / P1.3 is-active=0 今天都没跑）→ 验收脚本化并挂 build 前自检。
- 建议立 P1.5（类名单一写入者）/P1.6（契约入规范+门禁+验收脚本）。

## P1.5/P1.6 根治落地（backend-node ee1e696，重启 PID 80016；docs 94cd59e）
- **P1.5 类名单一写入者**：新增 `utils/class-dialect-normalizer.js`（R1 布尔方言 is-active→`${元素基类}--active`；R2 修饰符形态对齐元素基类；R3 样式 `.base.is-active`→`.base--active`；保守：无基类不动）。**engineer writeFiles 顺序固化为「归一 → collectClassFacts → 收敛」**——这是根治关键：全链看到同一套类名，消除「facts 看到 is-active、下游看到 --active」的新裂缝。class-prefix-fixer 改 facts 驱动（旧路径对 1afdb842 = 0 fixes，已废）。
- **P1.6 契约**：新增 `utils/classname-contract.js` = **门禁与不变量同一实现**（C1 方言/C2 基类形态/C3 激活态样式/C4 无死规则）；`CODE-024` fail-closed BLOCK 接入 code-structure-validator；I7 委托 contract（修复合选择器盲区：0b95f5cf 由「全通过」变为可检出）；I6 口径修正（快照无 images 条目 → fail-open，消 34 条生成期假 error）。
- **口径收紧教训**：契约只认 BEM `--mod` 与布尔别名——`c-device-monitor-active`（语义类名）被 splitModifier 当 `-active` 修饰符 → C2 误报 71 条；加 `isContractModifier` 后降到 16。
- **P1.6 治理三件**：`scripts/classname-writer-audit.mjs`（单一写入者白名单审计，8 写者全登记/0 未登记，退出码可挂 CI）+ `scripts/r1-3-acceptance.mjs`（方言/C1-C4/I7/DOM 命中率，`--strict`）+ npm scripts `verify:classname` / `verify:r1-3` + 规范新增 **M7 类名契约**章节。
- 存量基线（20 组件）：C1 6 / C2 16 / C3 11 / C4 148(warn) / I7 33 / 方言 44；新产物判据 = C1=C2=C3=0 且方言=0。
- 回归：utils+validators **1035 passed**（4 失败 suite 全存量）；新增 spec 22 例全绿。

## P1.7 资源契约根治（backend-node 9c49988，重启 PID 87220；docs 已更新 M8）
- **事故 13890774（`icon4 is not defined`）根因**：mapping 变量名与模型引用**完全一致**（icon1…icon14/bg1…bg14，来自 assignedVarName），但**注入只认「模板可识别使用形态」**（`${var}` / `:src`）→ `const deviceIcons = [icon3, icon4, …]` 写在 **script 内**完全看不见 → 只注入 3 个 icon import、模型引用 12 个 → ReferenceError；兜底 T08 `stripUndefinedResourceRefs` **同样只扫模板** → 全部漏网。**原始块实测 R1 = 24 条**（不是 1 条）。
- 治本：① 新增 `utils/resource-facts.js` 引用采集单一实现（模板 `${}`/`:src`/拼接/url() + **script 内数组·对象·函数引用**；先剥离 import 语句与字符串字面量——否则路径里的 `images`/`icon`/`bg` 会被误判为引用，实测产生 R2 误报）；② `ensureResourceImportsForRefs` **引用驱动补齐 import**（事实源命名 + 路径基准，幂等）；③ T08 扩展到 script（降级 `undefined`）；④ 不变量 I8 + 门禁 CODE-025 共用 `checkResourceContract`（单一实现）；⑤ 审计扩到资源写者（14 个全登记）；验收脚本加 R1/R2。
- **验证**：原始块 R1 24 → 补齐 24 import → R1/R2=0；新增 spec 14/14；回归 1208 passed（5 失败全存量）。
- ⚠️ **教训（重启时机）**：11:35 我的活动检查窗口是 03:05–03:19（空闲），但用户任务在 **03:34 启动**（检查后），kill 打断了在跑的生成（日志尾 `发起模型请求`/`仍在处理中`）。**kill 前必须查最近 1–2 分钟**（不能只看更早窗口），且检查与 kill 之间不要间隔其他耗时操作。
- 存量基线（20 组件，验收脚本）：R1 注入漏项 88 / R2 幽灵引用 26 —— 说明该缺陷族存量面很大。

## 「lite 产物微码规范报错远多于 figma」根因（P1.8 待做）
- **根因：两条路径各写一套产物骨架**（同一规范两处实现，与 P1.5/P1.7 同族）。
  - max/Figma：microcode-engineer + `microcode/file-writer.js` → 产全量平台骨架：`resources/config/css-vars.js`(buildCssVarsFile) + `declare.js`(buildDeclareJsFile) + `component.js` + `themes/{theme-vars.less, dark.less, light.less}`(THEME_WRAPPER_TEMPLATES:212) + `common.less`(LLM 分块 runStyleChunks:1757) + `index.less` 引用；另有 M5-6/M5-7 字体归一、变量归一、引用注入等确定性自愈链。
  - lite：`lite/lite.service.ts:460-530` **自造精简落盘** → 只写 index.vue + declare.json + declare.js(内联字符串) + `resources/styles/index.less`(私有 buildDefaultLess:2378，自造 `--primary-color` 等) + component.js + 预览图 + meta。**不写** themes/*、config/css-vars.js、common.less → 截图全部报错映射：M2-3(css-vars 缺失)/M2-4(common.less 缺失)/M3-5,M3-6(theme-vars 缺失)/M4-8(index.less 未引用)/M4-1,M4-2(declare 字段空)；M5-6/M5-7 字体：lite 的 index.vue 只过 SFC 语法校验+scoped 选择器修复+重复声明去重，**不过 file-writer 字体归一链** → 硬编码 px 原样落盘。
- **量化**：backend workspace 140 个产物中 max 组 138 个仅 7–11 个缺骨架（5–8%，早期/失败）；lite 组 11/11 全缺（temp-components 统计）。
- **治本 P1.8**：抽共享 `utils/mc-skeleton.js`（复用 buildCssVarsFile/buildDeclareJsFile/buildComponentJsFile/THEME_WRAPPER_TEMPLATES/style-tokens.buildVue3ThemeMixinSnippet）→ lite 落盘调用它、删 buildDefaultLess 自造逻辑；抽 `normalizeFontSizeLiterals` 供 lite 用；骨架完整性门禁（M2/M3/M4 必要文件缺失即报/自愈）+ spec。可一并收敛 max 组 5–8% 存量残缺。

## P1.8 微码平台骨架单一生成器 + lite 接入（backend-node 8d82660，重启 PID 3901；docs 5ab1d3b / M9）
- 治「lite 产物微码规范全红」：新增 `utils/mc-skeleton.js`（buildMcSkeleton / buildIndexLess / buildThemeVarsLess / buildCommonLessSkeleton / THEME_WRAPPER_TEMPLATES 迁入 / checkSkeletonCompleteness）。骨架文件集 = config/css-vars.js + declare.js + component.js + themes/{theme-vars,dark,light}.less + common.less + index.less（复用 file-writer 的 buildCssVarsFile/buildDeclareJsFile/buildComponentJsFile）。
- lite.service 两处写者（主写盘段 + `writeMicrocodeAssets`，后者是此前遗漏的第二处）统一改调 buildMcSkeleton；新增 `prepareMicrocodeVueContent` 单一实现（补 `@import '../resources/styles/index.less'` 满足 M4-8 + 字号归一）；停止使用私有 `buildDefaultLess`。
- 新增 `utils/font-size-normalizer.js`：`font-size: Npx`(N>5) → `var(--fontSize, 14px)` / `calc(var(--fontSize, 14px) * N/14)`（与 max 合格产物实样同款，4 位小数）；注释/JS 字符串不动、幂等、只改 CSS 声明。
- 门禁 **CODE-026**（fail-closed BLOCK，复用 checkSkeletonCompleteness）；验收脚本加「骨架完整性」——当场抓出 `c-today-overview-4996e153` 全套缺失（正是用户截图那个 lite 组件）。
- 验证：新增 spec 17/17；回归 **1225 passed**（5 失败 suite 全存量）；build 通过（修 2 处 TS unknown 类型）。
- 存量基线（20 组件）：骨架缺失 8 项（3 个组件）。
- 🔧 工程注意：`lite.service.ts` 内 `Object.entries(skeleton)` 需 `as [string,string][]` 断言否则 TS2345。

## 生产 401 认证故障修复（12:06，ECS nginx 路由修正）
- **症状**：生产页面持续报 401，无痕模式依旧。
- **根因**：nginx `/api/auth` 路由指向 Node 13030，但 Node 的 AuthController 已注释掉（`auth.module.ts:29` 标注「已迁 Java」）→ 请求到 Node 后 404。
- **修复**：ECS `/home/mvbt/nginx/vhost/gzxj.conf` 的 `location /api/auth` 块 `proxy_pass` 从 `http://192.168.112.1:13030/api/auth` 改为 `http://192.168.112.1:8080/api/auth`；`docker restart nginx`。
- **验证**：`curl http://localhost:80/api/auth/current -H "Token: test"` 返回 `{"success":false,"code":401,"message":"未登录或登录已过期","data":null,"source":"java"}` —— `"source":"java"` 证明路由正确，401 是无效 token 的预期响应。
- **教训**：AuthController 从 Node 迁 Java 时，nginx 路由未同步更新。迁移认证模块需检查三处：① 源端注释/删除 ② 目标端实现完整 ③ nginx 路由指向目标端。

## 重复 tabs 根因审计（mc-max-1789097821000-c6194696，设备监测）
- 现象：生成重复 tabs（c-device-monitor-switch + c-device-monitor-tab 两套）。
- 三层产物取证：Figma 真值只有一套 tab（89:39 tabs 竖导航 + 2:8437 cons 右内容，同属 slot-con 89:40）。
- 双轨根因：① Vision LLM 写语义壳 `section-main`（figmaNode=null、id=section-main 非数字:数字）；
  ② merger 从 bbox 重建几何行 `89:37 @antd/tab`（figmaNodeId=89:37，members=[89:39,2:8437]）。
  两者都进 structure table，planner 1:1 映射 → TabsSection + MainSection 两套。
- 去重为何失效（section-tree.js dedupeDuplicateSections）：① R1-1 靠 sourceNodeIds 交集——
  section-main 的 sourceNodeIds 为空（无 figmaNode/无数字:数字 id/子元素 figmaNode 也 null）→ 与 89:37 无交集；
  ② tabs 措辞兜底——89:37 type=tabs vs section-main type=body，不同组。两条都漏。
- merger 语义壳去重（inline-row-merger.js childCoveredByEvidence）也不触发：section-main 直接子元素是
  「左侧竖向Tab切换栏/右侧内容区」纯语义名，无 resourceFile/figmaNode token，且 childCoveredByEvidence 不递归。
- 附加 bug：section-main 里 tab 项引用的 bg-8788.png/bg-8807.png 实为 switch(89:38) active/default 背景图
  （resource-dom-mapping figmaPath 实锤），Vision 把 switch 背景张冠李戴到 tab 上。
- 治本方向：P0 给「无语义壳」反推 sourceNodeIds（用其深层 children 的 resourceFile 尾号→resource-dom-mapping 反查 figmaNodeId）；
  P0 childCoveredByEvidence 递归；P1 tab 项资源映射纠正。A(bbox 合并)/B(planner 字符串去重) 均不能根治。

## P1.8 回归修复 — lite 样式提取（本轮）
- 问题：P1.8 实施后 lite 生成质量下降（用户截图对比）
- 根因诊断：
  | 问题 | 症状 | 根因 |
  |------|------|------|
  | 骨架覆盖 LLM 产物 | `common.less` 只有空骨架，业务样式丢失 | `buildMcSkeleton` 无条件覆盖所有文件，但 LLM 把样式写在 `index.vue` 的 `<style>` 里 |
  | @import 注入位置错误 | 样式链路断裂，主题变量不生效 | `@import` 被注入到 `<style scoped>` 内，作用域隔离导致主题变量无法传播 |
  | theme-vars 被通用默认覆盖 | 组件特定颜色丢失 | LLM 生成的颜色直接在 Vue 文件里，但骨架用通用默认色覆盖了 theme-vars |
- 修复方案：
  1. 新增 `utils/lite-style-extractor.js`：从 `index.vue` 的 `<style>` 提取业务样式
  2. 业务样式移入 `common.less`：不再用空骨架覆盖，而是用提取的样式填充
  3. index.vue 只保留非 scoped 的 @import：主题变量能正确传播
  4. 两处写盘路径同步修复：主路径 + 续跑路径
- 修复后产物结构：
  ```
  index.vue          → <style lang="less">@import './resources/styles/index.less'; {业务样式}</style>
  common.less        → @import './themes/theme-vars.less'; .common(); {业务样式}
  theme-vars.less    → 骨架默认值（LLM 未生成时）
  ```
- 服务状态：PID 14636，端口 13030 ✓，新文件已进 dist ✓
- 待验证：重新测试 lite 生成，确认业务样式正确保留在 `common.less` 中

## 重复 tabs 治本落地（inline-row-merger 递归去重）
- 审计收敛：A(bbox 合并)/B(planner 字符串去重) 均不能根治；真根因是 childCoveredByEvidence 不递归。
- 证据：section-main 直接子元素（左侧竖向Tab切换栏/右侧内容区）无 token，但深层 tab 项 name（监控/照明/摄像机）
  命中 89:37 子树 characters → 递归后 coverage=2/2=100% 判定「被覆盖」→ 剔除 section-main。
- 落地：inline-row-merger.js childCoveredByEvidence 改为递归（depth≤8，走 children/items/elements），
  childRefTokens 保留（id 尾号/resourceFile 尾号/name/text/label 命中）。
- 验证：spec 新增 4 用例（必红→转绿，20 全绿）；真实产物回放 section-main 被剔除、89:37 保留；
  3 套件 51 测试全绿（inline-row-merger + section-tree + context-conflict-resolver）。
- 构建坑：nest build 报 lite.service.ts:2955 TS2554「Expected 4 got 5」是 tsbuildinfo 增量缓存残留假阳性，
  rm tsconfig.build.tsbuildinfo 后 tsc --noEmit 干净、build 成功。**build 前必 rm tsbuildinfo**。
- 附加发现（未修，P1）：section-main tab 项引用的 bg-8788.png 实为 switch(89:38) 背景，Vision 张冠李戴；
  tabs(89:39) 真实 bg 是纯 VECTOR（2:8826/2:8831）应走 CSS 替代。待后续治理。
- 服务：新 PID 17111 监听 13030，探活 200。

## P1 落地：tab 项资源引用幻觉治本（tab-resource-guard）
- 根因：Vision 在 tab 项写死 switch 的 bg-8788.png/bg-8807.png（figmaPath 属 slot-con/switch/active|default），
  tab 真实背景是 tabs(89:39) 子树里的 GRADIENT_LINEAR 渐变（2:8826 RECTANGLE / 2:8831 VECTOR），应走 CSS。
- 治本三件（按 P1-1→spec→P1-2→P1-3）：
  - P1-1 校验：新建 utils/tab-resource-guard.js 纯函数（零依赖、无 import.meta）。
    判据：tab 项 name 反查 Figma（name 或 TEXT.characters 命中）→ 取父容器子树 id 集合 →
    resourceFile 反查 figmaNodeId 不在该集合内 → 判幻觉。
    ⚠️ 关键坑：不能逐层到根（根子树含全组件会误判全命中），只取「tab 文字节点父容器」一层子树（bg 兄弟在同层）。
  - P1-2 纠正：摘除幻觉 resourceFile（含 icon.resourceFile）+ 从父容器找 GRADIENT_LINEAR 节点写 styles.background=linear-gradient。
  - P1-3 prompt：figma-css-style-mapping.md 第6节 tab 控件补「tab 项资源引用硬约束」——tab resourceFile 只能引 tabs 子树内资源，背景优先 CSS，找不到就留空。
- 接线：visual-parser.js _postProcessAnalysis 在 _calculateAllBackgroundSizing 之后调用 detectAndFixTabResourceHallucination。
- 验证：spec 5 用例全绿（含"真属不误伤""name 反查不到保守跳过"反向用例）；真实产物回放 4 处幻觉全识别（监控/照明两 tab 各错引 switch bg+icon）；
  tab-guard + inline-row-merger 25 测试全绿；build 成功；服务新 PID 33550 探活 200。

## 生产环境认证修复（14:00-14:30）
- 根因：`demo.controller.ts` 缺少 `@UseGuards(SessionGuard)`，导致 `/api/demo/download` 接口 `req.session` 永远为空
- 修复：
  - 生产：直接修改 `dist/demo/demo.controller.js`，添加 SessionGuard import 和 @UseGuards 装饰器
  - 本地：修改 `src/demo/demo.controller.ts`，添加 `@UseGuards(SessionGuard)` + import SessionGuard
- 验证：AT-158 token 下载成功（2.3MB zip），tsc --noEmit 编译通过
- 注意：AT-3340 token 门户返回 500，是门户侧问题，非 Node 问题

## xAxis "0" not found 根治（Loop 2.1.F，15:20-15:40）
- 根因三层：① chart-standards.md prompt 示例用对象格式 `xAxis:{}` → LLM 跟随；② 后处理链只守 series.type（2.1.E）不守 xAxis/yAxis 格式；③ 对象格式 + markLine 值定位点 `{yAxis:3000}` 在部分 ECharts 5.x 报 `xAxis "0" not found`。
- 治本：chart-type-guard.js 新增 normalizeChartAxesInSource()（纯函数 4 步：① xAxis/yAxis 对象→数组（括号配平）② 字符串索引→数字 ③ cartesian series 缺索引注入 xAxisIndex:0/yAxisIndex:0 ④ @fontSize 泄漏→var(--fontSize,14px)）；挂 mc/vue3 两产线 2.1.E 之后（Loop 2.1.F，与 2.1.E 并列）。
- prompt：chart-standards.md 示例改数组+显式索引，新增红线 9（坐标轴必须数组格式+显式索引）红线 10（禁 LESS 变量进 JS formatter）。
- 尾逗号坑：注入到闭合 } 前须 endsWithTrailingComma 检测复用尾逗号，否则 `data:[1,2], , xAxisIndex` 双逗号语法错误（实测踩坑两轮）。
- 验证：spec 44 用例全绿（含尾逗号/幂等/不误伤注释字符串/字符串索引）；真实组件 c-traffic-monitor-5nxelujp 用守卫批量修 6 文件 24 处（TrafficForecast 对象格式、ChartSection/ChartSection2/HourlyChartTunnel 各 @fontSize×2+索引）；build 成功进 dist。
- ⚠️ 遗留发现：VehicleTypeDistribution/Slot 的 series `type:'bar'` 但带 radius/center/labelLine（实为环形图 pie 写错 type），属图表类型语义错误（非 xAxis 范畴），待单独立项。
- 教训：Edit 同一文件多改动勿并行（竞态致部分丢失），须串行逐个 Edit 后 grep 复验。

## 用户配置隔离修复（15:34）

**问题**：用户只配置了 `z-ai/glm-5.3-flash`，但故障转移时切换到了开发者的模型池（`qwen3.7-plus`、`deepseek-v4-flash-vision-exp` 等）

**根因**：
- `getMergedAiConfig(userId)` 合并全局配置 + 用户配置
- 当用户只配了 1 个主模型，`resolveBindingToLegacy` 不会生成 `providers` 字段
- `resolveVisionConfig` / `resolveTextConfig` 发现 `merged.providers` 为空，回退到 `saved.providers`（从 `data/ai-config.json` 读取的全局配置）
- 全局配置包含开发者的 11 个 provider，导致用户请求的故障转移切换到了开发者的模型池

**修复**：
1. `config.service.ts:375-385` — `getMergedAiConfig`：当用户有自己的配置时，确保 `providers` 不为 null/undefined，显式设置 `[]`
2. `ai-defaults.js:376-382` — `resolveVisionConfig`：当 `merged.providers` 是数组时，使用 `merged`，不回退到 `saved.providers`
3. `ai-defaults.js:443-449` — `resolveTextConfig`：同上

**效果**：
- 用户只配了 1 个模型时，故障转移只在用户配置的主模型中切换（通过 `primaryEntry` 注入到供应商池）
- 不会切换到开发者的模型池
- 开发者本人的配置仍然用于开发者自己的请求

**验证**：
- ✅ 后端构建成功
- ✅ dist 中已包含用户配置隔离逻辑
- ✅ 服务重启成功（PID 78031，端口 13030 监听中）

**相关文件**：
- `backend-node/src/config/config.service.ts`
- `backend-node/src/ai-engine/utils/ai-defaults.js`
- `docs/生产问题解决文档.md`（已更新问题 4）

## 15:51 GLM thinking 参数自动降级（治本方案）
- **问题**：用户问"用户如何知道有的模型不支持关闭思考"，提出能否自动关闭，不能关闭则自动最低
- **方案**：先尝试不带 thinking 参数（关闭思考），如果返回 400 且错误信息包含"不支持关闭思考"，自动重试带 `thinking: { type: 'low' }`
- **修改**：`config-test.service.ts` 的 `testTextLLM` 和 `testVisionLLM`
  - 第一次请求不带 thinking 参数
  - 如果 400 + 错误信息匹配 `/不支持关闭思考|thinking.*type.*low|always.*think/i`，自动重试带 `thinking: { type: 'low' }`
  - 用户完全无感知，无需手动设置 thinkingType
- **优势**：比硬编码 `/glm/i.test(model)` 更通用，任何"始终思考"模型都能自动适配
- **验证**：构建成功，服务重启（PID 83363）
