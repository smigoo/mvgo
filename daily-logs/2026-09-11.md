# 2026-09-11 工作日志

## Loop 2 结构表接管 — 审计而非盲改
- 背景：接续 09-10 落地的 Loop 0/0.5/1 + 1.C/0.A，计划文档 `docs/pipeline-loop-234-execution-2026-09-10.md` 列了 Loop 2 四个落点。
- 审计结果（代码实证）：4 个落点中 **3 个已落地**，仅落点④真实缺位：
  - ① 水平行判据：`inline-row-rebuilder.js:26` `rebuildSectionsPreservingInlineRows` 已用 `inferFlexDirection.sideBySide`（y重叠≥50% + x不相交）做连通分量聚类，输出 `layout:'horizontal'` —— 已存在，阈值=计划值。
  - ② nav 强制：`visual-parser.js:1648` 已专写 `@antd/tab` 分支（cons/ 业务内容、tabs 竖导航），`figma-connector.js:769` 已下钻 cons/ 取真实图标/导航，planner 已含 nav 关键词。
  - ③ chrome 不进 headerSlots：`chrome-section-filter.js` 已剔除 title-itself/panel-title/sub-header；`inline-header-slot-inferrer.js:mergeHeaderSlots` 已带 `rejectedKeys`/`headerBBox` 几何拒回（C-1/C-2，0.A 闭环）。
  - ④ 节点只落一次：`resource-mounter.js` 只有 bg 别名去重（`dedupeBgMultiRefs/:404`、`dedupeSameImageAliases/:508`），**无**「同一 figmaNodeId 只落一次」的确定性 DOM 去重。❌ 真实缺口。
- 关键纠正：计划文档把落点①写成"y重叠→horizontal 重建 section"，与现有实现不符；`inlineCompositeRows` 实际已被消费（headerSlots 兜底推断，非布局重建，0908 P1-3 锁定）。**不新建布局重建路径**（重复造轮 + 自伤风险）。
## 落点④ 二次深查（避免臆造补丁）
- 计划落点④写"同一 figmaNodeId 只落一次（slots 或 content 二选一），重叠删 DOM 不只删合约"。但**实证不成立**：
  - `fixtures/0907/{device,env,traffic}/resource-dom-mapping.json` 三个样本：`dupNodeId=0`、`dupAssignedVarName=0`——每个 figmaNodeId 天然唯一映射一个 assignedVarName，映射层无重复。
  - `resource-mounter.js:ensureHeaderSlots`（T09）已含两道去重：P1-3 契约层（:2052 与子组件文本重叠则跳过注入）+ P5 DOM 级（:2086 已有 <template> 与子组件重叠则删整模板）；C-2 越界几何过滤（:2157）再拦 header 误判候选。
  - 三样本 `.vue` 产物里 `icon*/bg*` 多引用是**子组件各自引用**（如 HeaderStats.vue 用 bg4 作局部 var），非同一节点被挂两次——不构成"重复挂载"。
- 结论：落点④是**臆造缺口**，无真实复现。强行写 `dedupeSameFigmaNode` 纯函数 = 无复现补丁（违反治理"确定性、先复现"铁律）。**Loop 2 实际无实体代码可写**，四个落点全部已闭环。
- 推荐收尾：不盲改；把 0908 已覆盖的 ①/②/③/④ 行为补一层 spec 固化（水平行聚类 / tab 强制 / chrome 不进 headerSlots / T09 双保险），以防后续回归。或确认 Loop 2 已实质完成，转 Loop 3。
## Loop 3 类名与尺寸由 Manifest 写出 — 审计（同 Loop 2，脚手架已就位）
- 计划落点：① classNames 唯一生成器（genClassKey 单一 token）② flex 由装配写 ③ containsChart 装配写 min-height + VERT-004 并入装配。
- 实证：
  - `class-name-generator.js:19` genClassKey 已是单一事实源，被 `prompt-loader.js:441` + `microcode-engineer.js:641/1689/1770` 消费；`class-name-generator.spec.ts` 已锁 12 例。
  - `resource-mounter.js:2360 fixSectionHeightsForResource` 已由 ctx.sectionHeights（engineer `_buildSectionHeightsMap` 构建）驱动写 `flex: <系数> 1 0`，根类豁免 `-root\b`（:2390），量纲=比例（与 FLEX-005 兼容）——flex 已非 LLM 自由发挥。
  - `post-process.js:30 injectChartMinHeight`（主图160/紧凑100 分级，#577）已在 `postProcessIndexVue:1815` 接线；`vertical-layout-guard.js:200 VERT-004` 补 min-height:0；`flex-sibling-guard.js` FLEX-005 已是单一事实源（`detectFlexSiblingIssues` + `normalizeFlexSiblingScale`），`flex-sibling-guard.spec.ts` 锁量纲归一。
- 验收门槛可量化：CODE-003-HIT-RATE≥95% / FLEX-005 零 BLOCK / chart clientHeight>0。但三样本 `fixtures/0907/*.vue` 是**陈旧手工快照**（c-device-monitor-malvtjd5- 长类名、早于 genClassKey+consolidateSubComponentClasses），**不能**作为 Loop 3 度量证据。
- 结论：Loop 3 机制层已闭环。要做"真验收"必须走**真实图端到端重生成**（env/traffic/device 三样本），得到 CODE-003-HIT-RATE 实值——这是唯一能判定"是否还需实体代码"的动作。
## Loop 3 真验收的环境阻塞（结论：本轮无法安全完成）
- 凭证：data/ai-config.json 实测有有效 visionApiKey/textApiKey（health 接口 credentialsOk:false 是接口分辨率问题，文件凭证齐）。
- e2e runner (__e2e-run.mjs) 只接受 --image（screenshot），三样本 fixtures/0907 无截图（仅 figma 树+mapping+vision.json）→ 无法直接跑。
- HTTP POST /api/v2/pipeline/generate 支持 figmaUrl+figmaToken（fixtures 有真实 fileKey/nodeId+token 配置），但带 @UseGuards(SessionGuard) 需有效会话，且 figmaUrl 真实形态未知 → 盲调风险高（撞守卫/打线上/计费）。
- 视觉评分还需前端 :2610（实测 2610 通）。
- 结论：Loop 3「真验收」需真实端到端重生成，本轮无法安全完成。机制层（genClassKey/fixSectionHeightsForResource/injectChartMinHeight/VERT-004/FLEX-005）均已就位且有单测锁定。
## Loop 4 双裁判门禁 — 已交付（本轮唯一实体代码）
- 用户拍板：判定 Loop 2/3 已闭环（机制层全就位），直接进 Loop 4。
- Loop 4 两个文件原「完全不存在」→ 已新建：
  - `utils/figma-golden-extractor.js`：从输入侧（Figma 树 + 资源下载结果）独立生成与 Working Manifest 同构 Golden JSON（版本 wm-1 + golden 元信息）。树优先推导 figmaPath/bbox，树缺失回退 mapping 字段并记 provenance。绝不 import engineer/mounter/parser。
  - `utils/manifest-auditor.js`：diff(golden,working)+verifyProduct，structural.shifted(bbox>8px或>5%)/resource.misbound/contract.missingPass 任一非空→BLOCK（fail-closed）。同独立约束。
  - `docs/golden-manifests/{env,traffic,device}.json` 三样本锁定 hash：device=be8654d8dfd8aac8 / env=0d573094606d4908 / traffic=fb1277ed4cb927a5。
    - 实证：device/env 树派生 100% 命中（12/4 节点 path+bbox 与 mapping 完全一致）；traffic 9 节点 figmaNodeId 全不在 Figma 树中（mapping 与树跨 revision 不一致），已由 provenance.nodeMissing 记档、不误 BLOCK。
  - `utils/manifest-auditor.spec.ts`：9 项 TDD，锁「Golden 与自身 diff BLOCK=0」「注入错绑(改挂section/臆造变量/bbox偏移)必 BLOCK」「契约未启用不误 BLOCK」。
- 验证：acorn OK；61 项核心回归全绿（0907/0908/l5/l6/l7 + resource-attribution + header-slot-contract + manifest-auditor）；nest build 成功；env -i 重启 13030 → PID 46896 监听、health 200、无 ESM 加载错误。
- 提交：backend-node@4cb797e / docs@66730a0 / 根仓 88d2de72 + 7e7394bc（子模块指针）。含可复现锁样工具 `utils/__gen-golden.mjs`。
- 范围边界：Loop 4 交付=「独立模块+锁样+spec」；**未**接入运行图（计划 §落点4「门禁只留三类」+ §落点5「拆补丁堆」属下一离散步骤，按治理纪律单独走）。
- 待办：若要把 auditor 真正变成 gate，需在 phase2/vue3 graph 的落盘/校验节点接 audit(golden, working, files) 并据 blocked 拒绝；补丁堆（flex fixer/双通道 headerSlots/forceAll 残留/style-class-consolidator 取舍）需后续单独治理。

## Loop 4 门禁接入落盘（本轮，按用户「现在做但不重启」）
- 已接 microcode-engineer.js 终态落盘前（finalTruthFiles 写盘前，约 :6306→:6302 上移）。双档设计：
  - 无 Golden（活流量）→ `auditSelfConsistency(working, files)`：仅验契约声明即写入（verifyProduct，CODE-018 确定性探测），确定性不误杀。
  - 有 Golden（`this.goldenManifest`，外部重建/CI 注入）→ 完整 `audit(golden, working, files)`。
  - 关键纪律：禁用 `audit(null,...) `（会把每个资源判臆造全量 BLOCK）。默认 WARN 不阻断；`MANIFEST_AUDIT_ENFORCE=1` 升级 BLOCK（fail-closed 拒绝落盘）。门禁自身异常仅告警不拖垮流程。
  - auditor 新增 `auditSelfConsistency` 导出；extractor 补 spec（同构产出+hash 稳定+纯树 100% 命中）。
- 验证：acorn OK；66 项核心回归全绿（含新建 14 项 TDD）；nest build 成功；dist 新符号落地；静态模拟漏 import→BLOCK、契约完整→0 项、audit(null) 路径已禁用。**已按用户要求不重启**（运行 PID 46896 未动）。
- 提交：backend-node@53c2b0f / 根仓 361185a6（子模块指针）。
- 范围边界：vue3 graph 走同一 engineer.execute 落盘路径（vue3-engineer.js:1634 调父类 writeFiles），故本轮一次接两图；Golden 注入链路（重建任务/CI 预生成 this.goldenManifest）尚未接，属后续离散步骤。补丁堆拆除（forceAll 残留/双通道 headerSlots/flex fixer）仍待单独 Loop。

## 真实生成失败 CODE-019 死锁 — 治本（本轮，不重启）
- 失败任务：mc-max-1789002096933-a65c69f7 / c-env-monitor-wit1topz-a65c69f7 @ 09:04，CODE-019 3×BLOCK 不收敛禁止发布（子组件 TabSwitcher.vue 接收并使用 bg1，父 index.vue 漏传 :bg1）。
- 根因死锁链（铁证 workspace/custom-components/c-env-monitor-wit1topz-a65c69f7/package/index.vue）：
  父只 import bg2/icon1/icon2（恰是模板里写出的）→ 漏 :bg1 → 父不 import bg1 → autoWire 因「父未声明 且 fileSectionMap 契约漏命中 bg1 所属 section」而 inContract=false → 放弃接线 → CODE-019。
  本质：Loop 1 收口 forceAll 时落的「主组件走契约/默认已用变量注入」把「父模板漏写的资源」排除出主组件 script，父无该变量则 autoWire 补不上。
- 治本（props-wiring-guard.js autoWireSubComponentProps）：子组件实际使用的资源 prop 只要是该任务 success 资源变量（effectiveMapping 全集）即强制保证父级持有（与「资源只归主组件持有、子组件 defineProps+父透传」契约一致，不臆造数据源）。ensureParentImport 改用 contractMapping 精确注入该 var（默认分支只注入代码已用变量，bg1 恰是漏写，须先指定 allowedNames）。
- 验证：acorn OK；98 项核心回归全绿（新增 1 死锁回归）；nest build 成功；**端到端用真实失败产物复验**：修复后自动补 import bg1 + :bg1="bg1"，detectMissingPropsWiring 残留 0。未重启（用户明确测试中）。
- 提交：backend-node@4f50fe0 / 根仓 df5ac566（子模块指针）。
- ⚠️ 注：本次修复需 env -i 重启 13030 才对实时生成生效；当前 PGID 46896 未重启（按用户要求）。重生成同一组件前应重启。
