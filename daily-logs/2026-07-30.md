# 2026-07-30 工作日志

## 项目重命名：jy-mc → 感智晓界 / mvgo
- 全量扫描并替换所有 jy-mc、jymc、江阴、靖江、长江隧道 相关引用
- 中文显示文本统一替换为「感智晓界」
- 英文代码标识符统一替换为「mvgo」
- 涉及文件类别：
  - 根目录 README.md、docs/ 下 7 个 md 文件
  - 前端 3 个源码文件（ConfigPanel.vue、ProjectsView.vue、Intro.vue）
  - 后端 Node package.json、src 下 2 个 JS 文件、AI prompt schema
  - 后端 Java 全量：pom.xml（6个）、Dockerfile、application.yml、Java 类、README
  - Docker：docker-compose.yml
  - 运行时产物：custom-components 40+ 文件、data/ 下 tasks.json 和 api-catalogs
- Java 后端结构性改动：
  - 模块目录 jymc-* → mvgo-*（已 mv 重命名）
  - 包目录 com/jymc → com/mvgo（已 mv 重命名）
  - 启动类 JymcApplication.java → MvgoApplication.java（新文件，旧文件已删）
  - 数据库名 jymc → mvgo
  - Spring 配置 key jymc.node → mvgo.node

## 启动前后端服务
- Node 后端 (backend-node) 已启动，端口 13030，curl 验证返回 Hello World
  - 修复 TS 编译错误：phase2.service.ts 第 724 行多余的 `}`，导致 try/catch 语法错误
  - NestJS build 不复制 src 下的 .js 资源文件到 dist，手动批量复制（phase2/doc/*.js + ai-engine/*.js + *.md 等 80+ 文件）
- 前端 npm install --legacy-peer-deps --ignore-scripts 正在执行中（跳过 @antv/x6-geometry 的 preinstall 脚本以避开 bun 依赖）

## 同步 langgraph-server → mvgo 源码差异 (20:37-20:41)
- 全面对比 src 目录确认同步状态：前端 9/9 已同步，4 处架构差异为 polyrepo 有意为之
- **同步 2 个缺失的后端修复**：
  - `screenshot-renderer.js`：返回值增强 `{ renderedImage, isStaticFallback }` 替代 `string|null`
  - `mc-component-graph-phase2.js`：添加静态抽取降级时跳过视觉比对的逻辑（11 行）
  - dist/ 编译产物也已同步

## 完善各目录 .gitignore 文件
- **frontend/.gitignore**：修正 `*dist` → `dist/`（前者会误匹配含 dist 的任意文件）；`package-lock.json` 改为注释保留（--legacy-peer-deps 场景下 lock 文件有助于稳定安装）；补 `.env` + `.env.*` + `!.env.example`（原版只忽略 `.env.gen`，`.env` 本身含敏感配置未忽略）；AI 工具目录已有，重新归类整理
- **backend-node/.gitignore**：补 `workspace/`（AI 生成的运行时组件目录）、`.trae/`、`.lingma/`、`.claude/`、`.opencode/`、`.workbuddy/`、`.codex/`、`.gemini/`、`.qwen/`、`.codebuddy/`；重新归类分区
- **backend-java/.gitignore**：新建，包含 `target/`、`*.class`、`*.jar`、`*.war`、`.mvn/wrapper/maven-wrapper.jar`、`.env` 系列、`application-local.yml/properties`、OS/编辑器/AI 工具目录、日志
- **根目录 .gitignore**：新建，包含 `.DS_Store`、编辑器/AI 工具目录、`.env` 系列、`docker-compose.override.yml`、日志
