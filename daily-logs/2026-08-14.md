# 2026-08-14

## mc-max-1786694967787-c47fa026 预览空白最终修复（第 N 轮）
- **根因**：`index.vue` + `common.less` 中 3 处「同行黏连」`background-size: 100% 100%;flex-shrink: 0;` 等，之前的 `healLessSource` 自愈**未真正写入文件**（函数可能有 bug 或被覆盖）。
- **修复**：手动 Edit 5 份文件（frontend workspace index.vue+common.less、backend workspace index.vue+common.less、temp-components index.vue），每份 3 处 `100% 100%;[prop]` → 拆为两行。
- **验证**：less.render 输出 18166 字节 CSS 成功；grep 全部 6 份文件零残留；Vite `__invalidate` 缓存已清。
- **教训**：`healLessSource` 的 `fixGluedDeclarationsOnLine` 对此组件未生效，需排查 heal 函数为何静默失败（待后续）。

## 任务中心页（/tasks）对齐 prototype-task-list.html 设计
- 文件：`frontend/src/views/tasks/TaskCenter.vue`
- 依据 `artifacts/prototype-task-list.html`（暗色任务中心原型）做视觉对齐，保留卡片式列表与全部列表操作（不重写为表格）。
- 改动点：
  1. 页头新增主 CTA「新建生成任务」(primary) + 原有「刷新」(secondary)。
  2. 统计卡片加语义色图标（`StatCard` 的 `icon` + `color` + `layout="horizontal"`）：总/已完成/运行中/失败/今日 → UnorderedList/CheckCircle/Sync/CloseCircle/ClockCircle。
  3. 状态筛选由 `<select>` 改为分段控件（全部/进行中/已完成/失败/排队中），`statusOptions` + `setStatusFilter`，与统计卡片状态快速筛选共用 `filterStatus`。
  4. 新增排序下拉（最新优先/最早优先/名称排序），`sortBy` 接入 `filteredTasks`。
  5. 样式补 `.segmented` / `.sort-select`。
- 编译验证：`npm run build:safe` 通过（23s，无错）。
- 注意：原型有独立 app-header/footer，但 SPA 已有全局导航，未重复添加；卡片密度高（含审核/极速通过标记等），表格布局不适用，故保留卡片。

## TaskDetail 统计空 + 预览空白修复
- 文件：`frontend/src/views/tasks/TaskDetail.vue`
- **问题 1 - 预览空白**：iframe src 指向 `/api/preview/${gid}/${cid}`（PreviewController 静态文件服务），默认找 `index.html`。但微码组件只有 `package/index.vue`，没有 `index.html` → 控制器找到目录但找不到文件 → 404 空白。
  - **修复**：`previewUrl` 改为 `/preview/${cid}`，走前端路由 `views/preview/index.vue`（vue3-sfc-loader 运行时编译），同时支持 Vue3 和微码组件。
- **问题 2 - 统计数据残缺**：
  - `代码文件=0`：`getComponentFiles()` API 需认证，失败时静默 catch，`fileCount` 保持 undefined → `|| 0` 回退成误导的 0。改为 `?? '-'` 显示 `-`（接口成功时正常填充真实数量）。
  - `已用时间=-`：**根因=字段名错位**。后端任务实体用 `startTime`/`endTime`/`duration`（数字时间戳，见 `backend-node/src/tasks/tasks.service.ts:100-101, 1030-1039`），前端 `taskElapsedTime` 却读 `createdAt`/`completedAt`（不存在）→ 永远 `-`。已修：改为读 `startTime`/`endTime`；模板"创建"时间 `task.createdAt` 也改 `task.startTime`（`formatTime` 接受数字时间戳）。
- 组件 `mc-max-1786679099551-2fd9d3e8` 存在于 `workspace/custom-components/` 和 `frontend/workspace/custom-components/`，但不存在于 `backend-node/workspace/custom-components/`。PreviewController 的多候选路径能找到它（Root1/Root2)，但缺 index.html 导致无法服务。

## ⚠️ 预览/统计不行的真正根因：2610 + 13030 都挂了（进程保活方式错误）
- 用户报"依旧不能预览"时，2610 DEAD；本轮进一步发现 **13030 也 DEAD**（8080 Java 还活）。13030 死导致统计数据层全瘫。
- **根因（关键运维坑）**：**macOS 无 `setsid` 二进制**。`restart-all.sh` 的 `start_detached` 原先写 `setsid bash -c "..."` → 命令找不到，前端/Java 两条启动其实从未成功；`run_in_background` 起的进程约 14 分钟后被回收（后台任务 failed）。所以服务反复死。
  - **正确的脱离会话方式**（macOS 可用）：
    - Node 后端：`.start-server.sh` 用 `perl -e 'use POSIX qw(setsid); setsid(); fork() and exit; exec @ARGV' "$NODE_BIN" dist/main.js`（perl setsid，已验证可跨调用存活）。
    - 前端：`start-frontend.js`（Node `spawn({detached:true, unref()})`，Unix 下内部 setsid 创建新会话，最稳）。已创建 `/Users/smigoo/工作/mvgo/start-frontend.js`。
  - **已修 `restart-all.sh`**：`start_detached` 改为 perl setsid + 双重 fork（与 .start-server.sh 同模式），前端/Java 重启现在能真正保活。
- 本轮用 node-detached launcher 重启 2610、用 .start-server.sh 重启 13030，**三端口现已全活**，预览链路验证全绿（`/__raw` 200、`/preview` 200、HMR 无编译错误）。
- dev 下 MC 组件预览链路：`preview/index.vue` 的 `loadDev()` → `loadVue3FromWorkspace(isProd:false, workspacePath:'custom-components')` → 探测并读取 `/__raw/workspace/custom-components/${id}/package/index.vue`（vue3-sfc-loader 运行时编译）。**不走 /api/preview 静态文件服务**。
- 用户需**硬刷新浏览器**(Cmd+Shift+R)——2610 重启后旧 HMR 连接断开。

## ⚠️ 微码组件源码「整段 <script setup> 重复注入」生成 bug（#105）
- 现象：组件 `mc-max-1786679099551-2fd9d3e8` 预览报 `SyntaxError: [vue/compiler-sfc] Identifier 'runtimeBuilder' has already been declared`。
- 根因：`package/index.vue` 的整个 `<script setup>` 被生成了两遍（第一份 70–281 行含全部 import/状态/图表函数但**缺 onMounted/onUnmounted**；第二份 283–493 行是近完整副本且多了生命周期钩子），同处顶层作用域 → `runtimeBuilder`/`statsData`/`trendTabs` 等几十个重复声明，编译器首错即报 runtimeBuilder。
- 两份独立副本（不同 inode）：`workspace/custom-components/` 与 `frontend/workspace/custom-components/` 都中招，各 497 行。
- 修复（手写脚本去重两份）：删第二份副本（283–493），把第二份独有的 `onMounted`/`onUnmounted` 补回第一份（复用已定义的 `initTrendChart`/`handleResize`/`runtimeBuilder`，**不重复声明 handleResize**），并在顶部 `import {ref, watch}` 加 `onMounted, onUnmounted`。去重后 307 物理行，compiler-sfc 实编译通过。
- 性质：**生成管线 bug**（非个别文件问题），新生成组件可能复现。疑似 LLM 把整组件重复输出 / 重试拼接导致。现有 `sfc-semantics.js` 的 `dedupeScriptImports` 只去重 import 语句、不防整块重复——建议后续在写盘前增加「script setup 顶层声明去重 / 整体重复块检测」防线。待用户确认是否深入修管线。
- 验证：`vue/compiler-sfc` parse+compileScript 无 errors；dev `/__raw` 提供的文件 `runtimeBuilder` 重复数=1。

## ⚠️ 管线根因 + 写盘前防线（#105 续，已实施）
- **根因定位**：`_generateIndexVue` 的 `scriptSplit` 模式（complex 组件）把 index.vue 脚本拆 `状态段`+`行为段` 两段（`microcode-engineer.js:2893-2918`），`_mergeScriptParts`(2942) 直接 `dedupeScriptImports(a+b)` 拼接。但模型两段都吐了**完整脚本** → 拼接后整段 `<script>` 重复；`dedupeScriptImports` 只去重 import，漏掉 `const`/`let`/`function` 顶层声明 → 编译报重复声明。
- **修复（sfc-semantics.js）**：
  - 新增 `splitTopLevelStatements`（**行首声明关键字锚定**切分，不用逐字符括号扫描——后者对 ASI 无分号语句会把 `import`/`const` 跨行粘合导致漏检/漏去重）、`dedupeScriptDeclarations`（同名顶层声明保留最后一次、import 走 `dedupeScriptImports` 合并、非声明语句去重完全相同项；无重复原样返回不重排）、`findDuplicateScriptDeclarations`、`detectWholeScriptDuplication`。
  - `validateVueScriptSemantics` 增第 4 类（重复顶层声明）与第 5 类（整段重复）问题 → 写盘门禁(2280-2301)命中即 `throw` 拒绝坏文件。
  - `microcode-engineer.js` `_assembleIndexVue`（所有路径共用唯一拼装点）拼装时对脚本体调 `dedupeScriptDeclarations` 自动去重 → 常见重复场景自动修复、组件仍产出；门禁作兜底。
- **验证**：构造"脚本内容重复拼接"还原破损场景回归测试——检出 18 个重复顶层声明、门禁拦截；`dedupeScriptDeclarations` 修复后 0 残留、`vue/compiler-sfc` 实编译通过；正常无重复组件原样返回。已 `npm run build` 编译进 dist，并重启 13030（perl-setsid）激活新防线，三端口全活。
- 注意：`splitTopLevelStatements` 的"行首锚定"依赖本仓库组件风格（每个顶层声明独占行首）；若未来出现行首续行/无行首关键字的声明需重新评估。

## Ardot 画布 Page 1「组件生成」全面 UI 重设计
- 文件：`https://ardot.tencent.com/file/714364652932606`，Page 1（节点 `2:29`）
- 基于用户提供的当前页面截图做视觉重设计（9 批 batch_edit，~50 节点改动）
- 核心变化：
  1. **渐变顶带**：heroCard 顶部插入 4px 高薄荷绿→浅绿线性渐变条（`10:1`），品牌识别瞬间建立
  2. **标题增强**：「开始生成组件」→「新建生成任务」，28px→30px Bold，副标题精简为一句
  3. **Tab 下划线风格**：原背景填充 pill 改为无底+底部 12px 留白下划线式，选中态绿线+粗体，未选中灰线+常规体
  4. **输入区放大**：56px→64px 高，圆角 14→16，placeholder 更友好（含 Ctrl+V 提示）
  5. **分段控件压缩**：从满宽 bar 改为紧凑 pill 工具条（36px 高，描边未选中态），代码类型(Vue3/React/微码) + 规格(Lite/Max) 两组并列
  6. **CTA 按钮升级**：142px 固宽→fill_container 全宽，52→56px 高，圆角 14→16，加绿色辉光 DROP_SHADOW(a:0.3)，文字改「开始生成组件」+ 右箭头 SVG 图标
  7. **上传区绿虚线**：淡绿底(#f6faf7)+ 绿色虚线边框(35% 不透明度, 8-6 dash)，图标绿、按钮实心绿底白字
  8. **提示条微妙化**：圆角 12→10，padding 收紧，文字微调
  9. **右栏打磨**：统计数字 28→32px Bold，成功率值标绿 18px Bold，任务项高度 62→56px 更紧凑
- 视觉验证截图：`.workbuddy/screenshots/redesign-2_53.png`(主卡片)、`redesign-2_44.png`(全页双栏)

## Bug：任务详情「生成规格」徽章永远显示 Lite（max 被误标为 lite）
- **现象**：用户在 `/generator/components` 选 Max 生成，点进任务详情「生成规格」却是 Lite；任务列表(TaskCenter)却显示正确 Max。
- **根因**：`frontend/src/views/tasks/TaskDetail.vue` 的 `getTaskTier`(原 1201 行)与 `isFigmaLiteCompleted`(1176)、截图重试判断(1438)、重试 payload(1458) 全部读 `t.tier`——但任务记录字段叫 **`generationTier`**（后端 controller/phase2 `createTask` 写的是 `generationTier`，任务实体只有 `generationTier` 无 `tier`）。`t.tier` 恒为 undefined → `|| 'lite'` 兜底成 Lite。TaskCenter(531 行)读的是正确字段 `task.generationTier`。
- **修复**：TaskDetail 4 处 `t.tier` → `t.generationTier`，`getTaskTier` 加 sessionId 前缀兜底（与 TaskCenter 一致）。前端 HMR 已热更，无需重启。
- **佐证**：磁盘最新组件目录 `temp-components/.../mc-max-1786689082806-5db49040` 为 `mc-max` 前缀 = 后端确按 Max(Phase2 完整管线)跑，非 lite。即「实际是 Max，页面标错成 Lite」。
- **附带约束（设计如此，非 bug）**：`lite.controller.ts:309-311` 仅 figma 来源尊重 Max；截图/HTML 来源后端强制 `generationTier='lite'`，且前端 `tierDisabledValues` 对 non-figma 禁用 Max。若用户非 figma 来源，选 Max 会被静默降级——这是预期行为。

## 14:55 完成 v4 原型（按截图重做 · 任务创建表单页）
- 备份 v3: `prototype-generator-workbench-v3.html.bak-v4`
- 新文件: `artifacts/prototype-generator-workbench-v4.html`（827 行，单文件零依赖）
- 设计：头尾复用 v3（topbar + DockBar localStorage 持久化）；中间改为截图式「新建生成任务」表单
  - 来源 Tab（上传截图 / Figma 链接 / 需求文档）flex 等分 100%
  - Figma 字段为链接输入框；截图/文档为拖拽上传区
  - 技术栈 chips（Vue3/React/微码）+ Tier 分段（Lite/Max）+ 绿色 CTA「生成组件」
  - 非 figma 来源 Max 禁用 + 自动回落 Lite（tooltip 提示）
  - 提示条 + 其他方式卡片（从历史复用 featured / 高级工作台）
  - 右栏：今日概览 4 宫格 + 本周成功率 + 最近生成列表
- 验证：div 82/82、body/html/script 各 1/1、无未闭合 svg ✅

## Bug：sfc-semantics「仅 import 无逻辑」门禁误杀纯展示型子组件（生成失败 1786689969828 / 1786689082806）
- **现象**：`mc-max-1786689969828-6a44d15e`(DeviceOverview.vue) 与 `mc-max-1786689082806-5db49040`(StatCards.vue) 生成失败，报 `组件文件语义不完整，拒绝产出坏文件：package/components/Xxx.vue: <script> 仅有 import 无任何逻辑体（疑似截断）`。
- **根因（我之前加的门禁 false positive）**：`validateVueScriptSemantics` 的检查 #2 `isHeaderOnlyScript` 只要 `<script>` 去掉 import/注释后为空就判"截断"。但**纯展示型子组件**本就合法：完整 `<template>`（静态骨架 + 系统注入资源变量 bg1/icon2…）+ `<script setup>` 仅一行注释「资源变量由系统自动注入」。**模型产出完整无残缺**，是我的门禁误杀，且阻断了此前一直能正常渲染的纯展示子组件。
- **修复**（`backend-node/src/ai-engine/utils/sfc-semantics.js`）：新增 `hasSubstantialTemplate()`（去注释/空白后含 ≥1 真实标签且长度>15 视为有实质模板）。检查 #2 改为 `isHeaderOnlyScript && !(子组件 && 有实质模板)` —— 仅当**模板也缺失**时才判截断；`index.vue`(`requireScriptTag=true`，需 $mcComponentBuilder) 仍严格。
- **验证**：① 用真实 chunk + 模拟 `injectResourceImports` 注入资源 import 后跑 `validateVueScriptSemantics` → 通过；② 截断子组件(空 template+注释 script)、header-only index.vue 仍正确失败；③ 重复 import 回归仍拦截。已 `npm run build` 重编译 dist，重启 13030(PID 3777) 激活。三端口 2610/13030/8080 均活。
- **结论**：重新生成此类组件不会再被误杀；如要恢复本次失败的组件，重新触发一次生成即可（workspace 当时未落盘）。

## components.vue 整体重做成 v4 表单（用户选择「整体重做成 v4 表单」）

- 用 Python 字节级保留 `<script setup>`(原 1137-2582 行) 不动，仅重写 `<template>`(现为 1-357) 与 `<style scoped>`(1805-2014)。文件 4561→2014 行。
- 新布局对齐 v4 原型：topbar(品牌+主题切换+我的组件库) / 主栏(标题+副标题+来源Tab flex等分+单一画布[截图拖拽/Figma输入+预览/需求文档] + 技术栈CodeTypeToggle+规格TierToggle+绿色CTA + 提示条 + 其他方式卡片) / 右栏(今日概览4宫格+最近生成) / 底部 DockBar。
- 严格复用既有 ref/method/子组件绑定（sourceMode/selectSource/figmaUrl/confirmFigmaPreview/images/compType/genTier/go/cancel/taskSummary/recent/onTaskSelect 等），生成管线未破坏。
- 注意：按用户所选方案，移除了原 output-column 结果展示区（IdlePanel/ResultPanel/HTML拆分），生成进度/结果改在 /tasks 查看；v4 无 HTML 来源 Tab。
- CodeTypeToggle.vue：options 标签 V3/MC → Vue3/微码（仍仅 vue3/microcode 互斥，无 React）。
- DockBar.vue：移除 Apifox 预设与默认种子（DEFAULT_ENTRIES=[]，旧 localStorage apifox 经 loadEntries 过滤自动失效）。
- 验证：`npx vite build --mode production` 通过（✓ 22.23s）；dev 2610 运行中，HMR 生效。

## requests 2/3：重试 UI 卡死 + 剩余时间硬编码（TaskDetail.vue）
- **请求 2 - 点截图后「正在重新生成」永久转圈 + 不跳新任务**：`handleRetry()`(1438) Figma/Max 分支成功后从未调 `loadingMsg.destroy()`。修复：在 `response` 赋值后、`if (response.success)` 前补 `loadingMsg.destroy()`。跳转逻辑本就正确（后端所有路径返回 sessionId），修复后生效。前端 HMR 热更。
- **请求 3 - 「预计剩余 ~2min」一直不变**：`statsData`(1145) 的 `estimatedRemaining` 硬编码 `'~2min'`(1151)。改为动态估算 computed `estimatedRemainingTime`：基于 `taskElapsedTime`(已用秒) + `taskProgressPercent` + 阶段数线性外推 `remaining = elapsed × (100-progress)/progress`，含经验兜底（<30s→180、有阶段未完成→阶段数×45、已跑但进度低→已用×3）、上限 >10min、下限 <30s。前端 HMR 热更。

## ⚠️ 请求 4：RUNTIME-004 load-error 根因 + LESS 自愈门禁落地
- **现象**：`mc-1786691437322-81ba1bf1` 微码 Figma Max 生成失败，报 `运行时质量门禁阻断: [RUNTIME-004] 真实预览状态为 load-error`。
- **根因（模型真实代码缺陷，非门禁误报）**：`package/index.vue:312` 写 `background-size: 100% 100%` **漏结尾分号**，下一行 `transition: opacity 0.2s;` → `precompileVueLess`(vue3-sfc-loader 运行时编译)报 `313:13 Unrecognised input` → 预览 load-error → RUNTIME-004。同一 bug 还出现在 `resources/styles/common.less:171`（被 `index.less` 的 `@import` 链带入，门禁 critical-less 一并报出）。图片 import(bg1…) 30+ 条均由 `injectResourceImports` 注入且文件全在磁盘，非缺文件。
- **系统性缺口（为何漏到运行时才报）**：① phase2 微码图此前从未接 `LessCompileGate`（仅 vue3 图 2098 行有）；② 既有 `healUnclosedLessComments` 只修未闭合块注释，不修漏分号；③ `LessCompileGate` 的 heal 只写磁盘、不回写内存 `allFiles`，写盘用内存 `allFiles` 会把修复覆盖。
- **三处修复**（均 `npm run build` 编译进 dist 并重启 13030 PID 11431 激活，三端口全活）：
  1. `less-compile-gate.js`：新增 `healMissingSemicolons`（`<style>` 块内 `prop: value`**声明行值允许含空格**如 `100% 100%`、下一行是新声明/块结束/`&`/`@` 时补 `;`，跳过选择器/@规则/注释）+ 组合 `healLessSource`(先 `healUnclosedLessComments` 再 `healMissingSemicolons`)；并将两处 `healUnclosedLessComments` 调用(246/314)改为 `healLessSource`。**注意：开头漏加 `export` 导致 `microcode-engineer` 导入的是 undefined、自愈循环会 `TypeError`——已补 `export function`。**
  2. `mc-component-graph-phase2.js`：导入 `LessCompileGate` 并在 L0-B 的 `CodeStructureValidator.validate` 之后调 `LessCompileGate.validate(filesByPath,{outputPath})`，不通过则把 diagnostics 标 BLOCK 并入 `result`（与 vue3 图对齐）。`code-structure-validator` 节点本就"engineer 之后立即校验"，`filesForCheck` 从磁盘读 `state.generatedFiles`，键为相对组件根的 `package/index.vue` 等，路径约定正确。
  3. `microcode-engineer.js`：导入 `healLessSource`，在 `validateVueScriptSemantics` 的 throw 块之后加**内存自愈循环**：遍历 `allFiles`，对 `.vue`/`.less`/`.css` 调 `healLessSource`，`changed` 才回写——先于 `writeFiles` 修正内存，确保修复真正落盘（自愈循环原只覆盖 `.vue`，已扩到 `.less`/`.css` 覆盖 common.less）。
- **验证**：用真实失败组件跑 `healLessSource` + `LessCompileGate.validate(dist)` —— 312 行分号被补、`pass:true`；BEFORE 坏版本门禁自动 heal 后亦 `pass:true`（自愈式门禁）。`sed` 确认磁盘 312/171 行已带分号，失败组件现可直接重新预览。regression：纯展示组件/无 bug 组件 `changed:false` 不误修。
- **结论**：同类「漏分号/未闭合块注释」LESS 缺陷，新管线在 L0-B 静态阶段即自动自愈并干净编译，不会漏到 RUNTIME-004；若遇不可自愈的真实语法错误仍会 BLOCK 并提示。

## ⚠️ RUNTIME-004 再次：同行多声明黏连漏分号（mc-max-1786694967787-c47fa026）
- **现象**：`mc-max-1786694967787-c47fa026` 微码 Figma Max 生成失败，RUNTIME-004 load-error。L0-B 报 `LESS-COMPILE-001 .theme-dark is undefined`(dark.less) / `.theme-light is undefined`(light.less) 两 BLOCK + `CODE-006`(Tailwind) + `CODE-003`(common.less 37 class 缺前缀 WARN)。
- **真实根因（与上次同类但不同形态）**：`common.less` + `index.vue` 内 `background-size: 100% 100%` 与下一声明**写在同一行且无分号**——`background-size: 100% 100%flex-shrink: 0;`、`...min-height: 65px;`、`...box-sizing: border-box;`（common.less:76/135/223，index.vue:274/338/426）。LESS 把整行当一个声明 → `Unrecognised input` → 预览 load-error。
  - **门禁 `.theme-dark is undefined` 是误报**：`dark.less`/`light.less` 是 `&.dark{ .theme-dark(); @import '../common.less' }`，含嵌套选择器且 `.theme-dark()` 由 `theme-vars.less` 定义，本就不能脱离 `index.less` 的 @import 链被孤立编译；门禁 `critical-less` 循环把它们当独立文件编译才误报，且会触发 LLM 精修去"修"没坏的文件（疑似精修把分号搞丢——`_mc-gen/cache/code-chunks/` 里模型原始 chunk 都有正确分号，终态文件却黏连）。
- **两处修复**（`npm run build` 编译进 dist，重启 13030 PID 18771，三端口全活）：
  1. `less-compile-gate.js` `healMissingSemicolons` 新增 `fixGluedDeclarationsOnLine`：处理**同行内** `value` 紧接 `prop:` 无分号的黏连（如 `100% 100%flex-shrink` → `100% 100%;flex-shrink`）。仅对第 2+ 个 `prop:` 前字符非合法分隔符（空白/`;{}()/"/'/@,&>*`）时补 `;`，避开 `url(data:...)`/`http://`/`content:"a: b"` 等含冒号合法值。循环内对每行 inStyle 内容调用，changed 才回写。
  2. `less-compile-gate.js` `criticalLessFiles` 移除 `dark.less`/`light.less`（保留 `common.less`/`theme-vars.less`）。它们已由 SFC 的 `index.less` 链路覆盖校验，孤立编译必误报 `.theme-dark is undefined` + `&` 无父选择器。
- **验证**：模拟真实管线（microcode-engineer heal 内存→writeFiles 落盘 common.less+index.vue）后跑——真实预览 SFC 编译全部通过（RUNTIME-004 根因已除）、`LessCompileGate.validate` `pass:true` 无 BLOCK（`.theme-dark` 误报消失）。失败组件磁盘已 heal，可直接重新预览。
- **残留（非 RUNTIME-004 范畴，未动）**：`CODE-006` Tailwind 工具类（宿主原子类，微码沙箱不可用→布局错但非 load-error）、`CODE-003` common.less 37 class 缺 `c-monitor-` 前缀（WARN）。如要一并修需另开一轮（可能是模型/L0-B 前缀规范问题）。
- **关键经验**：LESS/JS 自愈 heal 必须覆盖「跨行漏分号」+「同行多声明黏连」两种形态；门禁对"只能在父上下文编译"的文件（含 `&`/依赖其他 mixin）禁止孤立编译，否则误报会诱使 LLM 精修引入次生损坏。

## CODE-006 误报修复（Tailwind 判定正则）
- **用户问"代码里有 Tailwind 工具类吗"** → 答：**没有**。失败组件 `mc-max-1786694967787-c47fa026` 33 个 class 全是 `c-monitor-*` 语义类，样式是 `<style>` 内手写 `display:flex`/`display:grid` 的自包含 LESS；CODE-006 仅命中 1 处 `class="c-monitor-device-grid"`，是**误报**。
- **误报根因**：`code-structure-validator.js:16` 的 CODE-006 正则用 `\bclass\s*=\s*["'][^"']*\b(?:flex|grid|...)\b[^"']*["']`——`\bgrid\b` 词边界会命中 `c-monitor-device-grid` 里的 `grid` 子串（`-` 是词边界）。
- **修复**：改为要求 Tailwind token 是 class 值里**以空格分隔的独立 token**——`["'](?:[^"']*\s)?(?:flex|grid|...)\b(?:\s[^"']*)?["']`（删去 token 前的 `\b`，加前/后可选空格锚）。语义类名 `c-grid`/`c-monitor-device-grid` 不再误伤，真实 `class="flex ..."`/`grid grid-cols-3` 仍正确拦截。`HOST_STYLE_LEAK_PATTERNS` 里 .vue 与 .less/.css/.js 共用此正则，一处修全生效。
- **验证**：复刻新正则对失败组件跑 → 命中 0；对 `class="flex items-center"`/`grid grid-cols-3 gap-2`/`justify-between p-4`/`w-full h-full` 仍检测到；`class="c-grid"` 不误报。`npm run build` + 重启 13030 PID 19296 激活。
- 同期前端 `TaskDetail.vue`/`generate/components.vue` + `ui-components.less` 的 tab 边框、CodeTypeToggle/TierToggle 放大改动均 HMR 热更生效，未重启前端。
