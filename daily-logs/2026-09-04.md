# 2026-09-04 工作日志

## 00:40 vision 请求错路由治本（f5d3ab9）——provider-pool `__primary__` 跨角色槽冲突
- **症状**：配置 visionModel=qwen3.7-plus，但 vision-agent 实际用 deepseek-v4-flash（providerId `__primary__`）跑图片分析 → mc-max-1788451635574「视觉识别未完成」中止；重启前（text 槽空）正常，跑过生成后必现。
- **根因链**：phase2.service.ts:614-615 连续 resolveVisionConfig/resolveTextConfig → text/vision 两槽各注入一个叫 `__primary__` 的主配置（ID 冲突）→ VisionAgent 持 visionCfg.providerId=`__primary__` → gateway `resolveProviderContext` 用 `pool.get('__primary__')` 解析 → **provider-pool.js:378 `get(id)` 恒先查 text 槽** → 命中 deepseek-v4-flash entry 执行 vision 请求。
- **修法**：ai-defaults.js resolveProvider 主配置 ID 角色化 `__primary_${role}__`（primaryEntry/primaryInPool relabel/setProviders opts.primaryId 三处同步），gateway/pool 无需改（slot.primaryId 按槽存取）。
- **验证**：探针脚本 /tmp/probe-primary-role.mjs（模拟 phase2 先 vision 后 text 两轮 setProviders）→ vision=__primary_vision__/qwen3.7-plus、text=__primary_text__/deepseek-v4-flash 互不串扰 PASS。
- **构建坑**：`nest build` 的 assets 步骤（nest-cli.json `**/*.js` 直拷）负责 ai-engine .js → dist；**裸跑 tsc 不会更新 dist/ai-engine**（allowJs 未开）。改动 ai-engine/*.js 后须手动 `cp` 对应文件到 dist（assets 是 verbatim 拷贝，diff 验证过）或跑完整 nest build。

## 00:10 接口对接向导默认「需求文档分析」（frontend 7b4cc09）
- ApiBindingWizard.vue mode 初始化 + handleClose 重置两处 'auto'→'doc'。

## 服务状态
- 后端 pid 62464（f5d3ab9），前端 vite pid 47887。
- 待用户回归：重跑失败任务 mc-max-1788451635574（复用 Figma 缓存），网关日志应出现 vision-agent `providerId=__primary_vision__` + model=qwen3.7-plus。

## 00:57 修复真实 API 级验证通过（/tmp/probe-vision-live.mjs）
- 探针走 dist 的 resolveVisionConfig→VisionAgent→gateway→真实 qwen API（模拟 phase2 先 vision 后 text 填池顺序）。
- 网关日志：`"model":"qwen3.7-plus","providerId":"__primary_vision__"`，两次真实调用 9.1s/8.5s 完成，返回内容准确描述设计稿（「环境监测界面…一氧化碳浓度趋势曲线及预警线」）。
- 附带收益：qwen 视觉 ~9s vs 错路由 deepseek 的 115-220s —— 视觉阶段提速 10-20 倍。

## 16:32 启动三端（env -i 方式）
- 守护 PID：node 4643（:13030，Nest started + /api 路由 401 鉴权正常）、java 4646（:8080，Started + /api 路由 401）、vite 4647（:2610，HTTP 200）。根路径 "/" 无 controller 返 500 属正常。
- 日志：backend-node/server.log、backend-java/java.log、frontend-dev.log。

## 16:59 重新启动三端（上轮进程已退出）
- 守护 PID：node 7972（:13030）、java 8098（:8080）、vite 8210（:2610）；全部 1s 监听、FE 200、Node/Java 启动日志就绪。
- 探针脚本可复用：/tmp/probe-primary-role.mjs（池隔离逻辑零 API 成本）、/tmp/probe-vision-live.mjs（真实视觉路由验证，单次 ~2 次 API 调用）。

## 01:13 配置污染排查（闭环）
- 全局 ai-config.json lastModifiedBy=6a77365d...(zhjie,isAdmin) 01:09 刷新；MongoDB `user_ai_configs` 5 条文档全部对上真人（zhjie/mxin/lius/weil/liuqh），**无污染**。users._id 是 ObjectId，字符串查归属查不到（须 new ObjectId(id)）。生产无 mongosh，用 backend-node node_modules 的 mongodb driver + `set -a; source .env.production; set +a` 读连接串（自带 authSource=admin）。
- **users.isAdmin 盘点**：83 人，仅 zhjie=true，9 人显式 false，74 人字段缺失（等效非管理员）。AdminInterceptor fail-closed 符合预期，无需修复。

## 01:40 方案 B 落地：个人 AI 配置与全局模板彻底解耦（未 commit，待部署）
- **动机**：用户要「每个人都不同」；旧语义未配置用户回退读全局（=跟随管理员最后保存）+ 面板保存双写全局（saveCurrentProfile），个人选择互相泄漏。
- **改动 3 文件（backend-node，+90/-16）**：
  1. `config.service.ts getMergedAiConfig`：全局文件仅贡献系统凭证（figma/apifox/gitlabToken）+ models 模型库；个人模型选择只来自用户级；无用户配置时按 capability 从库推导默认绑定（text→第一个 text/both 条目，vision 同理）；老用户仅存 legacy 扁平字段时保留生效（per-slot 判断 hasBound||hasLegacy）；个人 models 副本强制被全局库覆盖；**全局无 models（旧格式）保持旧兜底**。
  2. `config.controller.ts`：save-current 不再调 saveCurrentProfile 刷全局（只写用户级 saveUserConfig）；apply/create/update/delete（全局模板管理）加 `assertProfileAdmin`（users.isAdmin===true，与 Java AdminInterceptor 同口径）。
  3. `config.module.ts`：注册 User schema（供 assertProfileAdmin 查询）。
- **验证**：tsc 0 error；config.service.spec 2 failed=基线既有（stash 对比一致）；探针 `/Users/smigoo/工作/mvgo/probe-plan-b.cjs` 13/13 PASS（5 场景：出厂默认推导/用户绑定/legacy 老用户/旧格式兼容/个人副本不覆盖）。探针可复用（临时写 data/ai-config.json + finally 恢复备份）。
- **坑**：块注释里写 `text*/vision*` 的 `*/` 会提前闭合注释 → 605 errors 级联，报错位置远离真凶；定位法看 tsc **最早**一批错误（head 不是 tail）。
- **部署语义变化须知**：部署后未配置用户不再跟随全局文件的 text/vision 模型字段，而是用全局 models 库的出厂默认——生产全局文件已有 models 库（legacy-text/m1、legacy-vision/m2），即默认=deepseek-v4-pro/vision-exp，行为平滑。管理员以后改模型：进面板改模型库（需 isAdmin，保存走 apply/create 路径才写全局；save-current 只影响自己）。
- **01:36 生产闭环验证 PASS**：面板保存后日志出 `[UserAiConfigService] 已保存用户 6a77365d... 的 AI 配置`，全局 ai-config.json updatedAt 保持 17:09:34 不再被刷，无 Unrecognized/ERROR。方案 B 上线完成，**待 commit**（config.service/controller/module 3 文件 + probe-plan-b.cjs）。
## 01:03 生产「配置保存失败」复发诊断与 Node dist 重部署
- **症状复发**：生产配置面板保存报「服务端未确认落库」，与 9-3 上午 schema 自举 bug 同文案。
- **根因**：9-3 23:06 上传生产的 backend-node dist 是 schema 修复（5b65905，09:27 commit）**之前编译的旧源码**——`dist/config/config.service.js` 无 `lastModifiedBy`，生产读 ai-config.json 仍抛 `Unrecognized keys`。教训：grep 验证产物先确认 dist 结构（多文件，schema 在 dist/config/ 下，不在 main.js；首查 main.js 为 0 是误判路径）。
- **重部署**：00:53 本地重建（`rm -f tsconfig.build.tsbuildinfo && NODE_OPTIONS="--max-old-space-size=4096" npm run build`，裸跑曾 Exit 137 沙箱杀），tar 上传；**生产真实加载路径 `/home/mvbt/mvgo/backend-node/dist/main.js`（pm2 script，exec cwd=/home/mvbt/mvgo/backend-node）**——首次解压到 /home/mvbt/mvgo/dist 是错位置，旧 dist 缺失时旧进程靠内存代码继续跑报旧错（restart 无效的根因）。01:08 解压到 backend-node/ 下重启 PID 565363 干净启动，面板保存成功（「保存当前方案」闭环）。
- **生产打包注意**：macOS tar 上传 Linux 报 `LIBARCHIVE.xattr` 警告无害；scp 被本机代理 127.0.0.1:7890 拦截时用户改为手动上传；bash 双引号内 JS 代码含 `!` 会触发 history expansion 报 `event not found`（用单引号包 node -e）。
- **配置污染排查（01:13 闭环）**：全局 ai-config.json lastModifiedBy=6a77365d...(zhjie,isAdmin) 01:09 刷新；MongoDB `user_ai_configs` 5 条文档全部对上真人（zhjie/mxin/lius/weil/liuqh），时间均早于或等于本人操作，**无污染**。users._id 是 ObjectId，字符串查归属查不到（须 new ObjectId(id)）。生产无 mongosh，用 backend-node node_modules 的 mongodb driver + `set -a; source .env.production; set +a` 读连接串（自带 authSource=admin）。
- Java AdminInterceptor（/api/admin/** isAdmin 403 校验）已打包 87M（md5 e8871f8e...）待部署；nginx 无需配置（/api 通配）。backend-java 本地未 commit：WebMvcConfig.java 改动 + AdminInterceptor.java 新增。

## 01:15 max_tokens 提限空转修复（code-generator.js，源码已改未构建）
- **症状**：2:8417 TabSection.vue 在 16000 max_tokens 处 finishReason=length 截断 → 走「提高到模型输出上限重试」但日志 `from:16000,to:16000` 空转 → 同 16k 重试 → `maxTokensEscalated` 已置位 → 第 3 次重试若再截断直接 throw「无法进一步拆分」。
- **根因**：`code-generator.js:545 modelOutputCap=Math.max(getModelMaxOutputTokens(options.model,16000),16000)`；model-config.js 内置能力表**没收录**池内任何模型（deepseek-v4-flash/glm-5.3-flash/minimax-m3/qwen3.7-plus/hy4-preview 全 miss）→ 全落 fallback 16000。
- **修法**：改用 `resolveModelCapability()`（env>内置表>实测缓存）；`limitKnown=false` 时提限兜底抬到 32000 —— 安全性论证：提限路径只在「模型已实际产出 ≥16k 仍被 length 截断」后触发，模型输出能力必然 ≥16k；limitKnown=true 用真实 tokens（claude-opus-4-8=32768），Math.max(.,16000) 保底不变。提限 warn 日志新增 capSource 字段。
- **实测口径**（resolveModelCapability）：池内模型全部 `test-unknown-limit`（model-capability-cache.json 里 identified:true 但都没记 outputTokens）；qwen-plus 表内 8192 仍被 16000 下限托住。
- 验证：file:// 动态 import PARSE_OK。**待两验证任务终态后重建+重启**（构建须 `rm -f tsconfig.build.tsbuildinfo`；nest build assets 才会同步 ai-engine 到 dist）。
- 「感觉慢」实锤归因：TabSection 单分块 444s = hy4-preview 300s 整超时 + deepseek 148s 空响应 + minimax-m3 259s 空响应 + 截断重试；文本 provider 空响应/超时连环 failover 是主因，vision（qwen 97-144s 一次过）不是瓶颈。

## deepseek 空响应根因修复 + 网关空输出快速熔断（df84b6d，已重启生效 pid 22875）
- **根因实锤**：deepseek 系是推理模型，ai-config.json 三个 deepseek provider（legacy-text/deepseek-v4-flash、p1786895670427_b5a4/deepseek-v4-pro、m1788344792569_dd0f/deepseek-v4-flash-vision-exp）`thinkingType` 均 null → 推理烧光输出预算 → content 空串。对照实证：默认开推理 reasoning 消耗 output 预算；`thinking:{type:'disabled'}` 后 reasoning=null、1.2s 返回、finish_reason=stop。
- **配置修复**：ai-config.json（gitignored）13 处 deepseek 条目补 `thinkingType:'disabled'`（顶层 providers/models + profiles[0] + profiles[1]，防切 profile 丢修复）。
- **透传链补齐**（ai-defaults.js）：primaryEntry 构造 + resolveProvider 返回值补 thinkingType 透传。**修正此前误判**：gateway `boundSnapshot` 不是断点（仅 sameProvider 比对，LLM 构建走 `createLLM(fresh)` 全量对象）；`resolveProviderContext` 仅记账不建 LLM。base-agent refreshProvider(124 行) → _buildLLM(180 行) 链路本就就绪，pool setProviders `...p` spread 保留自定义字段。
- **网关增强**（ai-request-gateway.js 空输出检测）：①遥测——finish_reason + reasoningTokens + outputTokens 入 warn 日志，直接区分「推理烧预算(length 截断)」vs「服务端故障(stop 全零)」；②快速熔断——`finish_reason==='length'` 型空输出是确定性故障，检测处额外 recordFailure 1 次（连同 catch 侧共 2 次 = failureThreshold → 立即 open 120s），后续 chunk 不再逐块踩同一 provider（mc-max-1788454414712 逐块踩雷拖 28+ 分钟的机制性消除）。
- **探针**（/tmp/probe-deepseek-thinking.mjs，已验证后从 backend-node 删除）：池链路 thinkingType 存活 ✅、resolveTextConfig 返回 thinkingType:disabled ✅、真实 API 1.2s 非空 ✅。坑：resolveTextConfig 会把池内主配置条目 id **原地 relabel** 为 `__primary_text__`（mutation），探针先 find by id 会 undefined，须按 model 名查。
- ai-engine .js 改动后手动 cp 到 dist（nest build assets 机制，tsc 不同步 .js）。

## 陈旧 payload 旁路 + 遥测实战（304bde8 / 57c33c2，dist 已同步待重启）
- **新遥测首战立功**：用户测试任务 mc-max-1788457395127 的 style-mapper 空输出被直接指认 `finishReason=length, reasoningTokens=6144(全烧)` → 证明 thinking:disabled **没注入**到该请求。
- **根因（第二层）**：`config.providers` 来自前端 ConfigPanel **页面加载时缓存**（请求 config > 用户 DB > 服务端 ai-config.json），配置修复后已打开页面仍携带旧 providers → 服务端配置修复被旁路。tasks.json 不存 config（空对象），取证要靠遥测。
- **治本（模型名级兜底）**：resolveProvider 新增 `withThinkingDefault`——任何来源的 deepseek provider 未显式声明 thinkingType 时默认 'disabled'（显式配置优先；lkeap/glm 会 400 只对 deepseek 系）。应用点：roleMatched 入池前 map + primaryEntry 构造。探针实证陈旧 payload 场景 deepseek 自动 disabled、qwen/glm 不受影响。
- **并行会话覆盖事故**：df84b6d 的 primaryEntry 透传 Edit 曾「成功」但未进提交（并行会话同文件覆盖），本轮按实测遥测发现后补齐。**教训升级：Edit 成功 ≠ 在提交里；commit 前必须 `git show <sha> -- <file>` 核对每个改动点**。
- **快速熔断实战验证**：17:52:34 microcode-engineer 在 minimax-m3 空输出（`length, reasoningTokens=0, outputTokens=16000`）→ 立即熔断 120s → 1s 后切 qwen3.7-plus 续跑，机制完整生效。
- **minimax 类空输出是第三类根因**：推理占 output 预算但不单独上报（reasoningTokens 报 0）或内容落 reasoning_content 字段；lkeap/minimax 网关不接受 thinking 参数（400），不能同款修，靠熔断换 provider。hint 已改三分支判读（57c33c2）。**待查**：minimax-m3 是否有官方关推理参数（其 provider baseURL 待确认）。
- 时间过滤 grep 坑：`grep -E "18:0"` 会误中 `16:18:08` 子串；日志区间截取用 `awk '/起始时间/,0'`。

## 方案 B++ 测试回归闭环（02:00 前后）
- `config.service.spec.ts` 改写后重跑 **7/7 全绿**：统一派生改走用户级路径、新增 B++ 空配置用例、`saved.config` wrapper 适配。
- 最后 1 个基线失败（merge 补默认值）根因：第 65 行 `expect(persisted).toMatchObject(saved.config)`——`saved.config` 含 migrateLegacyToModels 内存派生的 models/binding，磁盘只落扁平字段，断言口径过时。改为断言 4 个扁平字段后通过。
- dist 产物验证：`dist/config/config.service.js` 含 assertProfileAdmin + 凭证-only 合并循环，可直接部署。
- 待办：B++ 生产部署（tar→解压 /home/mvbt/mvgo/backend-node/→pm2 restart→冒烟）；backend-node commit 4 文件 + probe-plan-b.cjs。

## 自优化路由循环误报（诊断，未改）
- phase2 微码生成任务结束报「路由循环 microcode-engineer(4次)」WARNING，但实际完整跑完、质量崩（视觉 5/100）。
- 根因：self-optimizer.detectRoutingLoops（self-optimizer.js:264）只看 routingEvents.target 序列「连续≥3次同 target」即判循环。microcode-engineer 是**分块代码生成**节点（每 chunk 一次节点进入=1 条 routingEvent），历史窗口（最近10次运行）累积≥4 次 → 误判为死循环。
- 副作用（真 bug）：getOptimizationForNextRun 对 routing_loop 类型把 params.maxIterations=1（self-optimizer.js:229），反而**削弱后续精修迭代质量**；前端展示「检测到路由循环」误导。
- 伴生：该任务 microcode-engineer 7min + layout-style-refiner 10min（推理模型大 prompt 超时重试链，空响应的同胞根因）；但**无空内容告警**→ df84b6d thinkingType 透传已生效。视觉 5/100 = 质量门禁3项阻断未拦住 + 产物可能截断。
- 待定：是否修 detectRoutingLoops（按 sessionId 分组 / 排除已知分块节点 microcode-engineer）。

## 22:50 开发环境全模型测速（probe-model-speed.mjs，已完成回收）
- 用户请求「测试开发环境下配置的所有模型的速度」。探针覆盖 `data/ai-config.json` 去重后 8 个 distinct (model+baseURL)，串行 LangChain ChatOpenAI（真实同路径），deepseek 系按配置注入 `thinking:disabled`，maxTokens=800，180s 超时。结果 /tmp/model-speed-result.json + .md。
- **速度排行（升序）**：deepseek-v4-flash 1214ms / deepseek-v4-flash-vision-exp 2370ms / deepseek-v4-pro 2712ms / minimax-m3 3376ms / claude-opus-4-8 11549ms / qwen3.7-plus 14430ms / claude-sonnet-5 19359ms / glm-5.3-flash 24041ms。
- **关键结论**：deepseek-v4-flash 关推理后 **1.2s** 登顶，验证 df84b6d/304bde8 thinkingType 治本有效（此前同模型空响应 0 字）。
- **🔴 新异常 glm-5.3-flash 空响应**：`empty=true, emptyReason="length", outTokens=800`（正好打满 maxTokens），与 minimax 第三类同源——推理占 output 预算但内容不可提取（落 reasoning_content 或 reasoningTokens 未上报）。当前 ai-config.json 对 glm **未配 thinkingType**，withThinkingDefault 只覆盖 deepseek 系 → 任何走 glm-5.3-flash 的生成任务将产出空内容。待用户决策：① 若 glm 接受 thinking 参数则补 `thinkingType:'disabled'`；② 否则按 minimax 同款靠熔断换 provider；③ 或增强 content 提取回读 reasoning_content 字段。探针已删。

## 23:1x glm-5.3-flash 空响应根因闭环（A 路线失败，退回 B）
- 用户选 A（补 thinkingType:disabled）。探针实测：glm 网关 **400**「该模型始终思考，不支持关闭思考；请使用 low、high 或 max」→ disabled 对 glm 不可行。
- 原始端点探测 5 种 body：`thinking:{type:'enabled',budget:'low'}` / `{budget:'low'}` / `{type:'enabled',effort:'low'}` 返回 200，但 **completionTokens 仍=800、contentLen=0、reasoningLen=2761~3034** → budget 参数未把推理压到能容纳答案。
- 复跑 LangChain 无参版出现 contentLen=454（reasoningLen=1933）→ 印证 **推理长度非确定**（1933~3034 字符）。在生成管线固定 max_tokens=800 下，重推理批次把 800 预算吃光 → 答案被截断为空 = **间歇性空响应**（速度测试那次恰好重推理→空；复跑轻推理→有内容）。
- 结论：A（disabled）对 glm 不可行；budget:low 在 800 预算内仍救不了空响应。治本二选一：① 把 glm 移出生成 pool（用户 A 的兜底 B：靠熔断换 provider，但首块仍可能空一次）/ ② 给 glm 单独提 max_tokens（需代码加 per-provider token 覆盖，ai-config 当前无此字段）。content 提取回读 reasoning_content（C）不可行——reasoning_content 是推理不是答案。
- **用户决策：移出生成 pool（推荐项）**。已用脚本从 `data/ai-config.json` 顶层 + `profiles[0].config` 的 `binding.text.poolIds` 摘除 `m1788334344749_6b12`（各 7→6 项），`providers`/`models` 目录保留（仅退出 active pool）。JSON 校验通过、中文名幸存（`ensure_ascii=False`）。text pool 现：minimax / claude-sonnet / claude-opus / legacy-text(deepseek 主) / qwen / deepseek-v4-pro。**激活**：后端需重载配置（运行进程若仍按旧 pool 路由则重启；前端 ConfigPanel 陈旧 payload 同步刷新）。探针已删。

## 03:2x 接口对接向导「无匹配接口时禁用下一步」（frontend）
- 用户贴截图：Step 2 调整绑定「组件数据槽 0 个 / 未检测到数据槽」但「下一步」仍可用，要求「没有接口符合就不允许下一步」。
- 改动 `frontend/src/views/workspace/ApiBindingWizard.vue` Step2 底部「下一步」按钮 `:disabled` 由 `slots.length === 0` 改为 `boundCount === 0`（boundCount=已绑 API 数）。覆盖两场景：① 0 数据槽（截图）；② 有槽但全部 stub/skipped（无接口命中）。
- 验证：`vite build:safe` 因沙箱 broker 在输出写盘阶段 mkdir 报错（环境限制，非代码错；10319 模块已 transform 完、ApiBindingWizard.vue 已转译）；`vue-tsc --noEmit` 仅 ApiBindingWizard.vue 无 error，其余报错均在 src/workspace/custom-components/ 生成的微码组件（FlowPrediction.vue 等，既存、与本改动无关）。改动生效待 dev server HMR 或重启前端。

## 03:4x 手动停止管线是否真停——诊断（user 问 mc-max-1788464403021）
- 链路已逐层验证：前端 `POST /api/tasks/cancel/:sessionId`（generator.ts:109）→ `tasksService.cancelTask`（tasks.service.ts:1768）立即 `status='cancelled'` + `controller.abort()`（:1790）；该 controller 是 phase2 启动时 `registerAbortController` 的那个（phase2.service.ts:508-509），signal 注入图状态（:645）。图节点全用 `withTimeout` 包裹（mc-component-graph-phase2.js:104 import），其监听父 signal 触发本地 abort（orchestrator.js:219,234）。
- **结论：任务层真停（status 立即 cancelled、后续阶段入口 `signal.aborted` 拦截），但执行层不是硬杀**。根因 `withTimeout` 用 `Promise.race([fn, abortPromise])`（orchestrator.js:252）只丢弃 fn 结果、**不杀 fn 执行**；在途 LLM 调用（尤其并行子组件 `runParallelAnalysis` 的 Promise.all，phase2.js:2825）变孤儿继续跑并推 onProgress 日志，直到各自完成或撞节点超时（代码生成节点实测 180s）→ 用户看到「日志还在跳动」= 被丢弃在途生成排空，非新进度。
- 风险：孤儿任务若跑完调 sendComplete 可能把 cancelled 翻回 completed（cancel 与生成 finally 竞态），建议停后几分钟再确认终态稳定。
- 治本（未做，待用户决策）：把 `nextState.signal` 透传到 `ChatOpenAI.invoke` 的 call options `signal`，让底层 fetch 被 abort 即时掐断——目前只 Promise.race 丢弃、未传进 HTTP 层。

## 02:4x #522/#523 A4比例落CSS 完成+提交（commit 7f3b695）
- **#522 header容错**（figma-height-ratio.js）：regions 比 sections 多 1 且首项 header 名+≤80px 时剥掉再 1:1 配对 → 25e40782 从"配对跳过→50/50"变 `figmaHeightPx 65/317`。spec 19 绿。
- **#523 确定性落CSS**：新模块 `utils/figma-section-heights.js` `buildSectionHeightsMap`（纯函数，{子组件根class: px}）+ mounter 规则② A4加权（height:100%/存量盲注 flex:1 1 0 → `flex:<px> 1 0`+min-height:0）+ code-fix-rules 注入 `sectionHeights` ctx + engineer 委托。
- **🔴 实锤bug（map 恒 null 根因）**：`/<template>([\s\S]*?)<\/template>/` 非贪婪在**首个 `</template>` 停**——`package/index.vue` 的 `<template #header-right>` 嵌套插槽先闭合 → tpl 只截到 header，大写子组件标签 OverviewCards/DeviceListArea 全丢 → tags=[] → null → 规则②永不触发。**修复：extractRootTemplate 取「首个非插槽 <template> 开标签 → 文件最后一个 </template>」**（根模板必包全部插槽，闭合在最后）。教训：SFC 取根模板必须防嵌套具名插槽。
- E2E 实证（真实快照）：map `{"c-monitor-overview-cards":65,"c-monitor-device-list":317}`；OverviewCards `flex:65 1 0`、DeviceListArea `flex:317 1 0`。
- **build 137 教训**：`npm run build`（nest build）在沙箱默认堆下 SIGTERM 137/空输出假成功 → 必须 `NODE_OPTIONS="--max-old-space-size=4096" npm run build`（7s 成功）。
- 已重启 13030（旧 22875→新 80415，02:37 就绪，加载 02:34 dist）。

## 02:5x 视觉模型路由「设 claude5 却先 qwen」根因
- **配置侧正常**：GET /api/config/ai（dev-local）→ modelMode=separate、visionModel=`claude-sonnet-5`、binding.vision.primaryId=`m1787827173866_bc5c`、poolIds[0]=qwen(p1786894837102_67ff)。方案 B++ 后模型全在用户级 user_ai_configs（Mongo，configEnc 加密），全局 ai-config.json 只供 3 个系统 token，其 visionModel=qwen3.7-plus 是**旧残留不参与合并**。
- **根因=运行中后端跑旧 dist**：01:43 dist 晚于 ai-defaults.js src 01:52（`__primary_vision__` 角色化 primaryId+primaryInPool 标记）→ 旧 resolveProvider 主配置并入池时 ID 冲突 → vision 槽 pick 落 poolIds[0]=qwen。pool/provider-pool 08-27 未变，纯 ai-defaults 修复缺失。
- 修复=重启加载 02:34 dist（含角色化），primary-first 下 claude5 健康即独占，仅超时/熔断才降级池。验证方式：重跑视觉任务看进度"🤖 模型: claude-sonnet-5"。

## 07:2x 全量 405 修复（DELETE/PUT → POST，生产网关兼容）
- **根因**：公司 nginx 容器 `gzxj.conf` 方法过滤器 `if ($request_method !~ ^(GET|POST)$)` 拦 DELETE/PUT/PATCH 返回 405。
- **Node 后端改动（backend-node）**：
  - `page-skeleton.controller.ts`：`@Put(':groupId/:id')` → `@Post(':groupId/:id/update')`；`@Delete(':groupId/:id')` → `@Post(':groupId/:id/delete')`；`@Delete(':groupId/:id/file')` → `@Post(':groupId/:id/file/delete')`
  - `component.controller.ts`：`@Delete(':componentId/file')` → `@Post(':componentId/file/delete')`
  - `recipes.controller.ts`：`@Delete(':name')` → `@Post(':name/delete')`
  - `agent-builder.controller.ts`：`@Delete(':name')` → `@Post(':name/delete')`
  - `admin.controller.ts`：`@Delete('users/:id')` → `@Post('users/:id/delete')`（合并重复方法）
- **Java 后端改动（backend-java）**：
  - `AdminController.java`：`@DeleteMapping("operation-log")` → `@PostMapping("operation-log/delete")`；`@DeleteMapping("users/{id}")` → `@PostMapping("users/{id}/delete")`；`@DeleteMapping("components/{id}")` → `@PostMapping("components/{id}/delete")`
- **前端改动**：
  - `ManagementView.vue`：`http.delete('/api/admin/operation-log')` → `http.post('/api/admin/operation-log/delete')`
  - `page-skeleton.ts`：`http.delete(...)` → `http.post(.../delete)`
  - `demo/index.vue`：`http.delete(.../file?path=...)` → `http.post(.../file/delete?path=...)`
  - `workflow.ts`：`axios.delete('/api/recipes/${name}')` → `axios.post('/api/recipes/${name}/delete')`
- **验证**：前端 `http.delete(` / `http.put(` / `http.patch(` 全局搜索 0 匹配；`frontend npm run build` 通过 ✅（23.86s）。
- **部署注意**：Node `npm run build` 被沙箱 SIGTERM 137（内存限制，非代码错），需用户本地或有 JDK 环境时重建 dist；Java `mvn package` 需有 JDK 环境。
- **部署文档更新**：`docs/operations/部署指南.md` §5 坑 #6 已补充完整修复清单（7 处后端 + 4 处前端）。

## 07:4x token 校验缺口修复（workflow.ts 改用 http 封装）
- **发现**：`frontend/src/api/workflow.ts` 全部 20 处 `axios.get/post()` 直接调 `/api/` 接口，**绕过 `core/http.js` 统一封装**，不注入 `Token` 头。
- **影响范围**：工作流 CRUD、Agent Builder（创建/列表/上传/试跑/反馈）、Recipe 配方库、模型列表——全部 `/api/` 接口在生产会 401。
- **修复**：`workflow.ts` 全量改用 `http` 封装（`import http from '@/core/http'`），自动注入 `getAuthToken()` 返回的 token。响应解析从 `res.data` 改为 `res` 或 `res.data || res`（http 封装已解析过一层 JSON）。
- **验证**：`frontend npm run build` 通过 ✅（24.53s）。
- **全量 token 校验审计结果**：
  - ✅ `core/http.js` 统一封装：`buildInit()` 实时 `getAuthToken()` → `headers['Token']`
  - ✅ `api-token.js`：门户 `getToken` + URL search/hash 双路径兜底
  - ✅ `preview-resolver.ts`：`buildPreviewUrl()` / `buildSnapshotFileUrl()` 拼 token 到 query
  - ✅ `session.guard.ts`：`request.headers['token'] || request.query?.token` 双路径识别
  - ✅ `login/index.vue`：`axios.post('/api/auth/login')` 是认证入口，不需要 token
  - ✅ `ApiDocs.vue`：`fetch('/api/...')` 是示例代码字符串，非实际执行
  - ✅ `preview/index.vue`：dev 路径 `fetch('/__raw/workspace/...')` 不走 `/api/`，生产路径走 `http.raw()` 已有 token

## 09:4x 任务详情页概览卡「复制图标上移到任务号那行」（frontend，未 commit）
- 用户指令「上面修改为上」= 把复制图标从组件名那行挪到任务号（顶部大号）那行、跟任务号绑定。
- `frontend/src/views/tasks/TaskDetail.vue` 改动：
  - 概览卡 `identity-main-top` 内：原 `span.plugin-id`（含 copy 图标 + `@click=copyTaskId`）拆为 `span.identity-copy`（包 `h2.identity-name` + copy 图标，`is-copied`/`title`/`@click` 移至此）+ 纯文本 `span.plugin-id`（仅显示 组件名·组件ID）。
  - CSS：删除旧 `.plugin-id` pill/hover/is-copied/`.plugin-id-copy-icon` 规则；新增 `.identity-copy`/`.identity-copy-icon`/hover·is-copied 变绿 + `.identity-copy.is-copied .identity-name` 变绿；`.plugin-id` 改纯文本（去背景/padding/cursor）。`copyTaskId()` 逻辑不变（仍复制 sessionId）。
  - grep 验证：旧类 0 残留、新类 template+CSS 一致。前端 `vite build` 进行中验证 transform 通过。

- 已提交 frontend `6bc31b5`（#code# #ai-coding#），1 file +35/-29。

## 11:3x 任务详情页身份字段显式标签化（frontend，未 commit）
- 用户反馈当前顶部概览“用户无法知道”，要求直接写清：`任务号：xxx；组件名：xxx；组件ID：xxx`。
- `frontend/src/views/tasks/TaskDetail.vue` 顶部概览改为显式字段：第一行 `任务号：sessionId`（保留复制图标与复制 sessionId），第二行 `组件名：displayName||componentName`，若 `componentId !== sessionId` 再显示 `；组件ID：componentId`。
- 样式新增 `.identity-submeta/.identity-field-label/.identity-field-sep/.plugin-id-mono`，组件名用普通字体，组件ID保留等宽。
- 现状补充：左侧“任务信息”里的“名称”仍被 `v-if="task.componentName"` 控制，部分只有 displayName 的任务会直接不显示名称；顶部概览已不受该限制。

- 已提交 frontend `22ead14`（#code# #ai-coding#），任务详情页身份字段改为显式标签：任务号/组件名/组件ID。

- 分析 mc-max-1788485095835-e1432017（cp-流量监测 425x807）视觉分析高度比例：vision JSON 在 `.mc-gen/cache/vision-cache/vision_*.json` → `layoutStructure.layout.sections[i].styles.{flexGrow,flexShrink,flexBasis,figmaHeightPx}`；flexGrow≈figmaHeightPx/342，产物子组件根直接 `flex: figmaHeightPx 1 0`（DailyTotal 807 / TunnelChart 33 / BridgeChart 556 / VehicleDistribution 142 / TrafficForecast 174）。
- ⚠️ 对照 `.mc-gen/cache/figma-node-data.json` 真值，3/5 错位：当日总流量真值 slot h=556 却记 807(=整图高)；隧道图/大桥图真值各 h=131 却记 33/556。车型分布 142、流量预测 174 正确。错误比例已传导产物 flex-basis。

- 分析 mc-max-1788485095835-e1432017 无法切换主题/颜色变量（仅分析）：根因=组件样式不消费宿主 CSS 变量。宿主链路正常：demo/index.vue `getThemeConfig`(utils.js:39)→`getCssVarsConfig`(utils.js:6)→postMessage `apply-css-vars`(demo:2360)→preview/index.vue:1088 写 `:root` inline `--colorTextBase` 等。断链：index.css 编译产物 81 处 #hex 字面量、var(--color*)=0（仅字号 var(--fontSize) 生效）；6 个 .vue 子组件各自 scoped 内联 `@colorTextBase: #333333`(DailyTotal.vue:69) 本地覆盖；theme-vars.less/light.less/dark.less 编译期 mixin(snake-case) 与 css-vars.js(camelCase) 两套体系互不衔接且均无人消费；declare.json `cssVariableConfig=[]`。workspace 已发布 7 组件 common.less var(--color) 全为 0 → 生成侧普遍缺口非个例。

- 修复 mc-max-1788485095835-e1432017 视觉分析高度比例错位根因（2026-09-04）：
  - src/ai-engine/utils/figma-height-ratio.js：
    ① collectSectionRegions BG_RE 从 `/^(bg|background|背景)$/i` 增强为前缀+铺满判定（bg-[m]/bg-_m-35/bg-card 等命名+ bbox≥95% 根尺寸才视为背景层剥，业务性 bg-card 小尺寸不会被误剥）；
    ② applyFigmaSectionRatios 增加「复合 region 展开」：当 regions 数 < sections 数时找 bbox.h 最大且有 children 的 region 展开其子级（剥 header/sub-header 类小子项，按 y 排序插入），上限 2 轮；
    ③ 复合展开后用 stripLeadingHeaderIfExcess + stripSubHeader 迭代清理多余 1 项直到对齐 sections（处理「外 header 留到展开后 + 内 sub-header 留到展开后」两种场景）；
    ④ HEADER_RE 末尾加 `[-_]?` 兼容 `header-` 等带连字符设计命名。
  - 加 6 个单测（figma-height-ratio.spec.ts）覆盖 BG 增强 4 个 + 复合 region 展开 2 个，含本组件真实 fixture（5 sections vs 5 regions 剥 bg + 展开 88:33 后正确 1:1 配对：当日总流量=91 / 隧道图=131 / 大桥图=131 / 车型=142 / 流量预测=174）。
  - 跑回归 figma-height-ratio 25 + figma-section-heights 5 = 30/30 全过。`nest build` 出 dist 含新逻辑。
- 修复后本组件预期 vision cache figmaHeightPx = [91, 131, 131, 142, 174]（之前错 [807, 33, 556, 142, 174]）→ 产物 flex-grow 权重比例 [0.469, 0.674, 0.674, 0.731, 0.896]，自适应宿主高度（flex-basis 0，grow 权重等比分配）。
- ⚠️ 已生成产物（temp-components/.../package/components/*.vue flex: 807 33 556 142 174 1 0）写死旧值，需重出该组件或手动改产物 flex 权重才能生效。

- 实施「主题变量化 A+B」（commit backend-node 1416f2c #ai-coding#）：①A 生成即规范——新建 prompts/engineer/shared/style-theme-rules.md 并注入 prompt-loader buildSharedSections（microcode 用 var(--colorX,fallback)、vue3 用 @colorX mixin 变量，槽表+铁律）；修订 figma.md 内联 elementStyleMap「直接值优先」旧规则（颜色类改变量引用、尺寸直接值保留）；同步 references/css-variable-pattern.md。②B 门禁——新建 utils/theme-color-guard.js（detectThemeColorViolations）：槽值抄写 THEME-SLOT-COLOR BLOCK、预设名本地重声明 THEME-PRESET-OVERRIDE BLOCK（@colorTextBase:#333 反模式）、设计专色/渐变/SVG THEME-SPECIAL-COLOR WARN；豁免 themes/、config/、.vue 只扫 <style>（script echarts 色不误报）、var() 整值合法、theme-vars 无 hex 时降级 WARN。接入 code-structure-validator.js validate() 出口（THEME-COLOR-ERROR fail-open）。guard spec 8/8 + code-structure-validator.spec 85/85 过；真实组件 mc-max-1788485095835 诊断=27 槽值抄写 BLOCK+3 预设覆盖 BLOCK+21 专色 WARN。
- ⚠️ backend-node 工作区存在非本会话改动（疑似并行会话）：microcode-engineer.js/figma-height-ratio.js+spec/lite.service.ts/component-naming.js(新)/probe-*.mjs/debug-lite-r16.mjs/dist-node.tar.gz，均未纳入提交。
- ⚠️ 全量 nest build 报 2 处 TS2307 bcrypt（auth.service.ts/user.schema.ts import bcrypt 类型解析失败，@types/bcrypt@6.0.0 已装仍解析不到）；tsc 仍 emit 产物故 dist 可刷新；node require('bcrypt') 正常=运行时无碍，疑环境/依赖问题待查。

## 组件语义命名 方案 A+C 落地（componentId 确定性唯一化 c-<语义>-<sessionId 尾8hex>）
- 新增 utils/component-naming.js（纯 ESM .js，src+dist 双份）：ZH_SEMANTIC_RULES 中文词表（隧道域扩充：交通诱导/供配电/液位/水位/能见度/风机/照明/泵站等）+ toKebab/camelSplit/semanticTokenFrom/tailHexOf/buildComponentId/classPrefixOf/semanticSegmentOf/isEncodedSessionId。classPrefixOf 带 13 位时间戳守卫（历史实例形态 c-mc-max-<ts>-<hash> 不剥离，老产物兼容）；semanticSegmentOf 纯 hex≥8/纯数字判无效（堵 c-9abce496-9abce496 双尾编码）。探针 35/35。
- microcode-engineer.js（Max 管线）normalizeDeclareJson：有 sessionId 时强制 componentId=buildComponentId(语义段||中文映射, sessionId)；幂等跳过需「尾 8hex==session 尾 且语义段有效」；onload 事件与 class 前缀统一 classPrefixOf(componentId) 剥离尾段（c-monitor-43e7fe45→c-monitor-onload）；_forceChanged 接线日志；execute() 暂存 ctx.sessionId/编码 componentName→this._engineerSessionId。_mapChineseSemanticName 委托共享词表 zhToSemanticEn。
- code-structure-validator.js resolveClassPrefixId：CODE-003/autoFix class 前缀事实源改 classPrefixOf(decl.componentId)（剥尾段），L0-B 与 code-validator autoFix 同源同步生效（冒烟：c-monitor-43e7fe45→c-monitor；c-mc-max-<ts>-<hash> 保留）。
- lite.service.ts（Lite 管线 方案C）：deriveComponentName 兜底不再回退编码 sessionId→zhFallback(briefCn)（中文词表映射，命中不了 'component'）；微码分支 normalizeLiteDeclare componentId 传 buildComponentId(finalComponentName, sessionId)。发布目录由 publisher resolveWorkspaceComponentId 读 declare→custom-components/c-<sem>-<tail8> 自动一致唯一（-2/-27 后缀不再需要）。
- 前端展示补丁：generate/components.vue recent(displayName 优先)+html-split 列表、generate/component.vue 历史列表 displayName 优先。
- 验证：probe 35/35；resolveClassPrefixId 冒烟 8 例过；nest build 成功（bcrypt TS2307 复现一次后 tsc -p 全绿=瞬态）；code-structure-validator.spec 85/85、microcode-engineer.retry.spec 8/8；dist 符号 grep -E 全部同步。
- ⚠️ 未重启本地 13030（PID 95496 运行旧 dist），新逻辑待重启后生效；未 commit。
- ✅ 已提交：backend-node e0fc538（#code# #ai-coding# 命名 A+C，3 文件 +343/-46）；frontend 77be6e1（#code# displayName 优先展示）。code-structure-validator.js 的 resolveClassPrefixId 解耦已随并行会话 1416f2c 混入 HEAD（component-naming 悬空 import 由此补齐）。未重启 13030（用户选择稍后），逻辑待重启生效。

## 15:1x C 存量组件变量化 — 试点 c-env-monitor(mc-1788138863742-f9bd0808) 完成
- 摸底：workspace/custom-components 7 目录中 **2 个为空**（1786777124209/1786793106573，无文件），实改 5 个。5 实组件 cssVariableConfig 全 []、无 css-vars.js、CSS var(--color*) 引用 0 → 主题全链路断（宿主只下发 cssVariableConfig 声明的 key：demo getStyleConfig → postMessage apply-css-vars → preview:1092 setProperty；getThemeConfig 读 declareInfo.cssVars{common,light,dark}）。
- 规范权威：docs/微码管线改造方案-2026-09-03.md §5.3 双轨制。dark=枚举(M4-10: #ffffff/#44E4FF/#414141)，light=设计真值，装饰/图表色可硬编码(M5-10 WARN)。cssVariableConfig 只声明自定义变量；preset 免声明。微码 declare.js 必须 cssVars:{common,dark,light}(M4-2)。
- 试点改造 4 文件：①resources/config/css-vars.js 新建(common fontSize14/600 + light=现状板 + dark=枚举+colorTextSecondary #D6D6D6 + 自定义 colorTextInverse/colorError/colorWarning 双主题同值)；②declare.js 补 cssVars 注入；③declare.json cssVariableConfig 6 行（含 value=light 现状）；④common.less 7 色→var(--x,原值)（#333→colorTextBase、#666→colorTextSecondary、#2c9bea→colorPrimary、#fff×2→colorTextInverse、#f53f3f→colorError、#d32f2f→colorWarning）+ 5 处 calc(@fontSize*n)→calc(var(--fontSize,14px)*n)；text-shadow #006fe3 装饰保留。备份 /tmp/backup-c-env-monitor-20260904。
- 验证：detectThemeColorViolations = 0 issues；lessc 编译 index.less exit 0，产物 13 处 var(--)。echarts script 配色不扫（canvas 主题化另议）。
- 待办：同配方批量剩余 4 实组件（1788086817483 无 resources/styles 只有 .vue 内 style；1788176720155/1788179386544/1788327432319 有 themes 目录），先各读全样式面提取 light 现状板；mc-max-1788485095835 走重出管线。

## 14:35 完整构建部署命令清单（含所有已知坑）

**前端**：
- 本地构建：`cd frontend && npm run build`（~24s 稳定通过）
- 部署：上传 `dist/` 到 `/home/mvbt/nginx/www/mvgo/`，`docker exec nginx nginx -s reload`
- 坑：`index.html` 缓存（发布后硬刷新 Cmd/Ctrl+Shift+R）、dist 路径必须正确、不要只替换部分文件（建议清空后全量上传）

**Node**：
- 本地构建：`cd backend-node && rm -f tsconfig.build.tsbuildinfo && NODE_OPTIONS="--max-old-space-size=4096" npm run build`
- 部署：上传 `dist/` 到 `/home/mvbt/mvgo/backend-node/dist/`，加载 `.env.production`，`NODE_ENV=production nohup node dist/main.js`
- 坑：构建必须加 `NODE_OPTIONS="--max-old-space-size=4096"`（否则沙箱 SIGTERM 137 杀死）、`FIELD_ENCRYPTION_KEY` 固定不可变（随机化永久破坏 MongoDB 加密字段）、启动入口是 `dist/main.js`、不要删除 `dist/` 目录（按需清 tsbuildinfo 即可）

**Java**：
- 本地构建：`cd backend-java && mvn -pl mvgo-app -am package -DskipTests`（需 JDK 17+，本地无 JDK 时用 `/tmp/jdksetup` 环境）
- 部署：上传 jar 到 `/home/mvbt/mvgo/backend-java/`，注入 `MONGODB_URI` + `SPRING_PROFILES_ACTIVE=prod` + `NODE_BACKEND_URL=http://192.168.112.1:13030/api` + `PORTAL_BASE_URL=https://go.microvideo.cn/portlet/api`
- 坑：`MONGODB_URI` 必须带 `authSource=admin`、`NODE_BACKEND_URL` 必须用内网 IP 192.168.112.1（不能用 localhost/127.0.0.1）、不要设 `DEV_AUTO_LOGIN`（生产 fail-closed）

**Nginx 路由**：
- `/api/progress` 必须放 `/api` 之前（SSE 直连 Node 13030，精确前缀优先）
- `/api` → Java 8080（`proxy_pass http://192.168.112.1:8080/api`）
- 摘掉 `listen 8888`（历史遗留端口，与 80 同站无独立用途）
- `proxy_pass` 必须用 `192.168.112.1`（nginx 容器内 127.0.0.1 是容器自身非宿主）

**发布顺序（铁律）**：
1. 构建 Java jar → 上传到 ECS
2. 启动 Java（注入环境变量），确认 `:8080` 通
3. 构建 Node dist → 上传到 ECS → 重启 Node
4. 构建前端 dist → 上传到 ECS
5. 修正 gzxj.conf 路由 + `nginx -t && nginx -s reload`
6. 验证全链路
> ⚠️ Java 未起前不要改 nginx 路由，否则 `/api` 全 502

**已知坑速查表**：
| 坑 | 现象 | 根因 | 修复 |
|----|------|------|------|
| 405 Method Not Allowed | DELETE/PUT/PATCH 返回 405 | 公司 nginx 网关仅允许 GET/POST | 后端 `@DeleteMapping`→`@PostMapping`，前端 `http.delete()`→`http.post()` |
| 401 Unauthorized（预览） | 预览/快照 401 | SessionGuard 只认 Header | SessionGuard 兼容 query token + preview-resolver 拼 token |
| Node 构建 Exit 137 | `npm run build` 被杀 | 沙箱内存限制 SIGTERM | `NODE_OPTIONS="--max-old-space-size=4096" npm run build` |
| proxy_pass 502 | nginx 代理后端失败 | 用了 127.0.0.1（容器自身） | 改用 192.168.112.1 |
| /api/progress 被 Java 拦截 | SSE 不推送 | 路由顺序错误 | /api/progress 放 /api 之前 |
| FIELD_ENCRYPTION_KEY 变更 | MongoDB 解密失败 | 密钥随机化/变更 | 固定不可变 |
| MongoDB 不一致 | 数据互不可见 | 两端连不同库 | MONGODB_URI 完全一致 |
| index.html 缓存 | 发布后加载旧版 | 浏览器缓存 | 发布后硬刷新 |

## 16:0x C 存量组件变量化 — mc-lite-1788491849328-99e8660b（流量监测 Lite）完成
- 位置：temp-components/6a51f7caed28c5d5b13c61a5/（Lite 单文件 848 行 vue + index.less + declare，**浅色设计**全幅 #EBF2F9 浅蓝灰 + 白卡片 + 蓝主色 #3B9CFF，非深色）。原 index.less 持有 --mc-primary/--mc-text/--mc-border/--mc-bg 死变量（宿主不注入），themeConfig={}, declare.js 无 cssVars。
- 改造 4 文件：①resources/config/css-vars.js 新建（common fontSize14/600；light=设计真值#4A5568/#666/#3B9CFF/#5DB5FF/#2E86DE + 自定义4项；dark=派生深色皮肤 #fff/#D6D6D6 + **主色沿用 #3B9CFF** 保证与 echarts 数据色同步 + colorBgPage #0E1A2B/colorBgCard #16263F/colorBorder #2B3D5C）；②declare.js 注入 cssVars{M4-2 必传}；③declare.json 补 themeConfig{default:light, list:light+dark} + cssVariableConfig 7 行；④index.less 重写（删除视觉死代码白底/描边/阴影，保留尺寸+框架变量本地别名兼容旧引用）；⑤package/index.vue style 段 25 处 UI 语义色→var(--x,原值)（14 个 Edit，1 个 replace_all 渐变，剩 rgba 装饰/4DD4D8+FF9957 数据色保留=M5-10 允许）。
- 主色沿用策略：浅色 BI 组件切暗皮肤时 colorPrimary 不随 #44E4FF 翻转（与 legend-beijing/echarts bar 数据色 #3B9CFF 同步），仅翻转文字/底色/描边语义色——避免 UI 主色与图表数据色分裂。
- 验证：guard=1 WARN（10 处 rgba/渐变装饰字面量，符合 M5-10 非阻断）/ 0 BLOCK；lessc index.less exit 0；脚本 echarts 配色（#3B9CFF/#4DD4D8/#FF9957/#D0E0F0/#999/#666/#333）保留为 canvas 数据色（M5-10）。备份 /tmp/backup-mc-lite-1788491849328-20260904。
- 后续：剩余 4 实组件（mc-max-1788086817483/1788176720155/1788179386544/1788327432319）+ 待决定 mc-max-1788485095835 是否走重出，按同配方批量。Lite 改造经验已纳入：浅色 BI 组件主色沿用品牌避免数据色分裂、defaultTheme=light、dark 派生面板/底/边框深蓝系。

## 16:4x 主题切换预览不刷新 — 根因 declare.json 缺 cssVars（mc-lite-1788491849328 报告）
- 症状：mc-lite 切深色主题预览不刷新（CSS 已 var() 化但颜色不变）。
- 根因链路：demo 页主题下拉 formState.themeType → change('themeType')→init()(demo/index.vue:2581) → getThemeConfig(themeType, declareConfig) 读 **declare.json.cssVars{common,light,dark}** → rows value 按主题取色 → watch(formState)→loadDebounce(1s)→getStyleConfig→sendStyleToPreview→iframe apply-css-vars→setProperty(:1092)。**断环**：后端 GET /api/component/:componentId/declare（component.controller.ts:329 → component.service.ts:1015 getComponentDeclare）= **原样 JSON.parse(declare.json)**，不合并 css-vars.js；此前只给存量组件加了 resources/config/css-vars.js + declare.js（运行时 declare.js 才拼 cssVars），declare.json 无 cssVars → themeVars={} → 行 value 恒 undefined → 下发空 cssVars → 预览不动。
- 修复：declare.json **必须内嵌 cssVars{common,light,dark}**（与 resources/config/css-vars.js 双写同源；新规范产物 mc-max-1788491328748 的 declare.json 本就带 cssVars——对照模板漏了这一步）。已补 mc-lite-1788491849328-99e8660b 与试点 mc-1788138863742-f9bd0808 两个 declare.json，node 精确比对 css-vars.js↔declare.json.cssVars 全量一致 ✅。
- ⚠️ 教训写进配方：存量组件变量化 = 4 个改动点缺一不可 —— ①CSS var(--x,原值) ②resources/config/css-vars.js ③declare.js import cssVars 注入 ④**declare.json 内嵌 cssVars（演示/设计器读它，后端接口原样返回 declare.json）** + themeConfig + cssVariableConfig。前端 demo 配置面板主题下拉只认 declare.json.cssVars。

## 17:0x 主题切换预览不刷新 — 实证根因：vue3-sfc-loader scoped style 未注入（mc-lite 单文件组件）
- 浏览器实证（agent-browser，/demo/mc-lite-1788491849328-99e8660b）：
  - declare.json cssVars ✅：rows 切深色时值正确刷新（字体主色 #4A5568→#ffffff、页面底 #EBF2F9→#0E1A2B、卡片底 #FFFFFF→#16263F、描边 #D0E0F0→#2B3D5C、主色 #3B9CFF 品牌沿用）→ **demo 数据链路全通**。
  - iframe :root `--colorTextBase/--colorBgPage/--colorBgCard/--colorPrimary` 全空字符串 → **demo apply-css-vars postMessage 似乎没到达**（或到达后被覆盖），但手动在 iframe documentElement setProperty 同样**无效**（cardBg 仍 #FFFFFF fallback、contBg 透明）。
  - 关键证据：**`.c-mc-lite-…-content / .c-mc-lite-…-card` 元素 `data-v-*` 属性为 null** → vue3-sfc-loader 编译 lite 单文件 SFC 时 scoped style **未注入 iframe 文档**，导致 CSS 规则不生效，var 永远取 fallback。
- 真正根因：mc-lite 单文件 vue (848 行) 经 microcode runtime 加载时 scoped <style> 未正确插入 preview iframe（base-panel 未知 tag 走 light DOM，scoped 选择器无 data-v 锚点，样式不命中）。组件 CSS 整体未生效，与主题切换无关——切深色"没变化"是因为组件样式从来就没生效过。
- 修法不在 C 改造范畴（架构/loader 兼容问题）：需排查 microcode runtime / vue3-sfc-loader 对 lite 单文件 SFC 的 scoped style 注入逻辑；或将该组件拆为标准微码结构（package/index.vue + 独立 resources/styles/common.less + 子组件）由 generate 管线产出（mc-gen 形式）。
- 试点 mc-1788138863742-f9bd0808 (c-env-monitor 标准微码) 仍可能正常——是更推荐的参照形态。

## 18:0x C 存量组件变量化 — 批量 4 标准微码组件完成（mc-max-*）

按用户 C 方案（"先批量改造 4 个标准微码组件"），完成 mc-max-* 系列：

| 组件 | componentId | styles 形态 | vue 子组件 | guard BLOCK/WARN | 备注 |
|---|---|---|---|---|---|
| mc-max-1788176720155-9560d082 | c-env-monitor | themes/ + common.less (182) | ChartArea/HeaderTabs | 0/2（渐变） | 与 f9bd0808 试点同图，结构略不同；scoped 段不动视觉 |
| mc-max-1788086817483-4e4799ad | c-monitor | **无 styles 目录** | AlertCards/HeaderStats/DeviceGrid | 0/0 | 补 resources/styles/index.less 兜底；无 theme-vars.less 槽值表，guard 0 警告 |
| mc-max-1788179386544-86ab341b | c-env-monitor | themes/ + common.less (208) | ChartArea/ChartControls/HeaderTabs | 0/1（数据色） | **删 .common() 局部 @fontSize 重声明**（THEME-PRESET-OVERRIDE BLOCK） |
| mc-max-1788327432319-a6198738 | c-env-monitor | themes/ + common.less (154) | ConcentrationChart/SectionHeader/TabsTools | 0/1（8 装饰色） | theme-vars.less 23 行全 var() 别名（无 hex 槽值）→ guard 0 BLOCK；保留装饰黑外圈/渐变/蓝高光 |

**统一改造配方（5 步缺一不可）**：
1. **建 `resources/config/css-vars.js`**：common{fontSize:14px} + light{设计真值 6-7 色} + dark{派生/规范枚举}，命名导出 + default 导出（M4-8 只认 default）。
2. **改 `declare.js`**：从 './resources/config/css-vars.js' import {common,dark,light} → `$createMcDeclare({...cssVars:{common,dark,light}})`。
3. **改 `declare.json`**：cssVariableConfig 6 行（colorTextBase/Secondary/Primary/TextInverse/Error/Warning，value=light 真值）+ **内嵌 cssVars{common,light,dark}**（后端 getComponentDeclare 原样返回 declare.json 不合并 css-vars.js，必须双写同源）。
4. **样式面 var() 化**：色值 `var(--colorX, 原值)`；UI 语义色→框架 8 变量；自定义命名避开预设 8；渐变/装饰/数据色保留（M5-10 WARN 豁免）。
5. **删 THEME-PRESET-OVERRIDE**：styles 内 `.common() { @fontSize: 14px; }` 等本地重声明框架预设（@colorTextBase/@colorPrimary/--colorPrimary/@fontSize 等）→ 必删（BLOCK）。

**验证脚本**（`/tmp/verify-mc.mjs`）：node 22 ESM 加载 css-vars.js default + declare.json.cssVars 按主题归一 key 序 JSON.stringify 比对；调用 backend-node `theme-color-guard.js#detectThemeColorViolations(files)` 扫所有非 themes/config 的 .less/.vue；用 node workspace 装的 `less` 包 `less.render(content, {filename})` 编译。
- less 索引：index.less/dark.less/light.less 因脚本传 filename 错位 → FAIL（脚本 bug，**与组件无关**，前端 vue3-sfc-loader less 编译正常）
- common.less 独立编译 OK（3240/0/3765/2813 chars）

**guard 三类规则**（`backend-node/src/ai-engine/utils/theme-color-guard.js`）：
- `THEME-PRESET-OVERRIDE` BLOCK：本地重声明框架预设变量（`@|--<preset>:`）。
- `THEME-SLOT-COLOR` BLOCK：颜色属性值直接抄写 theme-vars.less 槽值集合内 hex → 必须 var() 化。
- `THEME-SPECIAL-COLOR` WARN：独立 hex 不在槽值集合（图例/数据/渐变/SVG）→ 允许保留。
豁免：themes/ 目录（槽值定义处）+ config/ 目录 + .vue 只扫 <style> 段 + 值整体为 var(...) 含 fallback → 合法。

**5.3 双轨制（docs/微码管线改造方案-2026-09-03.md）**：
- light=设计真值（原 UI 浅色板） / dark=规范枚举（M4-10：colorTextBase #ffffff / colorPrimary #44E4FF / colorPrimaryBg #414141）
- 自定义变量双主题同值（colorTextInverse/Error/Warning），命名避开框架预设 8 变量
- 装饰/图表色允许硬编码（M5-10 WARN）—— legend/数据/渐变/SVG fill 保留
- 4e4799ad 的 #1890ff、9560d082/86ab341b/a6198738 的 #2c9bea 各自 light 真值（不同组件不同品牌色）；colorError 9560d082=#f53f3f，4e4799ad=#ff4d4f（各自 UI 实际）
- dark 板 4 组件统一用 M4-10 规范枚举：colorTextBase #ffffff、colorTextSecondary #D6D6D6、colorPrimary #44E4FF/Hover #78ECFF/Active #00BBFF、colorPrimaryBg #414141/BgHover #4B4B4B

**教训追加**：
- 4 组件 verify 一致 ✅ / guard 0 BLOCK ✅ / 4 套 css-vars ↔ declare.json.cssVars 全量一致 ✅
- Edit 工具并发锁坑：单 message 内 4 个 Edit 同时发，会出现"success 但实际未改"（line 102 单行 Edit 成功，其它 3 个未生效）。**改完必读 Read 验证或直接 Write 重写**。
- declare.js Write 必须先 Read（之前 86ab341b 踩过：未先 Read → "File has not been read yet"）。
- theme-vars.less 槽值集合 = 该文件所有 hex 槽值；a6198738 的 theme-vars.less 全用 `var(--colorPrimary)` 别名无 hex → 槽值集合空 → 任何独立色 WARN 不 BLOCK，**最宽松**。
- 备份全部在 /tmp/backup-mc-max-*-20260904/。


## 13:55 组件库卡片预览图永远占位根因修复（生产反馈）
- 现象：生产组件库「我的组件」所有卡片停留「滚动后加载预览」占位。
- 根因：ComponentLibrary.vue loadComponents 在 nextTick 后 observePreviewCards，但 loading 仍 true，卡片区 v-else-if 未渲染 → querySelectorAll 空 → IntersectionObserver 永不触发。
- 修复：observe 挪到 finally 中 loading=false 之后再 nextTick。提交 8f45bc0 已推送。
- 附带：today-overview 生产缺 resources/images/mc-preview.png（文件不存在），修复后该卡显示「预览加载失败」，产物缺预览图待单独处理。生产生效需重新构建前端部署。


## 14:30 视觉覆盖率门禁三项改动落地 + 验证（识别率低根治）
- 背景：环境监测/设备监测组件（56 Figma 元素）反复 low-coverage BLOCK，生产同错（mc-max-1788500607191-577eae48，覆盖率 32%=18/56）。根因不是资源下载（17 下载成功/11 复用/28 资源节点均正常），而是 Vision 模型输出 section 级结构 vs Figma 元素级节点粒度错位 + 细粒度枚举能力弱。
- 三项改动（均已在 dist 生效）：
  1. #535 prompt 注入 Figma 文字真值白名单（prompt-builder.js `buildTextTruthWhitelist` + text-truth-guard.js `collectFigmaTextTruth`，过滤中文→长度降序→限 50→「🛡️ 文字真值白名单」强制段）。
  2. #536 TEXT-TRUTH 门禁放宽：code-structure-validator.js `TEXT_TRUTH_BLOCK_THRESHOLD=2`，≤2 处 WARN / >2 处 BLOCK（防少量 OCR 误读误杀）。
  3. 强化 Vision prompt「逐元素枚举铁律」+ 覆盖率匹配逻辑（visual-parser.js）：`_traverse` 加 characters 字段、`addIdentified` 加 value/content、`parsed.headerSlots` 提取进 identifiedNames、匹配改 `matchCandidates = charsLower.trim() ? [charsLower, nameLower] : [nameLower]`（TEXT 节点用文字内容参与匹配而非无语义 name）。
- 阈值：visual-trust.js `coverageRate < 35` → LOW_COVERAGE（40%→35%）；35~60% WARNING；≥60% TRUSTED。
- 验证：新代码覆盖率 32%→**79%（44/56）** summary「良好」，不再 BLOCK；旧代码 25%/34%/38%/46% 常 <40% 被拦。用户手动停止测试任务（cancelled 非失败）。
- 补修：mc-component-graph-phase2.js 定向重分析引导语 "覆盖率 <40%/提升到 40%" 同步为 35%（与 visual-trust.js 硬阈值一致，之前 40→35 遗漏此处文案）。
- 服务：当前 PID 23216（06:27 重启），「逐元素枚举铁律」已在 debug-prompt 命中。
- 遗留：生产（独立部署）vision 模型用 claude-opus-4-8，本地 qwen3.7-plus，配置不一致；本地改动需同步部署生产才生效。模型选型建议 Kimi K2.6/K3 > GLM-5V Turbo > Qwen3.7 Plus > claude-opus-4-8。


## 15:00 任务 mc-max-1788504554318-b476b3fe 生成超时（10+ 分钟）根因诊断
- 现象：环境监测组件（nodeId 2:7879）生成 12+ 分钟未完成，UI 显示"生产很久"。
- 直接原因：任务 textModel 配置 = **deepseek-v4-flash**，其子组件大 prompt（~130k chars）请求被服务端把 16000 token 输出预算**全烧在 reasoning**（reasoningTokens=16000, finishReason=length, budgetExhausted=true）→ 空内容 → gateway 熔断换 provider 重试。每次失败浪费 126~192s（等模型烧完预算），多个子组件 × 多轮 = 5-7 分钟纯浪费。
- 实锤日志（06:55-06:59）：deepseek-v4-flash attempt1 126s 失败 / attempt2 128s 又失败（316s 两轮全废）；deepseek-v4-pro 188s 失败；连 gpt-5.5 也 reasoningTokens=15996 烧光（192s 失败）→ 均熔断切 claude-sonnet-5/claude-opus-4-8 重试才成功。熔断机制本身工作正常。
- **兜底缺口（治本点）**：`model-config.js isReasoningModel()` 名单只认 deepseek-reasoner（/reasoner/），**未覆盖 deepseek-v4-flash/v4-pro**（不带 reasoner 字样）→ resolveProvider 的"推理模型自动标 skip 过滤"防御失效 → 推理模型仍作为 __primary_text__ 主模型发起请求。记忆已知"deepseek 空响应需 thinkingType:disabled + resolveProvider 模型名级兜底"在此路径未闭环。
- 修复方向：A) isReasoningModel 名单补 deepseek-v4*；B) gateway/primaryEntry 对 deepseek 系未显式配 thinkingType 时注入 thinking:{type:'disabled'}（保留作主模型但关推理）；C) 前端 textModel 不选推理模型。
- 服务仍未重启（用户要求先不重启）；dist 已含 14:43 构建（引导语 35% 文案），等下次重启生效。


## 15:20 生产 THEME 门禁 3 轮重试耗尽治理（A/B/C/D 四项落地）
- 背景：生产 mc-max-1788505197306（环境监测简单版，10 元素，覆盖率 50%）L0-B 3 轮 BLOCK 耗尽禁止发布。BLOCK=THEME-PRESET-OVERRIDE（@colorPrimary: #409EFF / file-writer 补的 @fontSize: var(--fontSize) 也被误判）+ THEME-SLOT-COLOR（common.less:69/112 color:#ffffff 三轮未消）。另 style-class-consolidator 报 6 次 "Cannot read properties of undefined (reading 'log')"。
- 根因四连：① file-writer「补全缺失 less 变量」注入 @fontSize: var(--fontSize)（透传合法）被 guard 当本地覆盖 BLOCK → 自愈→门禁死循环；② SFC theme 自愈只覆盖 .vue 不覆盖 common.less；③ guard 对值形态不识别（只看声明不看值）；④ LLM 生成路径无法稳定 0 违规时硬 BLOCK = 任务 100% 失败。
- 落地：
  - **A**（theme-color-guard.js）：PRESET 命中后提取声明值，`/^var\(/` 透传形态豁免（@fontSize: var(--fontSize) 不再 BLOCK）。
  - **A2+B**（code-healer.js 新函数 healPresetLiteralDecls/healSlotHexToVarRefs/applyHealToVueStyleBlocks + file-writer.js .vue/.less 分支接入）：预设字面量声明 @colorPrimary: #409EFF → @colorPrimary: var(--colorPrimary, #409EFF) 透传；颜色属性纯槽 hex → var(--cssVar, #hex)（hex 从 theme-vars.less `@kebab-name: #hex;` 提取转 camel，同 hex 多变量歧义跳过）。保守：gradient/rgba/url 内不碰、script 不碰（wrapper 只扫 style 块）、themes/ 目录豁免。
  - **C**（microcode-engineer.js writeFiles）：consolidate catch 加 this.logger 判空 + stack 记录。本地 src 复现无 'log' bug（consolidate 本体 warn/info 判 typeof 安全）——疑生产 dist 旧版，需部署验证。
  - **D**（theme-color-guard.js）：PRESET_WARN_UP_TO=2 / SLOT_WARN_UP_TO=3，阈值内降 WARN（放行），超阈值仍 BLOCK。
- 测试：guard spec 更新（透传豁免 + ≤阈值 WARN + 超阈值 BLOCK 共 11 用例）+ 内联实测 3 heal 函数全部符合预期。回归：theme-color-guard 11/11、style-class-consolidator 6/6、code-healer+file-writer.less-heal 43/43 全绿。
- 本批还含方案 B（deepseek-v4* 模型名级 thinkingDefaultFor → disabled）——见上方 15:00 记录，同一次构建一起生效。

## 14:45 C 项代码层根治：竖排布局反截断护栏（VERT-001/002/003）
- **症状**：竖排侧栏按钮（sidebar tab / nav）生成后「只显示单字 / 断列 / 长标签被固定小高度裁切」，与 P0-4「竖排文字按钮必须 nowrap + 高度按最长标签预算」prompt 铁律失守面完全重合。
- **实锤**：mc-max-1788499007313-6ac539e0 SidebarTabs —— 窄栏导航文本带 text-overflow:ellipsis + overflow:hidden + min-width:0 → 容器宽度不足时被截成「交…」；按钮固定 height:20px 且无 min-height → 交通诱导只显示「交通」。
- **新增工具**：`src/ai-engine/utils/vertical-layout-guard.js`（纯 ESM .js，zero-dep，可单测）：
  - VERT-001：writing-mode 竖排（vertical-rl/lr/tb-*）块缺 white-space:nowrap → 补（防断列：供配电→电供/配）
  - VERT-002：按钮/导航语义文本块（selector 命中 -tab/-nav/-sidebar/-menu/-btn 等）带 text-overflow:ellipsis / overflow:hidden / min-width:0 截断三件套 → 移除（防窄栏单字截断）
  - VERT-003：按钮/导航语义块固定 height 且无 min-height → height 降级为 min-height（防按钮高裁切文字）
- **接线**：`src/ai-engine/roles/microcode/code-validator.js` 2.9 段（FLEX-002 之后、return 之前）调用 `autoFixVerticalLayout`，目标文件 common.less 全文 + 各 .vue <style> 体（scoped 也覆盖）。
- **判定纪律**（宁可漏报不可误杀，与 FLEX-005 一致）：
  - VERT-002/003 仅作用于「按钮/导航语义」selector（BUTTON_NAV_SELECTOR_RX 命中 -tab/-nav/-sidebar/-menu/-btn/-item），普通文本省略（.xxx-text/.xxx-desc 等）不在触发面内；
  - VERT-001 仅作用于含 writing-mode 竖排值的块，横向文本零影响；
  - VERT-003 仅当块内无 min-height 才降级，已有 min-height 的规范写法不触碰。
- **验证**：jest spec 15/15 全绿（含嵌套块扫描、导航语义判定、三规则触发/不误伤）；code-structure-validator.spec 85/85 + microcode-engineer.retry.spec 8/8 回归通过；nest build 成功（dist 符号 14 处 VERT-00x + 5 处接线）；后端重启 PID 43755 加载新 dist（1s UP）。
- **BSD grep `\|` 陷阱复现**：`grep -c "VERT-001\|VERT-002\|VERT-003"` 返回 0（macOS BSD grep 不认 `\|` 交替），换 `grep -cE "VERT-001|VERT-002|VERT-003"` 返回 14 ✅。
- **A2 重生成验证**：用户选择暂不重生成，留到下次有需要时。后端已就绪（PID 43755），触发命令：`curl -X POST http://127.0.0.1:13030/api/phase2/generate -H "Token: dev-smoke-0904" -d '{"fileKey":"1t7Dmmpsl5i0PC8BJt2QLf","nodeId":"2:8417","groupId":"6a51f7caed28c5d5b13c61a5","componentName":"设备监测","target":"microcode"}'`。

## 15:40 生产排障：部署 + Lite 快照 404 + 下载 401 治本（frontend，未 commit）
- **生产重启**：Java 8080 曾因旧进程占端口 Exit 1（`fuser -k 8080/tcp` 后重启 OK，pid 702972，`Started in 3.974s`；jar 实际在 `/home/mvbt/mvgo/backend-java/` 根目录，非 `mvgo-app/target/`——cd 路径写错会让 java 在错误 cwd 找不到 jar）。Node 13030 部署新包（dist md5 `c71b37650ad1ba3091f47a749f885088`，vision 误报修复含「纯文本模型，无法处理截图分析」特征）。
- **Lite 组件预览 404（预期行为非 bug）**：`mc-lite-*`/`mv-lite-*` 快照只有 `resources/styles/index.less` 无 `index.css`——index.css 仅 Max 管线确定性编译产物；前端 `loadVue3Runtime.js` `injectGlobalIndexCssIfExists` 先 `?exists=1` 探测（404→静默跳过注入，靠 SFC scoped 渲染）。console 红字是 DevTools 网络噪音，不影响渲染。
- **下载 401 治本（frontend 3 文件 + 新 util）**：AI WorkSpace 接口导出全用原生下载（`<a :href>`/`window.open`）不带 `Token` 头 → 生产 Java SessionGuardInterceptor fail-closed 401 → Chrome「请先尝试登录相应网站」。dev 走 Vite 代理宽松鉴权不复现。修复：新建 `utils/download-file.js`（`downloadByUrl`：http.raw fetch 带 Token → Blob → a[download]，文件名优先 Content-Disposition）；`GenerateApiView.vue`（fullResult/result 两卡 + downloadItem 历史卡）、`ApiCatalogDetail.vue`（下载 ZIP）、`MyApisView.vue`（下载 zip）4 处全部改 @click 调 downloadByUrl。vue-tsc 过（exit 0），vite build 25.8s，产物含 `download-file-B82DxY3L.js`。**待部署前端 dist**（`frontend-dist-0904-1538.tar.gz` 54MB → `/home/mvbt/nginx/www/mvgo/`）。
- **GitLab test-gitlab 403 排障**：token 本地 curl 200 / ECS 403（nginx HTML）→ ECS 出口 IP 被 scm.microvideo.cn 前置 WAF 拦截，非 token 问题；待运维加白 ECS 公网 IP。

## 16:0x mc-max-1788504554318-b476b3fe 生成失败根因分析（14:42 + L0-B 重试耗尽）
- **时间账**（tasks.json progress 仅存末轮 114s=15:02:02→15:03:56；更早段靠日志）：vision 101s、style-mapper 82s（deepseek-v4-flash 超时空内容→熔断→gpt-5.5）、第 1 轮代码生成 7:33（gpt-5.5/deepseek-v4-pro 连续超时熔断 + claude 兜底 346s）、L0-B fail→engineer 全量重试 ×2（65s/114s）。**慢的根因=LLM 超时熔断链 + 回退全量重试，非组件复杂度**。
- **失败根因链（3 层）**：
  1. guard 判定 vs LLM 生成能力不匹配：L0-B SLOT-COLOR（抄写 #559eff/#f53f3f/#ffffff 槽位色）5 处 > 3 阈值 → BLOCK，3 轮 LLM 都抄字面量。
  2. **确定性自愈 B 治本当时不存在/未生效**：`healSlotHexToVarRefs`+`applyHealToVueStyleBlocks` 在 code-healer.js 是 15:16-15:18 才写 src、15:25 才 build dist；任务 15:03:56 已失败 → 跑的旧 dist 无槽位色自愈，只能靠 LLM 重试碰运气 → 耗尽。
  3. consolidate crash 噪音：`[style-class-consolidator] reading 'log'` 每轮 2-3 次（现版 consolidate 已 typeof 防御 warn/info 且不调 log；microcode-engineer.js:3511 catch 已判空 + 记 stack）。旧 dist 版本遗留，级别「按原样写盘」不阻断。
- **当前状态**：修复已进 src（guard 阈值降级 PRESET≤2/SLOT≤3→WARN，theme-color-guard.js 15:16）+ dist 15:25（healSlotHexToVarRefs/applyHealToVueStyleBlocks/阈值全含）✅；**但进程 PID 43755 14:45:58 启动 = 旧代码仍在跑，需重启才生效**。
- **healer 口径实证**（node 调 dist code-healer）：`healSlotHexToVarRefs` 只认 `@kebab: #hex;` 裸槽 + **同 hex 多槽歧义跳过**（#1E293B 同时是 text-inverse/bg-container → 不修；#FFFFFF 同）；guard `collectSlotHexSet` 无歧义概念全量收 hex → **guard 判 BLOCK 的槽 hex，healer 因歧义可能不修**（口径差，靠 ≤3 阈值降级兜底）。
