# 2026-08-24

## TaskDetail.vue 详情页身份上下文优化

用户反馈：生成详情页无法快速识别"当前是哪个组件"。

### 改动清单（4 项，零后端改动，纯前端）

| # | 改动点 | 位置 |
|---|---|---|
| 1 | 面包屑 `任务详情` → 动态 `componentName`，sessionId 降为次要灰色 | L19 |
| 2 | 概览区 overview-left 改为身份卡：h2 组件名 + 来源/类型 pill + 批次 `3/5` + sessionId 缩略 | L224-248 |
| 3 | 新增设计稿对照块（design-compare）：设计稿缩略图 64×64 + "打开 Figma 设计稿"链接 | L270-286 |
| 4 | 侧边栏输入源 tabs 下方显示具体来源值：Figma URL / 截图缩略图 / 批次信息 | L81-91 |

### 新增 computed
- `figmaUrl`：从 `task.fileKey + task.nodeId` 合成完整 Figma URL
- `figmaUrlShort`：截短至 48 字符
- `sourceDesignImageUrl`：指向已有的 `/api/component/{cid}/file?path=resources/images/mc-preview.png&raw=1`
- `batchContext`：从 `task.childIds` 计算 `{index, total}` 批次位置

### 未做（用户明确说不要）
- 预览框 URL 栏 `component://...` 未改（3处），因为用户说不要重启，改这些会涉及预览逻辑
- 未触碰后端

### 编译验证
- `vue-tsc --noEmit`：TaskDetail.vue 无新增错误（已有错误均为其他文件的旧 TS 问题）
- 未重启前端，Vite HMR 会自动生效

### 关键发现
- `mc-preview.png` 在生成开始前就落盘（Figma 来源 = Figma API 下载节点图；截图来源 = 用户原图转 png）
- 完整 Figma URL 未落库，但 fileKey+nodeId 已有，前端合成即可
- task.childIds 已持久化，批次上下文可纯前端计算

---

## （另一会话线）生成管线 Prompt 优化全天工作（摘要）

### 已完成修复（均构建验证通过）
- **P0-1**：layout-reviewer 删 48KB 错配规范（保留内嵌字段速查）、maxTokens 4096→8192、输出限长；首轮重生成实锤：31.9s 直接解析成功（原 35.9s + JSON 截断降级）
- **P0-2/P1-3**：$mcComponentBuilder 全仓 11 文件统一为直接解构 `const { componentProps, businessProps, runtimeBuilder, componentApi } = $mcComponentBuilder()`（权威定义 core/microcode/core.js:165-283；let+try-catch 分离是 TDZ 元凶，活标本 mc-1787285457662 L39）；scriptSplit 状态段声明/行为段引用归属明确；retry-prompt segmentType 感知输出格式。首轮重生成零 TDZ/零语义完整性错误
- **shared 去重（-45%）**：root-container 与 layout-rules 三节逐字重复删除、style-files.md 删除（与 mc-builder 4️⃣ 重复）、layout-rules 编号撞车修正（2.2️⃣卡片→2.3️⃣）；备份 /tmp/mvgo-prompt-dedup-backup-20260824/
- **P1-4 文件级裁剪**：buildSharedSections 加 `<!-- shared-file: path -->` 标记 + _trimSharedForFileType 双层裁剪；**真实生效仅 script 段**（-19%文件级，合计约-48%）——declare/style 走专用构建器不加载 shared，removeFiles.declare/style 是死配置待清理
- **高度自适应规范统一**：三种矛盾规范并存→统一为 `flex: <Figma高度px> 1 0`（flex-grow 直接取设计稿高度值，零算术）；图表 min-height 主图160/紧凑图100（替代一刀切200）；删百分比换算/calc(100%-Xpx)/手工比例缩除等算术陷阱；改 5 文件；备份 /tmp/mvgo-height-rules-backup-20260824/
- **P1-5-c**：figmaNode 按数据来源可选（Figma 阶段必须/纯截图省略禁臆造）；确认 vision 输出 figmaNode 无代码消费端

### 首轮重生成遗留问题（待修）
- CODE-003：class 前缀用完整 sessionId（期望 c-monitor）——COMPONENT_NAME 变量传递链疑点
- CODE-012：子组件 TrafficPrediction echarts 缺 ResizeObserver——子组件分块疑不带 chart-standards
- LESS-COMPILE-001：子组件 @import 路径层级错（../ 应为 ../../）

---

## TaskDetail.vue 失败任务用户体验持续优化（下午）

延续上午的身份上下文优化，针对**失败任务**的体验做了一系列改进。核心问题：用户面对失败的组件，不知道问题出在哪、该选哪个修复方案。

### 已完成改动（4 项）

| # | 改动点 | 位置 |
|---|---|---|
| 1 | 失败原因智能诊断（failureDiagnosis computed）：从 failureSummary 中分类超时/质量门禁/语法错误/网络错误/未知，给出推荐路径和解释 | L1955-2009 |
| 2 | 概览区失败摘要卡片（.failure-summary-card）+ 预览区强引导卡片（.failed-guide-card），显示诊断标签+推荐按钮+元操作按钮 | L373-393, L907-1010 |
| 3 | "复制错误上下文"按钮：一键复制结构化信息（组件名+失败原因+诊断+关键日志），可直接粘贴到 AI 工具 | copyFailureContext() |
| 4 | 代码 Tab 自动定位问题文件：切换到代码 Tab 时自动展开并选中第一个失败文件 + 显示文件级错误摘要条 | failedFilesCount/locateFirstFailedFile/selectedFileErrors/snapshot-failed-summary/snapshot-file-error-bar |

### 用户价值
- **决策摩擦降低**：从"三个按钮平等"变为"系统告诉我最佳方案"
- **上下文切换减少**：错误信息直接显示在代码视图顶部，无需切换到日志 Tab
- **修复效率提升**：一键复制错误上下文可直接粘贴到 AI 工具询问解决方案

### 关键技术点
- `failureDiagnosis` computed 基于错误文本关键词分类（超时/质量/语法/网络/未知）
- `selectedFileErrors` computed 从 logLines 中提取与当前文件路径匹配的错误
- `watch(activeTab)` 监听 Tab 切换，失败任务切换到代码 Tab 时自动定位
- 所有复制操作都有 fallback（navigator.clipboard → textarea + execCommand）

### 未做（后续可选）
- 代码高亮中语法错误行标记（需要 Monaco 编辑器集成，工作量大）
- 失败任务的"修复进度"跟踪（多轮修复的状态展示）
- provider __primary__ 空串 120s 超时 ×3（子组件段，重试兜底正常）

### 任务状态文案细化 + 横幅优化（14:55+）
- **方案A**：TaskDetail statusLabel running 时按 SSE 阶段流细化——STAGE_PHASE_LABEL 映射 50+ stage key → 数据采集中/分析中/代码生成中/质量校验中
- **横幅**：completed→✓绿 / 门禁未过→⚠黄（.delivery-icon-warn #faad14）；标题"组件代码已生成（草稿）"；描述"质量门禁未通过，当前为未验收草稿代码；可进入 Playground 查看与修复"
- vue-tsc 无新增错误 + vite build 通过；dev 模式 HMR 自动生效
- 另：TaskDetail SSE 先建连再 loadTask（冷启动竞态修复）

---

## mc-max-1787541760839 失败复盘（15:20）
- 本轮实际供应商池：主配置 `__primary__=deepseek-v4-flash`；备用 `deepseek-v4-pro`（text）、`qwen3.7-plus`（both）、`kimi-k2.6`（both）；`pickStrategy=weighted-spread`，因此不是固定单模型。
- 成功调用覆盖：declare（deepseek-v4-flash 1.7s）、template/script/子组件/style 前的多个块分别使用 deepseek-v4-pro、deepseek-v4-flash、qwen3.7-plus；失败发生在 style 块，Prompt 33,713 字符（约14k tokens），先 qwen3.7-plus 180s 超时，再切 deepseek-v4-pro 180s 超时，重试预算耗尽。
- 代码层确认 provider 切换成功：`ai-request-gateway.js` 每次 attempt 通过 resolveAttempt 重新 pick 并排除 previousProviderId；日志记录 `excludeIds=[p1786894837102_67ff]` 后切到 `p1786895670427_b5a4`。本轮不是“没有切换模型”，而是 maxRetries=1 只允许切换一次。
- 结构性瓶颈：三个子组件 Prompt 各约 202KB（约84k tokens），style 虽已裁到 33.7KB，但仍由单个请求同时生成 common.less + theme-vars.less，并依赖全部 Vue 上下文；应优先拆 style 为 common.less 与 theme-vars.less 两块，并改为结构化 class/主题摘要。
- 本轮形成通用优化方案：先建立唯一规范源与 GenerationContext，再按固定依赖链拆分 declare/template/script/subcomponents/style，所有组件通过 capability/profile 选择能力，禁止组件名、尺寸、颜色、布局比例和模型名硬编码；新增输入契约、跨文件一致性、资源/LESS/样式视觉回归门禁，采用影子运行→小流量→全量并保留回滚。

### 本轮执行（15:41+）
- 后端资源守卫继续收口：`microcode-engineer.js` 生成写盘后接入 `validateCssUrlsInWorkspace`，仅非阻断告警，不改变 CSS 产物。
- `mc-component-graph-vue3.js` 与 `mc-component-graph-phase2.js` 的合并/串行精修出口统一调用 `postRefineResourceImportGuard`，按目标类型复核并幂等补回资源引用，避免 refiner 删除 import 后漏网。
- `frontend/src/views/lite/LiteGenerate.vue` iframe 去掉 `allow-same-origin`，保留 `sandbox="allow-scripts"`，预览仍可加载但不再与父页面共享同源权限。
  - 验证：backend `npm --prefix backend-node run build` 通过；frontend `npm --prefix frontend run build` 通过（Vite 23.20s）；关键符号 grep 确认定义与调用存在。直接加载 graph 源文件的 ESM 探针因 TS-only `src/config/runtime-env.js` 别名文件缺失而失败，不影响 Nest 构建；`git diff --check` 暴露既有未清理的 trailing whitespace，未做无关清理。
- 提交：frontend 创建 `e027caa`（任务阶段状态与 Lite 预览沙盒）；backend-node 创建 `5578c37`（统一生成规范与资源校验链路）。两边提交信息均含公司规范字段，未推送；backend-node 仍有未跟踪运行产物 `server.log.bak`、`tasks.db`，明确未纳入提交。
- 后端已按约定重建并重启：停止旧 13030 进程，清理 `tsconfig.build.tsbuildinfo`，`npm run build` 通过；`start-node.js` detached 启动新 PID `58327`。`dist/main.js` 为 4684 bytes，13030 正在监听，`/api/tasks` 返回 HTTP 401。

## vue-max-1787560125404-e12061a5 失败复盘与修复（16:28 任务）
- 失败点：日志显示「✅ 文件写入完成」后紧跟 `Vue3 组件生成失败` / `writtenFiles.map is not a function`，即写盘已成功、失败发生在写盘后的返回值消费处。
- 根因：`Vue3Engineer extends MicrocodeEngineer` 未重写 `writeFiles`。父类 `microcode-engineer.js:4065` 的 `writeFiles` 返回 `{ written, skipped }` 对象（微码路径 L5247 已正确解构），而 `vue3-engineer.js:1314` 仍按旧数组形态直接接收，L1318 `writtenFiles.map(...)` 抛 TypeError。返回形态改为对象时漏改了 vue3 路径。
- 修复（vue3-engineer.js L1316）：`const { written: writtenFiles, skipped: writeSkipped } = this.writeFiles(...)`，并对 `writeSkipped` 补 warn 日志。最小改动，不改写盘行为与产物。
- 验证：`npm run build` 通过；detached 重启 PID 61017，13030 监听、`/api/tasks` HTTP 401；dist 产物 grep 命中修复符号（dist/ai-engine/roles/vue3-engineer.js:1316）。修复未提交，待复测 vue-max 任务通过后再提交。

## mc-max-1787560119416-c72885db 失败复盘（16:28 任务，与 vue-max 并行）
- 前端 SSE 日志不含模型名；真实模型链在 `logs/app-2026-08-24.log`（UTC 时间戳，+8=本地）。`server.log` 被 16:45 重启截断，查模型必须看 `logs/` 下的文件日志。
- 失败分块确认：**style 双文件块**（`common.less + themes/theme-vars.less`，35,743 chars ≈14.9k tokens）。attempt1 `deepseek-v4-pro` 180s 超时（16:41:23 记冷却）→ attempt2 `qwen3.7-plus` 180s 超时（16:44:24，isLastAttempt）→ 任务失败。
- 与 mc-max-1787541760839 复盘结论一致：**style 双文件块是 2/2 任务的同位复现失败点**（两次均为双 provider 连续 180s 超时），而 90k tokens 的子组件块反而能在 105~317s 完成。requestMaxRetries=1 仍只允许 2 次尝试，轮不到 deepseek-v4-flash/kimi-k2.6。
- 结论：结构性修复（P1 拆 style 为 common.less / theme-vars.less 两块）优先级应提前，单纯加超时/重试治标不治本。

## P1 style 拆分实施（16:50+，microcode-engineer.js）
- **改动 1**：`generateCode` 闭包内新增 `runStyleChunks(idx, styleCtx, classList)`（L1208）：theme-vars 分块先行（无 Vue 上下文、无 class 清单，仅颜色事实）→ common 分块后行（theme-vars 作为 📎 参考上下文首位 + 原 Vue 上下文 + class 硬约束）；两块各自独立 180s 预算与 provider 重试，失败只重试失败分块；部分修订/断点续跑天然兼容（runChunk 跳过非目标文件并从磁盘回填 allFiles）。
- **改动 2**：complex/medium/simple 三条复杂度分支的 style 双文件分块全部替换为 `runStyleChunks` 调用（旧 `chunkStyle` 定义清零，grep 确认无残留）。
- **改动 3**：`_buildStyleChunkPrompt` 改为分发器——单 theme-vars → `_buildThemeVarsChunkPrompt`（三 mixin/禁 CSS 变量/默认主题/颜色事实子集：elementStyleMap 按 `/color|background|border|shadow|fill|opacity/i` 通用属性过滤，无组件特例）；单 common → `_buildCommonLessChunkPrompt`（class 命名 + class 硬约束 + elementStyleMap 直接值优先 + 主题变量引用规则「必须用 theme-vars 已定义名，不确定直接写精确值」）；双文件 legacy → `_buildStyleChunkPromptDual`（原 prompt 原样保留，防回归）。共用段抽为 `_buildStyleRevisionSection` / `_styleOutputFormatTail`。
- 视觉产物不变：颜色/尺寸事实仍来自同一 elementStyleMap/Figma 源；拆分只影响请求组织，不重新估算视觉值。写盘门禁缺失变量自动补 theme-vars 的兜底（L4045）继续生效。
- 验证：`npm run build` 通过；detached 重启 PID 62296，13030 监听、HTTP 401；dist 产物 grep 命中 8 处拆分符号。retry-prompt 按 chunk.files 动态适配单文件，无需改。未提交，待 mc-max 复测通过后一并提交（连同 vue3 writtenFiles 修复）。

## mc-max 17:02 重跑复盘 + 二轮修复（17:16+）
- **style 拆分已生效**：chunk 5（theme-vars）/chunk 6（common，prompt 仅 11.7k chars）独立落盘；但任务最终失败于**新根因**：拼装后 index.vue script 76 行孤儿 `* 加载汇总数据`（`/**` 开注释被拼装/重试替换吃掉）→ SFC 编译 Unexpected token → 拒绝产出。缓存分块 `4-package_index.vue` 本身完好（68 行 `/**` 在），损坏发生在内存拼装环节。
- **theme-vars prompt 68k 根因**：本轮 style-mapper JSON 截断解析失败 → elementStyleMap 空 → 我的 builder 回退全量 figmaStyleTree（60k+）。同时 45k 预算裁剪未触发（enforceInputBudget KEEP_ANY 全保）。
- 另一模式：**deepseek-v4-flash 絮叨打满 16000 max_tokens → 分隔符解析不出 → 空串**（theme-vars/common 各 1 次），空串重试切 provider 兜底有效但耗时。
- **二轮修复（3 项）**：
  1. theme-vars 颜色事实三级兜底：elementStyleMap 颜色子集 → figmaStyleTree 正则调色板提取（去重限 60，<2k chars）→ visualElements；**禁止回退全量 figmaStyleTree**。
  2. 孤儿 JSDoc 行修复 `_fixOrphanJsdocLines`/`_fixOrphanJsdocInVue`：块注释外 `* xxx` → `// xxx`、孤立 `*/` 丢弃；带模板字符串反引号奇偶守卫；在最终 SFC 校验循环（原 L1520 抛错点上游）无条件应用。Node 单测 3 例通过（孤儿修复/正常 JSDoc 不动/模板串保留）。
  3. 验证：build 通过、PID 65884、HTTP 401、dist grep 5 处命中。
- 遗留优化项（未做）：①子组件分块 207KB 瘦身（SubcomponentContract：按 section 过滤 figmaStyleTree/elementStyleMap/index.vue 上下文，预计 -60%+）；②style/declare 分块 maxTokens 收紧到 ~8k 防絮叨；③style-mapper 输出截断（maxTokens 8192 仍不够，输出限长策略需复查）；④enforceInputBudget KEEP_ANY 全保失效复查。

## SubcomponentContract 子组件分块瘦身（17:26+，已实施）
- **数据实锤**：`input.figmaNodeData` 原始 JSON 476k 字符（.mc-gen/cache 的 figma-node-data.json 是 {fileKey,nodeId,document} 包装，运行时传的是 document 节点）；每块 207KB prompt 的主因就是逐块全量重发这棵树。Figma section 节点命名约定 `slot-<标题>`（slot-当日总流量/slot-车型分布/slot-流量预测），但部分 section 标题（江阴靖江长江隧道/江阴大桥）只出现在深层 TEXT 标签（如 slot 内 `t-江阴大桥`、`@antd/tab` 按钮文字）。
- **实现**（microcode-engineer.js，vue3-engineer.js 同步接入）：
  - runChunk 支持 `chunk.scopedInput`（分块级事实覆盖，`{...input, ...chunk.scopedInput}`，未提供行为不变 fail-open）。
  - `_extractSubcomponentKeywords`：标签词 + usage 处字符串属性值（title="江阴大桥" 等，与 Figma 节点名同源的最强匹配键）+ class 名。
  - `_pickFigmaSubtree`：DFS——(a) `slot-*` 节点名直接命中 → 返回该 slot；(b) 深层 TEXT 命中 → 回溯最近 slot 祖先；守卫：节点名 <2 字符禁用 k.includes(n)（防 'g' 匹配 'bridge'）、@echarts 占位组/VECTOR 不作直接目标。
  - elementStyleMap 子集过滤（键匹配 + panel/root/bg/theme/common 全局键保留，零命中回退全量）；layoutStructure.sections 子集（零命中回退全量）。
  - contextFiles：`_extractUsageSnippet`（import 行 + 标签使用处 ±12 行，最多 2 处）替代全量 index.vue，找不到使用处回退全量。
- **真实数据验证**（用 17:02 任务 figma-node-data）：5/5 子组件正确命中 section——DailyTotal/Tunnel/Bridge→slot-当日总流量（304k raw，内嵌图表，保真优先）、Vehicle→slot-车型分布（89k）、Forecast→slot-流量预测（67k）；figma 段平均 -55%（格式化后口径），加 elementStyleMap/sections/usage 摘要合计预计每块 -50%~65%。
- 验证：build 通过、PID 67770、HTTP 401、dist 命中（microcode 6 处 / vue3 4 处 scopedInput 符号）。孤儿 JSDoc 修复 + theme-vars 调色板兜底 + style 拆分同批已部署。
- **style-mapper 摸底**（回答用户提问）：LLM 角色（maxTokens 6144，model 配置），输入 visualElements + 可选 figmaStyles，输出 `{themeVars(--colorPrimary camelCase), lessVariables(@color-primary), cssClasses, tokenCoverage, mappingNotes}`；另有程序化 per-element 提取 `_extractElementStyles` → `elementStyleMap: {elementId→{cssProp:value}}`（不经 LLM、最精确）。本轮失败模式：LLM 输出 JSON 截断（Unterminated string）→ robustJSONParse 全策略失败 → 兜底用 per-element 真值合成最小主题变量。

## mv 任务 "[microcode-engineer]" 日志之谜（17:36 审计结论）
- **路由本身正确**：`mc-component-graph-vue3.js:514` 固定 `Vue3Engineer`（无 microcode fallback）；`mc-component-graph-phase2.js:737` 固定 `MicrocodeEngineer`。两图互不回落。
- 真相 = 标签泄露 + 并行任务交错：
  1. vue3-engineer 有自己的模块 logger（`name:'vue3-engineer'`），但继承自 microcode-engineer 的 `_generateSingleChunk`/网关调用等用的是 **microcode-engineer.js 模块级 logger** → mv 任务所有分块请求日志都打 `[microcode-engineer]` 前缀（纯标签误报，产物管线正确）。
  2. 截图中「微码资源import注入完成（门禁前）」「跨文件一致性校验」等 microcode generateCode 专属行（mc-engineer.js:1442）属于**并行运行的 mc-max 任务**（时间戳 17:14:56 与 mc 失败链完全吻合：mappingCount:198 → 孤儿 `*` 编译失败都是 mc 的 index.vue），不是 mv 跑了微码管线。
- 遗留（P2，不阻断产物）：跨类日志标签统一（inherited 方法改用 this.logger 或注入 logger name）。

## Unexpected template string 事故与修复（17:39）
- 现象：17:37 任务 mc-1787564243408 创建即失败，`SyntaxError: Unexpected template string` 栈在 `compileSourceTextModule`——**Node ESM 运行时动态 import 编译源码 .js 失败**，非 Vue SFC 问题。
- 根因：我给 `_fixOrphanJsdocLines` 写的方法级 **JSDoc 注释文本里含字面量 `*/`**（"孤立 \`*/\` 行"），把块注释提前终止，其后文字变成代码。nest build 只编 .ts，ai-engine .js 作为 assets 拷贝不做语法检查 → 构建通过但运行时炸。
- 修复：注释改写为不含 `*/` 序列的措辞（"孤立注释结束符行"）。
- 教训（重申既有约定）：**改 ai-engine 的 .js 后必须跑 acorn `sourceType:'module'` 全量校验**，仅靠 nest build 通过不等于运行时可用；JSDoc/块注释中禁止出现字面量 `*/`。已用 acorn 校验 microcode-engineer / vue3-engineer / 两个 graph 全部 parse OK，dist 模块动态 import OK，服务已重启（HTTP 401 存活）。

## 生成过程可见性：分块级实时反馈（17:50+）
- 用户痛点：心跳「模型处理中（已等待 Xs）」无上下文；代码 Tab 文件一直为空直到全部生成完。
- 基建现成：前端 TaskDetail `applyFileLifecycle` 已维护 `fileStates` 逐文件状态图；`onFilesReady → taskCodeSnapshotService.createCandidate` 每次发布新 revision 候选快照。缺的只是 mc/vue3 分块层接线。
- **三项改动**（mc 与 vue3 两路径）：
  1. runChunk 分块开始发 `fileLifecycle{phase:'modeling', files:[working]}`，完成发 `phase:'writing', files:[modified]` —— 文件逐个点亮。
  2. `_generateSingleChunk` 心跳注入：onProgress 包装器把「⏳ 模型处理中...（已等待 Xs）」改写为「⏳ 正在生成【<分块名/文件名>】（已等待 Xs）」（ai-request-gateway.js:420 的心跳消息按 level:'log' 流经此处，无需改网关）。
  3. **增量候选快照**：每分块落盘后即调 `onFilesReady({files: {...allFiles 累计, ...本块产物}})` —— 代码 Tab 逐文件出现真实代码；失败中断时最后快照保留（与草稿横幅语义一致）。execute→generateCode 补传 `onFilesReady`（此前只在最终完整组发布时使用）。
- 关于「慢」的答复：等待本身主要是模型生成时长（子组件块 105~317s 实测），已通过 SubcomponentContract 瘦身降低输入；心跳现在带分块名，用户可看到具体进展而非无意义等待。
- 验证：acorn parse OK ×2、build 通过、dist 动态 import OK ×2、PID 71581、HTTP 401。前端无需改动（fileStates/候选快照消费链路已在）。

## CODE-003 自动修复双重前缀 bug 修复（17:57+）
- 现象：mc-max 17:43 重跑「成功但预览不可用」——RUNTIME-004 render-error + L0-B 报 `CODE-003 common.less 21 个 class 缺 c-monitor 前缀: .c-mc-max-...-c-monitor-root`。
- 根因（接口契约不一致）：
  - L0-B `checkCodeStructure` **正确地**以 `declare.componentId`（如 `c-monitor`）为期望前缀（validators/code-structure-validator.js:364-375 显式读 declare）。
  - **但** `autoFixPrefixViolations` 调用点（microcode-engineer.js:5345）传的是 `componentName`（长 sessionId 派生名 `c-mc-max-1787560119416-c72885db`），不是 `declare.componentId`。
  - LLM 写的正确类 `.c-monitor-root` 被 autoFix 用错误前缀二次加工为 `.c-mc-max-...-c-monitor-root` → .vue/.less 错配 → 预览 render-error。
  - 副因：autoFix 的 .vue 正则 `\bclassName\b` 无负向后顾，会在 `c-monitor-root` 中匹配到裸名 `root` 二次注入。
- **两项修复**：
  1. microcode-engineer.js:5339-5363 `autoFixPrefixViolations` 调用前先解析 `mergedFiles['declare.json'].componentId`（缺失则回退 normalized componentName），与 L0-B 检查器同源。
  2. code-structure-validator.js:720-730 两条 .vue 正则加 `(?<![a-z0-9-])` 负向后顾，防御在已带前缀类中二次匹配。
- 验证：acorn parse OK ×2、build 通过、PID 72321、HTTP 401。下次重跑 mc-max/mv-max 预览应正常。
- 另：mv-max 17:43 任务的失败是**我自己造成的**——17:51 部署文件生命周期改动时重启后端，中断了在飞任务。草稿已存可下载；此后再改后端会避开用户重跑窗口（如有可能，先告知）。

## 任务完成后预览不刷新修复（18:01）
- 根因（TaskDetail.vue）：SSE `task-status` 终态分支只 patch `status` 就关连接——预览 iframe 的放行条件 `artifactReady` 与 `previewKey` 都没更新，完成时 iframe 不挂载/不刷新。
- 修复：`task-status` 与 `error` 两个终态分支关 SSE 后 `loadTask().finally(() => previewKey.value++)`——重拉任务实体（API 返回 artifactReady/endTime/fileCount），previewKey++ 强制 iframe 重挂载；失败但草稿可用时同样放行预览。
- 验证：grep 命中两处；vue-tsc 对 TaskDetail 仅剩 L999 既有错误（FailureDiagnosisCard props，与本次无关）；vite dev HMR 自动生效。

## vue3 任务日志出现 [microcode-engineer] 之谜（18:14 复查+修复）
- **路由正确**：`mc-component-graph-vue3.js:514` 实例化 `Vue3Engineer` 并 `engineer.execute({...})`；`Vue3Engineer.generateCode` (L827) 有独立的 runFullGeneration/3-segment/子组件循环，**不调** `super.generateCode`。产物是纯 vue3（验证：代码 Tab 不含 $mcComponentBuilder/base-panel）。
- **两个真因**（**不是**路由错误，全是标签问题）：
  1. **vue3 graph 的代码生成节点名字就叫 `microcode-engineer`**（L492，节点名仅内部 ID，但 header 日志说"节点: Microcode Engineer - 生成代码"）—— 历史遗留命名错误。
  2. **继承式 logger 标签泄露**：Vue3Engineer extends MicrocodeEngineer，chunk 级日志 `_generateSingleChunk` / runChunk / `_buildSubcomponentScopedInput` / `_generateIndexVue` 等**全部用微码模块级 logger**（microcode-engineer.js:31 `const logger = createLogger({name:'microcode-engineer'})`）→ 100+ 行 chunk 日志前缀都是 `[microcode-engineer]`，且"index.vue 拼装完成""type-aware shared 裁剪""🧩 子组件分块事实裁剪"等微码专属字符串经继承方法被 vue3 任务触发。
- **最小安全修复**（不破坏既有 7 处边/条件路由对节点名 `microcode-engineer` 的引用）：
  1. mc-component-graph-vue3.js:490-496 header 日志 "节点: Microcode Engineer" → "节点: Vue3 Engineer"，并加注释说明节点名仅内部 ID。
  2. vue3-engineer.js:827 `generateCode` 入口加 `🟢 [vue3-engineer] 开始生成 Vue3 组件代码` 明确标识。
- **遗留（P2，技术债）**：要把 chunk 级 `[microcode-engineer]` 前缀和"index.vue 拼装完成"等字符串也改成 vue3 正确标签，需把微码 5000+ 行的模块级 `const logger` 改造为实例 `this.logger`（构造函数注入 name，Vue3Engineer override），是较大机械重构，未做（风险/收益评估留给后续）。
- 验证：acorn parse OK ×2、build 通过、PID 75537、HTTP 401。











## 组件推送到公司 GitLab（平台内一键推送）实现 + 实测

### 背景
用户问「生成的组件如何上传到公司级 GitLab 某个仓库、分支」。查证：前端 demo 页早有「推送到 GitLab」按钮（调 POST /api/component/push-to-gitlab），但后端接口从未实现，且 `git.push` flag 默认关。本次补齐全链路。

### 改动清单
| # | 文件 | 内容 |
|---|---|---|
| 1 | backend-node/src/component/gitlab-push.service.ts（新增） | GitlabPushService：authorizeWorkspaceComponent 鉴权 → 双 workspace 定位产物（根 workspace 优先、frontend/workspace 兜底）→ 递归收集文件（跳过 .checkpoint/.mc-gen/.cache/quality-backup/_figma-size.json）→ 解析 repoUrl（ssh:// / https:// / git@host:path）→ GitLab Commits API 推送，仓库内以 {componentId}/ 为前缀，二进制 base64 |
| 2 | component.controller.ts | 新增 POST push-to-gitlab（必须放在 @Post(':componentId') **之前**，否则被参数路由吞 → 404 组件不存在） |
| 3 | component.module.ts | 注册 GitlabPushService |
| 4 | frontend/src/config/feature-flags.js | 'git.push' enabled: false → true |
| 5 | start-node.js | 补 DEV_AUTO_LOGIN=true（本地联调免门户） |

### 公司 GitLab 实测（opensource/mvgo/components，id 6980，internal）
- ✅ 推 main（已存在分支 + create）：33 文件，commit 9a84c11b
- ✅ 自动创建含斜杠分支 feat/test-auto-branch：commit 786530a1
- ✅ 同分支二次推送（update action）：commit a4c02161
- ✅ 二进制 png 字节一致（197215 bytes，archive 下载 cmp 验证）；文本 index.vue 正常
- 测试分支已删，main 保留测试组件 mv-1785590539039-fe143a0f

### ⚠️ 公司实例 GitLab API 大坑（scm.microvideo.cn）
- **`%2F` 编码的多级路径统一 404**：projects/opensource%2Fmvgo%2Fcomponents、groups/.../projects、repository/branches/feat%2Fxxx、repository/files/...%2F... 全部 404（数字 ID 或 query 参数正常）
- 解法：项目 ID 用「快路径 projects/{encoded} → 404 则 groups?search=组名 → 组 id → groups/{id}/projects?search=仓库名 → 精确匹配 path_with_namespace」；分支存在性用 `repository/branches?search={branch}` query 判断（禁路径端点）
- glab repo create 嵌套组（opensource/mvgo/xxx）CLI 会 404，改用 `glab api --method POST projects -f name=.. -f namespace_id=<组id>`（组 id 用 `glab api "groups?search=mvgo"` 拿）
- 实测方法：start-node.js 已开 DEV_AUTO_LOGIN → 任意 Token 头建 dev-local 身份 → 直接插 DB 造组件记录（creatorId=dev 用户、groupId=目录名 ObjectId、target=vue3）→ curl 调接口

### 使用方式
生成页 → demo 预览页「推送到 GitLab」→ 填仓库URL（https://scm.microvideo.cn/gitlab/组/仓库.git）+ 分支 + PAT → 提交信息自动带公司规范字段。分支不存在自动创建，二进制自动 base64。

## vue3 产物微码 API 兜底修复（19:05+）
- 现象：mv-max-1787566005737 完成但预览 RUNTIME-004 render-error → 截图 "Cannot read properties of undefined (reading 'title')"。
- 根因（**真路由问题，不是标签问题**）：Vue3Engineer 继承 MicrocodeEngineer.buildCodePrompt 但**未重写**，prompt 仍指示模型使用微码专属 API `$mcComponentBuilder()` → 解构出 `componentProps/runtimeBuilder/componentApi`。产物 index.vue 出现 `<h3>{{ componentProps.title || '微码组件示例' }}</h3>` 这种微码风格模板，但 vue3 预览环境**未注入** $mcComponentBuilder 全局变量 → componentProps undefined → 渲染崩溃。
- **最小兜底修复**（治本需 vue3-engineer 独立 buildCodePrompt，大改不在本次范围）：
  - microcode-engineer.js index.vue 自动修复区：当 `this.componentType === 'vue3'` 且产物含 `$mcComponentBuilder()`，统一替换为 `({ componentProps: {}, businessProps: {}, runtimeBuilder: null, componentApi: null })` 安全兜底；`runtimeBuilder?.publishEvent` 替换为 no-op。`{{ componentProps.title }}` 仍会因 undefined 用 `|| '微码组件示例'` 兜底（或后续换成 vue3 的 `defineProps`）。
  - L5225 $mcComponentBuilder 验证告警对 vue3 跳过（避免误报）。
- 验证：acorn parse OK、build 通过、PID 82408、HTTP 401。下次重跑 mv 任务预览应不再 render-error；产物代码 Tab 内仍会出现 `componentProps` 这种微码风格痕迹（治本需 vue3 独立 prompt + defineProps 范式，本次未做）。

## vue3 生成治本：componentType 感知的 shared 规范（19:12+，已构建未重启）
- 用户要求治本但暂不重启。**根因链**：Vue3Engineer 继承 MicrocodeEngineer.buildCodePrompt 未重写 → `buildSharedSections` 无条件加载 `mc-builder.md`（教模型用 $mcComponentBuilder/componentProps/runtimeBuilder）+ `base-panel.md` → 产物带微码 API → vue3 预览环境未注入全局变量 → `Cannot read properties of undefined (reading 'title')`。
- **六项改动**（全部 componentType 感知，微码路径行为不变）：
  1. 新增 `engineer/shared/vue3-builder.md`：3.1️⃣ Vue3 脚本范式（defineProps/defineEmits/ref/onBeforeUnmount 清理；❌禁止清单含 $mcComponentBuilder/componentProps/runtimeBuilder/base-panel/declare.json 负例）+ 4️⃣ 样式文件结构（与 mc-builder 同构，.c- 前缀 + 三 mixin 主题）。
  2. prompt-loader.js `buildSharedSections` 接受 `componentType`：vue3 时 mc-builder.md→vue3-builder.md、剔除 base-panel.md 与 base-panel-slots.md（vue3 真实还原外壳）。
  3. microcode-engineer.js `buildCodePrompt` 传 `componentType: this.componentType`。
  4. `_buildChecklistByType` vue3 分支：主组件自检替换为 Vue3 范式/外壳真实还原/生命周期清理（去掉 $mcComponentBuilder/onload/panelKey/declare.json 项）。
  5. script 未闭合兜底：vue3 只补 `</script>`，不注入微码样板。
  6. declare-json.md 规范段对 vue3 跳过（vue3 不产出 declare.json，省 token）。
- **验证**：acorn parse OK ×2；`buildSharedSections` 实测——vue3 分支 17469 chars（含 Vue3 脚本范式/无 mc-builder/无 base-panel，比 mc 分支省 ~2k chars），mc 分支行为不变（19456 chars）；残留微码字眼仅存在于 vue3-builder.md 的禁止负例中，其余 shared md 全干净；build 通过、dist 已含 vue3-builder.md。
- **服务未重启**（用户要求，PID 82408 仍是旧代码）——重启后生效；重启前 mv 任务仍会产出微码风格代码（有 19:05 的兜底替换保预览不崩）。

## 任务监控 FAB 残留已取消/删除任务修复（19:20+，纯前端）
- 现象：用户取消并删除一个任务后，FAB 仍显示「1/2 运行」，被误解为「还剩 2 个任务」。
- 真因：
  1. `WorkflowMonitor.vue:387` `tasks.value = resp.data.tasks || []` 未过滤终态——cancelled/deleted/completed/failed 记录仍在内存中，导致 `totalCount > 0` 让 FAB 保持 stats 形态、误以为还有任务。
  2. FAB 顶部显示 `{{runningCount}}/{{maxConcurrent}}`，分母 `maxConcurrent` 是后端**并发槽位上限**而非「任务数」，容易被误读为「还有 N 个任务」。
- 修复（前端 HMR 自动生效）：
  1. `pollTasks` 过滤 `status !== 'cancelled/deleted/completed/failed'` 后再写 `tasks.value`——已取消/删除的任务从监控列表中清除，FAB 回到正确的 stats 形态或 idle 形态。
  2. FAB 顶部去掉分母，只显示「{{ runningCount }} 运行」；容量信息保留在面板顶 chip「{{runningCount}}/{{maxConcurrent}} 运行」和 tooltip 中。
- 验证：vue-tsc 无 WorkflowMonitor 错误。无需重启后端。

## 双 c- 前缀（c-c-monitor-root）根因与修复（19:23+，已构建未重启）
- 现象：mc-max-1787565993770 生成中产物 class="c-c-monitor-root"（双 c-）。
- 证据：code-chunks/2-package_index.vue 的 **LLM 原始输出**就含双前缀（非后处理引入）；componentName 传入为 sessionId（mc-max-...），但模板分块上下文含 declare.json（componentId=c-monitor，本身已带 c-）→ 模型把 componentId 当语义名再套「.c- 前缀」规则 → c-c-monitor-。
- 修复（两层）：
  1. **确定性坍缩**：`_collapseDoubleCPrefixInVue`（class/:class 属性值内 `\bc-c-` → `c-`，单双引号两式；不碰非 class 文本，避免误伤 mc-c- 子串）+ `_collapseDoubleCPrefixInLess`（`.c-c-` → `.c-`）；接入最终 SFC 校验循环（.vue）与 LESS 自愈循环（.less/.css）。单测 5 例全过（双前缀坍缩/正常类不动/非 class 文本不动/less 坍缩/less 正常不动）。
  2. **prompt 防御**：`_buildChunkMiddle` class 命名约束加「🚫 禁止双 c- 前缀」条款（componentId 已含 c- 时直接 .c-monitor-xxx，附正误示例）。
- 验证：acorn parse OK、build 通过、dist grep 3 处命中。**服务未重启**（PID 82408 旧代码，重启后生效）。

## 任务中文名（displayName）展示链路实施（19:29+，已构建未重启）
- 需求：任务信息展示组件中文名（大多数组件可识别）而非 sessionId 风格 componentName。
- **设计**：新增独立 `displayName` 字段（**不覆盖 componentName**——retry 参数提取/GitLab 推送/输出目录逻辑依赖它），两级回写：
  1. **早提取**（任务启动 ~15s）：两图 figma-connector 完成事件携带 `displayName`（根节点名剥 cp-/mc-/mv-/page- 前缀，仅接受含 CJK）→ phase2.service onProgress 回写任务实体 → 监控浮窗尽早显示中文。
  2. **权威回写**（生成完成）：phase2.service 从磁盘 `outputPath/declare.json` 读 componentName（引擎已归一化纯中文），含 CJK 才覆盖。
- **改动清单**：
  - tasks.service.ts：Task 接口 + `displayName?` 字段（L109 后，带用途注释）；getTask 序列化输出 displayName；getAllTasks 用 `...task` 展开自动带出。
  - phase2.service.ts：onProgress 处理 `data.displayName` 回写（去重判断 + 日志）；ensurePreviewImage 后读盘 declare.json 权威回写（fail-open）。
  - 两图 figma-connector：完成事件附 displayName（CJK 守卫）。
  - 前端（HMR 生效）：WorkflowMonitor `getTaskDisplayName` 优先 displayName；TaskCenter 搜索（displayName+componentName 双字段）/排序/componentInfo/删除确认；TaskDetail 面包屑 + 身份卡 h2。
- **顺带修复**：gitlab-push.service.ts:199 `defaultMessage` 未定义（全量重建暴露的存量错误，`commit?.title || commitMessage || defaultMessage` → `commit?.title || resolvedCommitMessage`）。
- 验证：前端 vue-tsc 仅剩 2 个既有错误（TaskCenter L68 过滤类型 / TaskDetail L999 组件 props，均非本次引入）；后端 build 通过；dist grep displayName：phase2.service 7 处 / tasks.service 1 处。**服务未重启**（PID 82408），重启后新任务生效；前端 HMR 已生效（旧任务无 displayName 自动回退 componentName）。

## mc-max-1787565993770 失败复盘 + style 分块专属超时（19:36+，已构建未重启）
- 本次失败点：**style 拆分本身已生效**（「主题变量」块独立成功；心跳带分块名也生效：「正在生成【主题变量】」「正在生成【子组件 xxx】」），倒在「业务样式」（common.less）块——双尝试各 180s 超时耗尽。
- 根因：common.less 是**输出型分块**（硬约束要求为 index + 全部子组件的完整 class 清单逐个生成规则），输出 token 量大，180s 预算按输入型分块标定不够（子组件块历史有 335s 完成先例；qwen3.7-plus 慢）。
- 修复：`_generateSingleChunk` 中 `segmentType==='style'` 分块超时放宽为 max(基础, env STYLE_CHUNK_TIMEOUT_MS 默认 300000)；invokeWithTimeout 第三参与 options.requestTimeoutMs 同步使用（网关默认 300000 可承载，无钳制）。其余分块仍 180s。
- 验证：acorn parse OK、build 通过、dist 命中 2 处。**未重启**（PID 82408）。
- 待重启累计变更：vue3 治本（componentType 感知 shared）、双 c- 坍缩、displayName 中文名、style 专属超时、GitLab push TS 修复。

## 任务日志「数据采集阶段冻结、刷新才出现」根因与修复（19:50+，已构建未重启）
- 根因（progress.service.ts 两层缺陷）：
  1. **sendLog 不走 recordEvent（无 seq/事件历史）**：SSE 断线重连时浏览器带 Last-Event-ID，后端只回放有 seq 的事件（code-snapshot 走了 recordEvent，日志行没有）→ **断线窗口内日志永久丢失**（不在 eventHistory、无 buffer 兜底）→ 前端日志冻结；刷新走 task.progress 全量重建才显示。数据采集阶段事件密集、连接经 vite 代理易空闲断开，故高发于此阶段。
  2. **timestamp 不对称**：持久化 addProgress 不带 timestamp、SSE 广播才加 → 前端 buildReplayKey（含 timestamp）两侧永远对不上（replay 去重失效，重复靠 appendFoldedLog 折叠掩盖）；且历史日志时间被 createTaskLogEntry 兜底成「打开页面时刻」。
- 修复：
  1. sendLog 统一走 recordEvent（带 seq + eventHistory），断线重连由 Last-Event-ID 机制补发（与 sendCodeSnapshot 同模式）；持久化与广播同一 timestamp。
  2. sendProgress 持久化与广播统一使用同一 `_progressTimestamp`（progressData.timestamp ?? Date.now()）。
- 验证：build 通过、dist grep recordEvent 命中 2 处（sendCodeSnapshot + sendLog）。**未重启**（PID 88304 在跑用户重试任务，等任务结束或用户指示再重启）。
- 前端无需改动：重连回放的日志带 seq 正常进 onmessage；replay 去重随 timestamp 对称恢复工作。

## GitLab 推送弹窗项目/分支自动补全（19:50+，已构建未重启）
- 需求：推送弹窗的项目/分支字段支持手动输入 **和** 从下拉选择，两者数据双向同步
- **后端代理**（避免前端 CORS）：
  - `POST /api/component/gitlab-projects`：转发 `GET /projects?membership=true`，返回当前 Token 用户参与的项目列表（id/name/pathWithNamespace/httpUrlToRepo）
  - `POST /api/component/gitlab-branches`：先 `parseRepoUrl` + `resolveProjectId`，再转发 `GET /repository/branches`，返回分支列表（含 isDefault 标记）
  - 两个端点均放在 `@Post('push-to-gitlab')` 之前
- **前端改造**（GitLabPushModal.vue）：
  - 项目/分支字段从 `a-input` 改为 `a-auto-complete`（可手动输入也可从下拉选择）
  - "加载项目"按钮：填 PAT 后可点击，调用后端代理拉取项目列表，填充到下拉选项
  - "加载分支"按钮：填项目 URL + PAT 后可点击，拉取分支列表；**选择项目后自动触发加载分支**
  - 下拉选项展示项目名 + 路径（灰色小字），分支列表标记默认分支（绿色 `(默认)` 标签）
  - 手动输入 URL 仍有效（auto-complete 不限制非列表值），localStorage 缓存行为不变
- 验证：前端 `npm run build` 通过、后端 `npm run build` 通过。**未重启**（用户要求）

## 日志链路同族缺口审计（19:55+）
- 全链路审计 progress.service.ts 各通道：
  - ✅ sendLog：已修（recordEvent + seq，断线可回放）；logger.js:146 桥接确认**所有 [INFO]/[WARN]/[ERROR] 日志行都走 sendLog**——修复覆盖全部日志，不只网关心跳。
  - ✅ sendComplete/sendError：终态事件若在断线时发出，重连后由 connect 分支的 isReconnect + 终态补推覆盖（L89-124）。
  - ✅ sendFileLifecycle：瞬态设计（不持久化），断线丢失可接受。
  - 🟡→✅ **同族缺口已修**：`maxHistoryPerSession=120`，长断线导致事件窗口溢出时后端发 `snapshot-resync`，但前端该分支只拉 loadCodeSnapshot、**不重建日志**（同样会冻结）。已补：snapshot-resync 分支在 running 态同时 `void loadTask()`（从 task.progress 全量重建日志）。前端 HMR 生效，vue-tsc 仅剩 L1006 既有错误。
  - 残余边界：sendProgress 走 buffer 恢复（isReconnect 时 clearBuffer 补发），多标签页先到先清 buffer 的极端场景可能少收 progress 阶段事件——影响小（阶段状态可由 loadTask 修复），暂不处理。

## 候选快照缺标准样式文件 bug 修复（20:00+，已构建 + 手动补 7ada59d5 快照）
- 现象：mc-max-1787565993770 完成后预览失败 `找不到 LESS 文件: resources/styles/index.less` → `$mcComponentBuilder is not a function`。
- 根因（合并 bug）：
  1. 「处理一个出现一个代码」的 runChunk 级 onFilesReady 触发在标准文件（index.less/dark.less/light.less/component.js/declare.js）落盘**之前**（执行顺序：engineer execute() 末尾 L5894-5926 才写 standardFiles），chunk 级快照天然少这些文件。
  2. 工程师 execute() 末尾的最终 onFilesReady（L6007）虽含 standardFiles，但**最终快照不一定是最新**——candidate 指针被 chunk 级覆盖（最后 chunk 完成时已写入），最终 onFilesReady 反而成为「老版本」或与 chunk 级内容重复，pointer 指向最后写入的那个（chunk 级，缺标准文件）。
- 治本修复（phase2.service.ts onFilesReady）：**通用兜底从磁盘 outputPath 递归补全快照**——images 已有此模式（仅扫一层），扩展为递归 mergeFromDisk 扫 resources/images + resources/styles（含 themes/ 子目录），并补 component.js + declare.js。engineer 无论怎么调 onFilesReady，磁盘在的就一定在快照里。
- 即时补救：手动把重试产物（mc-1787571858004...）的 resources/styles/、component.js、declare.js 复制到原任务最新 revision（r-3365abc7），更新 manifest：47 文件 / 6 styles。**预览刷新即可**。
- 验证：build 通过、dist 命中 mergeFromDisk ×4；该任务预览路径已修。下次新任务自动走通用兜底，无须手动补。

## $mcComponentBuilder is not a function 真凶（20:06+，前端 HMR 已生效）
- 现象：补齐快照 styles 后预览仍报 `$mcComponentBuilder is not a function`。
- 根因（loadVue3Runtime.js:308 前端注入 bug）：`common.js` 只有**具名导出**（$mcComponentBuilder/$createMcDeclare/$runtimeBuilder/$mcCssBuilder/$getMcDefaultConfig），**没有 default**。旧写法 `globalThis.$mcComponentBuilder = common.default || common` 把整个 module **namespace 对象**赋给全局 → 组件调用 `$mcComponentBuilder()` 即 TypeError。之前 mc 预览一直有 RUNTIME-004 render-error，此 bug 是长期存在的共因之一。
- 修复：遍历 namespace 把所有 `$` 前缀具名函数逐一注入全局（幂等不覆盖），另加兜底确保 $mcComponentBuilder 为函数。**顺带修复 declare.js 用的 $createMcDeclare 等全局**。
- 验证：vue-tsc 无 loadVue3Runtime 错误；HMR 生效，刷新预览即可。

## 视觉保真四项根因修复（20:20+，已构建未重启，vue3/微码双端共享）
- 背景：mc-max-1787565993770 成功但 5 处视觉差异（header 数据错位/分类名臆造/卡片背景色丢失/结构扁平/数字臆造）。真实数据审计结论：**真值全在 Figma（figmaStyleTree 56KB 含全部文字+每节点颜色），断点全在传递层**。
- 四个确凿根因（代码证据）：
  1. `_buildCommonLessChunkPrompt` 互斥逻辑：elementStyleMap 非空（哪怕只有 flex 属性、0 颜色）→ figmaStyleTree 被抑制 → 模型无颜色真值 → 白卡。
  2. `_extractElementStyles` 只转 vision LLM 报告的 style 字段，vision 没报颜色 elementStyleMap 就没颜色（本轮 29 元素全是 display:flex）。
  3. 内容/结构靠 vision（覆盖率 52%）传递，无确定性文字清单 → CategoryStats 分块臆造「网络/安全/存储/计算设备」（DeviceList 分块反而对了——子组件级事实传递不一致）。
  4. `findPanelTitleNode` 两级匹配（name 含 title/标题、y<100）对语义命名（`header` GROUP）全失败 → validatedSlots 空；且 validatedSlots 只打日志**从未回写 state.headerSlots**（第二层断点）。
- 修复（全在双端共享层，构建已过、未重启）：
  1. prompt-loader `buildTextInventorySection`：TEXT characters 去重保序 cap150 + 文字铁律（逐字使用/冲突以清单为准/禁编数字）→ figma.md 模板 `{{TEXT_INVENTORY_SECTION}}`。实测 498 chars 含全部 6 分类+68562+3/3740+设备名。
  2. prompt-loader `buildStructureMarkersSection`：`@antd/tab`→tabs 结构提示；`header` GROUP→头部文字+双端指引（微码 header 插槽/vue3 标题栏同行，禁入 body）；`slot-*` 分区 → `{{STRUCTURE_MARKERS_SECTION}}`。
  3. microcode-engineer `_extractFigmaColorEssentials`：fills/strokes/effects/圆角/字号紧凑单行表（cap 220 行，实测 16.9KB/221 行含每节点真值色）；common.less 分块互斥→互补，始终注入颜色真值表+「不同卡片不同色调必须分别使用」铁律。
  4. figma-connector `findPanelTitleNode` 第三级 fallback：name 匹配 header 的 GROUP/FRAME 内最大字号 TEXT。
- scopedInput 联动：子组件分块的文字清单基于子树自动聚焦（更省 token）；匹配失败回退全量也有全量清单兜底。
- 验证：acorn ×3 OK；真实数据实测文字清单/结构标记/颜色表全部命中；build 通过、dist 符号确认（prompt-loader ×2 / engineer ×2 / connector ×1 / figma.md ×1）。**未重启**——重启后新任务生效。

## headerSlots 契约回写闭环（20:35+，已构建未重启）
- 完整断链 5 层（代码证据）：① vision 漏识别 headerSlots=[]；② validator 只「验证」vision 候选不推导（候选空则 validatedSlots 恒空）；③ validatedSlots 只打日志从不回写 state.headerSlots；④ HEADER_SLOTS mdVars 无任何模板消费（死变量）；⑤ base-panel-slots.md 的 {{HEADER_SLOTS_TITLE_LEFT/TITLE_RIGHT/HEADER_RIGHT}} 三变量从未填充（渲染字面量）。declare.json 无 headerSlots 字段——消费终点就是 prompt。
- 修复（5 处，vue3/微码双端）：
  1. header-slot-validator 新增 `deriveHeaderSlotsFromFigma`：从 Figma header 容器确定性推导**契约形态** `{slotType, elementType, content, figmaNodeId}`（子树含数字→statistic/header-right；tab/switch 命名→tab；icon→title-left；纯小字→label/title-right；*注释与标题排除）；主流程产出 `contractSlots`（validated 转形态+derived 去重合并）；vue3 分支同样推导（仅记录，无 prompt 副作用）。
  2. **标题判定修正**（关键实测发现）：大屏设计数值字号(20px)常大于标题(16px)，按最大字号找标题会把「28」当标题→标题泄漏+配对丢失。改为 **x 最靠左的文本为标题**（面板标题惯例居左）——figma-connector 第三级 fallback 与推导器两处同步。
  3. 两图（phase2/vue3）回写：contractSlots merge 进 result.headerSlots（后续 state 赋值与 _visualParserCache 自动生效，去重 by figmaNodeId/content）。
  4. prompt-loader buildSharedSections 填充三细分变量：按 slotType（连字符/下划线归一）分组渲染 content 清单，无分配时显示兜底提示。
  5. hasHeaderSlots 非空→base-panel-slots.md 加载（已有），vue3 分支不加载（已有）。
- 真实数据实测：推导器 3 statistic 全对（设备类型 28/设备总数 68562/完好率 98%），无注释泄漏、无标题泄漏；base-panel-slots.md 渲染 #header_right 栏正确显示三组指标、无 {{}} 字面量残留；vue3 分支不加载 slots 表 ✓。
- 验证：acorn ×5 OK、build 通过、dist 符号确认（validator ×8/两图各 ×3/loader ×1）。**未重启**（13030 当前 PID 2763 为用户自行重启的旧构建，本轮改动待下次重启生效）。

## 后端重启加载视觉保真两批修复（20:45）
- 确认 dist 含最新符号（deriveHeaderSlotsFromFigma ×3 / buildTextInventorySection ×2）后重启：旧 PID 2763 终止，新 PID 5803 detached 启动，13030 监听、HTTP 401 存活。
- 本次重启生效：20:20 视觉保真四项（文字清单/结构标记/颜色真值表/findPanelTitleNode fallback）+ 20:35 headerSlots 契约回写闭环。
- 另澄清：用户 20:39 截图看的 mc-1787571858004 是 19:58 完成的老任务页面（日志确认该 sessionId 无重跑活动），非缓存回放；20:34 实际触发的是新任务 1787574672211。验证修复需跑全新任务。

## Figma 限流器排队超时根因修复 + 重启（20:50-21:05）
- 现象：`Figma 限流器等待超时（已等 20s，冷却剩余 0s，活跃 1/1）`，mc-1787575622774 任务失败（20:47:22 日志实锤，同时刻另一任务在跑）。
- 根因：**并行任务共享单并发槽（minInterval 7s / maxConcurrency 1）**，acquire 默认 20s 超时是 preview 接口防 nginx 504 的设计，对后台生成任务必超时（单请求持槽 connector 90s / client 30s）。非 429、非泄漏。
- 排除项：模拟测试证明 429 递归 acquire 不自锁（async finally 时序：return promise 求值后 finally 先执行、槽已释放）。
- 修复三项：①生成管线两处 acquire 20s→120s（env FIGMA_ACQUIRE_TIMEOUT_MS 可调，figma-client + figma-connector）；②429 重试重构 release 提前（不依赖 async 时序）；③限流器泄漏自愈（持槽>5min 强制重置）。
- 21:05 重建重启：新 PID 8007，13030 监听、HTTP 401，dist FIGMA_ACQUIRE_TIMEOUT_MS 确认。

## mc-max-1787574672211 失败复盘 + 瘦身关联增强（21:20+，已构建待重启）
- 复盘（文件日志证据）：任务倒在两个子组件块并行 180s×2 超时。好消息：findPanelTitleNode 修复生效（找到标题节点"流量监测"）。坏消息：**SubcomponentContract 未命中**——裁剪报告 `keywords:3, esmKeys:"1/12"` 无 figmaSubtree → TotalTraffic 块仍 200,849 chars（83,688 tokens）全量 prompt。
- 根因：子组件文件名是 LLM 起的英文名（TotalTraffic），Figma 子树名是中文（slot-当日总流量），英文 tagWords ↔ 中文节点名永远匹配不上。subComponentPlan.effectiveSections 里有中文 title（当日总流量/车型分布/流量预测）但没进关键词集。
- 修复两项：
  1. **section 中文标题关联**：tagWords ↔ section.id/responsibility 英文词匹配（section-daily-total ↔ 'total'）→ 命中后引入 section.title 中文 → Figma 子树匹配。实测关联命中 2/3（TotalTraffic→当日总流量 ✓、VehicleTypeDistribution→车型分布 ✓、TrafficForecast↔flow-prediction 词不同义未关联）。
  2. **usage 摘要 CJK 补充**：usage ±12 行内的中文片段（模型常写 title="流量预测"）也进关键词，兜底 TrafficForecast 类。
  3. **component 分块超时放宽**：SUBCOMPONENT_CHUNK_TIMEOUT_MS 默认 300s（与 style 同理：83k tokens 输入 + 全量 SFC 输出，180s 实测不够）。
- 验证：acorn OK、build 通过、dist 符号 ×3、关联模拟实测命中。**未重启**（21:22 仍有任务在 LLM 调用中，避免打断；用户指示后执行）。

## vue3 产物背景图/icon 失效 + 尺寸失真根因修复（21:30+，产物已改可立即预览；后端已构建未重启）
- 现象：mv-max-1787576934056（环境监测）生成完成但背景图/icon 全无、组件尺寸非 Figma 原比例（原 420×186）。
- 根因（三层）：
  1. **injectVue3ResourceUrls 历史设计反了**：它把静态 import 特意转成 `const x = new URL('...', import.meta.url).href`（假设正式 Vite 构建支持），但预览走 vue3-sfc-loader 沙箱，import.meta 不可用（loadVue3Runtime L485 有替换但只覆盖严格 `import.meta` 拼写）。
  2. **`import .meta` 空格变体**：LLM 偶发产出 `import .meta.url`，前端替换正则 `/\bimport\.meta\b/g` 匹配不到 → 沙箱求值失败。
  3. **根容器无尺寸锚定**：LLM 写 width/height:100%，父容器无显式高度时塌陷/被外层撑大。
- 修复：
  - **A（产物直改，立即可见，无需重启）**：4 个 .vue 的 new URL 全改静态 import（`import bg1 from '../resources/images/bg-7880.png'`）——这正是 loadVue3Runtime handleModule BINARY_EXTS 官方支持的通道；根容器改 width:420px; height:186px; max-width:100%; max-height:100vh。同步三份副本（frontend/backend workspace + temp 源）。
  - **B（治本，已构建未重启）**：①resource-import-guard injectVue3ResourceUrls 停止「import→new URL」反向转换，保留静态 import；兼容清理历史 new URL 声明（含空格变体）统一转静态 import；②vue3-engineer 写盘前新增根容器尺寸锚定（Figma absoluteBoundingBox → template 根 class → style 块内 width/height 替换 + max-* 兜底）；③loadVue3Runtime L485 正则放宽 `/\bimport\s*\.?\s*meta\b/g`（前端 HMR 已生效）。
- 验证：acorn ×2、vue-tsc 无 loadVue3Runtime 错误、注入函数实测（历史 new URL 产物→静态 import 无残留）、build 通过 dist 符号确认。
