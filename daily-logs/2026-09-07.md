# 2026-09-07

## 0907 组件生成问题分析（环境监测章节完成）
- 新建 `docs/0907-组件生成问题分析.md`，覆盖同批三个新生组件分析框架，第一章环境监测已完整。
- 环境监测 `mc-max-1788745566921-ffb74dd8`（c-env-monitor-kdb60bc8-ffb74dd8，Figma 420×186）证据链：
  - P0-1：`MonitorDataChart.vue` 写盘门禁失败，FE/BE workspace + temp-components 三处产物全缺，仅 chunk 缓存有完整版（parse 0 错误）；根因指向 `code-healer.js` fixSpuriousLineBreaks 合并 script 误伤（avgLen 14.72≤25、maxLen 80≤80 命中触发）。任务却 completed + codeVal=true，门禁跳过未降级。
  - P0-2：MonitorTypeTabs.vue scoped `height:100%`（common.less 里正确 27px 被 scoped 特异性覆盖）→ Tab 区独占 186px 全高，布局坍塌主因。
  - P0-3：chart `flex:113 1 0`、icons `flex:32 1 0` 像素高度误写 flex-grow；prompt 根因 09-05 已修，此组件是 09-04 旧产物，待回滚验证。
  - P1：根容器背景写死 #edf4fb、主题变量零消费；tab active 渐变/背景图双源冲突 + `isActive && index===0` 逻辑 bug；badge 无背景资源。
- 三个新生组件任务确认：环境监测 ffb74dd8、流量监测 9048ee55（c-traffic-monitor-es3tegsc）、设备监测 50ae925f（c-device-monitor-malvtjd5）。
- P0-2 机制修正（重要）：不是 scoped 特异性覆盖——**template 类名（裸，如 `c-env-monitor-tabs-section`）与 common.less 类名（带 kdb60bc8 实例前缀）不匹配，27px 是死样式**；scoped `height:100%` 是唯一生效高度。flex 计算：tabs basis 186px + chart/icons basis 0 → 剩余空间 0 → chart/icons 恒 0 高。三子组件类名匹配情况各不相同（FunctionIcons scoped 裸名反而不匹配自己 template）。新增管线问题：CODE-003 只查前缀不查 template↔common.less 命中，需 L0-B 类名命中率门禁。
- P0-0 结构根因（最上游，用户指认设计上 tabs/icons 同行）：Figma `sub-t`（380×32 普通 FRAME 无 layoutMode）行内装 tabs-list(295×27)+tabs-icon(52×24)+num(14×14)，y 区间 898-930 完全重叠；但 vision layoutStructure 输出 vertical-stack 三 sections（header-tabs 28 / chart 113 / header-controls 32），把顶行拆成两个垂直 section → 产物三子组件垂直堆叠，图标组跑到图表下方。vision 数据次要缺陷：tab 高 27/21/12/24 取文字 bbox 而非统一 27px。修复方向：分区后 bbox 几何校验（y 重叠+x 不相交→合并同行 flex row），vision schema 支持行内左右复合。vision 判定浅色风格→根容器 #edf4fb 非臆造（P1-4 表述已修正）；y 轴「辆」设计稿原生（P2-5 勘误，非串味）。
- 图表显示实测（HTTP 证据）：Vite 直载 MonitorDataChart.vue 200 是 SPA fallback 假阳性；index.vue 500（`Failed to resolve import ./components/MonitorDataChart.vue`）主入口 transform 失败；`.task-code-snapshots` FE+BE 均空（candidate/partial/last-good 全清理，预览回退 workspace）；`/api/component/:id/files` 的 resolveComponentBaseDirs 候选目录不含 chunk 缓存。结论：chart-container 三重阻断（文件层 async 404 + 快照层兜底失效 + 布局层 0 高 echarts 不 init），图表必然不渲染。Vite 200 需验内容（.vue 请求可能落到 index.html fallback，勿只看状态码）。
- 流量监测分析完成（0907 文档第二章）：文件完整（Vite 六文件真 200，无门禁跳过）、数值真值对（22350/16270/66109/16270 与 Figma 一致），但发现**灾难级资源错绑矩阵**——resource-dom-mapping 权威归属 vs 产物绑定全面错位：bg-_m-34（统计卡背景）→大桥标题背景、bg-_m-36/bg-_m-35（车型分布卡背景）→统计卡背景、bg-3475（环形图标题条）→图例激活/图表背景；根背景 bg-[m] downloadStatus=missing 无 CSS 替代；VehicleTypeDistribution 用 defineProps({icon1,bg2,bg4}) 接资源但 index.vue 未传 prop → 三资源全 undefined（CODE-018 未覆盖此场景）；FlowPrediction 标题 icon 用 CSS 3×12 竖条臆造替代。其余同批共性：flex:131 1 0 像素误写、类名双轨死样式（DailyTotalFlow 反向：template 带前缀 scoped 裸名）、图例激活背景不对称（仅北京绑 bg3）、less 直写资源路径违 P2-4。根因：跨 chunk 资源分配契约缺失（分块生成时 LLM 自选资源变量），对应方案 C 容器 ID 绑定。排查注意：resource-dom-mapping.json 是资源归属权威，分析资源错绑先对照它。
- 流量监测四症状机制闭环（用户 11:16 截图反馈，当前模型不可读图但代码证据全对应）：①北京/上海图例漂到标题区=图表区塌 0 高 + chart-wrapper 无 overflow:hidden、图例 flex-shrink:0 溢出可见（headerSlots 已排除）；②icon 三种失败模式并存（DailyTotalFlow 三 img 叠放/车型分布 props 未传/流量预测 CSS 竖条臆造）；③隧道卡背景错绑 bg-_m-36；④两大柱图+折线图消失=basis:0 弹性链归 0 → ResizeObserver 不触发 echarts 不 init，对照 60×60 固定尺寸环形图存活。修复方向：图表容器 min-height 保底（T2 扩展）。
- 资源错绑根因精确到层（重要）：vision sections[].body.children[].resourceFile 分配完全正确（daily 卡=bg-_m-34、车型隧道卡=bg-_m-36、大桥卡=bg-_m-35），resource-dom-mapping 也正确——**错在 Microcode Engineer chunk 生成层**：LLM 拿全量资源清单未按 section 过滤，自由挑选绑了名字相邻的图。视觉效果=背景整体向左错位一格（当日总流量显示车型分布背景，车型分布白卡）。治本：chunk prompt 按 vision section 过滤资源清单 + 写盘前绑定资源∉section 归属即 BLOCK（方案 C 具体化）。
- 设备监测分析完成（0907 文档第三章，用户两张截图内联可见对照）：四症状全定位。①插槽重复 2 次=vision 把三条统计同时放进 headerSlots 和 sections.header-stats（数据重叠），生成器兜底注入插槽+HeaderStats 子组件两路渲染，缺重叠去重校验；②竖排设备类型切换丢失=vision 分区把 @antd/tab(46×317) 的 tabs(监控/照明/通风/供配电/消防/交通诱导) 整列丢掉，只提取 cons 网格（device-grid 317=cons 高度），与环境监测 P0-0 同族行内复合结构丢失；③大卡布局错位（设计 icon 左上+标题左下+数值右两行 vs 产物垂直堆叠）+「南北接线 设备」被臆造成「房屋建筑设备」（TEXT-TRUTH 未拦）+3/3740 角标错绑到隧道卡 progress；④item 设计 icon 左+文字右两行 vs 产物垂直堆叠，数值 (2/484) 蓝色带括号 vs 产物 2/484 无括号 #333。新增管线缺陷家族：headerSlots/sections 数据重叠、文案臆造漏拦、语义角标错绑。HeaderStats 还出现 bg4~bg14 同图 11 别名绑定灾难+T1 剥离后空 div 残留背景。
- 0907 文档完稿（五章）：第一章环境监测（P0-0 vision 分区错/P0-1 门禁跳过缺文件/P0-2 类名双轨+height100%/P0-3 flex 像素误写）、第二章流量监测（P0-1 资源错绑矩阵 vision 对 engineer 错/P0-2 defineProps 未接线/P0-3 flex131/2.5 四症状机制）、第三章设备监测（P0-1 headerSlots+sections 重叠双渲染/P0-2 竖排 tab 整列丢/P0-3 南北接线臆造+3/3740 错绑/P0-4 item 布局错）、第四章资源问题分类框架（A 位置错误/B 没有使用/C 假性使用 9 子类）、第五章整体汇总（S1-S6 体现/12 根因按 5 阶段/12 方案 P0×6+P1×5+1 已修/验证路径）。核心结论：全部根因可用确定性约束（过滤/校验/兜底/回滚）治理，无需提升模型能力。
- 管线审计完成（0907 文档第六章）：12 根因分类——5 写错了/2 不严谨/5 漏了。**重要新发现**：①root-container.md:20 仍写 flex 错误示例（「flex-grow 直接取设计稿高度值」）——09-05 只修了 engineer.js p 段，.md 事实源漏修；②min-height 三处口径矛盾（T2 硬编码 80px vs post-process.js 注入 min-height:0 vs chart-standards.md 160/100px）；③visual-parser.js:1515-1564 兜底重建硬编码 layout:'vertical' 拍平行内并列节点——行内结构丢失是确定性代码主动破坏，非 vision 识别弱；④TEXT-TRUTH 阈值洞：09-04 放宽「≤2 处 WARN」致单处标题篡改放行；⑤CODE-018 defineProps 声明即过、父级传参不在校验面；⑥T09 只查插槽空不查内容区。病态结构：prompt 规则+后处理+门禁三层堆叠、同语义多处表达互相矛盾（4 对实锤矛盾）。根源方案：止血层 F1-F4 修错误事实源统一口径；架构层 Assembly Manifest 单一事实源+装配层（资源表→布局表→文案表→结构表迁移顺序），LLM 只做交互/echarts option 创造。判别标准：新增 prompt/fix/门禁=补丁，装配接管/修事实源/数据结构免疫=根源。
- 执行计划制定（0907 文档第七章）：外层 Loop（1 改动单元+验证+提交=1 循环）+ 内层 TDD（纯函数 fixture 先行）——Phase 0 测试基线（三组件 fixture 固化到 backend-node/test/fixtures/0907/ + 症状断言全红集）→ Phase 1 止血 L1-L4（F1 flex 事实源清零/F4 兜底重建不拍平/F2 min-height 三合一/F3 TEXT-TRUTH 分级）→ Phase 2 装配层 v1 资源表 L5-L7（Manifest 生成器/chunk prompt 按 section 过滤/绑定校验 BLOCK）→ Phase 3-5 布局/文案/结构表。DoD 五条（测试绿+基线比对+build+重生成冒烟+grep+commit+log）。风险四条（几何阈值误判/prompt 漂移/挂载器职责重叠/存量误伤→新门禁先 WARN）。
- 待办：待用户确认后从 Phase 0 开工。

## 0907 管线回归执行（Phase 0 + Phase 1 止血层 L1-L4 完成）
- Phase 0 测试基线：`backend-node/src/ai-engine/pipeline-regression-0907.spec.ts` + `src/fixtures/0907/{env,traffic,device}/`（figma-node-data 裁剪版/三组件 vision/问题产物）。10 项症状断言初始全红。
- L1 flex 事实源清零（#code#1）：三个 prompt 错误示例（root-container/chart-standards/constraints）→ `flex: <设计稿px> 1 0` 改 `flex:1 自分配 + min-height 兜底`；planner 归一化 flexGrow 系数透传 engineer 消费。T1 三项全绿。
- L2 行内复合 bbox 几何校验（#code#2）：新增纯函数 `rebuildSectionsPreservingInlineRows`（抽到 `utils/inline-row-rebuilder.js` 无 import.meta，jest 可 require；visual-parser.js 仅 import 复用），y 重叠显著(≥50%)+x 基本不相交(<15%较小宽)→`layout:'horizontal'`；挂 `inlineCompositeRows` 到 vision 结果。T3a/T3b/T3c 全绿。坑：visual-parser.js 含 import.meta，jest require 失败、动态 import 需 --experimental-vm-modules；故纯函数独立成文件。
- L3 min-height 口径三合一（#code#3）：`post-process.injectChartMinHeight` 注入 `0`→`160px`；`engineer.js` echarts 兜底 `80px`→`160px`；统一对齐 chart-standards 主图兜底 160px。T2 双绿。
- L4 TEXT-TRUTH 分级（#code#4）：新增 `resolveTextTruthSeverity(unknowns, doc)`；标题级篡改（`t-` 前缀 Figma TEXT 节点 / 产物 `title:` 字段上下文）必 BLOCK，不受 ≤2 处阈值豁免；`detectUnknownText` 加 `titleContext` 标记（extractScriptFieldsWithKey 区分 title 字段）。validator 阈值段接线：titleSeverity==='BLOCK' → BLOCK。T4a/T4b 全绿。
- 验证：0907 spec 10/10 绿；backend-node build 通过；validator/visual-parser 既有 spec 114+12 测试无回归。Phase 1 全部提交（#code#1-4）。
- 后排期：Phase 2 装配层 v1 资源表 L5-L7（Manifest 生成器/chunk prompt 按 section 过滤资源清单/绑定资源∉section 归属即 BLOCK），解决 traffic 资源错绑矩阵；Phase 3-5 布局/文案/结构表。

## 0907 管线回归执行（Phase 2 装配层 v1 资源表 L5-L7 完成）
- L5 资源 Manifest 生成器（#code#5）：新增纯函数 `buildResourceManifest`（抽到 `utils/resource-manifest.js` 无 import.meta，jest 可 require），按 `figmaPath` 首个 `slot-*` 段分 section；无 slot 段的根容器 bg（skipMount）→ 落 `panel`。单一事实源，下游 L6/L7 及未来布局表均消费此 Manifest。T5a-T5f 全绿（traffic 3 slot+1 panel、错绑拦截、env/device slot-con 资源归属、resolveSectionKey 回解）。
- L6 chunk prompt 按 section 过滤资源清单（#code#5）：`prompt-builder.js` 的 `buildSubcomponentScopedInput` 内调用 `scopedResourceDomMapping(manifest, matchedSection)` 覆盖 `resourceDomMapping`，LLM 只看到本 section 资源。T6a-T6d 全绿（按 section 过滤、未匹配返回 undefined fail-open）。
- L7 绑定校验 BLOCK（#code#5）：新增 `detectCrossSectionResourceBindings(manifest, files, fileSectionMap)`，资源绑定∉本 section 归属即 BLOCK；`microcode-engineer.js`/`vue3-engineer.js` 调用 `validateResourceAttribution` 时传 `sectionManifest: buildResourceManifest(...)`。T7a-T7e 全绿（用真实 traffic 错绑产物 VehicleTypeDistribution.vue/DailyTotalFlow.vue 验证跨 section BLOCK 拦截）。
- 验证：0907 spec 三文件 15/15 绿（L5:6 + L6:4 + L7:5）；backend-node build 通过；Phase 2 全部提交（commit 6d31089，12 files changed, 860 insertions(+), 63181 deletions(-)，含删除 server.log.1 从 git 追踪）。
- `.gitignore` 增量治理：新增 `dist-*.tar.gz`、`server.log.*`、`data/*.bak-*`、`data/api-binding-snapshots/`、`check-user-configs.cjs`、`debug-*.mjs`、`probe-*.mjs`；`git rm --cached server.log.1` 从索引移除已追踪的日志轮转文件。
- 后排期：Phase 3 布局表（L8-L10，vision sections[].layout → 子组件 flex basis/min-height 确定性注入，解决 env P0-0 行内复合结构丢失+traffic 高度比例自适应）；Phase 4 文案表；Phase 5 结构表。可选：先重生成 traffic 验证错绑清零（端到端验证 L5-L7 运行态效果）。

## 0907 管线回归执行（Phase 3 布局表 L8-L10 完成）
- L8 flexDirection 推断集中化（#code#6）：新增纯函数 `inferFlexDirection`（抽到 `utils/flex-direction-inferrer.js` 无 import.meta，jest 可 require），从 vision sections[].layout 确定性推断 flex-direction；`inline-row-rebuilder.js` import 复用该算法（单一事实源，消除行内复合判定逻辑重复）。T8a-T8d 全绿。
- L9 类名同源治理（#code#7）：新增纯函数 `genClassKey`/`genClassKeyBatch`（抽到 `utils/class-name-generator.js`），按 Figma 节点语义角色/名称/类型确定性生成类名；`prompt-loader.js` 新增 `buildClassNameManifestSection` 遍历 Figma 节点树收集有意义节点，用 `genClassKey` 生成 Markdown 表格注入 `figma.md` 模板的 `{{CLASS_NAME_MANIFEST}}` 占位符；`prompt-builder.js` 传递 `componentPrefix: componentId`。T9a-T9e 全绿（15/15 测试通过）。**单一事实源原则**：`buildClassNameManifestSection` 必须 import `class-name-generator.js` 的 `genClassKey`，禁止内联重复逻辑（已清理残留内联代码）。
- L10 定宽/定高区块归一（#code#8）：新增纯函数 `normalizeFixedSizeFlex`（抽到 `utils/fixed-size-normalizer.js`），将显式 px 尺寸 + `flex-grow` 归一为 `flex:0 0 auto`，避免尺寸被撑破；`code-validator.js` 2.10 节动态 import 调用。T10a-T10c 全绿。
- 验证：0907 spec 三文件 24/24 绿（L8:4 + L9:15 + L10:5）；backend-node build 通过；全量 jest 850 通过（18 套件失败是基线问题：`backend-root.js` 的 `__filename` 重复声明 + `import.meta.url` 兼容性，非 L8-L10 引入）。
- Phase 3 三个 Loop 集成状态：L8 ✅ `flex-direction-inferrer.js` → `inline-row-rebuilder.js`；L9 ✅ `class-name-generator.js` → `prompt-loader.js` + `prompt-builder.js`；L10 ✅ `fixed-size-normalizer.js` → `code-validator.js` 2.10 节。
- 后排期：Phase 4 文案表（L11-L12，vision text 真值 → 产物文案确定性注入，解决设备监测 P0-3 文案臆造）；Phase 5 结构表（L13-L14，vision sections 层级 → 子组件嵌套结构确定性生成，解决设备监测 P0-2 竖排 tab 整列丢失）。

## 0907 管线回归执行（Phase 3 布局表 L8-L10 完成）
- L8 flexDirection 推断集中化（#code#6）：新增纯函数 `inferFlexDirection`（抽到 `utils/flex-direction-inferrer.js` 无 import.meta，jest 可 require），从 vision sections[].layout 确定性推断 flex-direction；`inline-row-rebuilder.js` import 复用该算法（单一事实源，消除行内复合判定逻辑重复）。T8a-T8d 全绿。
- L9 类名同源治理（#code#7）：新增纯函数 `genClassKey`/`genClassKeyBatch`（抽到 `utils/class-name-generator.js`），按 Figma 节点语义角色/名称/类型确定性生成类名；`prompt-loader.js` 新增 `buildClassNameManifestSection` 遍历 Figma 节点树收集有意义节点，用 `genClassKey` 生成 Markdown 表格注入 `figma.md` 模板的 `{{CLASS_NAME_MANIFEST}}` 占位符；`prompt-builder.js` 传递 `componentPrefix: componentId`。T9a-T9e 全绿（15/15 测试通过）。**单一事实源原则**：`buildClassNameManifestSection` 必须 import `class-name-generator.js` 的 `genClassKey`，禁止内联重复逻辑（已清理残留内联代码）。
- L10 定宽/定高区块归一（#code#8）：新增纯函数 `normalizeFixedSizeFlex`（抽到 `utils/fixed-size-normalizer.js`），将显式 px 尺寸 + `flex-grow` 归一为 `flex:0 0 auto`，避免尺寸被撑破；`code-validator.js` 2.10 节动态 import 调用。T10a-T10c 全绿。
- 验证：0907 spec 三文件 24/24 绿（L8:4 + L9:15 + L10:5）；backend-node build 通过；全量 jest 850 通过（18 套件失败是基线问题：`backend-root.js` 的 `__filename` 重复声明 + `import.meta.url` 兼容性，非 L8-L10 引入）。
- Phase 3 三个 Loop 集成状态：L8 ✅ `flex-direction-inferrer.js` → `inline-row-rebuilder.js`；L9 ✅ `class-name-generator.js` → `prompt-loader.js` + `prompt-builder.js`；L10 ✅ `fixed-size-normalizer.js` → `code-validator.js` 2.10 节。
- 后排期：Phase 4 文案表（L11-L12，vision text 真值 → 产物文案确定性注入，解决设备监测 P0-3 文案臆造）；Phase 5 结构表（L13-L14，vision sections 层级 → 子组件嵌套结构确定性生成，解决设备监测 P0-2 竖排 tab 整列丢失）。

## 0907 microcode-engineer 性能优化四方案实施
- 流量监测组件 `mc-max-1788759433261-b76845e7` 生成失败确认为 microcode-engineer LLM 调用超时（300s），非 Phase 3-4 修复方案问题。
- 方案 1 动态超时策略（Task #553 ✅）：`code-generator.js` L711-742，按 chunk 类型（declare=90s/template=180s/script=180s/style=300s/component=300s）+ 复杂度因子（complex ×1.5/medium ×1.2/simple ×1.0）+ token 规模因子（estTokens>50k 额外 ×1.3 封顶）动态计算超时。
- 方案 2 子组件分组生成（Task #554 ✅）：`microcode-engineer.js` genSubComponents，按子组件路径父目录分组（同目录→同组），同组共享 scopedInput 缓存减少重复构建；分组数>子组件数/2 时不分组（太散不如全并发）。
- 方案 3 超时重试机制（Task #555 ✅）：`code-generator.js` invokeWithTimeout 调用处，超时自动重试 1 次（env: CHUNK_TIMEOUT_RETRY_MAX），重试时延长超时 ×1.5（env: CHUNK_TIMEOUT_RETRY_MULTIPLIER）；与 attempt 循环的正则重试独立（超时重试在 invoke 层，attempt 处理产出质量问题）。
- 方案 4 性能监控（Task #556 ✅）：`microcode-engineer.js` runChunk 层，每个 chunk 开始/完成推送 SSE（耗时/累计/ETA）；genSubComponents 层，每个子组件完成后推送累计进度；_perfMonitor 对象记录全部 chunk 耗时统计。
- Prompt 瘦身分析结论：已完成的瘦身（条件化+去重+scopedInput）是安全的零影响优化；进一步激进瘦身（删反例/删防御规则）会显著降低生成质量，不建议实施。
- 验证：两个文件 `node -c` 语法检查通过；tsc --noEmit 仅测试文件既有错误，无新增。

## 0908 构建错误修复
- `resource-mounter.js` 中 `injectFailedResourceFallbacks` 函数重复声明（L1215 和 L1441），导致构建报错 `Identifier has already been declared`。
- 根因：两个版本逻辑相似但新版更宽松（④ url(none) 无前置检查、⑤ 兜底类注入无 `!styleReplaced` 条件），可能是合并冲突遗留。
- 修复：删除旧版（L1215-1358），保留新版（L1441-1584）。17 个 JS 文件语法检查全部通过。
- 沙箱内存限制导致 `nest build` 被 SIGTERM (exit 137)，需本地终端执行构建。
