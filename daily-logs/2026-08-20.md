# 2026-08-20 工作日志

## 流量监测组件 (mc-1787122835747) 背景图+布局修复
- **问题**：`bg-_m-34.png`（852×184, 宽高比 4.63:1，汽车图标装饰横幅）被错误应用到根元素 `.c-monitor-root` 作为整个组件背景
- **根因**：`visual-parser.js:_findContentBackgroundResource` 策略3按面积选最大 bg 资源，未过滤宽高比极端的装饰图
- **修复1（生成器）**：策略3 candidates 过滤增加宽高比守卫：`ratio > 4 || ratio < 0.25` 排除，且 `h < 120 && ratio > 2.5` 也排除
- **修复2（已生成组件）**：
  - `index.vue`：移除根元素 `backgroundImage` + `bg1` import
  - `TotalTraffic.vue`：添加 `decorBg`（bg-_m-34.png）为两统计数字间的装饰 `<img>`，新增 `.c-total-traffic-decor` 样式
- **构建验证**：nest build 0 error，后端 PID 97624 已重启

## 生产管线根因链排查（bg-_m-34 错放根元素）
**证据链**（任务 mc-1787122835747, 8/19 15:11 产物）：
- `resource-dom-mapping.json`：`bg-_m-34.png` role=bg, figmaPath=`cp-流量监测/slot-当日总流量/Group 2136636802/bg-[m]`（TotalTraffic 横幅）
- `figma-node-data.json`：该节点 bbox=**425.8×91.4**（宽>200 但高仅 91.4）
- `debug-prompt.txt:938`：资源清单标注 `bg1 (bg-_m-34.png), 用法: backgroundStyle`
- `debug-ai-response.txt`：Vision AI 未在任何 section 引用 bg1（识别为 icon-3441 装饰）——AI 判断正确
- `_enrichContentBackground`：TotalTraffic section body 有子资源（deco-car）→ `_bodyChildrenHaveResources` 拦截 ✅（正确）

**完整失误链（5环）**：
1. `figma-connector.js:_inferRecommendedUsage:1899`：`width>200 || height>200 → backgroundStyle`——横幅宽 425.8 被误判整体背景（**主根因**）
2. Vision prompt 资源清单：bg1 标 backgroundStyle，语义误导
3. Vision AI：未引用 bg1（正确，但无强制）
4. visual-parser 拦截：正确（无问题）
5. `microcode-engineer.js:288`："bg1 必须 backgroundImage 引用" + backgroundStyle 语义 → 模型无位置可选只能放根容器（**放大器**）

**修复（3处）**：
1. `figma-connector.js:_inferRecommendedUsage`：整体背景需「宽高都>120、至少一边>200、宽高比 0.4~2.5」；横幅（ratio>2.5 或 <0.4）→ backgroundBlock。验证：420×807→style✅ / 425.8×91.4→block✅ / 191×80→block✅ / 500×300→style✅
2. `microcode-engineer.js` 资源清单：输出 recommendedUsage + 横幅警告（"禁止贴根容器/整面板背景"）
3. `microcode-engineer.js` RESOURCE_UTILIZATION：新增「使用位置判定」——backgroundBlock/横幅只放对应区块，禁止根容器；横幅用 `auto 100%` 不拉伸
- 后端 PID 99919 已重启，dist 验证通过

## 布局差异排查（Figma vs 生成代码）
**Figma 真实布局**（figma-node-data.json 提取）：
| 区块 | Figma 高度 | 生成代码 | 问题 |
|---|---|---|---|
| .c-monitor-root | — | **无 display:flex** | ❌ 根因：子组件 height:100% 全叠满 807px |
| TotalTraffic | sub-header 30.4 + 横幅 91.4 = 121 | 121px 固定 | ✅ |
| HourlyFlowChart | 标题 21 + @echarts/bar **131** ≈ 155 | height:100% → **807px** | ❌❌ 溢出 |
| VehicleTypeDistribution | 142 | height:100% → 807px | ❌❌ 溢出 |
| FlowForecast | 174 | flex:1 1 174（父非flex失效） | ❌ |
- Y轴 0-4000 与 Figma 一致（Figma 文字 "4000 3000 2000 1000 0"），数据量级 mock 差异非 bug

**修复（当前产物 mc-1787122835747）**：
1. `common.less .c-monitor-root`：+ `display:flex; flex-direction:column`
2. `HourlyFlowChart.vue`：height:100% → **155px + flex:0 0 auto**
3. `VehicleTypeDistribution.vue`：height:100% → **142px + flex:0 0 auto**
4. `FlowForecast.vue`：flex:1 1 174px → **flex:0 0 174px + height:174px**
5. `TotalTraffic.vue`：121px 保持（已有 flex-shrink:0）

**生成管线修复（microcode-engineer.js ROOT_CONTAINER 规则）**：
- 文字描述补「根容器**必须** display:flex + flex-direction:column」（原示例代码有 flex 但文字"仅此而已"矛盾，模型跟随文字）
- 新增「子区块高度规则」：子组件必须固定像素高度 + flex:0 0 auto，**禁止 height:100%**
- 后端 PID 468 已重启，dist 验证通过

## ⚠️ 模板字符串反引号事故（mv-max-1787192699501 立即失败）
- **现象**：新任务 Phase 1 立即失败 `SyntaxError: Unexpected identifier '用法'`，发生在 ESM 模块加载阶段
- **根因**：在 microcode-engineer.js 的 `const shared = \`...\`` 大模板字符串中新增的「使用位置判定」prompt 文本，直接写了 `\`用法: backgroundStyle\`` **未转义反引号** → 提前终止模板字符串，`用法` 变裸标识符
- **关键教训**：
  1. `ai-engine/**` 的 .js 是 **ESM 且 nest build 只转译 .ts，.js 原样复制不做语法检查** → 语法错误构建不报，运行时才炸
  2. **改模板字符串后必须 acorn/vm.SourceTextModule 验证语法**（`node --experimental-vm-modules -e "new vm.SourceTextModule(fs.readFileSync(f,'utf8'))"`），不能只看 nest build 成功
  3. 大模板字符串内不要用裸反引号（`\`` 转义或改用「」引号）；ROOT_CONTAINER 部分已有的 `\`` 是转义形式所以没事
- **修复**：292-294 行 `\`用法:...\`` → 「用法:...」；重新构建 + vm 验证 3 个文件 ✅；后端 PID 929 已重启

## FLEX-002 冗余 flex prompt 优化
- **问题**：LLM 生成 `display:flex; flex-direction:column` 但没有任何 flex 子属性（flex:1/align/justify/gap），与块级布局等价，触发 FLEX-002 WARN
- **根因**：layout-refiner.md 的映射规则说"无 layoutMode 时默认 display:block"，但 AI 仍主动加 flex
- **修复**：`ai-generation-constraints.md` 新增「第 5 节 flex 布局约束」
  - 🔴 禁止冗余 flex：明确列出 4 种合理使用场景（flex:1 子项 / align/justify / gap / Figma 明确要求）
  - 错误示例 + 正确示例（3 种）
  - 自检清单："写完 flex column 后问自己需要什么——答不上来就是冗余"
  - 补充 flex:1 + min-height:0 规则
- **章节编号**：5→6→7→7.5→8 重新对齐
- **构建验证**：语法错误 0，145 个 .js 全部同步，MD_SYNC_DONE ✅
- **Node 后端重启**：11:16 重启（PID 929 → 6576），所有修复生效

## 🆕 从零创建最小智能体（hello-agent 示例，端到端跑通）
**能力沉淀**：用户要「从零建最初的智能体给管道使用」→ 实做了最小示例并验证全链路

**最小智能体 = 1 个类 + 3 处注册**：
1. `roles/hello-agent.js`：继承 BaseAgent，`execute(state)` 返回结果对象（自动合并回 state）+ `parseOutput`
2. `workflow.constants.ts` HANDLER_REGISTRY 加条目（label/description/category/configSchema）→ 编辑器可见
3. `dynamic-workflow-graph.js` import + `instantiateRole` case → 运行时实例化
- 前端无需改：WorkflowEditor 节点 label 自动取注册表 `handlers[key].label`（NODE_LABEL_MAP 是 pipeline-lab 的）

**架构改进**：BaseAgent 新增 `skipLLM` 支持——纯确定性智能体（不调 LLM）构造时跳过 `_buildLLM()` + attachUnifiedInvoke，**无 API key 也能跑**。默认 false 向后兼容。⚠️ 改动时注意构造函数闭合括号（else 块会吃掉原 `}`，已踩坑修复）。

**验证链（全部 ✅）**：
- `GET /api/workflows/meta/handlers` 返回 hello-agent（第 9 个）
- 保存管线 hello-demo（start→hello→end）→ 运行 → 图构建 → skipLLM 初始化 → execute → 图执行完成 iterations:3
- 当前后端 PID 7141

## 第二个确定性智能体：url-parser-agent（Figma URL 解析）
- **需求**：WorkflowEditor 运行对话框每次手填 fileKey/nodeId → 做成管线节点自动解析
- **五步法落地**：①判断(规则可写死✅) ②契约(figmaUrl→fileKey/nodeId) ③roles/url-parser-agent.js(skipLLM, URL正则+decodeURIComponent) ④注册 constants+调度器 ⑤vm验证+3用例冒烟
- **测试**：file+编码node-id✅ / design+冒号✅ / 非法URL优雅降级✅
- **要点**：execute 同时返回 `urlParseResult`（自描述结果）+ 直接注入 `fileKey/nodeId`（下游 figma-connector 零转换消费）
- 后端 PID 8408 已重启

## 从零到多智能体：生态冷启动规划（产品通会话）
- 用户诉求：什么都没有的环境下，规划出 脚本→模板→提示词→单智能体→多智能体 的完整生长体系
- 五层模型：L0空环境→L1脚本(skipLLM确定性)→L2模板(参数化骨架)→L3提示词(LLM灵魂)→L4单智能体(统一execute契约)→L5多智能体(DAG)
- 核心认知：模板=已验证脚本的参数化版本；先人工写第一段脚本再参数化（种子已在：hello/url-parser）
- 两条飞轮：脚本→模板→新脚本；单智能体→另存为模板→模板库增长
- 分期：P0冷启动(动态注册表+Agent Builder工具节点+模板种子5-6个) → P1(AI节点+提示词库) → P2(UGC+市场)
- 文档：artifacts/agent-ecosystem-cold-start.md + agent-builder-prd.md

## 跨 chunk 超时记忆修复（provider-pool + ai-request-gateway）
- **问题**：LLM 超时后 resolveAttempt 只在单次调用重试链中排除超时 provider，新 chunk 重置 excludeIds 为空，导致又选回刚超时的 provider
- **根因**：failureThreshold:0 禁用熔断器；resolveAttempt 不跨 chunk；excludeIds 每次新 chunk 重置
- **修复**：
  1. provider-pool.js：新增 `_recentTimeouts` Map 记录 providerId → 最后超时时间戳，timeoutMemoryMs=60s
  2. provider-pool.js：新增 `recordTimeout(providerId)` / `_isRecentlyTimedOut(providerId)` / `cleanupExpiredTimeouts()` 方法
  3. provider-pool.js：`_pickFrom` 增加过滤层 `notTimedOut`，候选优先级：正常候选 → 忽略超时记忆的 retry 排除候选 → 满足能力要求 → 全量
  4. provider-pool.js：`snapshot()` 增加 `recentlyTimedOut` 字段（at/elapsedMs/remainingMs）便于调试
  5. ai-request-gateway.js：超时时调用 `getProviderPool().recordTimeout(ctx.providerId)`
- **验证**：nest build 0 error，PID 9505 已重启，grep 确认 recordTimeout/_isRecentlyTimedOut/_recentTimeouts 定义存在

## 假注册修复 + phase2 节点开放注册（未重启）
- **假注册修复**（doc-analyzer / config-generator 曾有注册表但调度器无 case、roles 无类）：
  - `agents/doc-analyzer-agent.js`：+ execute 包装（解构 document/requirementDoc/docContent → analyze → {docAnalysis}）
  - `agents/config-generator-agent.js`：+ execute 包装（解构 docAnalysis/analysisResult → generate → {configs}）
- **开放注册 4 个 phase2 节点**（workflow.constants.ts + 调度器 case）：layout-refiner / style-refiner / layout-style-refiner / vue3-engineer（均有 execute 接口直接兼容）
- **暂缓 3 个**（无 execute 需适配器）：screenshot-renderer（函数式导出）/ visual-comparator（compare 接口）/ subcomponent-planner（无 BaseAgent）
- 编辑器可用节点：8 → 14（10 真实可运行 + 4 新开放）
- **验证**：构建 ✅ + vm 语法 ✅ + 离线接口验证 6/6 ✅（doc/config execute 包装解构正确；4 refiner/engineer 实例化+execute 存在）
- ⚠️ **未重启服务**（用户要求，PID 8408 跑旧代码）——改动在 dist，下次重启生效

## 适配器包装 3 节点 + phase2 变体管线（管线二可编辑，未重启）
**适配器（3 个新文件 roles/*-node.js）**：
- screenshot-renderer-node：包装函数式 renderScreenshot → BaseAgent（skipLLM，缺参容错）
- visual-comparator-node：包装 VisualComparator.compare → BaseAgent（缺图跳过，不阻断）
- subcomponent-planner-node：包装 SubcomponentPlanner.plan → BaseAgent（skipLLM，空输入容错）
- 注册表 + 调度器 case +3；编辑器节点 14 → 17

**phase2 变体机制（管线二每步可增删改 = 删/停已实现）**：
- `mc-component-graph-phase2.js`：createPhase2Graph 新增 `addNodeWithSkip`（skipNodes 数组内节点注册 pass-through 保留图结构）；⚠️ replace_all 曾误替换 helper 内部 graph.addNode 致无限递归，已修；新增 `getPhase2NodeRegistry()` 轻量导出（拓扑合并视图→真实节点映射，避免 OOM）；runPhase2Generation 透传 skipNodes
- `phase2.service.ts`：startGeneration 透传 config.skipNodes
- `workflow.service.ts`：run() 识别 phase2 变体（engine:'phase2' 标志 或 节点与拓扑 id 重叠≥80% 且含 init/figma-connector/microcode-engineer/complete 自动识别）→ 委托生产引擎 + realToTopo 映射展开 skipNodes
- **关键认知**：拓扑视图是合并视图（parallel-analysis 含 layout-reviewer+style-mapper），不能直接比较节点名，必须 realToTopo 映射

**验证**：构建✅ vm 语法 5 文件✅ 适配器实跑 2/3✅（planner 规划+容错、screenshot 缺参容错；visual-comparator 重依赖待重启验证）注册表映射 3 变体场景✅（删审查+截图→4 跳过、删比对→1、全保留→空）
- ⚠️ 未重启（用户要求，PID 8408）；沙箱 import phase2 完整模块会 OOM（puppeteer 等重依赖），生产服务无此问题
- 已知边界：变体"新增节点注入 phase2 引擎"未实现（需统一引擎 P2）；自定义管线含 init 等 4 核心 id 会被自动识别为 phase2 变体（概率低）

## 重启 + phase2 变体端到端验证通过（PID 17267）
- 用户要求重启，全部改动生效（此前 2 轮"不重启"积累的 dist）
- **验证结果**：注册表 17 节点✅ / hello-demo dynamic 引擎跑通✅ / phase2 变体完整链路✅
- **变体验证**（p2-variant-test：动态拓扑 20→18 节点，删 screenshot-renderer+visual-comparator）：
  - 识别：engine:'phase2' 标志 + 自动识别（重叠≥80% 且含 4 核心 id）✅
  - skipNodes 计算：5 个（用户删 2 + 内部节点 l0b-fail/do-not-invent-check/generate-runtime-verify）✅
  - 委托生产引擎：日志 "⏭️ 跳过节点（变体管线配置）" + Figma 取数正常推进 ✅
- **过程修复**：TASK_CONFIG_SNAPSHOT_SCHEMA 是 .strict() Zod schema，skipNodes 不在内 → 委托时 "Unrecognized key" 报错 → 已加 `skipNodes: z.array(z.string()).optional()`
- ⚠️ 拓扑注意：GET /workflows/original-phase2 返回静态 15 节点副本，GET /workflows/graph/phase2 返回动态 20 节点（编辑器用）；变体测试必须用动态拓扑
- 已知副作用：变体删掉内部保护节点（l0b-fail）后，对应校验失败不再 fail-closed——用户删除即代表接受弱化

## Agent Builder 落地（编排页新建节点，工具+AI 全类型）✅
**用户要求**：管线2 完全不动；编排页面做新建节点功能，覆盖智能体所有可能节点类型

**后端（src/agent-builder/ 新模块）**：
- templates/tool-templates.js：8 类工具模板（url-parser/field-mapper/json-transform/validator/aggregator/router/formatter/passthrough），每类 render 函数生成完整代码
- templates/llm-templates.js：llm-analyzer（AI 节点，prompt+模型）；⚠️ 曾用嵌套模板字符串致转义地狱（Unexpected end of input），改为数组 join 构造 prompt
- agent-builder.service.ts：create（渲染→acorn 语法校验→写 roles/custom/{name}.js→写 config/agents/{name}.json→冒烟测试）；⚠️ validateSyntax 用 SourceTextModule 需要 --experimental-vm-modules 服务没开，改 acorn
- dynamic-loader.js：纯 ESM，供调度器动态 import（mtime 时间戳打破缓存）；⚠️ customRolesDir 路径错（dist/agent-builder/../roles → 应为 ../ai-engine/roles）已修
- agent-builder.controller.ts：create/list/templates/delete；workflow.controller meta/handlers 改 async 合并动态注册（扁平结构，前端期望 Record）

**调度器动态化**：instantiateRole 改 async，内置 case 优先 + default 兜底 instantiateDynamicAgent；调用处 await

**前端**：WorkflowEditor 工具栏「＋ 新建智能体」+ 弹窗表单（逻辑类型 radio/模板下拉/输入输出字段/prompt/JSON 参数）+ 提交后 loadMeta() 刷新；api/workflow.ts + fetchAgentTemplates/createAgent

**端到端验证全过**：模板目录 8+1 ✅ / HTTP 创建 my-url/my-check/my-formatter/my-ai（smoke passed）✅ / meta/handlers 20 节点含动态 ✅ / fmt-demo 管线运行动态智能体成功（skipLLM 初始化+执行完成）✅ / formatter 占位符修复（{name} 与 {{name}} 都支持）✅
- 当前后端 PID 18687；前端 build 验证中

## Agent Builder 资源清单展示（新建时显示每类需要什么资源）
- **需求**：新建节点要支持不同类型，且每种类型"需要哪些资源"在界面展示
- **后端**：9 类模板注册表补 `resources` 元数据（inputs 建议+必填/可选+desc、outputs、params 说明+示例、requires:{model,prompt}）；templates API 返回 resources
- **前端**：WorkflowEditor 弹窗模板下拉下方新增「📦 该类型需要的资源」区块（输入/输出/参数/额外要求 3 栏），computed currentTemplateResources 按模板切换
- **验证**：templates API 9 类资源清单全对 ✅（工具类"无需模型"、llm-analyzer"需模型"）；vue-tsc 仅剩既有 defaultEdgeOptions 错误
- 后端 PID 19233

## P1-1 完成：6 个基建工具模板（gap-analysis 补全第一步）
- **新增模板**（tool-templates.js）：init（初始化，对应 phase2 init）/ figma-fetch（Figma API 取数）/ screenshot（复用系统 renderScreenshot）/ complete（产物发布 cpSync）/ l0b-fail（fail-closed 自定义头部无容错）/ do-not-invent（臆造检查）
- **验证**：6 模板渲染语法 ✅ / HTTP 创建 6 节点冒烟全过 ✅ / 行为验证：l0b-fail 阻断抛错✅+通过态✅ / figma-fetch 无 token 容错+URL 解析✅ / init 默认值✅
- **do-not-invent 修复**：启发式误报"组件含"（中文句子片段）→ 加 STOP_WORDS 过滤（含/包含/和/的/与…子串命中）+ 子串匹配（known 元素与 token 互相包含即已知）→ 全匹配 ok=true✅、臆造项精准检出✅
- **差距更新**：25 节点 12 不可建 → 6（剩 visual-parser/visual-comparator/code-structure-validator/parallel-analysis/parallel-refine/generate-runtime-verify 待 P1-2/P2-1）
- 动态节点现共 13 个（my-url/my-check/my-formatter/my-ai + p2-×6 + 早前 my-*）；后端 PID 19702

## P1-2 完成：视觉支持 + 代码结构校验模板
- **llm-analyzer 加 vision 模式**：params.vision=true 或输入含 type='image' 字段 → 生成代码读图（fs.readFileSync → base64 data URL）→ HumanMessage content 数组（text+images）多模态消息
- **新增 vision-compare 模板**（LLM_TEMPLATES）：双图比对（figmaImage/renderedImage 两个 image 输入 + 内置对比 prompt 骨架），对应 phase2 visual-comparator
- **新增 code-structure 模板**（TOOL_TEMPLATES）：内置 L0-B 规则（CODE-001 scoped less / CODE-002 @import index.less / CODE-003 类名前缀），对应 phase2 code-structure-validator
- **调度器动态 vision**：dynamic-workflow-graph default 分支读动态定义 requires.vision → 用 visionAIConfig 构造 roleCfg；service.create 写入 requires:{vision}
- **验证**：4/4 模板语法 ✅ / HTTP 创建 p2-visual/p2-cmp/p2-cs 冒烟全过 ✅ / requires.vision 标记正确 ✅ / code-structure 行为：合法 pass=true✅、非法报 CODE-001+002✅
- **差距更新**：25 节点剩 2 个待补（parallel-analysis/parallel-refine 并行编排 P2-1；generate-runtime-verify 运行时验证模板可后续加）
- 动态节点 16 个；后端 PID 20827

## my-formatter 竞态 + 拓扑漂移修复（PID 20650）
- **my-formatter 未知 handler（竞态 bug）**：agent-builder 先写 JSON 再写 JS，图在 JS 未落盘时调度 → `fs.stat(js)` ENOENT → 误报 `未知的 handler`。第一次跑(15:26:45)失败、第二次(15:27:22)skipLLM 初始化成功，印证创建期竞态。
  - 修复：`dynamic-loader.js:instantiateDynamicAgent` 增加 ENOENT 瞬时重试（最多3次，间隔 300/600/900ms，仅重试文件暂未就绪，其他错误直接抛）
  - 验证：直接 import 测试 instantiateDynamicAgent('my-formatter') 返回 OK DynamicAgent ✅
- **拓扑漂移告警（维护类误报）**：漂移检查用「精确集合相等」对比，但拓扑视图是有意合并视图（parallel-analysis→[layout-reviewer,style-mapper]、parallel-refine→[layout-refiner]），且缺3真实内部节点(l0b-fail/do-not-invent-check/generate-runtime-verify)、多非真实节点 docAnalyzer（注释明确"S9 文档子管线，非独立图节点"）
  - 修复：`TOPOLOGY_NODES` 删 docAnalyzer + 补入3个真实内部节点；漂移检查改为先展开合并节点（合并节点自身+原子成员都计入）再对比
  - 验证：getPhase2Topology() 运行后不再打印 `⚠️ 拓扑漂移检测` ✅
- **写盘门禁 fail-closed→warn+skip（上轮）**：microcode-engineer.js 单文件门禁失败不再 throw 作废整轮，改为 warn + skippedFiles.push + continue
- **跨 chunk 超时记忆（上轮）**：provider-pool.js `_recentTimeouts` Map + recordTimeout/_isRecentlyTimedOut，超时 provider 60s 内不被选中
- 后端 PID 20650 已重启；重启后日志确认：拓扑漂移不再报 ✅、my-formatter 不再报 ✅、无新文件写入失败 ✅
- 15:41 用户重新触发 Figma 生成（mc-component-phase2-figma 新图创建），观察中——具体失败原因仍需用户贴 [[INFO]] SSE 流或等本次跑完确认

## LESS 编译门禁深层诊断（Figma fail-closed 根因排查）
- 读 less-compile-gate.js:300-470：对已自动修复(heal)的未闭合块注释标记 `severity:'INFO'`+`healApplied:true`，且 `blockDiagnostics = diagnostics.filter(d => d.severity === 'BLOCK')` → INFO **不计入阻断判定**（462-463 行）
- dist 当前版本已含此逻辑（grep 实证 462/342/348）
- 结论：02:49:37 那次 `LESS 编译门禁阻断...已修复并写回文件` 是更早旧 dist（今日 build 前）触发；**现网不会再因"已修复的未闭合注释" fail-closed**
- Figma 生成现存的 fail-closed 关卡：(1)写盘门禁→已改 warn+skip；(2)LESS·已heal→INFO不阻断；(3)LESS·真BLOCK编译错误（如模型 LES 语法错误）→仍 fail-closed（合理，但不排除误伤）
- ❗Figma 失败真正根因仍缺日志：用户贴的 [[INFO]] 是前端 SSE 转发、不落盘到可读文件；结构化日志 15:18 起无 Phase 2 Figma阶段失败 记录。需用户贴失败 [[INFO]] 行（ERROR/❌/Phase 2 Figma阶段失败）才能精准定位关卡类型

## P2-1 完成：并行编排 + runtime-verify 模板（差距几乎全关）
- **dynamic 引擎 parallel 节点**：buildNodeHandler 加 type==='parallel' 分支——config.handlers 数组，Promise.all 并行执行子角色，结果合并进 state + parallelResult（含各子节点 status）；graph.js 顺序引擎不改（phase2 共用安全）
- **前端并行组**：控制节点加"并行组"（拖拽 type:'parallel'，teal 色）+ 类型下拉 + 属性面板（子节点多选下拉 + 增删）；保存序列化已含 config ✅
- **runtime-verify 模板**：产物存在性/非空检查（package 目录 + 文件清单，默认 index.vue），对应 generate-runtime-verify
- **验证**：par-demo 管线（parallel: [hello-agent, url-parser-agent]）运行成功——两子节点同一毫秒并行初始化+执行+完成 ✅
- **模板总数**：16 工具 + 2 AI = 18 类
- **差距最终**：phase2 25 节点全部可建（工具 16 覆盖确定性 + AI 2 覆盖文本/视觉 + parallel 编排）；剩余 P1-4 资源上传（用户目标"手填上传"中上传部分）
- 后端 PID 21904

## P1-4 完成：资源上传能力（用户目标"手填+上传"全部达成）✅
- **需求**：新建节点需要哪些资源 → 支持手填上传。手填已支持，本次补上传半边
- **后端**：
  - `POST /agent-builder/upload`（FileInterceptor multipart 字段 file）→ service.uploadResource：白名单 ext（md/txt/json/yaml/csv/less/vue/js/ts/png/jpg/gif/webp/svg/pdf）≤10MB → 存 config/agent-resources/ → 返回 {name,url(绝对路径),type,size}
  - **⚠️ busboy latin1 乱码修复**：curl/浏览器 multipart filename 中文被 latin1 解码成乱码 → fixLatin1ToUtf8（Buffer.from(s,'latin1').toString('utf8') 且无 U+FFFD 才采用）——磁盘文件名正常
  - create 接受 referenceFiles → 写入动态定义 + 注入 llm-analyzer 生成代码（REF_FILES 常量 + execute 内读取：文本读内容前 8000 字符拼进 prompt「# 参考文档」段、图片/pdf 仅声明存在）
- **前端**：WorkflowEditor 弹窗 llm 类型显示「📎 参考资源」上传区（input file + 列表 + 删除 + 大小显示）；api/workflow.ts + uploadAgentResource
- **过程中的坑**：① controller Edit 时误删 @Post('create') 方法 → create 404，补回（grep 验证 5 个路由）② 生成代码 refDocs 声明在 prompt 数组之后 → TDZ "Cannot access 'refDocs' before initialization" → refPrelude 移到 prompt 之前
- **验证**：上传中文文件名正常 ✅ / 创建带参考文档节点 smoke passed ✅ / mock LLM 捕获 prompt：参考文档标题+规范内容（主色#0c9dbe）+输入数据全部注入 ✅ / 注册表 32 节点（17 内置 + 15 动态）✅ / 前端 vite build ✅（首次 emptyDir 报错为 dist 占用，重跑过）
- 动态节点 15 个：my-url/my-check/my-formatter/my-ai + p2-ai-spec/p2-check/p2-cmp/p2-cs/p2-fetch/p2-gate/p2-init/p2-pub/p2-shot/p2-verify/p2-visual
- 后端 PID 22656

## 🔥 前端 500 根因修复（vite 代理指向死 Java）
- **现象**：前端(2610)触发生成/加载任务页 → `Request failed with status code 500`
- **根因**：`frontend/vite.config.js` 代理把 AI 生成与 Node 自有接口全指向 **Java 8080（本地未启动，缺 Docker/MySQL）**，仅 `/api/progress` 走 Node 13030
  - 生成页加载必调 `/api/tasks/recent`、`/api/tasks/summary` → 命中显式 `/api/tasks`(原 8080) 或默认 `/api`(原 8080) → 死 Java → Vite 代理 500
- **修复**（vite.config.js）：
  - AI 入口：`/api/phase2` `/api/vue3` `/api/lite` `/api/demo` `/api/preview` `/api/config` → 13030
  - 工具类：`/api/screen-layout` `/api/user/git-credential` `/api/workflows` `/api/apifox` → 13030
  - 业务 CRUD：`/api/component` `/api/tasks` → 13030
  - 默认兜底 `/api` → 13030（与线上 Nginx /api→NestJS 一致）
  - **保留 8080（真 Java 已迁）**：`/api/microcode/doc` `/api/models` `/api/auth` `/api/group` `/api/projects` `/api/admin` `/api/sessions` `/api/chat` `/api/skills` `/api/documents` `/api/component-split`
- **验证**：curl 经 2610 代理 → `/api/tasks/recent|summary` `/api/phase2/figma-preview` 均返回 **401**（到 Node，非 500）；直连 8080 为 000（确认 Java 死）
- **必须重启前端才生效**：`kill 2610` + `node start-frontend.js`（vite 配置变更需重启）。前端已重启 PID 23112/23159
- ⚠️ 注意：408 行块注释写"Node.js 后端 13030"却把 auth/group 指 8080，与模块"已迁 Java 空壳"一致；tasks/component/page-generator/token-usage 实为 Node 但曾被误指 8080（本次改 tasks/component，page-generator/token-usage 仍 8080 待必要时再改）

## 故障修复：/api/auth/current 500（Java 8080 掉线）
- **现象**：前端 2610 的 /api/auth/current 500
- **根因链**：vite 代理 /api/auth → Java 8080（配置正确，Node auth.module.ts 注释「AuthController -- 已迁 Java」不挂载）→ 本地 Java 8080 无服务监听（lsof 只有到远程 120.232.31.208:8080 的出站连接，无 LISTEN）→ 连接被拒 → 代理 500
- **修复**：node start-java.js 重启 Java（PID 23302，dev profile + DEV_AUTO_LOGIN，Mongo localhost:27017 连接正常，1.6s 启动）
- **验证**：8080 直测 200 ✅ / 经 2610 代理 200 ✅ 返回 {role:member, permissions:[...], user:{uid:dev-local}}
- **经验**：auth 类 500 先查对应后端端口是否有 LISTEN（lsof -iTCP:PORT -sTCP:LISTEN）；本地三服务（2610/13030/8080）任一掉线都会出现同类 500/代理错误

## 体验设计规划：从 0 到 1 建复杂智能体（ProductManagementExpert 会话）
- 用户诉求：如何把"建复杂功能智能体"做得体验好，从 0 到 1 顺畅，且能衍生到各种功能
- **核心结论**：复杂智能体 = 配方（六要素：目标/节点蓝图/数据契约/参数资源/测试集/使用说明），不是节点堆叠；体验靠"脚手架优先 + 语言即入口 + 数据流可观测 + 一切可衍生"四原则
- **三层入口**：L1 对话式（一句话→AI生成蓝图→校验器→确认落地）/ L2 蓝图式（场景配方库选配方改参数）/ L3 编排式（现状画布，保留为高级模式）；层级可互相转换
- **4 个体验断层**：空白画布恐惧 / 连线靠猜（字段级数据流不可见）/ 模板粒度错位（只有单节点模板无整机蓝图）/ 调试靠猜（无节点级快照断点）
- **落地分期**：P0 衍生闭环（配方数据模型+另存为模板+配方库内置 3-5 个）→ P1 对话式生成器+字段级依赖/节点快照 → P2 测试集/一键回归+配方市场
- 文档：artifacts/agent-builder-experience-design.md

## Agent Builder 快赢优化 ①②③ + 动态节点持久化修复
**① 三步使用步骤**：弹窗选模板后显示"1 准备输入 → 2 配置 → 3 使用"（computed templateSteps 从 resources 生成）
**② 字段默认值预填**：watch templateId → inputsText/outputsText 为空时自动带出合法 key（正则过滤描述性 key）
**③ 即建即测**：后端 POST /agent-builder/test（实例化动态节点+mock 输入执行；LLM 缺 Key 返回友好错误"拖入画布运行"）；前端创建成功不关弹窗显示试跑面板（输入框+▶试跑+输出 JSON 深色代码块）

## 🔧 运行时质量门禁 fail-closed 修复（generate 模式）
- **现象**：Figma 生成代码已成功（文件写入×4、L0-B pass），却卡在最后 `generate-runtime-verify` 节点 `[运行时质量门禁] 真实预览检查未通过（2 项阻断问题）` → `[ERROR] Phase 2 Figma阶段失败`，20+ 分钟成果作废
- **根因**：图的 `generate-runtime-verify` 节点（mc-component-graph-phase2.js:3051 / mc-component-graph-vue3.js:2591）在 `action==='fail' && status==='BLOCK'` 时直接 `throw`，**未读 `state._v3Mode`**；而 `phase2.service.ts:634-638` 已设计「generate 模式跳过质量门禁」（`qualityGateSkipped = _v3Mode==='generate'`），service 层本不拦，但图节点先 fail-closed 抛错导致根本走不到 service 收尾
- **修复**：两图节点同改——`action==='fail' && BLOCK` 分支先打**诊断日志**（打印具体阻断 issue id/message），再判 `state._v3Mode==='generate'`：是则 **return 降级为完成（不抛错，产物照常发布到 workspace 供人工审查）**；非 generate 模式（minimal/full）保留原 fail-closed（service 层亦据此 terminalError）
- **验证**：build 通过；dist 两图各含「generate 运行时门禁未通过，降级为完成」+「诊断阻断项」各 1 处 ✅；后端已重启 PID 24656
- ⚠️ 注意：降级后若真有 RUNTIME-012（空白截图）/RUNTIME-004（组件渲染 error）仍会发布到 workspace 让用户白屏——但诊断日志会暴露具体 id 供下一轮针对性修；本次"2项阻断"具体 id 需等下次运行日志确认
- ⚠️ test 响应被统一信封包裹：成功解包为直接结果（拦截器解 {ok:true,data:X}）、失败带 {ok:false,error}——前端 runTest 兼容两种
- **🔴 关键架构缺陷修复（build 清空 dist 丢动态节点）**：nest build 清空 dist/ai-engine/roles/custom → 15 个动态节点代码全丢（config/agents 定义还在）→ 运行时"找不到模块"
  - 方案：运行时目录保持 dist/ai-engine/roles/custom（模板 import 路径基于此）；新增持久备份 config/dynamic-roles/；create 写双份、remove 删双份；**构造时自愈 rebuildMissingRoles**（定义在代码缺 → renderCode 重写回 dist+备份）
  - 验证：build 重启后自动"自愈重建 15 个" ✅；formatter 试跑输出"组件 测试组件 共 3 个" ✅
- 前端 vite build ✅（仅剩既有 defaultEdgeOptions 错误）；后端 PID 24033

## P0 配方库完成：场景配方（内置 5）+ 另存为模板（衍生闭环打通）✅
- **新模块 src/recipes/**：builtin-recipes.ts（5 个内置配方，全用内置 handler 保证开箱即用：component-gen 微码组件 7 节点 / vue3-gen 5 节点 / doc-config 文档转配置 / url-pipeline 示例 / render-compare 渲染对比）+ recipes.service（list 内置+config/recipes/*.json / get 详情含拓扑 / saveRecipe 另存为 / remove 仅用户配方，内置只读）+ controller + module（注册 app.module）
- **配方数据模型**：{name,label,description,scenario,source:builtin|user,workflow:{entryNode,nodes,edges},author,version}
- **前端**：WorkflowEditor 顶栏「📦 配方库」+「存为配方」；配方库弹窗（搜索/内置/我的标签/节点数/使用→拓扑加载到画布新工作流模式/删除用户配方）；存为配方弹窗（name/label/场景/描述，复用保存序列化逻辑）
- **验证**：内置 5 个列表 ✅ / save fmt-recipe（3 节点）→ 列表 6 含用户 ✅ / 详情含拓扑 ✅ / delete ✅ / 前端 vite build ✅
- **意义**：这是"衍生到各种功能"飞轮的起点——跑通的管线一键参数化进库，别人选配方改参数即用（拷贝式，不影响原配方）
- 后端 PID 24844

## ④模板搜索 + ⑦节点快照 + ⑧连线契约提示 完成 ✅
**④ 模板搜索**：新建弹窗模板下拉上方加搜索框（filteredTemplates 按 id/label/description 过滤）
**⑦ 节点快照（可观测性）**：
- dynamic-workflow-graph.js：buildDynamicGraph/runDynamicWorkflow 支持 runtime.onNodeSnapshot；gate/parallel/agent 三分支执行后记录 {nodeId,nodeLabel,handler,type,inputKeys,output,status,durationMs}
- workflow.service：nodeSnapshotStore Map（sessionId→快照数组）+ getNodeSnapshots；controller GET /api/workflows/:sessionId/nodes
- 前端：运行对话框运行后「🔍 查看节点快照」→ 弹窗列表（状态点/耗时/输入字段数）点开看输出 JSON；⚠️ 内存态，进程重启即清
**⑧ 连线契约提示**：
- 数据层：meta/handlers 动态节点带 inputs/outputs；HANDLER_REGISTRY 给 11 个内置节点补 inputs/outputs（figma-connector/visual-parser/layout-reviewer/style-mapper/microcode-engineer/adversarial-checker/doc-analyzer/config-generator/hello-agent/url-parser-agent/3 refiner）
- 前端：edgeContract helper——连线显示上游输出∩下游输入字段，交集空且双方有定义→「⚠ 契约不匹配」红线；应用在 onConnect/useRecipe/loadOriginalTopology/loadSelectedWorkflow
- **验证**：快照链路（url-parser-agent 1ms 16 输入字段→fileKey/nodeId 输出）✅ / 契约元数据（内置+动态）✅ / vite build 8286 ✅
- **遗留**：run 入口强制校验 fileKey/nodeId，url-parser-agent 自动解析场景被堵（figmaUrl 不传也能跑）——需 run 校验放行 figmaUrl
- 后端 PID 25423

## Vue3 流量监测组件三问题修复（mv-max-1787192699501-724aa82b）
- **问题 1（宽高不符）**：`.traffic-monitor-root` 用 `width:100%;height:100%` 撑满预览容器；Figma 实际尺寸 **426×807px**（figma.json absoluteBoundingBox）
- **问题 2（图表外多卡片框）**：
  - 根因 A：`common.less` 根节点自带 `background:#edf4fb` + `box-shadow:0 4px 10px rgba(74,117,141,0.25)` + `padding:24px 23px`——Figma 中 bg-[m] 的 fills 为 `visible:false`，不应有面板外壳
  - 根因 B：`index.vue` scoped 样式给 `.chart-item` 和 `.forecast-chart-container` 添加了 `background:#fff / border:1px solid #e3eef7 / border-radius:8px / box-shadow` 卡片装饰，Figma 原设计无此包裹
- **问题 3（图例重复）**：模板 DOM 层手写了 `<span class="chart-legend">` 图例（北京方向/上海方向），同时 ECharts `createBarOption/createAreaOption` 的 `legend:{data:[...]}` 也渲染了一份 → 出现双份图例
- **修复**：
  1. `common.less`：移除根节点 `background`/`box-shadow`/`padding`，加注释说明由预览环境/宿主面板提供外壳
  2. `index.vue` scoped：`.chart-item` 移除 bg/border/radius/shadow/padding；`.forecast-chart-container` 同理
  3. `index.vue` script：两处 `legend:{data:[...],...}` → `legend:{show:false}` + 注释说明 DOM 层已渲染
- **验证**：grep 确认无残留 `background:#ffffff`/`box-shadow:0 2px 8px`/`background:#edf4fb`/`legend.*data:` ✅

## run 校验放行 figmaUrl（url-parser 前置解析场景打通）
- **改动**：workflow.service.run() 参数加 figmaUrl?；fileKey/nodeId 缺失时若 figmaUrl 存在 → 复用 UrlParserAgent.parseFigmaUrl 自动解析补齐（resolvedFileKey/resolvedNodeId），日志「figmaUrl 自动解析」；错误提示改「请提供 fileKey + nodeId，或 Figma URL」
- **前端**：runWorkflow API 类型加 figmaUrl?；executeRun 传 figmaUrl；运行对话框「填 URL 即用」
- **踩坑**：python 批量替换把 createTask/executeWorkflow 的**字段名**也改成了 resolvedFileKey（TS2353 TaskMetadata 无此字段）→ 键名恢复 fileKey/nodeId、值用 resolved*；8 空格与 6 空格缩进两轮替换
- **验证**：file URL 编码 node-id ✅ / design URL 中文名冒号 ✅ / 缺 figmaUrl 仍拦截友好错误 ✅ / build 0 error ✅ / 前端 vite build 8286 ✅
- **意义**：用户运行管线只需填一个 Figma 链接（即使管线里没接 url-parser 节点，figma-connector 也能拿到解析后的 fileKey/nodeId）
- 后端 PID 26121

## 实操演示：从 0 建"Figma URL → 组件数量分析"智能体
- **用户问**：新建"传 figma url 分析有几个组件"的智能体怎么做
- **实际做完**（4 步全走通）：
  1. 新建 LLM 节点 figma-analyzer（llm-analyzer 模板，输入 figmaNodeData，输出 componentCount/components，prompt 分析顶层 FRAME/组件）
  2. 编排管线 figma-count：start → url-parser-agent → figma-connector → figma-analyzer → end（保存 ✅）
  3. 运行：只传 figmaUrl → url-parser 自动解析 ✅ → figma-connector 因**环境无 Figma Token** 报 "Figma Token is required"（非功能问题）
  4. 存为配方 figma-component-count（5 节点）✅
- **要点**：这就是"从 0 到 1"标准路径——新建节点（选模板填参数）→ 画布连线 → 保存 → 运行（fill URL）→ 跑通后存配方复用；LLM 节点无模型 key、figma-connector 无 token 时运行受限（环境配置问题）

## 分析汇合层重整：代码与行为基线
- 用户确认采用方案 A，只备份视觉管线相关源码，不纳入未跟踪的 figma-analyzer/recipe/workflow 文件。
- 5 个目标源码相对 HEAD 无未提交差异，因此未制造空提交；创建不切换工作区的基线分支 `backup/context-assembler-baseline-20260820`，固定提交 `614f6557dec2b03a80a7de39c5d9849beeb209e3`。
- 建立 `artifacts/context-assembler-baseline-20260820.json`：记录 5 个源码 SHA-256、历史流量监测 checkpoint/手工修正产物哈希及关键坏样本事实（Figma 425.83×807、Vision 覆盖率 44%、layout review score=0/degraded、style fallback 暗色背景+8px 圆角）。
- 当前仍在 main，未暂存/提交未跟踪文件，Node 后端 PID 26121 未重启。下一步边界：只创建 Context Assembler 骨架与测试，不接入生产图。

## 外部能力模板：http-request + web-fetch（"加载外部脚本/服务"第一条现成路径）✅
- **用户诉求**：新建智能体要加载外部脚本/skills（本地/网络）→ 建议先通"外部能力"，做两个模板
- **http-request**（通用 HTTP）：url/method/headers/body/timeout → status/data/ok/headers；JSON 自动解析；GET/POST/PUT/DELETE；AbortController 超时
- **web-fetch**（网络文档抓取）：url/maxLength → text/title/ok；去 script/style/HTML 标签 + HTML 实体还原 + 空白折叠 + 截断；UA 头
- **🔴 SSRF 防护（安全底线）**：_blockedUrl helper——localhost/127.0.0.1/::1/0.0.0.0/.local/.internal + 内网段 10.x/172.16-31.x/192.168.x 全部拒绝；两个模板共用
- **验证**：模板渲染语法 ✅ / HTTP GET httpbin 200 JSON 解析 ✅ / POST+headers+body 回显 ✅ / 文档抓取 example.com title+text ✅ / SSRF 拦截 127.0.0.1 与 192.168.x ✅
- 模板总数：工具 16→18 + AI 2 = 20；动态节点 17 个；后端 PID 33870
- **意义**：第三方 API 调用与网络参考现在是"选模板建节点"即可用，无需等 P2 市场

## Context Assembler 骨架完成（尚未接入生产图）
- 新增 `src/ai-engine/context/` 5 个纯函数模块：schema、Figma design facts、冲突裁决、预算、assembler；新增离线验证脚本 `scripts/validate-generation-context.mjs`。
- 历史流量监测样本验证结果：trust=`restricted`；根尺寸保留 425.8275×807 CSS px；根 fill 无证据→background.allowed=false；识别 5 个 open issues（含 layout degraded、style fallback、背景冲突、两处 legend owner）；generationInput 最终 3867 bytes，预算通过。
- 上下文收敛：Engineer 输入契约不含 raw figma/vision/review/style/previousCritiques；componentPlan/resourceManifest 白名单投影；被拒绝声明只保留 ID/数量，详细值不进入主上下文；超预算时明确 `budget.passed=false`，不静默截断。
- 验证：ESM 语法通过、历史样本断言通过、超限用例通过、nest build 通过；grep 确认 Vue3/Phase2 graph 对 assembler/generationInput 仍为 0 引用，因此生产行为未改变，服务未重启。
- 工作期间并行出现无关的 agent-builder/workflow 修改和 my-fetch/my-http/text-summarizer 等未跟踪文件，未读取/暂存/修改；后续接 shadow 时继续严格隔离。

## URL→AI 总结配方闭环（外部能力组合演示）
- **新增**：text-summarizer 动态节点（llm-analyzer：text→summary/keyPoints）+ url-summarize 用户配方（start→my-fetch→text-summarizer→end）
- **🔧 新能力：自定义入口参数 inputs**——run 接口支持 `inputs:{}` 透传进 initialState（任意字段，如 url 总结的 url），并放行 Figma 参数校验（hasCustomInputs 时跳过 fileKey/nodeId）；initialState 展开 ...inputs
- **验证**：url-summarize 运行 → my-fetch 真实抓取 example.com（title+142 字符文本）✅ → 数据流转 ✅ → text-summarizer 因环境无模型 Key 失败（"Anthropic API key not found"，环境配置问题）→ 配 Key 即完整闭环
- **意义**：外部能力（抓取→AI 分析）组合成配方的完整链路打通；自定义 inputs 让非 Figma 类管线（如 URL 总结、竞品分析）可用任意入口参数
- 动态节点 18 个；后端 PID 34991

## Context Assembler shadow 旁路接线完成（未重启）
- 新增 `generation-context-shadow.js`，Vue3/Phase2 两张图在 Engineer 入口各调用 1 次；旧 `engineer.execute({...})` 散装输入保持原样，新 generationInput 不传 Engineer。
- shadow 报告写 `.checkpoint/context-shadow.json`，明确 `productionInputMode=legacy`、`engineerInputChanged=false`；旧上下文仅内存估算字节数，不把旧正文写入报告。
- 历史样本离线结果：legacy 估算 208850 bytes，新 generationInput 3869 bytes，压缩 98.15%；trust=restricted；checkpoint 写入异常被捕获并降级，不阻断旧链路。
- 验证：两图各 1 处 shadow 调用；无 `engineer.execute({generationInput})`；ESM 语法、离线断言、nest build、dist 同步均通过。当前只构建未重启，运行中服务仍不会执行 shadow，是否重启由用户下一决策决定。
