# 2026-08-18

## 视觉分析超时熔断修复 (P0)
- 现象：max 任务 `mc-max-1786957657363-53d48c75` 视觉分析阶段 fail-closed：`LLM invoke timeout after 150000ms [vision-agent.analyzeImage]`，阻断后续代码生成。
- 根因：`visual-parser.js` 的 `VISION_ANALYSIS_TIMEOUT_MS=150000` 经 `analyze()` 内 `Math.min(reqTimeout ?? 150000, 150000)` 把**单次 attempt 超时钳到 150s**，低于网关默认 `DEFAULT_REQUEST_POLICY.requestTimeoutMs=300000`。qwen3.7-plus 首请求冷启动 >150s 被熔断；但重试（18s）、第二次调用（77s）均成功 → provider 可达，纯超时预算不足。日志另见 09:13:58 一次独立 VisionAgent 创建（疑似图重入/并行分支，未深查）。
- 修复：`VISION_ANALYSIS_TIMEOUT_MS` 150000 → 300000（与网关默认对齐）。`backend-node/src/ai-engine/roles/visual-parser.js:23`。
- 重启：停旧 PID 34909，`rm -f tsconfig.build.tsbuildinfo && npm run build`（0 error）；本机无 `start-node.js`（memory 提到的启动器未落地），改用 `.start-server.sh`（perl setsid+双 fork 真正 detached，会加载 `.env/.env.development` 含 MONGODB_URI 等）→ 新 PID 46727，`:13030` 监听正常。
- 验证建议：重跑该 max 任务；若仍偶发 >300s 超时再上调常量。

## 微码布局还原偏差诊断 (P0)
- 现象：任务 `mc-max-1787018107366-84ef2c25` 生成完成，但预览布局与设计稿严重不符。设计稿为三段式（顶部统计栏 + 并排双卡 + 左导航+3×4设备网格），生成结果为上下堆叠（统计 + 左环形图右列表）。
- 诊断过程：逐层排查预览图→AI响应→分析结果→生成代码：
  - **预览图**（mc-preview.png）：✅ 完整设计稿截图，无裁切
  - **AI 视觉响应**（debug-ai-response.txt）：✅ 正确识别了完整3段布局，含 section 名称/排列方向/12个设备卡片/6个导航 tab
  - **preview-analysis.json**：✅ 与 AI 响应一致，结构完整
  - **子组件** DeviceStats.vue / DeviceList.vue：✅ 内容正确（统计+双卡并排 / 左tab右grid）
  - **index.vue**：❌ 模板编排完全错误——把三段平铺改成了「上DeviceStats + 下左chart右list」的伪分栏结构，还多塞了一个不存在的 DeviceStatusChart
- 根因：`microcode-engineer.js` 对复杂组件走**分块生成**路径时，`_buildTemplateChunkMiddle()`（第2850行）构建 template chunk prompt **没有注入 layoutStructure**。主 prompt（第741行）有完整 JSON，但分块模板 prompt 只有 class 命名/资源白名单/拆组件建议，**没有任何布局信息** → LLM 基于"设备监测"名字自己编了通用模板。
- 修复方向：在 `_buildTemplateChunkMiddle` 注入精简布局摘要（sections 列表 + 每段的 layout direction + 子组件映射建议），让 LLM 知道该用什么顶层结构。→ 已实施（见下条）。

## 微码布局还原偏差修复 (P0, 用户确认实施)
- 根因（复确认）：`microcode-engineer.js` 复杂组件走分块生成路径时，`_buildTemplateChunkMiddle()` 构建 template chunk prompt **未注入 layoutStructure**。主 prompt（第741行）虽含完整 JSON，但分块模式 LLM 易聚焦末尾"只生成 template"指令而忽略 → 凭组件名臆造通用结构（把并排双卡+左导航右网格 臆造成 上统计+下左图右列表）。
- 修复：新增 `_buildLayoutSkeleton(layoutStructure)`，用 `extractSections()` 把 sections 转成精简骨架（段落顺序 + 每段排列方向 horizontal/两列并排/竖向/grid + 子元素 names + gridColumns），并带"禁止凭名臆造/网格按列数/并排必 row"强约束，注入 `_buildTemplateChunkMiddle` 的「🧱 组件结构骨架」段。`backend-node/src/ai-engine/roles/microcode-engineer.js:2857`。
- 重启：停旧 PID 59577，build 0 error，`.start-server.sh` → 新 PID 60054，`:13030` 监听正常。
- 验证建议：重跑 mc-max-1787018107366-84ef2c25（或同类复杂组件）确认 index.vue 顶层结构与 design 一致。

## Vue3 写盘门禁「两个 template」修复 (P0)
- 现象：任务 `mv-max-1787020658011-b362d016` 倒写盘门禁：`package/index.vue:9:2 SFC: Single file component can contain only one <template> element`。链路：整文件生成超时截断 → 降级 template/script/style 三段生成 → 三段拼装完成 → 编译报双 template。
- 根因：`vue3-engineer.js:_assembleVue3IndexVue` 对三段产物**盲拼接**（tpl+scr+sty）。而三段降级路径里 `chunkS.contextFiles` 携带完整 `tpl`、`chunkY.contextFiles` 携带 `tpl+scr`，LLM 在 script/style 段顺手把整个 SFC（含第二个 `<template>`）一并生成 → 拼装出两个 template。
- 修复：`_assembleVue3IndexVue` 改为**按块类型抽取+唯一化**——新增 `_extractSfcBlock(content, tag)`，从各段抽取对应块（template 优先 rawTpl、script 优先 rawScr、style 优先 rawSty，缺漏/泄漏从其他段回退）；对嵌套 `<template v-for>` 做平衡匹配防截断根模板；纯 less 段自动包裹 `<style lang="less" scoped>`；任一缺失给兜底占位（仍带 c-monitor 前缀）。`backend-node/src/ai-engine/roles/vue3-engineer.js:764`。
- 重启：停旧 PID 46727，重新 build（0 error），`.start-server.sh` → 新 PID 59577，`:13030` 监听正常。
- 验证建议：重跑该 Vue3 任务确认不再双 template；此修复对"微码布局还原偏差"无直接影响（那是 mc 分块模板 prompt 缺 layoutStructure，另议）。

## Vue3 写盘门禁「less 变量 undefined」容错修复 (P0)
- 现象：任务 `mv-max-1787020658011-b362d016` 再次倒写盘门禁（双 template 已不出现，说明上条修复生效），新错：`package/index.vue style: variable @text-color is undefined`。server.log 完整 error：`{"error":"Vue SFC 写盘门禁失败: package/index.vue: package/index.vue style: variable @text-color is undefined"}`。
- 完整失败链路：① `style-mapper` 解析失败（`[style-mapper] 所有解析策略失败: all_strategies_failed`，模型返回 JSON 被截断）→ `themeVars/lessVariables/cssClasses` 兜底为空；② `ensureVue3StyleEntry`（vue3-engineer.js:1089）补齐的 `resources/styles/theme-vars.less`/`common.less` 为**空模板**；③ 模型在 index.vue 的 `<style lang="less" scoped>` 里引用 `@text-color` 等主题变量，但 theme-vars.less 空 → less 编译 `undefined`。
- 根因（写盘门禁侧）：`validateVueSfc`（sfc-syntax-validation.js:77-94）编译 style 块时，`filePath` 是相对路径 → `canResolveLocalStyleImports=false` → **先 `stripDeferredLocalStyleImports` 删掉 `@import '../resources/styles/index.less'`**（行82），内联 `@text-color` 无定义源 → 报 undefined。`phase2.service.ts:1273` 那条"提取缺失变量注入 theme-vars.less"自愈逻辑**只存在于共享 less 编译路径**（编译磁盘 index.less，@import 链生效），写盘门禁这条 strip 了 @import，故 fail-closed。
- 修复：`writeFiles`（microcode-engineer.js:4711）的 `.vue` 校验失败分支新增容错——捕获 `variable @X is undefined`，把缺失变量声明**内联注入组件 `<style>` 块**（保证内存门禁通过）+ 累积**回写 `theme-vars.less`**（保证落盘后目录门禁绝对路径 @import 链也通过），重编译一次；仍失败才 fail-closed。新增 helper `_extractUndefinedLessVars` / `_safeLessVarValue`（按变量名语义给中性默认值：bg→#fff、border→#e8e8e8、primary→#409EFF、text/color→#333、尺寸→0px 等）。`backend-node/src/ai-engine/roles/microcode-engineer.js:4711 / 4788 / 4797`。
- 重启：停旧 PID 60054，build 0 error，`.start-server.sh` → 新 PID 65391，`:13030` 监听正常。
- 备注：此为**防御网**，治标（让 style-mapper 偶发失败时任务仍跑通）。治本在 style-mapper 角色：其返回 JSON 被 max_tokens 截断（`[style-mapper] 直接解析失败: Unexpected end of JSON input`），可增大其 max_tokens 或失败时回退到已成功的 `per-element` 样式结果（日志显示 `per-element 样式提取完成 ×63` 成功）生成最小 themeVars。→ 已实施（见下条）。

## style-mapper 截断回退（治本，用户确认实施）
- 承接上条「less 变量 undefined」：仅写盘门禁兜底为治标。本次在 style-mapper 角色内做**截断回退**，从已成功的 per-element 真值合成主题变量，源头消除空 themeVars。
- 数据流确认：`style-mapper.execute()` 返回 `fixedResult`（含 themeVars/lessVariables）即 `styleMappings`（microcode-engineer 输入 line 5622），注入主 prompt「## 样式映射结果」段（line 820）。故合成的变量能直接传给代码生成模型，模型生成 theme-vars.less 时引用真实值。
- 机制：`map()` 解析失败时不抛（parseMappingResult 内部 catch 返回全空兜底对象，tokenCoverage:0 合法），故 `execute` 合并出空 themeVars/lessVariables + 非空 elementStyleMap。本次在 `execute`（line 677-690）检测 `themeVars 与 lessVariables 皆空 且 elementStyleMap 非空` → 调用 `_synthesizeThemeVarsFromElementStyles` 反合成最小主题变量。
- 合成逻辑（style-mapper.js:489）：遍历 elementStyleMap 收集 color/background-color/background(非url)/border-radius/font-size/gap/padding，取众数归纳出 7 个变量，命名刻意对齐 microcode-engineer._safeLessVarValue 常用名（`@text-color`/`@bg`/`@primary`/`@border`/`@border-radius-base`/`@font-size-base`/`@gap`），深色面板文字强制白系、背景默认 `#0f0f0f`、主色 `#409EFF`、边框半透明白。themeVars 由 lessVariables 派生（`--xxx`）。
- 协同：合成真实值（上游）优先于上一轮写盘门禁中性兜底（下游 undefined 时补 #333 等），双保险；即使模型引用合成未覆盖的变量名，写盘门禁仍防 fail-closed。
- 修复位置：`backend-node/src/ai-engine/roles/style-mapper.js:489`（helper） / :677（execute 回退）。
- 重启：停旧 PID 65391，build 0 error，`.start-server.sh` → 新 PID 66292，`:13030` 监听正常。
- 验证建议：重跑 `mv-max-1787020658011-b362d016`；若 style-mapper 再截断，应见 `⚠️ style-mapper 宏观映射为空（截断/解析失败），已从 per-element 真值合成最小主题变量兜底` 日志，且生成的组件颜色为设计稿真实值而非中性灰。

## Vue3 写盘门禁「less 变量 undefined」broad-net 加固 (P0)
- 现象：任务 `mv-max-1787020658011-b362d016`（11:36 重跑）再次倒写盘门禁，新错 `variable @radius-lg is undefined`。本轮 style-mapper 实际**成功**（未触发上条 per-element 合成），但模型在 index.vue 引用了 `@radius-lg` 等 design token，且这些 token 未出现在合成/真实 themeVars 里。
- 排查：本地 repro 证明「直接引用 `@radius-lg` 注入 `4px`」可过门禁 → 说明旧逻辑对直接引用有效。但真实失败必为**链式/间接 undefined**（如模型写 `@radius-lg: @radius-base * 2`，或同时引用多个 token），而旧逻辑只从 `syntaxResult.errors` 的「首条 undefined 变量」提取注入，一旦 retry 因另一个未覆盖的变量仍失败，抛出的错误只显示首条 `@radius-lg`，掩盖真正残留。属「按错误列表窄匹配」的固有脆弱性。
- 修复（microcode-engineer.js:4721）：把窄匹配改为 **broad-net**——校验失败时直接扫描整个 `<style>` 块，用 `_extractLessVarReferences` 提取所有 `@var` 引用（排除 @media/@import/@keyframes/@mixin/@include 等 at-rule 与 `@name(` 混入调用），再用 `_isLessVarDeclared` 过滤出「被引用但未声明」的变量，全部按 `_safeLessVarValue` 注入内联 + 累积回写 theme-vars.less 后重编译。这样链式（`@radius-lg: @radius-base*2`）、多令牌、混入并存场景都一次覆盖。retry 仍失败时改抛 **retry.errors**（真实残余），便于定位真语法问题。
- `_safeLessVarValue` 加固：新增 radius→8px、gap/padding/margin/space/width/height/size→0px、font-size→14px、line-height→1.5、opacity/zindex→1、weight→400、duration→0.2s；颜色类走原色值；其余兜底 `unset`（CSS 全局关键字，对几乎所有属性合法，保证编译通过）。数值类给真实数值以兼容算术（`@gap + 4px`）。
- 本地 repro 验证：场景A（链式 `@radius-lg: @radius-base*2`）→ 注入 `@radius-base` 后通过；场景B（@radius-lg/@spacing-lg/@text-secondary/@bg/@shadow-lg/@gap/@font-size-base + 混入 `.foo()` + @media）→ 全部注入（shadow-lg→unset 可编译）后通过。
- 修复位置：`backend-node/src/ai-engine/roles/microcode-engineer.js:4721`（writeFiles 分支） / :4805（`_extractLessVarReferences`） / :4825（`_isLessVarDeclared`） / :4829（`_safeLessVarValue` 加固）。
- 重启：停旧 PID 66292，build 0 error，`.start-server.sh` → 新 PID 74513，`:13030` 监听正常。
- 验证建议：重跑 `mv-max-1787020658011-b362d016`；应见 `🛡️ 写盘门禁自动补全缺失 less 变量 ...` 日志且任务跑到成功（颜色中性默认值，非设计稿精确色——精确配色需 style-mapper 成功合成，本轮已具备）。

## Vue3 三段生成「子组件 import 未声明」fail-closed 根治 (P0)
- 现象：任务 `mv-max-1787020658011-b362d016`（12:22 重跑）倒写盘门禁：`分块生成失败（package/index.vue）：输出被截断或语义不完整（脚本段: 模板引用了 script 中未声明的变量/组件（SectionHeader、SectionTopCards、SectionMainContent））`。链路：整文件生成 2×180s 超时 → 降级 template/script/style 三段 → 脚本段单独生成时漏写子组件 import → 语义门禁 fail-closed。
- 根因：subcomponent-planner 只产出 section 结构、**不命名**子组件（命名权交 LLM），子组件名（SectionHeader 等）由 LLM 起并作为 PascalCase 标签出现在模板。三段路径里脚本段单独生成，常漏写对这些子组件的 `import`；而 `_detectSubComponents`（microcode-engineer:3558）只靠 `import ... from '...components/xxx'` 检测子组件 → 无 import 就检测不到 → 子组件文件永不生成 → 主组件引用悬空 → 写盘门禁报"未声明组件"→ 重试耗尽 fail-closed。这是**连锁**缺陷（检测依赖 import，而 import 正是模型漏写的）。
- 修复（三处协同，双保险）：
  1. **门禁容错**（`_generateSingleChunk` / microcode-engineer.js:3519）：脚本段语义校验把模板中的子组件 PascalCase 标签 + Vue 内置组件（Transition/KeepAlive 等）加入 `implicitlyDeclared`，放行"将由系统接线"的子组件引用，仅对真正缺失的普通变量仍报错。新增 `extractComponentTagNames` + `VUE_BUILTIN_COMPONENTS`（sfc-semantics.js）。
  2. **检测补全**（`vue3-engineer.js:1041` generateCode）：`_detectSubComponents` 之外，额外据模板 PascalCase 标签补全子组件候选清单（去重），使无 import 的子组件也能被生成到 `package/components/<Tag>.vue`。
  3. **确定性接线**（`vue3-engineer._wireVue3SubComponentImports`，execute 写盘前调用）：把已生成的 `package/components/<Tag>.vue` 以 `import <Tag> from './components/<Tag>.vue'` 注入主组件 `<script setup>`（插到最后一个 import 后；无 import 则插到 `<script>` 开标签后）。仅接线真实存在的子组件文件、已 import/已在 components:{} 注册的跳过，幂等。
- 验证：sfc-semantics 单测确认 extractComponentTagNames 正确提取 SectionHeader 等、排除 Transition/a-table；_wireVue3SubComponentImports 复刻算法 4 用例全过（插到现有 import 后 / 无 import 插脚本后 / 子组件文件缺失不接线 / 已 import 不重复）。
- 重启：停旧 PID 74513，build 0 error，**改用项目根 `node start-node.js`**（detached 启动器，等同 setsid+unref，跨回合存活；`.start-server.sh` 已被弃用），→ 新 PID 82698，`:13030` 监听正常。
- 验证建议：重跑 `mv-max-1787020658011-b362d016`；应不再 fail-closed，且最终 index.vue 含 SectionHeader/SectionTopCards/SectionMainContent 的 import，子组件文件 `package/components/*.vue` 均生成。

## L0-B CODE-002 BLOCK 根治（vue3 主组件 @import 漏注入）(P0)
- 现象：13:18 重跑 `mv-max-1787020658011-b362d016` 三段降级生成成功（4 文件写入、子组件 import 接线生效），但 L0-B 校验 `pass=false, BLOCK=1, Issues=2`：
  - `[CODE-002] 未引入 resources/styles/index.less，必须 @import '../resources/styles/index.less'（package/index.vue）`（**BLOCK**，唯一阻断项）
  - `[CODE-007] Vue3 根面板存在背景 #edf4fb 但根选择器未直接落地背景字面量`（**WARN**，不阻断）
- 根因：CODE-002 要求主组件 style 块含 `@import '../resources/styles/index.less';`，而父类的自动注入 `_ensureIndexVueStyleImport`（microcode-engineer.js:5418，被 `validateAndFixGeneratedFiles` 在 5320 行调用）只在**微码旧路径（generateCode/5776）**触发；vue3 的 `execute`(vue3-engineer.js:1199) 链路是 generateCode→ensureVue3StyleEntry→_wireVue3SubComponentImports→writeFiles，**完全绕过 `validateAndFixGeneratedFiles`**，所以 vue3 主组件永不自动补这行 import → L0-B CODE-002 BLOCK → fail-closed。
- 修复：在 vue3-engineer.js `execute` 的 `ensureVue3StyleEntry` 之后、写盘前，确定性调用父类 `this._ensureIndexVueStyleImport(codeResult.files['package/index.vue'])`（仅当缺时补，幂等；已含则原样返回）。与子组件接线同一处、复用既有方法。
- 单测验证：模拟漏 import 的 index.vue → 修复后 CODE-002 判定由 true 转 false；幂等（import 数恒为 1）；已含 import 时原样透传。
- CODE-007 处理：为 WARN，不阻断阶段通过（L0-B 的 pass 仅由 BLOCK 决定）。其根因是模型把背景色落到别处/未直接落到根选择器 `background:#edf4fb`，属模型生成质量问题，本次不阻断、留作后续 prompt 优化项；未做自动注入以免过度改动。
- 重启：停旧 PID 82698，build 0 error，项目根 `node start-node.js` → 新 PID 88146，`:13030` 监听正常。
- 验证建议：重跑 `mv-max-1787020658011-b362d016`；应 `pass=true` 通过 L0-B（仅残留 CODE-007 WARN），阶段不再 fail-closed。

## 写盘门禁 mixin 未定义兜底（.common is undefined）(P0)
- 现象：13:57 重跑 `mv-max-1787020658011-b362d016`，视觉分析本次**降级**（`解析分析结果失败`→`视觉分析降级，已用 Figma 坐标重建布局结构`），但布局/样式映射/代码生成都过，最后写盘失败：`Vue SFC 写盘门禁失败: package/index.vue style: .common is undefined`（真实栈见 server.log:712-728）。
- 根因：模型生成的 index.vue style 块**直接调用了 `.common()`（及可能 `.theme-dark()`）mixin**——这些 mixin 本应定义在 `theme-vars.less`，经 `@import '../resources/styles/index.less'` 引入。写盘门禁 `validateVueSfc(relativePath)` 因相对路径 `canResolveLocalStyleImports=false` 会 strip 相对 @import（sfc-syntax-validation.js:39-44/82），于是 `.common()` 无法解析 → less 报 `.common is undefined`。现有兜底 `_extractLessVarReferences` 只识别 `@var` 变量未定义，**漏了 mixin 未定义**（`.X is undefined`），故 `missing.length===0` 直接抛真实错误。
- 修复（与既有变量兜底对齐）：
  1. 新增 `_extractUndefinedLessMixins(errors)`（microcode-engineer.js `_isLessVarDeclared` 之后）：正则 `/\.([A-Za-z_][\w-]*)\s+is\s+undefined/g` 从编译错误提取未定义 mixin 名；天然排除 `variable @X is undefined`（@ 前缀不匹配）。
  2. `writeFiles` 写盘门禁扩展：`missing(变量)` 与 `missingMixins` 任一非空即注入重编译——变量注入 `@X: 安全默认值;`、mixin 注入空定义 `.X() {}`。仅变量回写 `theme-vars.less`（mixin 真实定义本就在 theme-vars.less，不重复回写，避免污染）。
- 单测验证（less 真编译）：提取 `['common','theme-dark']`、排除 `variable @text-color is undefined`；注入前 `.common is undefined`、注入后编译通过；**空 mixin 与真实 `.common()@text-color:#ff0000` 合并无害**（真实变量生效，空 mixin 不干扰）→ 落盘目录门禁不受污染。
- 重启：build 0 error，PID 94616，`:13030` 正常。
- 后续关注：本次视觉分析降级（Vision 返回 JSON 在 position 31888 截断/末尾逗号，robustJSONParse 仅部分提取），属 Vision 输出质量抖动，不影响本次写盘；若下轮仍降级可关注 vision 输出截断。写盘门禁已可容忍 mixin 未定义，但真实 mixin 内容仍由 L0-B 的 LessCompileGate（目录门禁）权威校验。

## Vision 分析 JSON 截断根治（max_tokens 8192 → 16000 + 截断重试）(P0)
- 现象：任务 `mc-max-1787035082973-544de523`（14:38）视觉分析 fail-closed：`Failed to parse analysis result: 视觉解析结果结构为空（sections/charts/interactions 全空）`。Vision(qwen3.7-plus) 164s 返回 JSON 在 position 29381 被截断（`Expected ',' or ']' after array element`），robustJSONParse 部分提取只拿到标量、layout/sections 全 null → `isVisualAnalysisDegraded` 抛异常 → graph 层 `isUntrustedVisualAnalysis` fail-closed 阻断。与上一条 13:57 的 position 31888 截断是**同一顽疾复发**。
- 根因：`vision-agent.js:28` `this.maxTokens = getMaxTokens(model, config.maxTokens || 8192)`。qwen3.7-plus 不含 `qwen-vl` 前缀 → 走 defaultValue=8192；max 级组件（21 节点 / 76 effects / 38 资源）视觉分析 JSON 远超 8192 tokens 被硬截断。部分提取 `_partialExtract` 对嵌套对象（layout/sections）无能为力（只提取标量），故 sections 全空。
- 修复（`backend-node/src/ai-engine/agents/vision-agent.js`）：
  1. 默认 maxTokens 8192 → 16000（新增常量 `VISION_DEFAULT_MAX_TOKENS`）。
  2. `callOpenAICompatibleAPI` 支持 `options.maxTokens` 动态覆盖（operation 内 `max_tokens: options.maxTokens ?? this.maxTokens`），返回透传 `finishReason`（`finish_reason ?? stop_reason`）。
  3. `analyzeImage` 检测 `isTruncated = finishReason==='length' || finishReason==='max_tokens'` 且调用方未显式传 maxTokens 时，用 `VISION_TRUNCATION_RETRY_MAX_TOKENS=32000` 重试一次，避免半截 JSON 触发 fail-closed。
- 对齐：截断判据与 `microcode-engineer.js:3494` 的 `isMaxTokens = stopReason==='max_tokens' || finishReason==='length'` 一致。
- 重启：停旧 PID 94616，build 0 error，`node start-node.js` → 新 PID 98936，`:13030` 监听正常；已 grep 确认 dist 编译产物含新常量。
- 验证建议：重跑该 max 任务；vision 输出不应再截断（或截断时自动 `⚠️ Vision 输出被 max_tokens 截断...用更高上限重试` 后成功）。注意重试会再耗 ~164s，总视觉耗时可能到 ~5min，仍在 graph 层 `VISUAL_TIMEOUT_MS=10min` 内。

## 模型方案制（Profile）接口改动分析（仅分析，未落地）
- 需求：把「一组完整模型配置」当可命名/保存/一键切换对象，避免每次在十几个输入框手填。前一日已完成需求分析（`artifacts/model-profile-requirement-analysis.md`），今日聚焦**接口层改动面**。
- 关键约束盘点：①`AI_CONFIG_SCHEMA` 是 `.strict()`，`config.service.ts:137`，body 带 `profiles` 会被拒 → 必须扩 schema；②生成链路 `getMergedAiConfig`(config.service.ts:179) 第180行 `globalCfg=this.getAiConfig()` **不感知 profile** → 切换方案要生效必须让生成链路读 active profile；③前端 ConfigPanel 是 localStorage+后端双写(configStore.saveConfig 再 POST)；④user 级(MongoDB user_ai_configs) 是扁平快照，不参与 profiles。
- 推荐接口设计（最小破坏面，未新增独立路由）：`GET /api/config/ai` 增量返回 `{ config, profiles[], activeProfileId }`（`config` 仍指向 active 快照，兼容 components.vue/ApiBindingWizard 读取）；`POST /api/config/ai` 用 `action` 字段分流（`save-current`/`apply`/`create`/`update`/`delete`），缺省=`save-current` 向后兼容。
- service 层改动：`getAiConfig()` 改为返回带 profiles 元信息；新增 `getActiveProfileConfig()`（纯 active config，供 getMergedAiConfig 用，保内部调用兼容）；`saveAiConfig` 按 action 分流；新增 `migrateLegacy()`（旧根配置自动包成 default profile，零中断）；profile 内 config 复用 AI_CONFIG_BASE_SCHEMA（放宽 strict）。
- 落盘结构：`data/ai-config.json` 加 `profiles[]`（每项是完整配置快照）+ `activeProfileId`，根扁平字段保留作回退兼容。
- 前端改动点：ConfigPanel 顶部新增方案下拉 + 应用/另存为/更新/删除/改名；onMounted 改拉 GET 拿 profiles；切方案前未保存修改弹确认；删 active 拦截。components.vue/ApiBindingWizard 读 `data.config` 无需改。
- UX 红线：下拉项显示「方案名·模式·模型数」；删 active profile 拦截；切换前确认防覆盖。
- 决策点（待用户拍板）：profiles 仅全局文件（推荐，MVP 不做 user 级）；save-current 同步写根扁平字段（推荐是）；方案切换仅影响新任务（推荐）。
- 完整接口契约见 `artifacts/model-profile-api-changes.md`。下一步：用户确认后用 Craft 模式落后端 schema+service+前端下拉。

## Vue3 Figma 组件「效果与 UI 相差很大」根因：section 背景图统一错配 (P0)
- 现象：任务 `mv-1787034559271-06a65a45`（Vue3 Figma Max，设备监测）生成成功，但预览效果与设计稿相差很大。
- 根因（确证）：`visual-parser.js` 的 `_enrichContentBackground` → `_findContentBackgroundResource` 策略1 用 `find` 返回**第一个** figmaPath 含 `slot-con` 的 bg 资源（恒为 `bg-8788.png` = switch/active 卡片背景 193×65）。三个 section 的 body 都无 figmaPath（LLM 只给 bgPlaceholder 臆造名），策略2 不生效，策略1 对所有 section 无差别返回 bg-8788 → 三个 section body 的 backgroundImage 全被错补成同一张卡片小图。
- 证据链：LLM 原始响应（debug-ai-response.txt）里 bg-8788 只出现 1 次（仅 sidebar「分类导航」）；后处理后的 preview-analysis.json 里 bg-8788 出现 4 次（section-header / section-top-cards / section-main-content 的 body.backgroundImage 全错补）。生成时 index.vue 把 header(40px)/sidebar(46px)/device-grid(大区域) 都贴了这张 193×65 卡片背景，视觉严重错乱。
- 修复：容器判断。新增 `_bodyChildrenHaveResources(body)` / `_nodeHasResource(node)`（visual-parser.js:2037 / :2043）——递归判断 body.children 是否已有子节点带 resourceFile/bgResource/backgroundImage；若有（说明 section 是容器，背景由子元素渲染），跳过统一背景补全。flat content 分支与 schema sections 分支均加该判断。
- 位置：`backend-node/src/ai-engine/roles/visual-parser.js:1929`（flat content）/ `:1960`（sections 循环）/ `:2037` `:2043`（helper）。
- 重启：停旧 PID 98936，build 0 error，`node start-node.js` → 新 PID 3519，`:13030` 监听正常；dist 已含新方法。
- 遗留（LLM 层 resourceFile 错配，非后处理 bug，本次未修）：① sidebar「分类导航」的 resourceFile=bg-8788（应为 @antd/tab 的 bg-8831 或 tab-active CSS 渐变）；② card-tunnel 隧道设备用 bg-8807(default) 而非 bg-8788(active)。属视觉分析对 switch active/default、tab 背景的区分不准，需单独优化 prompt 或匹配逻辑。
- 验证建议：重跑该 Vue3 max 任务，确认 header/设备列表区不再被贴卡片背景（改为 CSS 纯色/渐变），仅卡片/导航/网格项各自贴自身背景。

## 背景图 size/position/repeat 精确计算（方案 B，用户确认实施）
- 现象（承接上条）：背景图 size/position/repeat 未按 Figma 实现，全部硬编码 `cover/center/no-repeat`。Figma 里 bg 图都是「1:1 精确填充」各自节点（bg 尺寸=节点尺寸，如 switch 卡片 193.4×64.8 的 bg 图就是 193.4×64.8），正确值应为 `100% 100%`。
- 根因：`_calculateAllBackgroundSizing` 的精确计算 `_calculateBackgroundSizing`（按 wR/hR 判断 100% 100% / 100% auto / repeat-x / contain / cover 等）**从未执行**——它需要 `parentDims = node.dimensions || structure?.dimensions`，但视觉分析结果（preview-analysis.json）里既无 `structure` 也无任何 `dimensions` 字段 → `parentDims=null` → 直接 return，全部落到 cover 兜底。此外 visitor 只识别 `background.resourceFile` / `backgroundImage.src` 两种格式，**漏了 card/tab/网格项的直接 `resourceFile` 字段**（`_enrichElement` 注入的）。
- 修复（visual-parser.js，方案 B）：
  1. `_postProcessAnalysis` 把 `figmaData` 传入 `_calculateAllBackgroundSizing`。
  2. 新增 `_buildNodeParentDimsMap(node, map, parentBox)`：遍历 Figma 树构建 `figmaNodeId → 父节点 absoluteBoundingBox` 映射（bg 节点的直接父节点即「背景被应用到的容器」）。
  3. `_calculateAllBackgroundSizing` 构建 `resourceFile → figmaNodeId` 映射，`parentDims` 优先级改为 `node.dimensions → figma 父节点尺寸 → structure.dimensions`。
  4. visitor 扩展第三格式：`node.resourceFile`（仅 `recommendedUsage` 含 background 才算背景，避免误伤 icon 的 imgSrc），计算后把 `backgroundSize/Position/Repeat` 落到节点自身字段，供 style-mapper / 代码生成 prompt 精确还原。
- 位置：`backend-node/src/ai-engine/roles/visual-parser.js:1015`（传 figmaData）/ `:1608`（_calculateAllBackgroundSizing 重写）/ `:1707`（_buildNodeParentDimsMap）。
- 验证：用真实 figma-node-data.json + resource-dom-mapping.json 模拟 `_calculateBackgroundSizing`，17/19 个实际 bg 资源正确算出 `100% 100%`（1:1 填充）；1 个是 tab-active 的 CSS 替代资源（无图片，不走背景图路径）；1 个 tab 背景 bg-8831 因 figmaNodeId 复用取首父尺寸（近似，影响极小）。
- 重启：停旧 PID 3519（实际监听 worker 3566），build 0 error，`node start-node.js` → 新 PID 6047，`:13030` 监听正常；dist 已含新方法（16 处）。
- 下游消费：style-mapper.js:436-452 读 `background.backgroundSize`/`backgroundSize`；microcode-engineer prompt 要求「backgroundSize/Position/Repeat 必须从 layoutStructure 精确还原」。vue3 路径需重跑验证最终 CSS 是否用 100% 100%（此前因 visual-parser 没算出值而拿到 cover 兜底）。
- 验证建议：重跑该 Vue3 max 任务，确认卡片/导航/网格项的 background-size 变为 100% 100%（而非 cover），背景图不再被拉伸/裁剪。

## 网格项背景归属根治（icon → 同节点 bg 匹配）
- 遗留问题（承接方案 B）：右侧设备网格 12 个 item，视觉分析只给了 icon 的 resourceFile，**漏了同节点的 bg**，导致代码生成时模型自行从资源池挑 bg（无 sizing 信息，最终写 cover）。
- 匹配逻辑验证：通过 item.icon.resourceFile → figmaNodeId → 向上找祖先节点 → 同祖先下的 bg 资源，12/12 精确匹配（icon-8444→bg-8439、icon-8473→bg-8468 … icon-8764→bg-8759）。
- 修复（visual-parser.js）：新增 `_enrichGridItemBackgrounds`（在 `_postProcessAnalysis` 里 `_calculateAllBackgroundSizing` **之前**调用），遍历 grid 容器（含 items）的每个 item，注入 `item.resourceFile` + `recommendedUsage=backgroundBlock`；配套 helper `_attachGridItemBackground` / `_findItemIconResource` / `_buildNodeParentIdMap`（构建 figmaNodeId→父节点 id 映射）。
- 时序关键：补 bg 必须在 `_calculateAllBackgroundSizing` 之前，否则新注入的 resourceFile 不被精确计算。
- 位置：`backend-node/src/ai-engine/roles/visual-parser.js:1010`（调用）/ `:1736`（_enrichGridItemBackgrounds）/ `:1781`（_attachGridItemBackground）/ `:1809`（_findItemIconResource）/ `:1827`（_buildNodeParentIdMap）。
- 重启：停旧进程，build 0 error，`node start-node.js` → PID 12324，`:13030` 监听正常（`/api/v2/pipeline/health` 返回 200）；dist 已含新方法（11 处）。启动时曾见瞬时 EADDRINUSE（12329 竞争失败、12324 成功），最终服务正常。
- 验证建议：重跑该 Vue3 max 任务，确认设备网格项也带 resourceFile + backgroundSize=100% 100%（此前仅卡片/导航有），网格项背景不再被 cover 拉伸。

## 15:35 Profile 方案制功能完成

### 前端实施完成
- **ConfigPanel.vue 修复**：
  - 模板调用名修正：`handleDeleteProfile` → `deleteCurrentProfile`
  - 函数体格式修复：`deleteCurrentProfile() {  if` → 分两行
  - 构建通过 ✅

- **方案管理 UI 完整实现**：
  - 方案下拉选择器（显示方案名称 + 模式标识）
  - 5个操作按钮：重命名、另存为、覆盖保存、删除
  - 图标化按钮 + tooltip 提示
  - default 方案删除保护

- **服务状态验证**：
  - 后端 13030 端口正常运行 ✅
  - 前端 2610 端口正常运行 ✅
  - Profile API 响应正确：`{config, profiles, activeProfileId}` ✅
  - 默认方案自动迁移：`{id:'default', name:'默认配置', modelMode:'separate'}` ✅

### 测试工具
- 创建 `test-profile.html` 独立测试页面
- 覆盖所有 profile 操作：加载/创建/切换/更新/重命名/删除
- 实时日志 + 状态指示器

### 功能完整度
- ✅ 后端 schema 扩展 + 迁移逻辑
- ✅ 后端 6 个 action 接口（save-current/apply/create/update/rename/delete）
- ✅ 前端 ConfigPanel 方案管理 UI
- ✅ 前端 stores/config.ts 兼容本地存储
- ✅ 生成链路读取 active profile
- ✅ 前后端联调验证通过

## Vue3 vs 微码 Figma 还原一致性检查 + 微码背景误伤 bug 修复 (P0)
- 检查结论：max 组件在 Vue3/微码两条路径的 Figma 还原**不一致**，根因是微码模式的面板外壳剥离有 bug。
- 共享步骤（两路径一致生效）：`_normalizeStatItemLayouts`、`_enrichGridItemBackgrounds`、`_enrichContentBackground`（容器判断）、`_calculateAllBackgroundSizing`（方案 B）——本日三条背景图修复对两路径通用。
- **发现的 bug**：`visual-parser.js:_filterPanelChromeResources`（仅微码调用）有第二个条件「按 name 匹配 `^(bg|header)$`」，但 Figma 里**所有背景填充节点统一叫 'bg'**（switch/active/bg、tab/bg、cons/Group/bg…），导致 22 个 bg 资源全部被误判成面板外壳资源而过滤 → 微码组件丢失所有卡片/导航/网格背景图，只剩 icon。
- 修复：删除 name 匹配条件，只保留 figmaPath 精确匹配（`^[^/]+/(bg|header)$`，即「根一级子节点恰好叫 bg/header」的两段路径）。修复后仅过滤 1 个面板背景 bg-8418，保留 19 个 bg。位置 `visual-parser.js:1419`。
- 重启：停旧 PID 12324，build 0 error，`node start-node.js` → PID 13137，`/api/v2/pipeline/health` HTTP 200。
- **遗留（未修，需关注）**：微码的 header 剥离 `_stripBasePanelChrome` / `_stripPanelChromeFromFigmaText` 依赖 `parsed.structure` 字段，但当前视觉分析输出是 `layout.sections` 格式（全文件只有读取 structure、无赋值，恒 undefined），故这两个剥离**实际不生效**（恒 return 0）→ 微码组件 header 不被剥离，可能与宿主 base-panel 标题栏重复（双标题栏风险）。若要根治需让这两方法兼容 layout.sections 格式，或补 structure 构建。

## 16:00 修复 auth/current 500 错误

### 问题
- 前端报错：`Failed to load resource: 500` + `获取用户信息失败` + `Navigation aborted`
- `/api/auth/current` 返回 404/500

### 根因
- Node 端 `auth.module.ts` 已注销 AuthController（写着"已迁 Java"）
- Java 后端在昨天某时刻崩溃，一直没重启（进程不存在、8080 无监听）
- 前端请求链路断裂 → 路由守卫拒绝导航

### 修复
- 用项目启动器 `node start-java.js` 重启 Java（detached 模式，跨回合不被回收）
- Java 在 8080 端口正常监听 ✅
- 前端代理 2610 → Java 8080 链路通畅 ✅
- `GET /api/auth/current` 返回 200：`{success:true, user:{uid:"dev-local", role:"member", permissions:[...]}}`

### 验证
- 刷新页面后导航正常，任务详情页可访问

## 微码 headerSlots 推断修复（统计指标映射 header-right）+ 纠正双标题栏误判
- **纠正上条「双标题栏风险」误判**：微码 header 的实际处理是双轨——`_stripBasePanelChrome`（依赖旧 structure 格式，已失效）+ `_inferHeaderSlots`（从 layout.sections 的 section-header 推断插槽，**新格式下生效**）。标题文字由 base-panel 通过 `analysisTarget`/`panelKey` 渲染，headerSlots 只负责标题栏装饰与统计 → **不会双标题栏**。
- **发现的格式 bug**：`_inferHeaderSlots` 返回元素格式是 `{ type, text }`，但 microcode-engineer 消费的是 `{ slotType, elementType, content }`（`microcode-engineer.js:700-702` 用 `s.slotType`/`s.content||s.elementType` 过滤），故旧推断结果被全部过滤、恒显示「(无)」。且旧逻辑只推断 title + tab-switch/icon-group，**漏了统计指标 text-group/stat-item**。
- 修复（`visual-parser.js:_inferHeaderSlots`）：① 返回格式改为 `slotType/elementType/content`；② 补 `text-group/stat-item/statistic` → `header-right`（content=`label value` 如「设备类型 28」）；③ `tab-switch/icon-group/icon-button` → `header-right`；④ 移除 title 映射（base-panel 渲染标题）。
- 验证：真实 preview-analysis.json 模拟，正确推断 3 个统计项 `设备类型 28 / 设备总数 68562 / 完好率 98%` → header-right。
- 重启：停旧 PID 13137，build 0 error，`node start-node.js` → PID 14364，`/api/v2/pipeline/health` HTTP 200。

## 16:13 截图 vs Figma 差异诊断（mv-1787034559271-06a65a45）
- 用户反馈 Image#1（生成效果）与 Image#2（Figma 设计）相差甚远。
- **诊断结论：截图来自修复前旧版本，不是当前代码渲染结果。**
- 铁证：截图数据（128/125/3、64台、水平Tab、仅3设备）在 index.vue 中均不存在；当前代码有正确数据（68562/98%、56302+1280、垂直sidebar 6 Tab、12 设备网格）。
- 视觉分析数据实际完整（vision cache 53KB、12 个 grid items 各含 icon+content+设备名、7 个导航项），问题出在**该任务在所有背景图修复之前生成**（bg-8788 贴满全屏 + cover 兜底 + Vision 截断降级）。
- 建议：重跑任务验证全部修复链路效果。

## 16:50 微码生成失败：子组件 import 重复声明（mc-max-1787041994844-2866a56b）
- 现象：跨文件一致性校验后 `Identifier 'TotalTraffic' has already been declared`（37:6 import vs 54:7 const），Vue SFC 编译错误 fail-closed。
- 根因：`_ensureSubComponentImport`（microcode-engineer.js:5496）只识别 `import X from` 绑定名，未识别 script 里模型已用 `const X = defineAsyncComponent(() => import(...))` 动态导入的子组件。本任务 chunk3（script 状态段）已写 5 个 `const TotalTraffic/TunnelTraffic/BridgeTraffic/VehicleType/TrafficForecast = defineAsyncComponent(...)`，系统又补 5 个静态 `import X from './components/X.vue'` → 重复声明。
- 修复（microcode-engineer.js:_ensureSubComponentImport）：`declared` 集合追加识别 `const/let/var X =` 与 `function X` 声明，已声明则不再补 import。
- 同步修复 vue3-engineer.js:_wireVue3SubComponentImports：1172-1173 追加 `const/let/var X=` 与 `function X` 判断（防御 Vue3 路径同坑，虽然 Vue3 prompt 已禁手写 import）。
- 重启：build 0 error，PID 14364 → 26345，`/api/v2/pipeline/health` HTTP 200。
- 注意：本次任务还出现 kimi-k2.6 模型 180s 超时熔断（p1786885201290_ee29），属 provider 质量/超时，非本次修复范围。

## 17:17 L0-B CODE-011 误杀：echarts 惰性守卫 + refresh 调用点（mc-max 重跑）
- 现象：L0-B 校验 `pass=false, BLOCK=1`，`[CODE-011] echarts.init 必须等待 DOM layout settle ... (package/components/VehicleTypeDistribution.vue)` → fail-closed。
- 根因（误杀）：真正产物 `VehicleTypeDistribution.vue` 的写法是**正确的**——`const renderChart = async () => { if (!chartRef.value) return; if (!chartInstance) chartInstance = echarts.init(...); ... }` + `onMounted(async () => { await nextTick(); await renderChart() })` + `handleRefresh = () => renderChart()`。
  - `echarts.init` 有 `if (!chartInstance)` 惰性守卫，且 onMounted 已 `await nextTick()`。
  - 但 `checkEchartsInitTiming` 跨函数层用 `callSites.every(延迟)` 判定，把 `handleRefresh` 里那个「chartInstance 已存在、不会真的 init」的同步刷新调用点也算进去 → every=false → 误判 BLOCK。
- 修复（`utils/echarts-init-guard.js`）：新增 `hasLazyInitGuard()` 识别 `if (!chart) chart = echarts.init(...)` 惰性初始化守卫；跨函数层新增第 3 判据——「惰性守卫 + 至少一个延迟调用点（some 而非 every）→ 放行」，因惰性守卫保证 init 只执行一次、首次时机由延迟调用点决定。
- spec 同步补 2 用例（放行 + 反例），`npx jest` 22/22 通过。
- 重启：build 0 error，PID 26345 → 31175，`/api/v2/pipeline/health` HTTP 200。
- 注意：本次任务产物目录是 `mc-1787043634763-67a15ca0-mc-max-1787041994844-2866a56b`（非 `mc-max-1787041994844-2866a56b` 旧残留目录）；16:33 的 code-chunks 是旧残留，别被文件名误导。
- 遗留 WARN（不 BLOCK，未修）：CODE-003（common.less 子组件 class 用 `c-bridge-traffic-chart-*` 而非 `c-monitor-*` 前缀，20 个）、CODE-013（chart 容器 min-height:220px/180px 固定像素）。

## 17:32 补充排查：图片下载失败 + 覆盖率低（mc-max 17:00 运行）
- `图片下载失败 ×1` 根因：失败的是 `隧道图标`（figmaNodeId 2:3473，name 为「2026-03-17-...隧道图标...」），是 Figma 里的 AI 生成嵌入图，Figma Images API 无法导出，属预期行为，系统已给 fallbackHint 提示 LLM 用 CSS 替代（`resource-mapping-formatter.js:337`「原图下载失败（可能是 AI 生成图或 Figma 嵌入资源无法导出）」）。
- resource-dom-mapping 分布 `{css:1, missing:188, success:9}`：188 个 missing 是 GROUP/FRAME 矢量组内部子节点（Group/Vector/柱/circle），本就合并为单个 icon 不单独下载，非 bug。
- 元素覆盖率 `16% (29/178)`：Vision(qwen3.7-plus) 只识别 29/178 元素，178 里多数是矢量组子节点（合理不识别），但 16% 仍偏低，可能致布局还原不完整——属模型识别能力问题，非代码 bug。
- max_tokens 截断多次（finishReason=length）：VehicleTypeDistribution.vue 首轮 outputTokens=16000 截断后重试；属 qwen3.7-plus 输出上限问题。

## 17:40 覆盖率 16% 根因修复：统计口径 + prompt 双修
- 用户选方向1「提高元素覆盖率」。排查发现覆盖率 16% 是**两个原因叠加**，非单纯 prompt 问题：
  - ① 统计口径缺陷（主因，16%→40%）：`_generateCoverageReport` 把 103 个无语义 Figma 节点名（`Group 2136638523`/`Vector`/`Rectangle 26`/`bg-[m]`/`@antd`/`btn`/`sub-header` 等）算进 missing，Vision 本就不该识别这些图层名。
  - ② 细粒度文本识别不足（40%→更高）：`北京方向`/`准确率98%`/`2小时前`/`实际流量`/`预测流量` 等图表/时间轴小文本确实没识别。
- 修复 A（`visual-parser.js`）：新增 `_isNonSemanticName()` 过滤无语义名（Figma 自动编号/占位/@技术组件/结构占位），`_generateCoverageReport` 交叉比对循环里跳过这些节点，不参与覆盖率分母。
- 修复 B（prompt 双文件）：`ui-element-checklist.md` 新增「细粒度文本」识别规则（时间轴刻度/坐标轴值/图例/方向标注/百分比/统计数值，<14px 也必须识别）；`self-checklist.md` 新增第 11 项自检「细粒度文本已完整识别」。
- 验证：真实数据模拟覆盖率 16%→40%（排除无语义名后仍 missing 的 30 个是真实细粒度文本，靠 prompt 增强提升）。
- 重启：build 0 error，PID 31175 → 32231，`/api/v2/pipeline/health` HTTP 200。
- 注意：prompt 增强是概率性收益（模型不一定严格遵守），统计口径修复是确定性收益。覆盖率当前只影响 visual-trust 的 warning 判定（不阻断生成）。

## 18:00 文件写入失败：虚假换行修复的关键词加空格正则误拆标识符（mc-max 重跑）
- 现象：覆盖率已升到 45%（统计口径+prompt 修复生效）；但最后「文件写入失败」→ `TrafficForecast.vue:52:69 Unexpected keyword 'for'`，代码里 `forecast` 被写成 `for ecast`。
- 根因：`_fixSpuriousLineBreaks`（microcode-engineer.js:4678）的「关键词后加空格」后处理，正则 `\bfor(?=[a-zA-Z$_])` 把 `forecast` 的 `for` 误判为关键字 → 拆成 `for ecast`。前瞻 `(?=[a-zA-Z$_])` 匹配「关键词后跟字母」（即标识符延续），完全搞反了——该匹配的是「关键词后跟非标识符字符」（如 `(`/`[`/`'`）。
- 影响范围：`forecast`→`for ecast`、`forEach`→`for Each`、`constant`→`const ant` 等所有以关键词为前缀的标识符都会被误拆。
- 修复：前瞻 `(?=[a-zA-Z$_])` → `(?=[^\w\s])`。验证：forecast/forEach/constant 不再误拆；for(let→for (let / if(x→if (x / return[→return [ / from"→from " 仍正确加空格。
- 重启：build 0 error，PID 32231 → 37938，`/api/v2/pipeline/health` HTTP 200。
- 注：`_fixSpuriousLineBreaks` 触发条件是「平均行长<25 且最大行长<80」，本次 TrafficForecast.vue（平均17/最大72）触发；此文件是 mock 数据较多的子组件。

## 18:20 SFC Invalid end tag：scriptSplit 状态段末尾 markdown 围栏残留（mc-max 重跑）
- 现象：覆盖率 40%；但最后 `index.vue:127:2 SFC: Invalid end tag`，组件代码生成失败。
- 根因：`_mergeScriptParts`（microcode-engineer.js:3140）的 `clean` 函数 `replace(/<\/script>\s*$/i,'')` 无法剥离「`</script>` 后跟 ` ``` `」的情况——chunk3（script 状态段）末尾残留 markdown 围栏 ` ``` `（模型用 ```vue...``` 包裹输出，分块解析没剥干净尾围栏），` ``` ` 不是空白，导致 `</script>\s*$` 不匹配 → `</script>` 和 ` ``` ` 都被包进 `<script setup>` 内部 → SFC 解析错乱。
- 修复：`clean` 函数里在剥 script 标签**之前**先剥 markdown 围栏：`x.replace(/^\s*```[a-zA-Z]*\s*\n?/,'').replace(/\n?\s*```\s*$/,'')`。
- 验证：模拟 chunk3 内容，clean 后 ` ``` ` 和 `</script>` 均正确剥离，最终 `<script setup>...</script>` 结构干净。
- 重启：build 0 error，PID 37938 → 39139，`/api/v2/pipeline/health` HTTP 200。
- 注：非 scriptSplit 路径（整段 script）末尾围栏由 `_sanitizeFileContent`（写盘门禁前 4540/4543 行）兜底剥离；scriptSplit 因围栏被包进 script 内部，须在 `_mergeScriptParts.clean` 提前剥离。
